# Android M7 M3 local evidence

The PNG and MP4 files in this directory are ignored local milestone evidence.
They are generated from the API 35 `hantap_test` emulator and are not release
device evidence. The reproducible source gate is
`M3DiscoveryFlowInstrumentedTest`; physical-device QA remains in release-gate
Issue #19.
