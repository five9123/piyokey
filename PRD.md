# PRD: 한국어 타이핑 연습 모바일 앱 (글로벌 시장, 일본 특화 UX 유지)

- 현재 문서 기준: **v6.20 (2026-09-03)**. iOS/iPadOS만 활성 제품 개발·출시 범위로 두고 기존 Android 포트는 참고용으로 동결한다. iOS 1.1 온보딩의 마지막 권한·개인정보 흐름은 TYP-71의 최초 두 버튼 안내를 유지하되, 이후 설정 화면의 정보 구조·재검토 경로·표시 섹션·내장 배열 명칭은 TYP-75와 TYP-89 계약을 따른다. 내장 키보드 다음 키 가이드, OS 입력 패널과 물리 키보드 참조 배열은 TYP-92의 v6.20 정정 정책을 따른다. Issue #87/PR #88의 `.typedeck` BCP 47·deck schema v2와 사용자 제작 덱의 `한국어 / 뜻 / 발음 / 발견용 덱 언어 1개` 정책은 유지한다. 시간에 따라 바뀌는 branch·PR·배포 상태의 상세 원본은 `PROJECT_STATUS.md`를 따른다.
- 문서 개정 순서: v6.4 `.typedeck` → v6.5 한국어 UI 제거 → v6.6 스페인어 UI → v6.7 독일어·프랑스어와 현지 학습 콘텐츠 → v6.8 기준선·화면 현황 감사 → v6.9 홈 추천 두 영역 → v6.10 챕터5·6 스테이지 확장 → v6.11 `.typedeck` BCP 47·deck schema v2 → v6.12 사용자 편집 덱의 설명 언어 1:1 → v6.13 발견용 덱 언어 → v6.14 Android 포트 동결과 iOS 전용 활성 범위 → v6.15 iOS 온보딩 알림·개인정보 두 버튼 흐름 → v6.16 Settings 개인정보 재검토 간소화·게임 표시 흡수 → v6.17 Settings 섹션·내장 배열 명칭·링크 위치 동기화 → v6.18 Settings 정보구조·내장 배열 선택 시인성 강화 → v6.19 iPad 전용 다음 키 가이드 → v6.20 다음 키 가이드 복구와 iPhone OS 입력·물리 참조 UI 제거. 아래의 과거 보충 기록은 이 순서로 해석하며 최신 버전 및 본문 계약이 우선한다.
- v1.1~v6.4 개정 기록 (2026-08-28까지) — v1.1: OS 키보드 모드 추가, v1.2: 읽기 전용 덱 발견·다운로드 추가, v1.3: 결과 화면(F12) 상세 스펙, v1.4: PIYOKEY 브랜드 확정, v1.5: 출시판을 공식 학습 콘텐츠·무계정·원탭 연습 중심으로 재정렬, v1.6: HanTap 공용 병아리 AppIcon 채택, v1.7: 첫 3챕터 부화 온보딩과 콘텐츠 접근 게이트 추가, v1.8: 창작 K-POP·K드라마 덱 6종 추가, v1.9: 홈·마이페이지 피요 데일리 응원 멘트 추가, v2.0: 부화 미션 연속 진행과 결과 하단 완료 CTA 추가, v2.1: 흐름·산성비 점진 가속·3목숨 조기 종료와 플레이 피요 정면 비율 고정, v2.2: 흐름·산성비 단어 카드 내부 장식 아이콘 제거, v2.3: 초성·받아쓰기 플레이 UI와 입력 방식 정책을 최신 게임 프레임으로 통일, v2.4: 흐름·산성비 단어 카드 내용 맞춤 폭과 발음 표기 추가, v2.5: 긴 자모 트랙의 화면 폭 격리와 현재 자모 자동 추적 추가, v2.6: 산성비 시간차 동시 낙하와 위험 카드 우선 입력 추가, v2.7: 산성비 OS 키보드의 화면 내 자유 단어 입력 추가, v2.8: 직접 입력 게임의 중복 조합 텍스트 제거·상태 문구 중앙 정렬과 흐름·산성비 목숨 손실 피드백 강화, v2.9: 게임 HUD 닫기 버튼 테두리 제거와 수치 중앙 정렬, v3.0: 초성별 입력 진행·연습형 조합 카드·일본어 뜻 워드박스 추가, v3.1: 초성 일본어 뜻 기본 표시와 설정 토글 추가, v3.2: 받아쓰기에도 연습형 조합 입력 카드 적용, v3.3: 게임 허브에서 받아쓰기와 단어 맞추기 카드 위치 교환, v3.4: 전용 게임 세트를 타이핑 난이도별 100단어로 확대하고 세션별 무작위 출제 추가, v3.5: 게임 결과의 모드·레벨 표기와 결과 이미지 직접 저장·SNS 공유 추가, v3.6: 여행·연애·친구 상황별 재미 표현 덱 8종 추가, v3.7: 최애 아이돌 라이브 댓글 덱 추가, v3.8: 게임 전용 100단어를 실용 TOPIK 수준과 타이핑 난이도의 복합 기준으로 재구성, v3.9: 부화 미션 결과의 보조 액션 제거와 단계별 성장 연출 순서 복구, v4.0: 부화 완료 후 홈 랜딩과 6단계 메인 앱 스포트라이트 튜토리얼 추가, v4.1: 음영이 있는 흰색 `ㅎ` 키캡 브랜드 이미지로 AppIcon·앱 내부·공유 카드 로고 통일, v4.2: 게임 플레이 중 피요 복장 고정, v4.3: 연습·게임 세션 외형 결정 통일, v4.4: TOPIK 안경을 게임 참여 보상으로 전환, v4.5: 단어 퀴즈를 일본어 뜻 기반 한글 직접 타이핑으로 전환, v4.6: 게임 HUD의 긴 수치 자동 축소와 아이콘 안전 영역 추가, v4.7: 플로우 가속 상한을 1.8배로 상향하고 플로우·피요컵 Game Center 계약을 v4로 분리, v4.8: 옷장의 알 무늬 4종 재선택 UI 제거, v4.9: 게임 허브의 최상단 Game Center 전체 랭킹 버튼 제거, v5.0: 외부 운영 메일 기반 신규 덱 제안·기존 콘텐츠 제보 추가, v5.1: 영어 콘텐츠와 글로벌 배포를 위한 `typee` 로케일 브랜드·콘텐츠 현지화·단일 앱 레코드 정책 추가, v5.2: 앱 1.1용 `.piyodeck` 무료 가져오기와 비소모성 Deck Maker 생성·편집 계약 추가, v5.3: 글로벌 App Store 표시명을 기능 설명 우선·브랜드 후위 형식으로 확정, v5.4: Deck Maker 초안 복구·원자적 편집 충돌 방지·안전 삭제·접근성 계약 보강, v5.5: 앱 1.1 `All Countries or Regions` availability 확정, v5.6: 앱 제공 고정 목표 전 영역의 오프라인 gTTS MP3와 공통 폴백 순서 확정, v5.7: 이후 추가·수정되는 앱 제공 고정 목표에도 동일 gTTS 생성·검증 규칙을 의무화, v5.8: Android M7 재개와 플랫폼 저장·파일·결제·랭킹·오디오 계약 추가, v5.9: iOS 내장 한국어 10키(천지인식) 연습 우선 지원 추가, v6.0: iOS 다섯 직접 입력 게임의 10키 연동과 로컬 기록 분리 추가, v6.1: 홈의 주간 피요컵·랜덤 단어 5개 원탭 보조 CTA 추가, v6.2: 사용자 덱 3개 무료 보관·4번째부터 `pro` 해금과 상품 리브랜딩 추가, v6.3: 세션 밖 최초 1회 개인정보 선택 안내와 재검토 UX를 추가했고 (F10, §10), v6.4: 사용자 덱 외부 파일 확장자를 `.typedeck`으로 변경했다 (F5.9, §8.4).
- v6.9 보충 (2026-08-29, Issue #81): iOS 홈 추천을 개인화 추천과 다음 단계 추천의 두 영역으로 분리하고 각 3개를 노출한다. iPhone은 영역별 가로 스크롤, iPad는 영역별 3열을 사용한다 (F5.6).
- v6.10 보충 (2026-08-29, Issue #85): iOS·Android 연습 탭의 챕터5 단어를 3개 스테이지, 챕터6 표현을 5개 스테이지로 확장한다 (F4).
- v6.11 보충 (2026-08-29, Issue #87): `.typedeck` container v1을 유지하면서 deck schema v2에 `default_locale`과 allowlist 없는 canonical BCP 47 콘텐츠 언어를 도입한다. 새 reader는 schema v1/v2를 모두 읽고, 표시 fallback과 legacy 일본어 base field 호환을 공용 계약 테스트로 고정한다 (F5.9, §8.4).
- v6.12 보충 (2026-08-29, Issue #87 후속): 공식 덱은 ja/en/es/de/fr 다국어 카탈로그를 유지하되 모바일에서 새로 만들거나 편집해 저장하는 사용자 덱은 한국어 학습 대상과 정확히 하나의 설명 언어를 1:1로 묶는다. 기존 다국어 사용자 덱은 읽기·그대로 내보내기를 유지하고, 편집 저장 전 보존할 한 언어를 명시적으로 선택한다 (F5.9, §8.4).
- v6.13 보충 (2026-08-29, 웹 Builder 후속 합의): v6.12의 사용자 노출 `설명 언어` 개념을 폐기하고 canonical BCP 47 `덱 언어` 하나를 발견·분류용 system metadata로 둔다. 항목은 언어 선택과 무관한 `한국어 / 뜻 / 발음` 단일 묶음이며 덱 언어 변경은 metadata와 모든 항목 값을 새 단일 schema-v2 key로 이동해 보존한다. 공식 덱의 ja/en/es/de/fr 다국어 카탈로그와 공용 codec은 그대로 유지한다 (F5.9, §8.4).
- v6.14 보충 (2026-08-30, 사용자 결정): iOS/iPadOS만 활성 제품 범위로 유지한다. 현재 `android/` 구현은 배포 후보나 유지보수 대상이 아닌 동결된 참고 자료다. Android 기능·수정·의존성·CI·Play 출시 gate를 이후 작업에서 제외하며, 재개 시 기존 M7의 연속 작업이 아니라 별도 승인된 새 PRD와 초기 설계부터 시작한다. 아래 Android 세부 계약과 완료 기록은 역사적 맥락으로만 보존하며 v6.14 이후 구현 지시로 사용하지 않는다.
- v6.15 보충 (2026-08-31, TYP-71): iOS 신규 사용자는 온보딩·부화·앱 투어의 마지막에 OS 알림 권한을 한 번 요청한 뒤 개인정보 안내를 한 번 검토하고 홈으로 간다. 1.0.2에서 리마인더 값을 이미 저장한 업그레이드 사용자는 명시적인 ON/OFF를 그대로 보존하고 알림 권한 단계를 처리한 것으로 기록해 개인정보 안내로 계속한다. 안내에는 토글이나 공급자명을 노출하지 않고 `참여하고 계속`은 분석·진단을 모두 ON, `공유하지 않고 계속`은 모두 OFF로 저장한다. 설정의 두 토글은 계속 독립적이며 당시 재검토는 같은 두 버튼 안내를 사용하도록 정했다. 고지 버전은 1을 유지하고 기존 사용자의 저장 선택은 보존한다. 설정 재검토 경로는 v6.16에서 대체한다.
- v6.16 보충 (2026-09-01, TYP-75): 최초 개인정보 안내의 두 버튼과 고지 버전 1은 유지하되, 설정에서는 분석·진단 독립 토글, 짧은 비수집 안내, 개인정보처리방침 링크만 제공한다. 같은 두 버튼 안내를 다시 여는 재검토 버튼과 공급자명 표시는 두지 않으며 공급자·처리 상세는 링크된 정책에서 확인한다. 독립 `게임 표시` 섹션을 없애고 초성 맞추기 뜻 토글을 `타이핑 표시` 하단에 흡수해 저장값과 동작을 유지한다. 이 보충은 v6.15의 설정 재검토 문장과 F10의 이전 게임 표시 위치를 대체한다.
- v6.17 보충 (2026-09-01, TYP-75): 설정 순서를 `화면 → 키보드 → 사운드 → 타이핑 표시 → 연습 알림 → 피요 → 개인정보 → 앱 정보`로 고정해 효과음 토글을 최상단에 둔다는 이전 F10 문장을 대체한다. 내장 배열 표시는 de=`2-Set`/`Cheonjiin (10 Tasten)`, en=`2-Set`/`Cheonjiin (10-key)`, es=`2-Set`/`Cheonjiin (10 teclas)`, fr=`2-Set`/`Cheonjiin (10 touches)`, ja=`2ボル式`/`天地人（10キー）`, ko=`두벌식`/`천지인(10키)`로 현지화한다. 개인정보처리방침 링크는 개인정보 섹션에만 두고 앱 정보에는 런타임 버전·덱 제안/내용 제보·일반 지원을 둔다. 이 보충은 F2와 F10의 이전 효과음 위치·고정 3개 언어 명칭·앱 정보 링크 위치를 대체한다.
- v6.18 보충 (2026-09-03, TYP-89): 설정 순서를 `키보드 → 사운드 → 타이핑 표시 → 화면(언어·텍스트 표시) → 연습 알림 → 피요 → 개인정보 → 앱 정보`로 재배열한다. 내장 두벌식/천지인 배열 선택은 키보드 섹션의 첫 항목에서 두 선택지를 동시에 보여 주는 segmented control로 제공하고, 기존 저장 키·현지화 키·접근성 identifier와 OS 키보드 선택 시 비활성화 동작은 유지한다. 이 보충은 v6.17의 섹션 순서와 기존 메뉴형 배열 선택 위치만 대체한다.
- v6.19 보충 (2026-09-03, TYP-90): 다음 키 가이드와 그 설정 토글은 iPad에서만 제공한다. iPhone에서는 덱 연습·커리큘럼 레슨·흐름·산성비를 포함한 모든 내장 키보드 입력에서 저장된 ON 값을 무시하고 가이드를 표시하지 않으며, 전역 Settings와 세션 설정에서도 토글을 표시하지 않는다. iPad는 기존 기본 ON·사용자 저장값·토글·강조 동작을 유지한다. TYP-89의 Settings 순서와 segmented 내장 배열 선택 계약은 변경하지 않는다.
- v6.20 보충 (2026-09-03, TYP-92): v6.19가 iPhone 내장 키보드의 다음 키 키캡 강조와 설정 토글을 제거한 결정을 철회한다. 다음 키 가이드와 전역·세션 토글은 iPhone과 iPad 모두에서 기존 저장값대로 동작한다. `OSIMEInputPanel`은 iPhone에서 compact 포커스 복구 UI를 숨기되 보이지 않는 전체 영역 `IMETextField`의 탭·자동 포커스·Return·foreground 복구를 유지하고, iPad에서는 기존 compact 패널을 유지한다. `PhysicalKeyboardGuideView`는 덱 연습·커리큘럼 레슨·온보딩을 포함한 모든 production 렌더 경계에서 iPhone에는 표시하지 않고 iPad에만 표시하며, 전역·세션의 `keyboard.shows_physical_keyboard_guide` 토글도 iPad에만 제공한다. 저장된 ON 값과 온보딩의 자동 ON은 보존하되 iPhone 렌더 정책에서 무시해 무해하게 만든다.
- v6.5 보충 (2026-08-28): 한국어 UI를 제거했다. 기존 ko UI 설정은 영어로 전환하며 학습 콘텐츠 원본과 저장 데이터는 수정하지 않는다 (F10, §11; Issue #65).
- v6.6 보충 (2026-08-28, Issue #67): 사용자 요청으로 iOS·Android 스페인어 UI(`es`, 지역 변형 공통)를 추가한다. 첫 지원 기기 언어 자동 선택·명시적 선택 유지·브랜드 `typee`를 적용한다. 스페인어 UI의 학습 뜻·읽기·덱 메타데이터와 덱 편집 언어는 기존 영어를 사용하며 스페인어 학습 schema·번역·음원은 추가하지 않는다. 한국어 UI 제거와 기존 `ko` 설정 영어 전환은 유지한다.
- v6.7 보충 (2026-08-28, 사용자 로컬 작업 요청): UI를 `ja`·`en`·`es`·`de`·`fr`로 확장하고 es/de/fr 기본 학습 뜻·공식 덱·메타데이터·편집 언어를 함께 제공한다. v6.6의 스페인어 영어 콘텐츠 제한을 대체한다. 한국어 목표·기존 ja/en/ko 콘텐츠·로마자·음원·사용자 덱 원문은 보존한다. 로컬 후보와 원격 배포를 구분하며 새 언어 카탈로그는 구버전 URL에 덮어쓰지 않는다. 반복 절차는 `docs/LANGUAGE_EXPANSION_CHECKLIST.md`를 따른다.
- iPad 보충 (Issue #10): 기존 단일 앱을 iPhone+iPad Universal로 확장하고 iPad 네 방향·가변 창을 지원한다(§12.2).
- 물리 키보드 보충 (Issue #17): 초기 온보딩의 내장/기기 키보드 선택은 첫 가 입력과 챕터1~4까지 유지하며, 다음 두벌식 키·권장 운지만 안내하고 실제 운지는 채점하지 않는다.
- 대상 독자: 코딩 에이전트(Claude Code / Codex), 개발자
- 활성 플랫폼: iOS/iPadOS 네이티브(Swift/SwiftUI). `android/` Kotlin/Compose 포트는 참고용 동결 상태다.
- MVP 범위: 코어(타이핑 학습) + 게임성(콤보·파티클·오브젝트 흐름 모드·스트릭)
- 제품명/브랜드: 앱 언어가 일본어면 `ピヨキー`, 영어·스페인어·독일어·프랑스어 또는 미지원 언어의 기본값이면 소문자 `typee`를 사용한다. 이 규칙은 스토어 국가가 아니라 기기 선호 언어와 사용자가 선택한 앱 언어를 기준으로 하며, 내부 프로젝트 코드·경로·모듈명 `Hanco`, 기존 Bundle ID와 Game Center ID는 유지한다.
- 배포 레코드: 기존 App Store Connect 앱 ID `6794853985` / Bundle ID `app.piyokey.Piyokey`의 **단일 앱 레코드**를 전 세계에 확장한다. 국가별 이름을 만들기 위한 중복 앱·별도 바이너리는 만들지 않고 App Store 메타데이터와 `CFBundleDisplayName`을 로케일별로 제공한다.
- App Store 표시명: 현지 검색 의도를 설명하는 기능명 뒤에 ` - `와 브랜드를 붙인다. 일본어는 `韓国語タイピング - ピヨキー`, 영어는 `Korean Typing - typee`, 한국어는 `한글 타자 연습 - typee`를 기준안으로 사용하며, 영어 기준안이 해당 로케일에서 저장되지 않을 때만 `Hangul Keyboard - typee`를 대체 후보로 사용한다.
- 앱 1.1 언어 범위: 앱 UI는 `ja`·`en`·`es`·`de`·`fr`, 미지원 언어는 영어 UI로 표시한다. 한국어 학습 원본과 기존 ja/en/ko 콘텐츠는 보존하며 es/de/fr UI의 공식 콘텐츠 뜻·메타데이터·편집 언어는 해당 언어를 사용하고 공통 로마자 읽기는 유지한다. 기존 스토어 메타데이터 6개 로케일과 10개 시장 미디어 현지화 작업은 유지하되 실제 UI 변경에 맞춰 한국어 스토어는 영어 UI, es/de/fr 스토어는 각 해당 UI로 최종 후보를 다시 촬영한다.
- 앱 1.1 판매 범위: App Store Connect의 `All Countries or Regions`와 향후 추가 storefront 자동 포함을 선택하고, `pro` IAP도 앱과 동일한 전체 storefront availability를 사용한다. 별도 국가 wave는 운영하지 않는다. 다만 Apple 또는 현지 규제로 특정 storefront가 `Action Required`·판매 불가 상태가 되면 전체 선택은 유지한 채 해당 지역의 필수 정보와 상태를 별도 추적한다.

---

## 0. 에이전트 작업 지침 (읽고 시작할 것)

- 이 문서는 단일 소스 오브 트루스다. 모호한 부분은 이 문서의 "설계 원칙"에 맞춰 보수적으로 결정하고, 결정 사항을 코드 주석이 아닌 `DECISIONS.md`에 기록한다.
- iOS/iPadOS를 유일한 활성 네이티브 제품으로 개발한다. 공유 스펙은 Python reference, Swift와 웹 Builder의 활성 소비자 계약으로 검증한다. 동결된 Android 포트의 동등성 유지나 회귀 실행은 요구하지 않는다.
- 입력은 이중 모드다: 기본은 앱 내장 커스텀 키보드 뷰, 옵션으로 OS 키보드(한국어 IME) 모드를 지원한다 (§F2a). 학습 가이드·판정 정밀도는 내장 키보드가 기준이며, OS 키보드 모드는 "실전 연습"용 보조 모드다.
- 오프라인 우선 앱이다. 공식 학습 덱 카탈로그는 **읽기 전용 원격 소스**(정적 JSON/CDN, §8.3)에서 가져온다. 계정·업로드·쓰기 API는 없다. 다운로드한 덱은 로컬 저장되어 오프라인 플레이 가능해야 한다.
- 출시판 카탈로그는 운영팀이 검수한 공식 덱만 노출한다. 아티스트·아이돌 고유명사, 실제 가사·대사의 직접 인용 기반 콘텐츠와 실제 근거가 없는 인기·급상승 수치는 노출하지 않는다.
- 모든 사용자 노출 문자열은 하드코딩하지 않고 로컬라이제이션 리소스로 관리한다. 앱 UI 지원 범위는 `ja`·`en`·`es`·`de`·`fr`다. 첫 실행은 기기 선호 언어에서 다섯 언어 중 첫 일치를 사용하고 일치하지 않으면 `en`으로 시작한다. 기존 저장 UI 언어 `ko`는 `en`으로 전환하며 일본어·영어의 명시적 선택은 유지한다. 학습 한국어 목표·기존 ja/en/ko 뜻·읽기·발음·덱/항목 ID는 보존한다. 새 언어는 선택적 현지화 필드로 추가하고, 미제공 사용자 덱은 영어 fallback을 사용하며 원문을 덮어쓰지 않는다.
- 수량은 해당 언어의 단수/복수 규칙을 적용하고 숫자 서식은 선택 앱 locale을 사용한다. 구매 가격·통화는 플랫폼의 현지화된 표시 문자열을 그대로 사용한다. 동일 원문이어도 학습 항목과 꾸미기 소품처럼 문맥이 다르면 번역 키를 합치지 않는다. 현재 적용 범위와 남은 검수는 `docs/LANGUAGE_REVIEW.md`에 기록한다.

---

## 1. 배경과 근거 (리서치 요약)

### 1.1 시장
- 일본 Duolingo 사용자의 22%가 한국어 학습 중. 영어에 이어 2위 언어로 고착. 동력은 K-POP·한국 드라마 팬덤("推し活").
- 학습 목적이 시험이 아니라 팬 활동(아이돌 SNS 읽기/댓글, 팬레터, 검색)인 층이 두텁다.
- 한글을 "읽을 줄은 아는데 입력을 못 해서" 검색·댓글을 못 하는 팬층이 실질 타겟.

### 1.2 경쟁 현황
- 기존 한글 타이핑 서비스는 대부분 웹 기반(코리안타이핑, Hancom Taja, konbu-studio)이거나 UI가 단순. 모바일 완성도 높은 앱은 공백.
- 직접 경쟁: **Hangul – type korean** (1인 개발, 2025.2 출시)
  - Google Play 약 19만 다운로드, 최근 30일 1.1만 (일 370). 평점 4.6~4.8.
  - 검증된 강점: 내장 키보드(OS 설정 불필요), 자모 조합 학습 효과, 타이핑 사운드, 저렴한 프리미엄.
  - 검증된 불만(리뷰 기반): ① 광고 과다 — "단어 하나마다 광고", 타이머 중 광고 삽입으로 기록 파괴 ② 입력 판정 버그 — 정답 자모 오답 처리, 유사 글자 오판정 ③ 콘텐츠 편중 — 음식 단어 위주, 카테고리 선택 불가 ④ 작은 글자, 불명확한 "다음" 버튼.
- 일본 타이핑 문화의 기준점: 寿司打(스시다) — 흘러가는 오브젝트(초밥 접시), 정확도 보너스로 시간 연장, "본전 뽑기" 점수 은유, 친숙한 단어의 리듬감.

### 1.3 전략 (이 앱이 이기는 방법)
1. 포지셔닝: **"가입 없이 바로 시작하는 한국어 타이핑 학습 앱"**. 표준 두벌식 배열과 한글 조합 원리에 기반한 연습임을 첫 진입에서 명확히 알린다.
2. **핵심 루프는 원탭 학습이다.** 홈에서 이어하기 또는 목적 기반 데일리 챌린지를 탭 → 3분 연습 → 출석 스탬프와 피요 보상으로 복귀한다.
3. 경쟁 앱의 3대 불만을 출시 시점부터 역이용: 광고는 세션 밖에서만, 판정은 네이티브 수준의 조합 오토마타로 정확하게, 콘텐츠는 덱 선택제.
4. 일본 유저가 이미 아는 게임 문법(寿司打식 흐름 모드 + 콤보/파티클 + 카와이 팝 UI)을 차용.
5. 일본 특화 학습 UX는 유지하되, 영어 UI에는 `typee`로 같은 학습 엔진과 공식 덱을 제공한다. 영어 UI에는 기존 영어 뜻과 로마자 읽기를 제공한다. 한국어 학습 콘텐츠 원본은 유지한다.

주: MVP에서는 **덱 업로드/제작 도구를 만들지 않는다**. 이 역사적 MVP 경계는 유지하되, 다음 앱 업데이트 1.1에서는 서버 업로드가 아닌 로컬 `.typedeck` 문서 가져오기와 유료 모바일 Deck Maker만 §F5.9 범위로 추가한다. 운영자가 배포하는 카탈로그는 계속 검수된 정적 콘텐츠만 제공한다. 대학·사전·시험기관 등의 명칭은 실제 제휴·감수 계약과 표시 허가가 있을 때만 신뢰 배지에 사용할 수 있다.

---

## 2. 목표와 성공 지표

### 2.1 제품 목표
- G1. 한글 자판(두벌식)을 모르는 학습자가 온보딩 완료 후 30일 내에 "보고 치기"에서 "안 보고 치기"로 전환된다.
- G2. 타이핑 연습이 "해야 하는 공부"가 아니라 "하고 싶은 게임"으로 느껴진다 (D7 리텐션로 측정).
- G3. 경쟁 앱의 광고 불만을 반사이익으로 흡수한다 (스토어 리뷰에서 광고 관련 부정 리뷰 0 유지).

### 2.2 성공 지표 (MVP 출시 후 90일)
| 지표 | 목표 |
|---|---|
| 온보딩 완료율 (설치→레슨1 완료) | ≥ 60% |
| 첫 홈 원탭 연습 시작률 (온보딩 완료 후) | ≥ 70% |
| 주 3일 이상 출석 사용자 비율 | ≥ 25% |
| D7 리텐션 | ≥ 20% |
| 평균 세션 길이 | 3~7분 (짧은 습관 설계 의도 범위) |
| 스토어 평점 | ≥ 4.6 |
| 크래시 프리 세션 | ≥ 99.5% |

### 2.3 비목표 (MVP에서 하지 않는 것)
- MVP의 **사용자 덱 업로드·제작·편집, 자동 게시, 앱 내부 검수/신고 서버**는 비목표다. 단, F5.8의 외부 운영 메일 접수는 허용하며 접수 내용이 카탈로그에 직접 반영되지는 않는다. 앱 1.1의 §F5.9는 서버 업로드·자동 게시 없이 로컬 파일 가져오기와 기기 내 유료 제작·편집만 추가하는 MVP 이후의 명시적 예외다.
- 계정/로그인, 서버 동기화, 소셜 랭킹 서버, 댓글·리뷰 작성
- 회화·문법·발음 학습 (타이핑과 그 부산물인 단어 암기까지만)
- 구독 결제, 광고 SDK 탑재 (수익화는 MVP 검증 후 v1.1+에서, §11 가드레일 준수)
- 별도 iPad 앱·Bundle ID, iPad 전용 sidebar·별도 내비게이션, PC·macOS 앱 (§12.2의 Universal 적응형 화면은 명시적 지원 범위)
- 세벌식 등 두벌식 외 자판

---

## 3. 타겟 사용자

**주 페르소나: 유이 (23세, 일본, 회사원)**
- 한국 여행·SNS를 통해 한글을 자주 보며, 한글을 더듬더듬 읽을 수 있음 (자모는 앎).
- 목표: 스마트폰 한국어 자판 위치를 익혀 검색·메시지를 직접 입력하기.
- 스마트폰에 한국어 키보드를 추가해봤지만 자판 배열을 몰라 포기.
- 하루 통근 지하철에서 10~15분 모바일 게임/SNS. 寿司打를 학교에서 해본 세대.
- 광고에 민감, 귀여운 UI에 지갑을 여는 소비 성향.

**부 페르소나: 미호 (35세, 주부)** — 한국 드라마 팬, TOPIK/한글능력검정에 막연한 관심, 데스크톱 없음(스마트폰 온리).

**글로벌 확장 페르소나: Alex (27세, 영어권, 직장인)** — 한글을 읽기 시작했고 K-콘텐츠 검색·메시지를 위해 모바일 두벌식 입력을 익히려 함. 영어 뜻과 로마자 읽기를 보조 단서로 사용하되 한국어 목표를 직접 입력하는 학습 흐름은 일본어 사용자와 동일하다.

---

## 4. 정보 구조 / 화면 목록

```
[탭바] ホーム | さがす(발견) | 練習 | ゲーム | マイページ
[공통] 모든 탭 우측 상단의 작은 기어 버튼 → 설정

S1  온보딩 (첫 실행): 신뢰·무계정 안내 + 관심사 선택 → 레벨 선택 → 자판 소개 → 보상형 첫 입력 → 연습 탭의 부화 미션(챕터1~3)
S2  홈: MY 피요+7일 스탬프 결합 카드, 이어하기/목적 기반 데일리 원탭 CTA, 주간 피요컵·랜덤 단어 5개 보조 CTA, 공식 추천
S3  덱 발견 (さがす): 공식 기초·TOPIK·여행·요즘 한국어 콘텐츠 브라우징·검색 (§F5)
S3a 덱 상세: 미리보기, 통계, 다운로드 버튼 (§F5.4)
S4  연습 콘텐츠: 레슨(커리큘럼) 스테이지 맵 (하단 자유 연습 진입 카드 없음)
S5  타이핑 연습 화면 (핵심 화면): 상단 진행 게이지 + 문제 표시 + 조합 프리뷰 + 내장/OS 키보드
S6  결과 화면: 점수, 정확도, 틀린 글자, "이 덱과 비슷한 덱" 추천, 공유 이미지
S7  게임 모드: 2열 게임 카드 허브에서 `흐름·산성비 / 초성 맞추기·받아쓰기 / 단어 맞추기·띄어쓰기` 순서로 모드 선택 → 덱 기반 게임은 모드별 전용 3단계와 추가한 덱 목록, 띄어쓰기는 긴 글 목록 → 시작
S8  마이페이지: 피요 프로필·성장 기록·설정 바로가기 + 설치 덱 정렬·가져오기·내보내기·삭제와 복습 덱(자동 수집)
S9  설정: 각 탭 우측 상단에서 여는 공통 시트. 글자 크기, 연습 알림, 효과음/햅틱, 입력 모드, 테마, 언어, 캐릭터 관리
S10 사용자 덱: `.typedeck` 검증·미리보기·충돌 선택 + `pro` 생성·편집·공식 덱 사본 편집·비소모성 IAP 구매/복원 (§F5.9)
```

### 4.1 v6.8 구현·화면 기준선

아래 표는 2026-08-29 `origin/main` `2b25a7d`의 iOS 소스와 같은 SHA로 새로 빌드한 iPhone 17 / iOS 26.5 화면을 기준으로 한다. “구현”은 소스와 자동화 가능한 화면 경로가 있다는 뜻이며 App Store 제출·실기기·결제·권리 gate 통과를 뜻하지 않는다. Android는 같은 제품 계약의 소스 후보가 있으나 Issue #19의 동일 signed AAB 실기기 통합 QA 전까지 출시 완료가 아니다.

| 화면 | iOS `main` | 이번 캡처 | 남은 경계 |
|---|---|---|---|
| S1 온보딩·레벨·부화 | 구현 | 온보딩 레벨 | 실제 알림 수신 #58 |
| S2 홈·MY 피요·데일리 | 구현 | 홈·출석·MY 피요 | 개인화/다음 단계 2행은 PR #82 미통합 |
| S3 발견·S3a 덱 상세 | 구현 | 검색·덱 상세 | 운영 HTTPS 카탈로그·권리 gate |
| S4 커리큘럼 | 구현, 자유 연습 진입 제거 | 스테이지 맵 | 6챕터·12스테이지; 챕터5 단어 3개·챕터6 표현 5개 포함 |
| S5 타이핑 연습 | 구현 | 두벌식·10키 진행 화면 | OS IME·물리 키보드 최종 RC 재확인 |
| S6 공통 결과 | 구현 | 연습·게임 결과 | 공유·저장 권한 실기기 재확인 |
| S7 게임 허브·6모드 | 구현 | 허브·흐름·산성비·초성·단어 | iPhone 12급 60fps와 Game Center #7 |
| S8 마이페이지·내 덱·MY 피요 | 구현 | MY 피요·내 덱 액션 | Files/iCloud/AirDrop 실기기 gate |
| S9 설정 | 구현 | 설정 | 개인정보 선택·외부 URL 최종 RC 확인 |
| S10 `.typedeck`·`pro` | 구현 | import 미리보기·편집기 | Sandbox 구매/복원/환불·IAP 심사 |

저장소 감사와 24장 화면 목록은 `artifacts/issue-83/README.md`에, 로컬 캡처 원본과 `xcresult`는 같은 디렉터리에 둔다. 캡처에 보이지 않는 상태를 구현 완료로 추정하지 않으며, branch·worktree·PR의 현재 상세는 `PROJECT_STATUS.md`의 “2026-08-29 저장소 감사” 표를 따른다.

- 탭 구조 원칙: **さがす 탭이 두 번째 자리**(엄지 접근성)에 오고, 홈 첫 화면에서는 덱 탐색보다 즉시 시작 CTA가 먼저 보여야 한다. 설정은 콘텐츠 탭을 차지하지 않으며 모든 탭에서 같은 우측 상단 버튼으로 접근한다.
- S5 세로 공간 원칙: 내비게이션 제목은 표시하지 않고 `닫기 | 전체 진행 게이지·문제 수 | 설정`을 한 줄에 둔다. 듣기·목표·뜻·읽기·자모는 하나의 문제 카드에 모으고, 조합 카드의 `프리뷰` 제목과 조합 카드~키보드 사이의 상태 문구·지표·보조 버튼은 화면에서 제거한다. 피요와 조합 프리뷰는 내장/OS 키보드 모두에서 키보드 바로 위에 최대한 유지한다.
- 문제 카드의 목표·현재 앱 언어의 뜻·읽기는 사용자가 순서를 바꿀 수 있다. 일본어는 일본어 뜻+가타카나 읽기, 영어·스페인어·독일어·프랑스어는 각 언어의 뜻+공통 라틴 문자 로마자 읽기를 사용한다. 한국어 UI 설정은 영어로 전환하며, 기존 영어 콘텐츠를 표시할 뿐 학습 원본은 변경하지 않는다. 지원하지 않는 언어는 영어로 해석하며, 일본어 이외의 앱 언어에서 일본어 뜻·가타카나를 대체값으로 노출하지 않는다. 목표는 큰 굵은 글씨로 표시한다. `ko` 목표 문구는 공백을 포함해 최대 10자로 제한하며, 목표 영역은 횡스크롤을 제공하지 않고 10자가 카드 폭을 넘으면 음절 칩과 글자 크기를 함께 축소해 한 화면에 모두 표시한다. 목표 안의 공백은 투명한 간격으로 두지 않고 작은 스페이스 키 모양으로 표시하며, 현재 입력할 공백은 강조 테두리, 입력을 마친 공백은 초록색 채움으로 진행을 구분한다. 완성한 음절은 초록색 채움만으로 구분하고 별도 체크 아이콘은 표시하지 않는다. 뜻·읽기는 목표의 약 절반 크기인 일반 굵기의 회색 글씨로 표시하되, 뜻은 따옴표 없이 내용 길이만큼의 아주 옅은 하늘색 캡슐 워드박스로 감싸고 읽기는 배경 없이 뜻보다 낮은 68% 글자 농도로 표시해 구분한다. DB 원문에도 장식용 따옴표를 저장하지 않는다. 기본 순서는 `목표 → 뜻 → 읽기`다.
- 목표·뜻·읽기·자모·피요·조합/입력 중 키는 각각 표시 ON/OFF를 제공한다. 최초 기본값은 목표·뜻·자모·피요·조합/입력 중 키 ON, 읽기 OFF다. 피요와 조합을 모두 끄면 해당 카드 자체를 제거해 키보드 위 세로 공간을 회수한다.
- 자모 영역은 입력할 자모 칩만 표시하며 `입력할 자모` 제목과 `완료/전체` 숫자 카운터는 화면에 표시하지 않는다. 짧은 자모열은 카드 가로 중앙에 배치하고, 화면 폭을 넘는 자모열은 현재 입력 칩을 중앙으로 자동 추적한다. 진행 상태는 완료 칩 색상과 현재 칩 테두리만으로 전달한다. 목표 음절과 자모의 활성 테두리는 칩 안쪽으로 그리며 스크롤 영역 끝에도 안전 여백을 두어 어느 방향에서도 잘리지 않아야 한다.
- 단어 완료 연출은 피요·조합 카드의 레이아웃에 포함하지 않고 타이핑 화면 전체 중앙에 오버레이한다. 중앙 하트는 표시하지 않으며 별·스파클·점 파티클만 폭죽처럼 퍼지고, 입력을 가로막거나 카드·키보드 배치를 밀지 않는다.

---

## 5. 기능 요구사항

각 기능은 `[P0]`(MVP 필수) / `[P1]`(MVP 포함하되 축소 가능) 로 표기. 수용 기준(AC)은 테스트로 구현할 것.

### F1. 온보딩 [P0]
- 4단계: ① 검증 가능한 신뢰 문구와 무계정 안내 + 관심사 선택(키보드 입문 / 여행·생활 / 검정·TOPIK / SNS·요즘 한국어) ② 한국어 입력 레벨 선택(처음 시작 / 자모를 앎 / 쉬운 단어 입력 / 문장 입력) ③ 두벌식 소개 1장("왼손=자음, 오른손=모음" 시각 다이어그램) ④ 레슨1 즉시 체험(가입 없이). 레벨은 사전 선택하지 않고 명시적으로 선택해야 다음으로 진행한다. 돌아가기 버튼은 제공하지 않으며 소개 건너뛰기는 유지한다. 관심사와 같은 2열 카드 레이아웃에 아이콘 대신 난이도 예시 `ㄱ · ㅏ` / `가 · 나` / `학교` / `같이 걷고 싶어요`를 표시하며 선택은 기기에 저장한다. 기존 완료 사용자는 온보딩을 다시 하지 않고, 기존 진행 중 단계 ID를 유지한다.
- 신뢰 문구는 "표준 두벌식 배열과 한글 조합 원리에 기반", "계정 등록 없음·학습 기록은 기기 내 저장"을 기본으로 한다. 제휴·감수 근거가 없는 기관명은 사용 금지.
- 목표 선택은 이후 추천 콘텐츠팩과 데일리 챌린지 정렬에만 사용하며 커뮤니티·로그인 생성을 요구하지 않는다.
- 첫 입력에서는 나머지 화면을 디밍하고 다음 키와 힌트만 강조한다. `ㄱ → ㅏ`를 완성하면 알이 깨지는 연출과 첫 보상을 즉시 보여 준다. 이 코치마크는 온보딩에서만 사용하고 일반 세션에는 모달을 띄우지 않는다.
- 두벌식 소개 단계에서 `내장 키보드로 시작`과 `기기 키보드로 시작` 중 하나를 선택한다. 선택 버튼이 기존 체험 시작 버튼을 겸해 첫 실제 입력은 두 방식 모두 6번째 상호작용 이내에 발생한다. 기기 키보드는 연결을 단정적으로 감지하지 않고 iOS/iPadOS와 Android의 한국어 두벌식 IME committed/marked 입력을 사용하며, 선택은 첫 `가` 입력과 챕터1~3 부화 미션 및 이어지는 챕터4에 유지된다. 한국어 입력 소스가 없을 때만 첫 세션 전에 설치 안내를 제공한다.
- 첫 입력 뒤에는 추천 덱·게임·홈으로 분기하지 않고 연습 탭의 부화 미션으로 바로 연결한다. 챕터1 기본 자음 → 챕터2 기본 모음 → 챕터3 글자 조합을 순서대로 완료하도록 현재 미션 하나만 활성화한다.
- 부화 미션 중에는 탭바를 노출하지 않으며 일반 덱, 발견, 게임, 홈, 마이페이지에 진입할 수 없다. 앱을 종료·재실행해도 완료한 챕터와 진행 중 체크포인트에서 이어 간다. 각 부화 미션의 결과 화면에서는 `もう一回`를 노출하지 않고, 아래의 주 CTA로 다음 미션을 바로 시작한다. 챕터3의 CTA는 결과를 확인한 뒤 전체 앱을 연다.
- 성장 단계는 소개 전 `알`에서 첫 `가` 입력 후 `금이 간 알`, 챕터1~2의 `부화 중`, 챕터3의 `병아리`로 전진하며 이전 단계로 되돌아가지 않는다. 챕터1 결과를 확인하고 다음 미션으로 진행할 때 `금이 간 알 → 부화 중` 전환 연출과 닉네임 입력을 먼저 완료하며, 그 전에는 `부화 중` 피요를 노출하지 않는다. 챕터3 결과를 확인한 뒤 최종 성장 연출을 거쳐 전체 탭을 열고 홈 탭에 머문다.
- 부화 완료 직후 홈의 오늘 학습 CTA → 둘러보기 검색 → 연습 커리큘럼 → 게임 모드 → 마이페이지의 피요·성장 기록 → 전 화면 공통 설정 순서로 6단계 메인 앱 튜토리얼을 1회 노출한다. 각 단계는 해당 탭으로 자동 이동하고 설명 대상만 스포트라이트로 남긴 채 나머지 화면과 입력을 딤 처리한다. 화면 탭으로 다음 단계, 별도 건너뛰기로 즉시 종료할 수 있으며 완료·건너뛰기 상태는 로컬에 저장한다.
- 소개 4단계의 스킵은 설명과 첫 입력만 건너뛰며, 챕터1~3 부화 미션 게이트는 건너뛰지 않는다.
- AC: 첫 실행에서 6번째 탭 이내에 실제 타이핑 입력이 발생한다. 소개는 스킵 가능. 챕터3 완료 전 일반 덱·게임 진입 불가, 챕터3 완료 후 홈 랜딩과 6단계 튜토리얼을 거쳐 전체 탭 진입 가능. 완료한 메인 앱 튜토리얼은 다음 실행에서 자동 재노출하지 않는다.

### F2. 내장 한글 키보드 뷰 [P0] — 기본 입력 모드
- 앱 내 커스텀 키보드 (기본값). 두벌식 표준 배열 (Shift로 된소리 ㅃㅉㄸㄲㅆ, 이중모음 ㅒㅖ).
- iOS는 OS 키보드 모드와 별도인 내장 키보드 배열을 선택할 수 있고 기본값은 계속 두벌식이다. 표시 명칭은 de=`2-Set`/`Cheonjiin (10 Tasten)`, en=`2-Set`/`Cheonjiin (10-key)`, es=`2-Set`/`Cheonjiin (10 teclas)`, fr=`2-Set`/`Cheonjiin (10 touches)`, ja=`2ボル式`/`天地人（10キー）`, ko=`두벌식`/`천지인(10키)`를 사용한다.
- 한국어 10키는 Apple 한국어 10키의 핵심 자판과 같은 `ㅣ · ㆍ · ㅡ / ㄱㅋ · ㄴㄹ · ㄷㅌ / ㅂㅍ · ㅅㅎ · ㅈㅊ / → · ㅇㅁ · 백스페이스` 3×4 배열과 별도 스페이스 바를 사용한다. 묶음 자음은 같은 키를 평음→격음→경음 순서로 반복 입력하고, 같은 묶음 자음이 다음 자모로 연속될 때는 `→`로 경계를 확정하며, 모음은 `ㅣ·ㆍ·ㅡ` 획 순서를 사용한다. 19자음·21모음·스페이스 recipe와 연속 묶음 자음 경계를 단위 테스트로 고정한다.
- 10키 raw 획은 순수 `Korean10KeyInterpreter`가 보유하고 목표의 다음 자모 recipe가 완성됐을 때만 표준 호환 자모 이벤트 하나를 기존 HangulEngine·자모 판정기에 전달한다. 미완성 획은 조합 프리뷰에 표시하되 정타·오타로 세지 않는다. 잘못된 10키 액션은 오타 1회로 기록하고 목표 문자열에는 반영하지 않는다.
- Backspace는 미확정 10키 획을 한 단계 먼저 되감고, 미확정 획이 없을 때만 기존 HangulEngine 자모를 하나 되감는다. 내장 배열은 세션 시작 시 확정하고 세션 도중에는 바꾸지 않는다.
- 챕터1~4 자판 학습은 두벌식으로 고정하고 챕터5+, 덱 연습에 선택 배열을 적용한다. 흐름·산성비·초성 맞추기·단어 맞추기·받아쓰기의 내장 입력도 세션 시작 시 선택 배열을 확정해 같은 인터프리터를 사용한다. 경계 선택형 띄어쓰기는 키보드 배열과 무관하며, 주간 피요컵은 공정한 기존 계약을 위해 두벌식으로 고정한다. 10키 점수는 전용 로컬 기록으로 분리하고 기존 Game Center 두벌식 리더보드에 제출하지 않는다.
- 시각 어포던스 (§7 상세):
  - iPhone과 iPad에서 다음에 칠 키 하이라이트 (색 + 글로우)를 제공하고 설정에서 "가이드 오프"가 가능하다 (졸업 모드).
  - 키 눌림 마이크로 애니메이션 (scale 1.0→0.95→1.0, ≤120ms).
  - 키캡에 자모 + 작은 로마자 병기 표시 옵션 (기본 ON, 설정에서 OFF).
- 햅틱: 키 다운 시 경량 햅틱 (iOS: `UIImpactFeedbackGenerator(.light)`), 설정에서 OFF 가능.
- AC: 터치 다운→화면 반영 레이턴시 p95 ≤ 50ms. 동시 멀티터치 롤오버(이전 키를 떼기 전 다음 키 입력) 정상 처리. 10키의 전체 golden recipe·미확정 Backspace·자모 시퀀스 판정과 기존 두벌식 회귀가 통과하며, 3×4 키보드는 좁은 화면·큰 글자에서도 화면 폭 안에 있고 VoiceOver가 각 묶음의 모든 출력 자모를 읽는다.

### F2a. OS 키보드 모드 [P1] — 실전 연습용 보조 입력
- 연습/게임 화면의 우측 상단 설정 메뉴에서 입력 모드를 전환할 수 있다: `내장 키보드(기본)` ↔ `OSキーボード`. 키보드 위에는 상시 전환 바를 두지 않는다.
- 목적: 사용자가 실제로 쓰게 될 자기 스마트폰의 한국어 IME(플릭·10키·QWERTY 등 무엇이든)로 실전 연습. "앱에서만 칠 수 있는 사람"을 만들지 않기 위한 출구.
- 동작:
  - 조합 프리뷰 위에 겹친 숨겨진 텍스트필드로 IME 입력을 받는다 (iOS: `UITextField` + `textDidChange`, Android: `EditText`/`BasicTextField` + IME). iOS의 `OSIMEInputPanel`은 iPhone에서 compact 포커스 복구 UI를 표시하지 않되 전체 입력 영역의 탭과 포커스 생명주기를 유지하고, iPad에서는 기존 compact 포커스 복구 패널을 표시한다. 앱 소유 물리 키보드 참조 배열도 iPhone에서는 렌더하지 않고 iPad에서만 제공한다.
  - 사용자가 한국어 키보드를 아직 추가하지 않은 경우 감지하여 설정 방법 안내 시트 표시 (iOS/Android별 스크린샷 가이드, 현재 앱 언어).
- 판정 차이 (중요 — 조합 엔진과 별도 판정기):
  - OS IME는 자모 키 이벤트가 아니라 조합 중/확정 텍스트를 넘기므로, 자모 시퀀스 판정(§6.3) 대신 **텍스트 diff 판정**을 쓴다: 입력 텍스트를 자모 시퀀스로 분해(NFD 유사 분해, §6 엔진의 역함수)한 뒤 목표 자모 시퀀스와 전방 일치 비교. 조합 중간 상태(마지막 음절 미완성)는 오타로 치지 않는다.
  - 오타 카운트는 "확정된 불일치"만 집계 (조합 중 흔들림 무시).
- 기능 제약 (명시적으로 허용되는 열화):
  - OS 키보드 자체의 키 눌림 연출은 제어하지 않는다. 사용자가 기기 키보드 학습 가이드를 선택한 경우에는 앱 소유의 QWERTY 두벌식 참조 배열에서 다음 키와 권장 손·손가락을 표시한다.
  - 레슨 챕터1~4의 기본값과 내장 배열은 두벌식으로 고정한다. 온보딩 또는 설정에서 기기 키보드를 명시적으로 선택한 사용자는 같은 `OSIMETextJudge` 판정과 참조 배열로 챕터1~4도 완료할 수 있다. 내장 한국어 10키는 챕터5+(단어/문장)와 지원 게임에서만 허용한다.
  - 기록 구분: 게임 스코어에 입력 모드 태그 저장 (`input_mode: builtin | builtin_korean_10key | os_ime`). 랭킹·자기 기록 비교 시 모드별 분리 표시하며 `builtin`만 두벌식 Game Center 랭킹에 제출한다.
- Bluetooth·USB·Magic Keyboard 등 물리 키보드는 별도 raw key mapper를 만들지 않고 OS 키보드 모드의 표준 `UITextField` 입력 경로를 그대로 사용한다. 사용자는 iOS의 한국어 두벌식 입력 소스를 선택해야 하며, 앱은 확정·조합 텍스트 diff로 판정한다. Backspace는 수락된 자모 진행을 되감고, Space는 목표에 포함된 위치에서만 수락하며, Return은 세션을 제출하거나 개행하지 않고 입력 포커스를 유지한다. 한/영 전환과 키 반복은 OS가 처리하고 연결 해제·재연결 및 백그라운드 복귀 뒤에는 현재 문제·입력 진행을 보존한 채 포커스를 복구한다. 물리 키보드 지원은 iPhone과 iPad의 iPhone 호환 실행 모두에 적용하되 iPad 적응형 화면 지원 범위와는 분리한다.
- OS 키보드 모드에서 영문 ASCII 입력이 들어오면 진행·점수·오타·현재 문제를 변경하지 않고 입력을 버린다. 현재 앱 언어로 영어 키보드 상태와 한/영 전환 방법을 세션 안에서 안내하고, 안내가 보이는 동안 영문 입력이 반복되면 안내 카드를 흔들고 색을 잠깐 강조한다(Reduce Motion에서는 색 강조만 사용). 한국어 두벌식으로 전환한 다음 입력부터 같은 문제를 계속한다. 이 안내는 특정 Bluetooth 기종의 연결 여부를 단정하지 않는다.
- AC: 내장↔OS 모드 전환 시 진행 중 세션 상태 유지. 한국어 IME 미설치 상태에서 크래시 없이 안내 표시. 영문 ASCII 입력은 비진행·비오타이며 ja/en/es/de/fr 전환 안내를 표시하고 한국어 입력 시 같은 문제를 이어 간다. diff 판정기는 §6.4 테스트 벡터의 확정 텍스트 버전으로 검증.

### F3. 한글 조합 엔진 [P0] — 품질의 핵심
§6에 별도 상세 스펙. 경쟁 앱의 "정답인데 오답 처리" 불만이 최대 기회이므로 이 모듈의 테스트 커버리지를 최우선으로 한다.

### F4. 커리큘럼 (레슨 모드) [P0]
- 구조: 챕터 → 스테이지 → 문제(자모/글자/단어/문장).
  1. 챕터1 기본 자음 (ㄱㄴㄷㄹㅁㅂㅅㅇㅈ…) — 키 위치 암기
  2. 챕터2 기본 모음 (ㅏㅓㅗㅜㅡㅣ…)
  3. 챕터3 글자 조합 (자음+모음: 가, 나, 노, 무…)
  4. 챕터4 받침 (간, 밥, 강…) — 받침 개념 시각 강조
  5. 챕터5 단어 — 일상 단어, 여행 단어, 학업·업무 단어의 3개 스테이지
  6. 챕터6 표현 — 띄어쓰기, 기본 표현, 여행 표현, 일상 대화, 팬 응원 표현의 5개 스테이지
- 각 스테이지: 문제 10~15개, 클리어 조건 정확도 ≥ 80%, 별 1~3개 (정확도/속도 기준).
- 진행 잠금: 직전 스테이지 클리어 시 해금. (단, 챕터5 이후는 자유 선택)
- 일본어 사용자는 모국어 문장에 띄어쓰기가 없는 점을 고려해 챕터6에서 띄어쓰기를 별도 스테이지로 먼저 제시한다. 모든 문제는 정확히 한 번의 스페이스 입력을 포함하고, 목표의 스페이스 키 표식과 다음 키 가이드로 입력 위치를 안내한다.
- AC: 스테이지 중단 시 진행 상태 복원. 앱 백그라운드 전환 시 타이머 일시정지 (경쟁 앱 버그 이력 있음 — 회귀 테스트 필수).

### F5. 공식 학습 덱 발견·다운로드 [P0]
콘텐츠는 전부 "덱" 단위다. 출시판에는 운영자가 검수한 공식 덱만 노출하고 클라이언트는 읽기 전용으로 소비한다 (§2.3).

#### F5.1 발견 탭 (일본어 `さがす`) 구성 — 위에서 아래 순서
1. **검색바** (상단 고정): 덱 이름·태그·제작자 닉네임 검색. 로컬 캐시된 카탈로그 인덱스 대상 즉시 검색 (서버 검색 API 불필요).
2. **학습 숏컷 칩 행렬**: `#入門` `#子音` `#母音` `#パッチム` `#日常` `#旅行` `#TOPIK` `#今どき`. 온보딩 목표 선택 + 다운로드 이력 기반으로 개인화 정렬.
3. **まずはここから**: 자음·모음·조합·받침 공식 기초 코스.
4. **今どき韓国語**: 뜻·사용 맥락·격식/주의 정도·검수일을 운영 데이터로 관리하는 유행 표현.
5. **目的から選ぶ**: 여행·생활·TOPIK 등 목적별 공식 콘텐츠.
6. **公式コンテンツ**: 전체 공식 덱. 실제 집계 파이프라인이 생기기 전까지 인기·급상승 수치를 섹션 제목이나 추천 근거로 사용하지 않는다.
- 모든 섹션에 "すべて見る" → 전체 목록 (무한 스크롤, 정렬 전환: 인기/신착/항목수).
- 연습 화면의 뜻은 현재 앱 언어에 맞는 짧은 직역을 사용하며 부연 설명이나 사용 주의를 합치지 않는다. 일본어 원본은 `meaning_ja`와 `reading_ja`, 영어·스페인어·독일어·프랑스어는 `items[].localizations.<언어>.meaning`과 `.reading`에서 읽고 읽기는 공통 라틴 문자 로마자로 제공한다. 해당 번역이 없는 사용자 덱은 영어로 fallback한다. 한국어를 포함한 미지원 UI 언어는 영어로 해석한다. 기존 `ko` 항목 현지화 데이터도 보존한다. 일본어 외 앱 언어에서는 어떤 경우에도 `meaning_ja`·`reading_ja`로 대체하지 않는다. 사용 맥락·격식·주의 정보는 향후 별도 상세 메타데이터로 관리하며 타이핑 문제의 뜻 줄에 이어 붙이지 않는다.

#### F5.2 덱 카드 (목록 아이템) — 발견성의 최소 단위
표시 요소: 덱 커버(자동 생성 — 태그 색상 + 이니셜, 이미지 업로드 없음), 덱 이름, 제작자 닉네임, 태그 최대 2개, 항목 수, 난이도 뱃지(단어/문장, 초급~상급), 다운로드 수(略記: 1.2万), 설치됨 체크마크.
- 카드 하나로 "무엇을·누가·얼마나 인기"가 3초 안에 읽혀야 한다.

#### F5.3 필터·정렬
- 필터: 타입(단어/문장), 난이도, 태그(복수), 항목 수 범위.
- 정렬: 인기순(기본), 신착순, 급상승순.
- 필터 상태는 탭 이탈 후에도 세션 내 유지.

#### F5.4 덱 상세 (S3a)
- 헤더: 커버, 이름, 제작자 닉네임, 태그 전체, 통계(다운로드 수·항목 수·평균 글자 길이).
- **미리보기: 첫 10개 항목을 다운로드 없이 표시** (`ko` + 현재 앱 언어의 뜻). 카탈로그 인덱스에 현지화된 `preview_items` 포함 (§8.3) — 다운로드 결정에 필수. 일본어는 일본어 뜻, 영어·미지원 언어(한국어 포함)는 영어 뜻을 표시하며 일본어를 대신 표시하지 않는다.
- "同じタグのデッキ" 가로 추천 행.
- CTA: `ダウンロード` (진행률 표시) → 완료 시 `プレイ` 로 전환. 용량 표시 (수십 KB 수준이므로 즉시 완료가 일반).
- AC: 미설치 덱 상세 진입 → 다운로드 → 플레이까지 3탭 이내.

#### F5.5 다운로드·설치 관리
- 다운로드한 덱은 로컬 저장, 오프라인 플레이 가능. 내 덱(S8)에서 삭제 가능 (기록은 유지).
- 카탈로그 인덱스는 앱 실행 시 백그라운드 갱신 (ETag/If-Modified-Since), 오프라인이면 캐시 사용. 갱신 실패가 UX를 막지 않는다.
- 덱 버전 필드로 업데이트 감지 → 내 덱에 "更新あり" 뱃지, 원탭 갱신.
- AC: 기내 모드에서 설치 덱 전부 플레이 가능. 카탈로그 서버 다운 시에도 발견 탭이 캐시로 렌더링.

#### F5.6 발견성 보조 장치
- 결과 화면(S6)에 "이 덱과 같은 태그의 덱" 추천 2개 노출 — 플레이 직후가 다음 덱 탐색 최적 시점.
- 홈(S2)은 역할이 다른 두 추천 영역을 순서대로 노출한다. 첫 영역 "あなたへのおすすめ"는 다운로드 이력·온보딩 목표 태그 기반의 미설치 공식 덱 3개다. 둘째 영역 "次のステップ"는 최근 공식 덱 연습 또는 커리큘럼 완료 단계와 정확도를 반영해 공식 학습 경로의 다음 미설치 덱 3개를 제안한다.
- 두 영역 사이의 덱 ID는 중복하지 않는다. iPhone·600pt 미만 분할 창은 영역별 독립 가로 스크롤, iPad·600pt 이상 창은 각 영역을 한 줄 3열로 표시한다. 덱 탭은 기존 상세 화면으로 이동한다.
- 다음 단계는 자판 입문→자음→모음→글자 조합→받침→기초 단어→동사·형용사→일상 표현의 결정적 학습 경로를 사용한다. 최근 정확도 80% 이상이면 다음 경로를 우선하고, 그보다 낮으면 현재 덱의 레벨·태그·유형에 가까운 덱을 우선한다. 연습 이력이 없으면 선택 수준과 학습 경로를 사용한다.
- 다운로드 이력이 없는 콜드 스타트에서는 온보딩 목표 태그를 추천 정렬에 반영하고, 이용 이력이 생기면 실제 다운로드 태그를 더 높은 가중치로 사용한다.
- 부화 미션 완료 후 홈의 콜드 스타트 추천 3개에 온보딩 목표 태그를 반영한다. 부화 전에는 추천 덱 다운로드·플레이 CTA를 노출하지 않는다.

#### F5.7 첫 실행 콘텐츠 (콜드 스타트)
- 공식 시드 덱 10종 이상(키보드 입문/자음/모음/글자 조합/받침/기초 단어/동사·형용사/일상 표현/여행/TOPIK I)과 편집형 `今どき韓国語`를 앱 번들에 동봉한다. 네트워크 없이도 첫 플레이 가능하며 모두 `official` 배지로 표시한다.
- 팬 학습 목적에는 특정 작품을 인용하지 않은 운영팀 창작 콘텐츠를 사용한다. `K-POP` 가사풍 3덱과 `Kドラマ` 회화 3덱을 제공하며, 각 덱은 10항목 이상, 각 항목은 공백을 제외한 완성형 한글 2~8음절이고 단어와 짧은 문구를 섞는다.
- 상황별 재미 표현은 여행·카페/맛집·연인과의 갈등·화해·첫 데이트·채팅 플러팅·친구 리액션·주말 모임·최애 아이돌 라이브 댓글의 9덱을 제공한다. 각 덱은 직접 인용이나 조종적 표현 없이 운영팀이 창작한 짧은 회화 12항목으로 구성하며, 갈등 덱은 감정 전달·경청·존중과 화해에 초점을 둔다. 라이브 댓글 덱은 특정 아이돌이나 실제 방송을 인용하지 않고 응원·무대 반응·건강 배려·다음 방송 약속을 다룬다.

#### F5.8 신규 덱 제안·기존 콘텐츠 제보 [P1]
- 발견 목록 하단에는 `ほしいデッキを提案する`, 덱 상세에는 현재 덱 문맥을 가진 `このデッキの内容を報告`, 설정의 앱 정보에는 두 목적을 합친 진입점을 제공한다. 연습·게임 세션 중에는 어떤 진입점도 표시하지 않는다.
- 앱은 계정·업로드·쓰기 API를 추가하지 않고 사용자가 명시적으로 탭했을 때만 OS 메일 작성 화면을 연다. 신규 제안은 주제·대상 레벨·원하는 단어·이유, 기존 콘텐츠 제보는 해당 표현·오탈자/뜻/읽기/음성/권리 유형·상세 설명의 현지화 템플릿을 제공한다.
- 메일에는 접수 유형·진입 화면·앱 버전/빌드·언어만 자동 포함한다. 덱 상세 제보에 한해 `deck_id`와 덱 버전을 추가하며 사용자 ID, 기기 식별자, 학습 기록은 포함하지 않는다.
- 비밀번호·주소 등 개인정보와 실제 가사·드라마 대사를 적지 말라는 안내, 운영팀 검수 전 공식 콘텐츠로 자동 게시되지 않는다는 안내를 작성 본문에 포함한다. 운영팀이 채택한 항목만 로컬 Deck Studio와 기존 스키마 검증을 거쳐 정적 카탈로그로 배포한다.
- AC: ja/en/es/de/fr에서 세 진입점과 메일 제목·본문이 현지화되고, 덱 상세 제보에는 정확한 덱 ID·버전이 포함되며 신규 제안에는 임의 덱 정보가 들어가지 않는다. 메일 앱 사용이 불가능하거나 URL 생성이 실패하면 공개 HTTPS 지원 페이지로 대체한다.

#### F5.9 사용자 덱 문서와 모바일 `pro` 기능 [앱 1.1 P0]

**제품 경계**

- `.typedeck`은 서버에 업로드하는 콘텐츠가 아니라 Files·iCloud Drive·AirDrop·공유 시트로 주고받는 로컬 문서다. 파일 선택·복사·검증·미리보기와 사용자 덱 **동시 보관 3개까지의 신규 설치**, 설치된 덱의 연습/게임·현재본 내보내기·삭제·동일 `deck_id` 교체 재가져오기는 구매 없이 제공한다. 공식·번들·복습 덱은 이 3개에 포함하지 않는다.
- 네 번째 서로 다른 사용자 덱의 신규 설치, 모바일에서 새 사용자 덱 생성, 가져온 사용자 덱 편집, 공식 덱을 사용자 사본으로 복제, 변경 내용 저장은 비소모성/일회성 상품 `app.piyokey.deckmaker.lifetime` 구매가 필요하다. 기존 상품 ID는 구매 호환성을 위해 유지하되 사용자 노출 상품명은 ja=`ピヨキー プロ`, en=`typee pro`, ko=`피요키 프로`로 현지화한다. iOS는 StoreKit 2 non-consumable, Android는 Play Billing one-time product로 구현하고 구매 복원을 제공한다.
- 무료 한도는 누적 열람 횟수가 아니라 현재 설치된 서로 다른 사용자 `deck_id` 수다. 삭제하면 무료 슬롯이 즉시 돌아오며, 동일 ID·동일 SHA의 no-op과 동일 ID 교체는 한도를 소비하지 않는다. 기존 데이터가 3개를 초과하더라도 읽기·연습·게임·내보내기·삭제를 잠그거나 자동 삭제하지 않고 새 ID 추가만 막는다.
- 과금 대상은 파일 자체의 소유·재생 권한이 아니라 3개를 넘는 로컬 보관과 앱 안의 제작 편의 기능이다. package 안에 영수증·라이선스·만료일·`premium` 플래그를 넣지 않고, 구매하지 않은 사용자도 유효한 `.typedeck`을 검증·미리보기하고 무료 슬롯 범위에서 설치할 수 있다.
- 앱 1.1은 로컬-only·무계정이다. 사용자 덱의 클라우드 동기화, 공개 카탈로그 게시, 검색 노출, 좋아요·댓글·신고, 원격 쓰기 API와 사용자 콘텐츠 검수 서버를 만들지 않는다. 공식 정적 카탈로그 §8.3과 사용자 문서 저장소는 분리한다.
- 플랫폼 스토어 entitlement를 확인할 수 없거나 구매가 pending·환불·취소되어도 기존 사용자 덱의 읽기·연습·게임·무료 내보내기·삭제는 막지 않는다. iOS의 검증된 StoreKit entitlement 또는 Android Play Billing의 `PURCHASED` 재조회 결과만 무제한 보관과 제작 권한을 열고, 확인된 entitlement가 없을 때 4번째 새 ID 설치와 새 생성·복제·편집 저장만 잠그며 사용자 데이터를 삭제하거나 워터마크를 넣지 않는다.

**진입과 편집 UX**

- 마이페이지 내 덱 상단에 무료 `デッキを読み込む / Import Deck / 덱 가져오기`와 잠금 상태를 표시할 수 있는 `新しいデッキ / New Deck / 새 덱`을 나란히 둔다. 사용자 덱 메뉴는 `연습 / 편집 / 내보내기 / 삭제`, 공식 덱 메뉴는 `연습 / 사본으로 편집 / 삭제`를 제공한다.
- 가져오기는 SwiftUI file importer와 문서 타입 연결을 모두 지원한다. Files에서 `.typedeck`을 탭하거나 AirDrop·메일·메신저 공유 시트로 앱을 열어도 동일한 검증·미리보기 흐름으로 진입한다. 파일 선택만으로 설치하지 않고 덱 이름·제작자·유형·난이도·태그·항목 수·첫 3항목·충돌 상태를 보여 준 뒤 사용자가 확인한다.
- 무료 사용자가 `새 덱`, `편집`, `사본으로 편집` 또는 3개가 설치된 상태에서 네 번째 새 ID의 `가져오기`를 누를 때만 세션 밖에서 구매 설명 화면을 연다. 1~3번째 가져오기·동일 ID 교체·연습 시작·게임 시작·내보내기에는 구매 화면을 삽입하지 않는다. 네 번째 파일도 결제 전에 검증·미리보기하며 구매·복원 성공 뒤 동일 staging 파일의 설치를 자동 재개한다.
- 편집기는 덱 이름·제작자 표시명·유형·난이도·일반 태그·발견용 덱 언어와 항목의 한국어·뜻·발음·순서를 편집하고, 저장 전 §8.1과 §8.4를 같은 규칙으로 검증한다. 항목의 뜻과 발음은 앱 UI 언어나 덱 언어에 따라 다른 입력 묶음으로 바뀌지 않는다. 항목 추가·삭제·재정렬, 덱 전체 삭제를 지원하고 v1에서는 이미지·녹음·원격 오디오·AI 생성을 제공하지 않는다.
- 덱 언어는 이 덱이 어떤 언어 사용자 커뮤니티를 위한 것인지 나타내는 필수 검색 metadata다. `ja/en/ko` allowlist 없이 `ar`, `fr-CA`, `zh-Hant` 등 canonical BCP 47 태그 하나를 선택하며, 추천 목록 밖 정상 태그도 저장·내보내기에서 보존한다. 앱 UI 언어를 자동으로 따라야 하는 값은 아니며 일반 자유 태그와도 분리한다. 향후 공개 발견 인덱스는 exact tag와 primary language를 함께 색인하지만, 현재 로컬-only 앱은 서버로 전송하지 않는다.
- 덱 언어 변경은 번역 교체가 아니라 콘텐츠 보존형 재태깅이다. 현재 단일 localization key의 덱 metadata·뜻·발음을 새 canonical key로 그대로 이동하고 `default_locale`을 함께 바꾸며 필드를 비우지 않는다. 기존 다국어 사용자 덱이나 다국어 공식 덱 사본은 먼저 보존할 콘텐츠 묶음 하나를 명시적으로 선택하고 다른 묶음 제거 범위를 확인한다. 선택이 끝난 뒤 덱 언어를 바꾸는 동작은 다시 콘텐츠를 삭제하지 않는다. 검증 실패 시 첫 오류 필드를 펼쳐 자동 이동·포커스하고 접근성 서비스로 오류를 안내한다.
- 웹·iOS·Android 제작 UI의 compact 정책은 한국어를 한글·공백으로 제한하고 공백 제외 최대 9음절이면서 공용 계약상 공백 포함 10자 이하로 제한한다. 뜻과 발음은 각각 1~20자로 제한한다. 공용 schema-v2의 10자·500자·300자 reader/writer 상한은 기존 문서 호환을 위해 축소하지 않으며, 더 긴 기존 문서는 읽기·연습·변경 없는 내보내기를 유지하고 편집 저장 시 제작 정책 오류를 보여 준다. 항목은 번호·한국어 요약을 가진 접이식 행으로 표시해 1,000항목에서도 모든 입력 필드를 동시에 구성하지 않는다.
- 공식·번들·원격 덱은 불변이다. `사본으로 편집`은 유료 기능이며 원본을 건드리지 않고 `official=false`, 새 `user_` ID, `version=1`, 새 item ID를 가진 사용자 덱을 만든다. 원본의 `deck_id`는 설치 레코드의 `derived_from_deck_id`에만 남기며 공식 업데이트·Game Center·추천 집계와 분리한다.
- 사용자 덱을 저장할 때 기존 `deck_id`와 변경하지 않은 item ID는 유지하고 `version`을 1 증가시키며 `updated_at`을 갱신한다. 새 항목만 새 item ID를 받는다. 저장은 검증된 임시 파일과 설치 인덱스를 함께 준비한 뒤 원자적으로 교체하며 실패하면 이전 덱과 편집 초안을 모두 유지한다.
- 편집 중 변경은 복구 가능한 로컬 초안에 debounce 자동 저장하고 닫기·앱 비활성 직전에도 flush한다. 최종 덱 설치 commit과 일치하는 `draft_id`만 정리하며 저장 실패·구매 취소·앱 종료·환불에는 초안을 삭제하지 않는다. 다음 마이페이지 진입에서 유효한 미완성 초안을 복구한다.
- 활성 초안이 있을 때 같은 create/edit/official-copy 흐름은 그대로 재개한다. 다른 제작 흐름을 누르면 기존 초안 재개·명시적 폐기 후 새 작업·취소 중 하나를 사용자가 선택하기 전에는 초안을 덮어쓰지 않는다.
- 사용자 덱 편집 저장은 초안의 `base_version`을 설치 transaction 안에서 현재 설치본과 원자적으로 비교한다. 원본이 가져오기 교체 등으로 바뀌었으면 현재 덱과 초안을 모두 유지하고, 최신본을 확인하거나 새 `deck_id`·item ID를 가진 별도 사용자 사본으로 초안을 저장할 수 있게 한다.

**세션과 외부 문서 규칙**

- 레슨·연습·게임 도중에는 file importer, 가져오기 미리보기, 충돌 선택, 편집기, 결제·복원·오류 모달을 표시하지 않는다. 외부에서 열린 파일은 보안 접근이 유효한 동안 앱 임시 영역으로만 복사해 `pending import`로 기록하고 현재 세션 종료 후 제안한다.
- 세션 중 받은 파일의 복사가 실패해도 세션 UI에는 알리지 않는다. 세션을 나간 다음 비모달 오류 또는 가져오기 화면에서 재시도한다. 백그라운드 전환 타이머 일시정지와 기존 세션 종료 순서를 그대로 유지한다.

**재가져오기·충돌·기록 보존**

| 입력 상태 | 동작 |
|---|---|
| 처음 보는 `deck_id`, 설치된 사용자 덱 0~2개 | 무료 신규 설치 |
| 처음 보는 `deck_id`, 설치된 사용자 덱 3개 이상 | `pro` 구매 설명; 성공·복원 뒤 동일 파일 설치 재개 |
| 동일 ID·동일 SHA-256 | `이미 가져온 덱` 안내, 디스크 쓰기 없음 |
| 동일 ID·다른 내용 | 자동 병합하지 않고 `현재 덱 유지` 또는 무료 `가져온 파일로 교체` |
| 동일 ID·다른 내용 + `pro` 구매자 | 위 선택에 `사본으로 가져오기` 추가 |
| 낮은 `version` | 다운그레이드 경고 후 명시적 무료 교체만 허용 |
| 같은 `version`·다른 SHA-256 | 버전 충돌 경고 후 명시적 무료 교체만 허용 |
| 공식 덱 ID 또는 `official=true` 사용자 package | 가져오기 거부 |
| 지원 버전보다 높은 package/schema | 파일을 변경하지 않고 앱 업데이트 필요 안내 |

- `사본으로 가져오기`는 내용은 복사하되 새 `user_` ID, `version=1`, 새 item ID를 부여한다. 이 변환본을 저장하는 행위는 `pro` entitlement가 필요하다.
- 충돌 화면은 현재본과 가져온 파일의 이름·버전·수정일·항목 수를 나란히 비교한다. `현재 덱 유지`를 안전한 기본 액션으로 두고 무료 `현재본 내보내기`를 제공하며, 교체는 파괴적 액션과 별도 재확인을 거친다.
- `가져온 파일로 교체`는 무료이며 package의 ID·version·item ID를 그대로 사용한다. 덱별 `plays`, 최고 점수·정확도, `GameRecord`, `last_played_at`과 동일 item ID의 복습 횟수·졸업 상태를 보존한다. 삭제된 item은 새 세션과 활성 복습 큐에서만 제외하고 과거 기록은 보존하며, 새 item은 진행 없음으로 시작한다.
- 덱 삭제는 payload만 제거하고 학습·게임·복습 이력과 삭제된 `deck_id`의 설치 이력은 유지한다. 같은 ID를 다시 가져오면 해당 기록을 다시 연결한다. 다른 ID의 사본에는 원본 기록을 복사하지 않는다.
- 삭제 전 덱 이름과 기록 보존 범위를 확인하고, 사용자 덱에는 `내보낸 뒤 삭제`를 제공한다. 삭제 실패 시 행을 유지하고 재시도 가능한 오류를 표시한다.

**AC**

- 구매하지 않은 새 설치에서 Files·iCloud Drive·AirDrop으로 정상 `.typedeck` 3개를 가져와 오프라인 연습과 호환 게임을 시작하고, 현재본을 다시 내보내고 삭제·동일 ID 재가져오기 할 수 있다. 네 번째 새 ID는 설치 전에 `pro`를 요구하고, 하나를 삭제하면 다시 무료로 설치된다.
- 구매하지 않은 사용자는 새 덱 생성·사용자 덱 편집·공식 덱 사본 저장을 완료할 수 없지만, 취소·오프라인·복원 실패 뒤에도 기존 덱의 학습과 내보내기를 잃지 않는다.
- 각 플랫폼의 StoreKit/Play Billing 테스트 환경에서 구매·취소·pending·중복 구매·복원·환불 상태를 검증한다. Android는 `PURCHASED`만 권한을 열고 미확인 구매를 acknowledge하며, 명시적 복원은 구매 재조회를 수행한다. 성공 구매 후 앱 재실행과 오프라인 실행에서도 마지막으로 확인된 일회성 entitlement로 생성·편집할 수 있다.
- 공식 덱 사본은 새 ID·version 1·`official=false`이고 원본 업데이트나 Game Center 제출 대상이 아니다. 사용자 덱은 Game Center CTA와 공식 추천·다운로드 통계에서 제외된다.
- 동일 파일 재가져오기는 no-op이고, 교체·사본·다운그레이드·동일 버전 충돌 선택이 표대로 동작하며 교체 후 덱 및 동일 item 진행 기록이 유지된다.
- 악성·손상·과대 package는 §8.4 제한에서 거부되고 기존 덱·인덱스·학습 기록을 한 바이트도 변경하지 않는다. ja/en/es/de/fr의 파일 선택·미리보기·구매·복원·편집·검증·충돌·완료·오류 문자열을 모두 로컬라이제이션한다.
- legacy ja/en/ko fixture와 `default_locale=ar`, `ar+zh-Hant` v2 fixture를 Python·Swift·Kotlin reader가 모두 수락한다. `fr-CA→fr`와 일본어 base 최종 fallback, malformed·duplicate·noncanonical 거부, unknown valid 태그 round-trip, 세 writer의 공통 deterministic golden을 계약 테스트로 고정한다.
- 연습·게임 도중 외부 문서를 열어도 모달·결제창·파일 선택기가 나타나지 않고, 세션이 끝난 뒤에만 pending import가 제안되는 UI 회귀 테스트를 통과한다.

### F6. 게임 모드 — "흐름 모드" (寿司打 문법) [P0]
- 화면 상단 레인에서 단어 카드가 오른쪽→왼쪽으로 흐른다. 화면 왼쪽으로 완전히 사라지기 전에 타이핑을 완성하면 획득.
- 테마: 초기 버전의 밝고 단순한 플로 레인. 앱 공통 핑크·라벤더 그라데이션 위에 흰 플레이 영역과 옅은 하단 컨베이어 띠·정적 도트만 두고, 일반 카드·입력 진행 스타일을 사용한다. 궁궐·공방·프레스·작업대·활자 서랍처럼 게임 규칙과 무관한 장식 배경은 넣지 않으며, 흐르는 타이핑 문법만 즉시 읽히게 한다.
- 플레이 중에는 덱 제목·설정 버튼·일반 Debug 성능 오버레이를 표시하지 않는다. 닫기와 남은 시간·점수·콤보·목숨을 같은 최상단 한 줄에 두어 레인의 세로 공간을 확보한다. 닫기 버튼은 원형 카드 배경만 사용하고 그 위에 중복 테두리를 그리지 않으며, 각 지표의 숫자와 보조 라벨은 카드 중앙축에 정렬한다. FPS 프로브는 명시적 성능 측정 실행에서만 노출한다.
- 시작·재도전 시 `3 → 2 → 1` 카운트다운을 먼저 보여 주고, 카운트다운이 끝난 뒤에만 제한시간과 카드 이동을 시작한다. 백그라운드 전환 시 준비 상태로 되돌리고 복귀 후 다시 센다. Reduce Motion에서는 짧은 `1` 단계로 축소한다.
- 규칙:
  - 제한시간 60초 기본. 노미스로 단어 완성 시 +2초 보너스 (정확도가 곧 생존 — 寿司打 핵심 재미 이식).
  - 기본 목숨은 3개다. 카드가 화면 밖으로 완전히 이탈할 때마다 1개를 잃고, 0개가 되면 남은 시간과 무관하게 즉시 결과로 이동한다.
  - 카드 이동은 시작 속도에서 플레이 경과에 따라 카드 단위로 조금씩 빨라지고, 50초 이후에는 시작 대비 1.8배로 상한을 둔다. 진행 중인 카드의 속도는 중간에 급변하지 않는다. 산성비는 F6b의 별도 1.5배 상한을 유지한다.
  - 콤보: 연속 노미스 단어 수. 콤보 구간별 이펙트 점층 (§7.3).
  - 점수 = Σ(단어 자모 수 × 10) × 콤보 배율 (1.0 / 5콤보 1.2 / 10콤보 1.5 / 20콤보 2.0).
  - 오타 시 콤보 리셋, 카드 흔들림. 페널티로 시간을 깎지는 않는다 (초심자 보호).
- 플로우 모드를 고르면 설치 덱보다 먼저 게임 전용 기본 세트 3개를 2×2 선택 그리드에 표시한다. 각 세트는 중복 없는 단어 정확히 100개이며 네트워크·다운로드 없이 즉시 플레이할 수 있다. 난이도는 `일상 활용도·TOPIK 의미 수준`과 `실제 두벌식 입력 키 수·음절 수·받침 수·복합 모음/받침 부담`의 두 축을 함께 사용한다. 의미 수준에 맞는 실용 어휘를 먼저 고른 뒤 단계 안에서 입력 부담을 정렬하며, 일상성이 높은 긴 단어를 초급에서 일률 배제하거나 사용성이 낮은 발음 예시를 고급에 넣지 않는다.
  - Beginner / 초급: TOPIK I와 일상 핵심 명사·동작·상태 중심. 최소 70개는 1~2음절·받침 최대 1개의 가벼운 입력이며, `지하철·재미있다·괜찮다`처럼 자주 쓰는 긴 기본어는 예외로 포함할 수 있다.
  - Mid level / 중급: TOPIK II 3~4급에 해당하는 생활·업무·공공 서비스 어휘 중심. 초급보다 받침·음절·입력 키 부담이 높은 항목 비율과 평균 입력량이 모두 증가한다.
  - High level / 고급: TOPIK II 5~6급의 뉴스·사회·경제·전문 실용 어휘 중심. 최소 80개는 3음절 이상 또는 받침 2개 이상의 복잡한 입력이며, 세 단계 중 평균 입력량이 가장 높다.
  - 세 단계는 서로 같은 단어를 중복하지 않고, `TOPIK`·`タイピング` 태그와 콘텐츠 버전으로 복합 기준 충족을 검증한다.
- 전용 초급·중급·고급 세트는 게임 화면에 들어갈 때마다 새 무작위 순서로 시작한다. 재도전도 이전 순서를 재사용하지 않고 새 순서·새 10문제 표본을 만든다. 한 세션 안에서는 같은 단어를 모두 소진하기 전에 중복 출제하지 않는다. 명시적 테스트 시드에서만 순서를 재현할 수 있다.
- 기본 3세트 옆의 `+` 카드에서 발견 탭의 덱 검색으로 이동한다. 사용자가 발견에서 다운로드한 덱은 기본 세트 아래 `추가한 덱`에 나타나 플로우에서 선택할 수 있다. 기본 세트는 일반 발견·연습 덱 목록에 섞지 않고 플로우 전용 번들 콘텐츠로 유지한다.
- 게임 탭 첫 화면에는 덱을 직접 나열하지 않는다. 주간 피요컵을 최상단에 두고 별도의 Game Center 전체 랭킹 버튼은 표시하지 않는다. 게임 이름·아이콘·한 줄 규칙 설명을 담은 2열 확장형 카드 그리드에서 게임을 먼저 고른다. 카드 순서는 위에서부터 `흐름·산성비 / 초성 맞추기·받아쓰기 / 단어 맞추기·띄어쓰기`다. 흐름·산성비·초성 맞추기·받아쓰기·단어 맞추기는 각 모드의 자체 기본 콘텐츠와 선택적으로 추가한 덱을 표시하며, 띄어쓰기는 덱과 분리된 번들 긴 글을 표시한다.
- 공통 상단 HUD의 남은 시간·점수·콤보·목숨·문제 수는 한 줄을 유지한다. 각 지표에서 아이콘과 수치의 영역을 분리하고, 자릿수나 현지화 구분자가 늘어나면 수치를 단계적으로 축소한 뒤 남은 폭에 맞춰 추가 축소해 아이콘·인접 지표와 겹치지 않게 한다. 짧은 수치는 기존 크기와 카드 중앙 정렬을 유지한다.
- 흐름 단어 카드는 한국어 단어·현재 앱 언어의 뜻·뜻 아래의 읽기를 표시한다. 일본어는 가타카나, 영어·스페인어·독일어·프랑스어는 해당 언어 뜻·공통 로마자를 사용한다. 미제공 번역과 미지원 UI 언어는 영어 뜻·로마자로 fallback하며 일본어로 대체하지 않는다. 카드 내부에 하트 같은 장식 아이콘을 두지 않는다. 카드 폭은 세 텍스트 중 가장 긴 내용에 맞추고 짧은 단어에 불필요한 고정 폭·큰 여백을 두지 않되, 긴 단어는 레인 폭 상한 안에서 축소한다.
- 자모 진행 트랙은 자모 수가 많아도 플레이 화면의 가로 폭을 확장하지 않는다. 화면 폭을 넘는 자모열은 트랙 내부에서만 가로로 이동하며 현재 입력할 자모를 자동 추적하고, 하단 내장 키보드는 항상 기기 화면 폭 안에 고정한다. 이 동작은 산성비에도 동일하게 적용한다.
- 흐름·산성비의 키보드 위 입력 상태에는 조합 중인 글자를 별도 큰 텍스트로 중복 표시하지 않는다. 자모 진행 트랙 아래에는 준비·정타·오타·완료 상태 문구만 영역 중앙에 작게 표시하며 내장 키보드와 OS 키보드에서 같은 정렬을 유지한다. 초성 맞추기·단어 맞추기·받아쓰기는 각각 F6a·F6c·F6d의 연습형 조합 입력 카드를 예외로 사용한다.
- 흐름·산성비에서 카드가 레인 끝이나 위험선에 닿아 목숨을 잃으면 전용 하강 실패음, 강한 오류 햅틱, 짧은 화면 흔들림·붉은 플래시·목숨 HUD 펄스를 동시에 제공한다. 연출은 입력을 막지 않고 0.6초 이내에 사라지며 마지막 목숨도 결과 전 짧게 손실을 인지할 시간을 둔다. 효과음·햅틱은 각각의 사용자 설정을 따른다.
- 플레이 피요는 움직이는 카드를 눈동자로 추적하되 몸체는 앱의 다른 화면과 같은 정면 비율로 고정한다. 카드 위치에 따라 몸체에 3D 좌우 회전을 적용해 가로 폭이 눌려 보이게 하지 않는다.
- 연습·게임을 시작하면 옷장의 사용자 선택(`자동`·`없음`·고정 아이템)을 세션 동안 유지한다. `자동`은 덱·카드 태그로 소품을 새로 장착하지 않고 성장 단계의 기본 외형만 사용하며 TOPIK을 포함한 카드·문제·콤보·점수·반응 이벤트가 바뀌어도 다른 복장으로 교체하지 않는다. 표정·시선·몸동작은 기존 입력 반응을 유지한다. `TOPIK 안경`은 TOPIK 태그 게임을 0점 이상으로 완료하면 영구 해금하며 이후 사용자가 옷장에서 직접 선택한다.
- 종료 후 결과: 점수, 최대 콤보, 정확도, 현재 앱 언어의 한 줄 코멘트(점수 구간별 문구), X/Instagram 공유용 이미지 생성 (§F9).
- AC: 프레임 60fps 유지 (파티클 포함, iPhone 12 / Pixel 6 기준). 게임 중 어떤 광고·모달도 삽입되지 않는다.

### F6a. 게임 모드 — "초성 맞추기" [P0]
- 초성 전용 세트 또는 사용자가 추가한 덱에서 세션마다 무작위로 최대 10개 단어를 골라 완성형 한글 음절의 초성만 문제로 표시한다. 공백은 단어 경계로 유지하고 구두점·한글이 아닌 문자는 문제 초성에서 제외한다.
- 초성 맞추기를 고르면 F6의 실용 TOPIK 수준·타이핑 부담 복합 기준을 적용한 Beginner·Mid level·High level 전용 100단어 세트 3개와 `+` 덱 검색 카드를 2×2 선택 그리드에 표시한다. 단계가 오를수록 생활→업무→사회·전문으로 의미 수준이 높아지고 정답 음절·받침·평균 입력 키 수가 함께 증가하며, 동일 초성 정답은 뜻 워드박스로 공정하게 구분한다. 기본 세트는 일반 발견·연습 목록에 섞지 않고, 발견에서 받은 덱은 아래 `추가한 덱`에 표시한다.
- 사용자는 보기에서 고르지 않고 정답 한국어 전체를 직접 타이핑한다. 내장 두벌식 키보드와 OS 한국어 IME를 모두 지원하고 판정·조합·백스페이스는 §6의 자모 시퀀스 규칙을 따른다. 오타 자모는 조합 문자열에 반영하지 않으며, 정답을 알려 주는 다음 키 하이라이트는 이 모드에서 표시하지 않는다.
- 플레이 화면은 플로우·산성비와 같은 `닫기 | 문제 수·점수·콤보` 상단 HUD, 중앙 문제 레인, 입력 상태, 하단 고정 키보드 구조를 사용한다. 덱 제목·세션 설정·입력 방식 변경 컨트롤은 노출하지 않고, 설정에서 미리 선택한 기본 입력 방식을 세션 시작 시 확정해 결과까지 유지한다.
- 중앙 문제 레인의 초성은 음절별 칩으로 표시한다. 현재 입력 중인 음절의 초성은 보조색 테두리·배경과 짧은 스프링 강조를, 해당 음절 입력을 마친 초성은 성공색 채움을 사용하고, 긴 초성열은 카드 폭 안에서 함께 축소해 화면 전체 폭과 하단 키보드를 밀어내지 않는다.
- 초성 모드의 입력 상태는 일반 타이핑 연습과 같은 흰 카드 안에 피요와 원형 한글 조합 프리뷰, `入力中` 라벨과 누적 입력 텍스트를 배치한다. 준비·정타·오타·완료 문구는 카드 아래쪽 중앙에 유지하며, 기존 중앙 문제 카드 오른쪽 아래의 중복 피요는 제거한다. 이 구조는 내장 키보드와 OS 한국어 IME에서 동일하다.
- 정답 단어의 마지막 자모가 완성되면 중앙 초성열을 성공색 정답 단어로 전환해 약 1.35초 동안 읽을 수 있게 유지한 뒤 다음 문제로 자동 전환한다. 마지막 문제도 같은 정답 노출 뒤 별도 버튼 없이 공통 결과 화면으로 자동 진입한다. 초성을 추출하고 조합 엔진으로 분해할 수 있는 단어가 1개 이상이면 플레이할 수 있다.
- 현재 앱 언어의 뜻은 기본 ON 설정에 따라 모든 문제의 초성열 아래에 자동 표시하고 점수를 차감하지 않는다. 한국어를 포함한 미지원 UI 언어는 영어 뜻을 사용하며 일본어로 대체하지 않는다. 노출된 뜻은 작은 캡션 한 줄이 아니라 17pt 이상 굵기의 중앙 정렬 텍스트와 여유 있는 안쪽 여백을 가진 워드박스로 표시하고, 긴 뜻은 최대 두 줄까지 줄바꿈한다. 설정에서 뜻 표시를 OFF로 바꾸면 워드박스 대신 뜻 힌트 버튼을 표시하며, 사용자가 임의로 연 힌트만 30점을 차감한다. 같은 덱에 동일 초성 단어가 둘 이상인 문제는 OFF 상태에서 버튼을 눌러도 불공정한 복수 정답을 피하기 위한 필수 단서이므로 점수를 차감하지 않는다. 완성 점수는 기본 100점 + 5초 이내 속도 보너스 + 노미스 단어 콤보 보너스이며, 오타는 정확도를 낮추고 콤보를 리셋하지만 사용자가 같은 문제를 계속 입력해 완성할 수 있다.
- 초성 맞추기와 단어 맞추기는 현재 문제의 한국어 발음을 듣는 무료 발음 힌트를 세션당 합계 3문제까지 제공한다. 한 문제의 첫 사용만 남은 횟수 1회를 차감하고 같은 문제에서 다시 듣기는 추가 차감하지 않는다. 첫 사용 즉시 현재 콤보를 리셋하고 해당 문제에 기존 힌트와 같은 30점 감점을 적용하며, 정답 완성 여부와 무관하게 해당 항목을 복습 대상으로 수집한다. 동일 초성의 필수 뜻 단서는 발음 힌트 횟수를 소비하지 않는다. 재도전은 남은 횟수와 문제별 사용 상태를 모두 3회로 초기화한다.
- 오타가 발생한 단어는 복습 덱에 즉시 수집하고, 노미스 완성 단어는 복습 덱의 졸업 진행에 반영한다. 단어 완성 시 피요의 `eureka` 반응을 발생시킨다.
- 결과는 점수·자모 정확도·최대 노미스 콤보·분당 문제 수·공유·복습·재도전을 제공한다. 최고 기록은 흐름 모드와 분리한 덱별 `choseong` 게임 기록으로 영속화하며 내장/OS 입력 기록도 각각 분리한다.
- 백그라운드에서는 유효 문제 시간을 일시정지하며, 플레이 도중 설정 모달이나 광고를 삽입하지 않는다.

### F6b. 게임 모드 — "산성비" [P0]
- 산성비 전용 세트 또는 사용자가 추가한 덱의 한국어 단어 카드가 화면 상단에서 하단 위험선으로 낙하한다. 앞 단어가 위험선에 닿거나 완성되기를 기다리지 않고 이동 시간의 약 42% 간격으로 다음 단어를 생성하며, 최대 4장이 3개 레인에서 시간차로 동시에 내려온다. 이동 시간이 매우 짧거나 길어도 생성 간격은 1.4~3.6초를 기준으로 조정하되 항상 해당 카드 이동 시간의 72% 이내로 두어 다음 카드가 먼저 등장하게 한다.
- 내장 키보드는 현재 낙하 중인 카드 가운데 가장 위험선에 가까운 카드로 시작하고, 단어 입력 중에는 대상이 바뀌지 않는다. 입력 대상 카드는 강조 테두리로 구분하며 완성하거나 바닥에 닿으면 이미 내려오던 카드 중 진행률이 가장 높은 카드로 즉시 이어진다. OS 한국어 IME에서는 가장 위험한 카드에 고정하지 않고 화면에 낙하 중인 단어 중 어떤 것이든 완성된 입력과 일치하면 해당 카드를 즉시 완료 처리하며, 입력 중 일치 후보만 강조한다. 대기 카드도 독립적으로 계속 낙하하고 각 카드가 위험선에 닿을 때마다 목숨을 1개 차감한다.
- 낙하 단어 카드는 한국어 단어·입력 진행률·현재 앱 언어의 뜻·뜻 아래의 읽기를 표시하며 F6의 ja/en/es/de/fr 및 미지원 UI 언어 fallback 규칙을 공유한다. 카드 내부에 물방울 같은 장식 아이콘을 두지 않는다. 폭은 내용에 맞춰 줄이고 긴 단어만 기존 레인 폭 상한까지 확장한다.
- 플레이 상단은 덱 제목·설정·일반 Debug 성능 오버레이 없이 `닫기 | 남은 시간·점수·콤보·목숨` 한 줄로 구성한다. 낙하 영역은 모드 배지 아래의 3개 세로 레인과 하단 위험 구역으로 나누며, 카드 하단이 위험선에 닿는 시점을 이탈로 판정한다. 피요는 위험선 아래 대기 구역 안에 두어 선이나 낙하 카드와 겹치지 않는다.
- 제한시간·점수·콤보·노미스 +2초·오타/놓침·복습 수집·3목숨 조기 종료와 새 카드 시작 시 가속을 적용하는 방식은 흐름 모드와 공유하되, 산성비의 가속 상한은 시작 대비 1.5배로 유지하고 카드 이동 방향과 시작 속도도 별도로 튜닝한다. 짧은 단어·일반 단어·문장 코스에 따라 낙하 시간을 자동 조정한다.
- 산성비 모드를 고르면 설치 덱보다 먼저 F6의 실용 TOPIK 수준·타이핑 부담 복합 기준을 적용한 100단어 기본 세트 3개를 2×2 선택 그리드에 표시한다. 낙하 중 빠르게 읽고 입력하는 규칙에서도 Beginner는 일상 핵심·가벼운 입력, Mid level은 생활·업무·중간 입력, High level은 사회·전문·복잡한 입력을 함께 만족해야 한다. 세션 시작과 재도전마다 100단어의 순서를 다시 섞는다.
- 기본 3세트 옆의 `+` 카드에서 발견 탭의 덱 검색으로 이동한다. 사용자가 발견에서 다운로드한 덱은 기본 세트 아래 `추가한 덱`에 나타난다. 산성비 기본 세트는 일반 발견·연습 및 플로우 기본 세트와 분리한 산성비 전용 번들 콘텐츠로 유지한다.
- 내장 키보드와 OS 한국어 IME를 모두 지원하고, 입력 중인 자모 진행률·남은 시간·점수·콤보·피요 반응을 동시에 표시한다. 백그라운드 전환 시 제한시간과 낙하 진행을 모두 일시정지한다.
- 결과·공유·재도전·덱별 최고 기록은 흐름 모드와 분리된 `acid_rain` 기록으로 저장한다.
- AC: 게임 허브의 산성비 카드 → 덱 선택 → 3·2·1 → 낙하 레인 진입이 가능하며, 첫 카드가 위험선에 도달하기 전에 두 번째 카드가 다른 레인에 나타난다. 모든 카드는 레인 내부에서 독립적으로 이동하고 위험선 도달 시 목숨을 차감하며, 세 번째 놓침은 60초 전이라도 결과로 이동한다. OS 키보드에서는 가장 아래 카드가 아닌 다른 표시 단어를 완성해도 그 카드가 사라지고 점수·콤보가 정상 반영된다.

### F6c. 게임 모드 — "단어 맞추기" [P0]
- 단어 맞추기 전용 세트 또는 사용자가 추가한 덱에서 현재 앱 언어 규칙으로 해석한 뜻이 있고 조합 엔진으로 분해 가능한 고유 한국어 항목을 세션마다 무작위로 최대 10개 고른다. 중앙 문제 카드에는 해당 뜻을 큰 워드박스로 표시하고 한국어 정답·초성·읽기와 선택 보기는 사전 노출하지 않는다. 조건을 만족하는 항목이 1개 이상이면 플레이할 수 있다.
- 단어 맞추기를 고르면 F6의 실용 TOPIK 수준·타이핑 부담 복합 기준으로 나눈 Beginner·Mid level·High level 전용 100단어 세트 3개와 `+` 덱 검색 카드를 2×2 선택 그리드에 표시한다. 단계가 오를수록 뜻의 의미 수준과 정답의 음절·받침·평균 입력 키 부담이 함께 증가한다. 기본 세트는 일반 발견·연습 목록에 섞지 않고, 발견에서 받은 덱은 아래 `추가한 덱`에 표시한다.
- 사용자는 보기에서 고르지 않고 내장 두벌식 키보드 또는 OS 한국어 IME로 정답 한국어 전체를 직접 타이핑한다. 자모 시퀀스 판정·조합·백스페이스·오타 무시·복습 수집·노미스 졸업·기본 100점과 속도·콤보 보너스는 초성 맞추기·받아쓰기의 직접 입력 규칙을 공유하며, 정답을 알려 주는 다음 키 가이드는 표시하지 않는다.
- 문제 카드의 발음 힌트 버튼은 F6a와 같은 세션당 3문제·문제당 최초 1회 차감·30점 감점·콤보 리셋·복습 수집 규칙을 사용한다. 남은 횟수는 버튼에 표시하고 0회면 새 문제에서 비활성화하되 이미 힌트를 사용한 현재 문제의 다시 듣기는 허용한다.
- 플레이 화면은 `닫기 | 문제 수·점수·콤보` 상단 HUD, 중앙의 현재 앱 언어 뜻 문제 카드, `피요 + 원형 조합 프리뷰 + 입력 중` 입력 카드, 하단 고정 키보드 구조를 사용한다. 덱 제목·세션 설정·입력 방식 변경 컨트롤은 노출하지 않고, 설정에서 선택한 기본 입력 방식을 세션 시작 시 확정해 결과까지 유지한다.
- 마지막 자모를 완성하면 입력 카드에 정답·완료 점수를 표시하고 0.65초 뒤 다음 문제로 자동 전환한다. 오타는 정확도를 낮추고 콤보를 리셋하지만 해당 자모를 조합 문자열에 반영하지 않아 같은 문제를 계속 입력해 완성할 수 있다. 마지막 문제도 같은 피드백 뒤 버튼 없이 공통 결과 화면으로 자동 진입한다.
- 최초 시작·재도전은 `3 → 2 → 1` 카운트다운 뒤 문제 시간을 시작한다. 백그라운드에서는 유효 문제 시간을 일시정지하고 복귀 시 카운트다운을 다시 거치며, 플레이 도중 설정 모달이나 광고를 삽입하지 않는다.
- 결과는 점수·자모 정확도·최대 노미스 콤보·분당 문제 수·입력 방식·공유·복습·재도전을 제공한다. 덱별 `word_match` 최고 기록은 다른 게임 및 내장/OS 입력 기록과 분리해 저장한다.
- AC: 게임 허브의 단어 맞추기 카드 → 덱 선택 → 카운트다운 → 현재 앱 언어의 뜻 → 한글 직접 타이핑 → 자동 전환 → 결과 흐름이 오프라인에서 가능하다. 실제 플레이와 재도전은 새 10문제 표본을 만들고 명시적 테스트 시드에서는 같은 순서를 재현하며, 문제 카드와 접근성 트리에는 정답을 사전 노출하지 않는다.

### F6d. 게임 모드 — "받아쓰기" [P0]
- 받아쓰기 전용 세트 또는 사용자가 추가한 덱에서 조합 엔진으로 분해 가능한 한국어 항목을 세션마다 무작위로 최대 10개 고른다. 문제 카드에는 정답·초성·뜻·읽기를 노출하지 않고 한국어 목표 발음만 자동 재생하며, 사용자는 언제든 발음 버튼을 눌러 다시 들을 수 있다.
- 받아쓰기를 고르면 F6의 실용 TOPIK 수준·타이핑 부담 복합 기준을 적용한 Beginner·Mid level·High level 전용 100단어 세트 3개와 `+` 덱 검색 카드를 2×2 선택 그리드에 표시한다. Beginner는 일상 기본어의 비교적 가벼운 소리, Mid level은 생활·업무 어휘의 받침 혼합, High level은 사회·전문 어휘의 긴 소리와 여러 받침을 정확히 입력하도록 구성한다. 기본 세트는 일반 발견·연습 목록에 섞지 않고, 발견에서 받은 덱은 아래 `추가한 덱`에 표시한다.
- 내장 두벌식 키보드와 OS 한국어 IME로 들은 정답 전체를 직접 입력한다. 판정·조합·백스페이스·오타 무시·복습 수집·노미스 졸업·점수·콤보는 초성 맞추기의 직접 타이핑 규칙을 공유하되, 받아쓰기는 0.65초 자동 전환을 유지하고 뜻 힌트와 정답 키 가이드는 제공하지 않는다.
- 플레이 화면의 HUD·중앙 문제 레인·입력 상태·하단 고정 키보드와 게임 중 설정/입력 방식 변경 금지 정책도 초성 맞추기와 공유한다.
- 받아쓰기 입력 상태도 일반 타이핑 연습과 같은 흰 카드 안에 피요와 원형 한글 조합 프리뷰, `入力中` 라벨과 누적 입력 텍스트를 배치한다. 준비·정타·오타·완료 문구는 카드 아래쪽 중앙에서 색과 흔들림으로 즉시 피드백하고, 문제 카드 오른쪽 아래의 중복 피요는 제거한다. 내장 키보드와 OS 한국어 IME에서 같은 구조를 사용하되 정답·초성·뜻·읽기는 완료 전까지 시각·접근성 트리에 노출하지 않는다.
- 최초 시작·재도전과 백그라운드 복귀 시 `3 → 2 → 1` 카운트다운 뒤 문제 시간과 발음 재생을 시작한다. Reduce Motion에서는 짧은 `1` 단계로 축소한다. 백그라운드 진입 시 문제 시간과 진행 중인 발음을 모두 중지하고, 복귀 카운트다운이 끝나면 현재 문제 발음을 처음부터 자동 재생한다.
- 앱이 제공하는 받아쓰기 기본 세트의 모든 고정 목표는 gTTS `ko` 보통 속도의 사전 생성 MP3를 번들에서 오프라인 재생한다. 공통 resolver는 `DeckItem.audio`의 선언 경로 → 목표 한국어의 SHA-256 기반 canonical MP3 경로 → `AVSpeechSynthesizer(ko-KR)` 순서로 시도한다. 선언·canonical 파일이 모두 누락·손상되었거나 번들에 없는 사용자·비공개 동적 콘텐츠인 경우에만 마지막 기기 TTS 대체를 사용한다. 사용자 설정의 문제 자동 발음 ON/OFF와 무관하게 이 게임의 필수 문제 단서로 재생하며 `.playback` + `.mixWithOthers` 정책을 적용한다.
- 받아쓰기의 자동 발음과 다시 듣기는 문제 자체의 필수 단서이므로 F6a·F6c의 3회 발음 힌트에 포함하지 않고 횟수·점수·콤보를 변경하지 않는다. 한국어 정답이 이미 표시되는 흐름·산성비와 음성 단서가 과제에 관여하지 않는 띄어쓰기에는 발음 힌트 버튼을 추가하지 않는다.
- 결과는 점수·자모 정확도·최대 노미스 콤보·분당 문제 수·공유·복습·재도전을 제공한다. 최고 기록은 덱별 `dictation` 게임 기록으로 다른 게임 및 내장/OS 입력 기록과 분리한다.
- AC: 게임 허브의 받아쓰기 카드 → 덱 선택 → 카운트다운 → 자동 발음 → 직접 타이핑이 오프라인에서 가능하며, 플레이 화면의 시각·접근성 텍스트 어디에도 정답이 사전 노출되지 않는다.

### F6e. 사이드 게임 — "띄어쓰기" [P1]
- 기존 덱·카탈로그와 분리된 번들 콘텐츠로 저작권 문제가 없는 자체 작성 한국어 긴 글 6개를 제공한다. 선택 화면의 위에서 아래로 레벨 1~6을 배치하고 레벨 번호·분량·정답 공백 수를 미리 표시한다. 단계가 오를수록 글 길이와 어휘·문장 구조·띄어쓰기 판단 난도가 높아지며, 모든 글은 공백·문장부호 포함 100~200자이고 레벨 6은 180자 이상의 고급 글로 구성한다.
- 플레이 화면은 원문의 모든 공백을 제거한 글을 먼저 보여 주고 첫 글자 사이 경계를 선택한 상태로 시작한다. 화면 하단에는 `이전 · 스페이스(띄어쓰기) · 다음` 3버튼만 고정한다. 좌우 버튼은 인접 경계로 한 칸씩 수동 이동하고 가운데 버튼은 현재 경계의 공백을 추가하거나 다시 눌러 취소하며 자동 전진하지 않는다. 원문 글자와 문장부호는 편집 입력 자체를 받지 않는다.
- 별도의 확대 포커스 카드는 표시하지 않고 원문 카드 안의 현재 경계 커서만 강조한다. 현재 위치·확인한 경계 수·진행 막대는 피드백 중에도 유지하고, 마지막 경계에 도달하면 결과 CTA를 본문 하단에 추가한다. 이동 커서는 글자 흐름 위에 오버레이해 줄바꿈을 바꾸지 않는다.
- 공백을 추가할 때마다 즉시 피드백한다. 정답 경계는 초록 체크·성공 효과, 오답 경계는 빨강 안내·오류 효과를 표시하고 선택한 `␣` 표식은 본문에 남긴다. 같은 버튼으로 공백을 취소하면 표식과 피드백을 제거한다. Reduce Motion에서는 흔들림을 생략하며 효과음·햅틱 설정을 즉시 따른다. VoiceOver에는 현재 위치 번호와 공백 추가 판정 결과를 알린다.
- 판정은 완성 문자열이나 자모가 아니라 비공백 원문 사이의 **공백 경계 집합**을 비교한다. 경계의 최초 판단은 처음 공백 추가를 시도하면 `띄어쓰기`, 공백 추가 없이 `다음`으로 이동하면 `붙여쓰기`로 한 번만 기록한다. `첫 판단 정확도 = 최초에 맞힌 경계 수 / 전체 경계 수`, 점수는 첫 판단 정확도×10의 1,000점 만점으로 산정한다. 이후 같은 경계의 공백 선택 변경은 수정 횟수로 누적하고, 현재 답은 기존 경계 집합으로 정답·누락·과잉을 계산해 최종 완성도로 별도 표시한다.
- 시간 제한과 카운트다운은 두지 않고 유효 풀이 시간만 초시계로 표시한다. 백그라운드에서는 유효 시간을 일시정지하고 세션 중 설정·광고·모달을 삽입하지 않는다.
- 결과는 공통 결과 연출을 사용해 최초 판단 기반 점수·랭크, 첫 판단 정확도, 최종 완성도, 수정 횟수, 풀이 시간을 제공한다. 최초로 틀린 경계는 `사용자 판단 → 정답` 형태의 짧은 단어 문맥으로 최대 8개 우선 표시하고 정답 원문·1탭 재도전도 유지한다. 덱 진행·자모 복습·입력 모드 기록과는 분리하되 완료한 게임 활동은 스트릭에 반영한다.
- AC: 게임 허브의 띄어쓰기 카드 → 1~6단계 긴 글 선택 → 좌우 수동 이동·가운데 공백 추가/취소·추가 시 즉시 피드백 → 마지막 완료 CTA → 최초 판단/최종 완성 결과 → 문맥 오답 복습 → 재도전이 설치 덱이나 네트워크 없이 가능하다. 선택 화면에는 순서대로 6개 단계가 표시되고 글 길이는 단계가 오를수록 늘어난다. 번들 글은 모두 100~200자이며 레벨 6은 180자 이상이다. UI는 비공백 문자를 수정하는 입력 경로를 제공하지 않으며 순수 판정 로직도 비공백 문자가 달라진 제출을 거부한다.

### F6f. 플랫폼 점수·랭킹 [P1]
- Game Center는 선택형 보조 계층이다. 로컬 `GameRecord`와 피요 성장 데이터가 단일 기준이며 로그인·네트워크·App Store Connect 장애와 무관하게 게임·성장·보상은 정상 동작한다. 인증된 플레이어에게만 저장된 최고 점수와 성장 업적을 비동기 동기화한다.
- 클래식 리더보드는 흐름·산성비·초성 맞추기·단어 맞추기·받아쓰기의 번들 100단어 복합 난이도 세트 v3에 게임별·난이도별로 3개씩, 총 15개를 운영하고 내장 키보드 기록만 제출한다. 산성비·초성·받아쓰기에는 `piyokey.v3.<game>.<beginner|intermediate|advanced>`를 사용한다. 가속 상한이 바뀐 흐름은 `piyokey.v4.flow.<beginner|intermediate|advanced>`, 4지선다에서 직접 타이핑으로 채점 조건이 바뀐 단어 맞추기는 `piyokey.v4.word_match.<beginner|intermediate|advanced>` 계약으로 분리한다. 게임 종류·난이도·채점 조건이 다른 점수는 섞지 않으며, 버전이 달라진 업데이트 세트, 다운로드·사용자 덱, OS IME는 로컬 최고 기록만 유지한다.
- 게임 허브에는 설치 여부와 무관하게 번들 `flow_topik_beginner` v3로 바로 시작하는 **주간 피요컵**을 제공한다. 피요컵은 흐름 60초·3목숨·0~50초 1.0→1.8배 점진 가속·내장 키보드로 고정하고 `piyokey.v4.cup.weekly.flow` recurring 리더보드와 `piyokey.v4.flow.beginner` 클래식 리더보드 양쪽에 제출한다. 피요컵 콘텐츠나 채점 조건을 바꿀 때는 기존 보드를 재사용하지 않고 새 계약 ID를 만든다.
- 게임 결과에는 해당 리더보드의 세계 순위와 열기 버튼을 제공한다. iOS 버전이 특정 리더보드 대시보드를 지원하지 않으면 전체 리더보드 대시보드로 안전하게 대체한다. 오프라인에서 만든 더 높은 로컬 기록은 다음 인증 때 보충 제출하되, 주간 피요컵은 JST 월요일 00:00부터 현재 회차 안에 만든 기록만 보충한다. 여러 대상에 제출되는 피요컵 기록은 가장 높은 세계 순위를 대표 순위로 표시한다.
- 로그인 UI와 Game Center 대시보드는 게임 결과 화면에서 사용자가 해당 리더보드 버튼을 눌렀을 때만 표시한다. 게임 허브에는 별도의 Game Center 전체 랭킹 버튼을 두지 않으며, 게임 플레이·레슨 세션 중에는 인증 요청, 배너, 대시보드 등 어떤 Game Center 모달도 삽입하지 않는다.
- 피요 성장은 기존 로컬 조건을 유지하면서 완료 챕터 1·3·6개, 누적 입력 12,000자모, 최장 스트릭 30일을 Game Center 업적 5개로 미러링한다. 최초 점수 제출 성공 또는 기존 서버 순위 확인 시 `챔피언 트로피` 소품을 영구 해금한다. 이후 변할 수 있는 현재 순위는 성장 단계나 영구 보상의 조건으로 사용하지 않는다.
- Android는 Play Games Services v2를 선택형 어댑터로 사용한다. 번들 고정 세트의 클래식 15개 논리 리더보드와 업적 5개만 Play Console이 발급한 opaque resource ID에 매핑하고, 로컬 `GameRecord`를 계속 단일 기준으로 둔다. Play Games의 recurring reset 시각은 JST 월요일 00:00 피요컵과 같지 않으므로 Android 피요컵 주간 기록은 로컬에서만 같은 계약으로 유지하고 원격 recurring 제출·순위 CTA는 서버 기반 회차 계약이 생길 때까지 제공하지 않는다.
- AC: Game Center 비로그인·오프라인 상태에서 플레이와 성장이 막히지 않는다. 피요컵은 덱 다운로드 없이 시작되고 내장 키보드로 고정된다. 인증 후 정확한 공식 덱·버전의 로컬 최고 점수만 5개 게임×3단계의 클래식 15개 리더보드에 제출되고, 피요컵 기록은 주간 보드와 고정 덱 보드 양쪽에 제출된다. 띄어쓰기 게임은 Game Center 랭킹 대상에서 제외한다. 성장 업적 진행률은 단조 증가하며 최초 성공 제출 뒤 트로피가 재실행·로그아웃 후에도 유지된다. App Store Connect에 코드와 동일한 클래식 15개·주간 recurring 1개 리더보드와 업적 5개 ID를 등록한 샌드박스 계정으로 대시보드·순위·업적을 확인한다.

### F7. 틀린 단어 자동 수집 + 복습 덱 [P0]
- 레슨/게임/홈 랜덤 단어 연습에서 오타가 발생한 단어를 자동으로 "복습 덱"에 수집 (경쟁 앱 TypeHangul 리뷰의 미충족 요구).
- 복습 덱은 마이페이지의 내 덱 영역에서 확인, "복습 연습 시작" 버튼으로 해당 단어만 재출제.
- 3회 연속 노미스로 치면 덱에서 자동 졸업.
- AC: 수집·졸업 로직 단위 테스트. 수동 추가/제거 가능.

### F8. 스트릭 + 데일리 챌린지 [P0]
- 첫 홈의 큰 CTA는 “이 덱으로 시작해요” 형태의 공식 덱 추천을 표시한다. 처음 시작은 1레벨 자판·자모 기초, 자모를 아는 사용자는 1레벨 쉬운 단어, 단어 입력 사용자는 2레벨, 문장 입력 사용자는 2~3레벨 문장 덱을 우선한다. 같은 적합도 안에서는 관심사 태그 → featured → 다운로드 수 → deck ID로 결정적으로 정렬한다. 미선택·스킵은 처음 시작 수준으로 fallback한다. 설치 여부는 첫 추천에서 제외 조건이 아니다. 카드 탭은 덱 상세로 이동하며 다운로드만으로는 이어하기가 되지 않는다. 부화 미션 완료 기록도 첫 홈 학습 이력으로 간주하지 않는다. 실제 진행 중 커리큘럼 또는 최근 플레이한 설치 덱이 있으면 같은 영역을 이어하기로 바꾸며, 이후 대상이 사라지면 기존 데일리 CTA로 fallback한다. 긴 덱명은 말줄임 대신 줄바꿈한다.
- 스트릭: 하루 1스테이지, 게임 1판 또는 홈 랜덤 단어 연습 1회 완료 시 스탬프. 홈에는 MY 피요 프로필과 7일 스탬프 캘린더를 하나의 카드로 결합하되, 마이페이지의 MY 피요 프로필은 그대로 유지한다.
- 홈에서는 MY 피요 결합 카드를 이어하기/데일리 CTA보다 먼저 표시한다. 스탬프 7칸은 출석 여부와 무관하게 JST 기준 현재 주의 월요일부터 일요일까지를 왼쪽에서 오른쪽으로 고정 배치한다. 완료·놓친 과거·오늘 미완료·예정인 미래를 서로 다른 시각과 접근성 상태로 구분하고, 주간 날짜 범위와 `이번 주 n/7` 진행을 연속 일수와 함께 표시한다.
- 홈과 마이페이지의 MY 피요 프로필에는 성장 단계명 대신 일본어 괄호 `「」`로 감싼 피요의 응원 멘트를 연한 분홍 말풍선으로 표시한다. 현지화한 10종을 학습 전·오늘 완료·스트릭 유지·복귀 상태별로 나누고, 각 상태 안에서는 JST 날짜 기반으로 결정적으로 순환해 같은 날의 두 화면과 앱 재실행 사이에 같은 멘트를 유지한다. 마이페이지 옷장 진입은 말풍선과 구분되는 우측 상단 44pt 원형 옷걸이 버튼으로 제공한다. 성장 단계명은 MY 피요 상세의 성장 정보에 계속 표시한다.
- 7일 카드에서 3/5/7일 달성 시 피요 옷장 아이템을 영구 해금한다. 홈의 결합 카드를 탭하면 MY 피요 상세 화면으로 이동하며, 피요 정보·7일 스탬프·현재/최장 스트릭·보상 조건과 획득 상태·피요 설정 진입을 한 화면에서 확인한다.
- 데일리 챌린지: 온보딩 목적에 맞춰 매일 바뀌는 단어 세트 1개 (로컬 시드 기반, 서버 불필요). "毎日3分" 프레이밍.
- 홈의 큰 이어하기/데일리 CTA 바로 아래에는 `주간 피요컵`과 `랜덤 단어 5개`를 일반 글자 크기에서 2열, 접근성 글자 크기에서 1열로 표시한다. 피요컵은 게임 탭과 같은 번들 덱·점수·리더보드 계약으로 추가 선택 없이 3·2·1 카운트다운에 직접 진입한다. 랜덤 연습은 다운로드한 공식 단어 덱 중 온보딩 선호 태그를 우선하고 부족하면 목표별 번들 공식 단어와 피요컵 초급 단어로 채운다. 세션은 공백 없는 고유 단어 5개이며 최근 20개를 우선 회피하고, 결과의 `다른 5문제`는 새 세트를 만든다. 완료는 스탬프에 반영하지만 데일리 챌린지 완료로 표시하지 않는다.
- 로컬 푸시 리마인더: iOS 신규 사용자는 온보딩·부화 미션·앱 투어의 마지막에 홈 진입 전 OS 알림 권한을 한 번 요청받는다. 허용하면 별도 앱 내 조작 없이 기기 현지 시각 20:00의 매일 알림을 예약하고, 거부하면 예약하지 않은 채 개인정보 안내로 계속한다. 시간대 변경·서머타임 뒤에도 현재 현지 20:00을 유지하며, 설정 화면의 ON/OFF·시간 지정은 선택적 오버라이드로 제공한다. 기존 사용자의 알림 선택은 덮어쓰지 않는다.
- AC: 자정(JST) 경계 처리, 날짜 변경 중 세션 처리 테스트.

### F9. 결과 공유 이미지 [P1]
- 결과 화면에서 게임명·선택 레벨·점수·콤보·스트릭을 담은 1,200×1,200 정사각형 PNG 이미지를 생성한다. 사용자가 추가한 덱은 레벨 대신 덱 이름을 표시한다.
- 액션은 `이미지 저장`과 `SNS로 공유`를 나란히 제공한다. 이미지 저장은 사용자가 버튼을 누른 시점에만 사진 추가 권한을 요청해 사진 라이브러리에 기록하고 성공·권한 거부·저장 실패를 화면 안에서 알린다.
- SNS 공유는 PNG와 현지화한 캡션·해시태그를 OS 공유 시트에 함께 전달해 X(트위터)·Threads 등 설치된 공유 확장에서 선택할 수 있게 한다.
- 이미지에 현재 앱 언어의 브랜드 로고(일본어 `ピヨキー`, 그 밖의 언어 `typee`)와 다운로드 유도 문구를 포함하고 X(트위터) 정사각형 프리뷰에 최적화한다.

### F10. 설정 [P0]
- 설정 섹션 순서는 `키보드 → 사운드 → 타이핑 표시 → 화면(언어·텍스트 표시) → 연습 알림 → 피요 → 개인정보 → 앱 정보`다. 키보드 섹션 첫 항목에는 내장 두벌식/천지인 배열을 항상 나란히 보여 주는 segmented control을 두며 OS 키보드가 기본 입력 모드이면 비활성화한다. 사운드 섹션의 효과음 ON/OFF는 기본 ON이며, 글자 크기 3단계 (경쟁 앱 불만 대응 — 문제 텍스트·키캡 모두 반영), 타건음 3종(기본/기계식/소프트), 햅틱 ON/OFF, 키 로마자 병기 ON/OFF, 입력 모드 기본값(내장/OS 키보드), 테마(라이트/다크), 언어(ja/en/es/de/fr)를 제공한다. 다음 키 가이드 ON/OFF는 iPhone과 iPad 모두에 제공하고, OS 모드의 기기 키보드 참조 배열 ON/OFF는 iPad에만 제공한다. 저장된 값이 없는 첫 실행은 기기 선호 언어의 첫 `ja`·`en`·`es`·`de`·`fr` 일치를 사용하고 그 밖의 언어는 `en`을 기본값으로 한다.
- 타이핑 표시 프리셋(학습/집중), 목표 단어·현재 앱 언어의 뜻·읽기의 6가지 순서 편집과 개별 ON/OFF, 자모·조합/입력 중 키·연습 중 피요 응원의 개별 ON/OFF, 문제 자동 발음 ON/OFF를 제공한다. 기본 학습 프리셋은 목표·뜻·자모·피요·조합/입력 중 키를 표시하고 읽기는 숨긴다. 이 섹션 하단에는 초성 맞추기의 뜻 ON/OFF를 두며 기본값은 ON이다. OFF에서는 문제 카드의 뜻 워드박스를 숨기고 플레이 중 뜻 힌트 버튼으로만 열 수 있다. 독립 `게임 표시` 섹션은 두지 않는다.
- 다음 키 가이드는 iPhone·iPad 내장 키보드 키캡의 상시 강조와 전역·세션 설정의 ON/OFF로 제어한다. 조합 프리뷰와 키보드 사이에는 별도 `ヒント` 버튼을 두지 않는다.
- 앱 정보에는 런타임 버전·빌드, 덱 제안/내용 제보, 일반 지원 링크를 제공한다. 개인정보처리방침 링크는 개인정보 섹션에 두며, 제안/제보 링크는 F5.8의 외부 운영 접수 정책을 따른다.
- 개인정보에는 `익명 사용 분석`과 `크래시 진단`을 서로 독립된 토글로 제공하고 둘 다 기본 OFF로 둔다. 변경은 즉시 반영·영속화하며, 세션 도중 별도 동의 모달을 띄우지 않는다. 분석 OFF는 제품 이벤트 전송을, 진단 OFF는 새 오류·크래시 보고 전송을 중단한다. 설정에는 입력한 글자·사용자 덱 내용·이름·이메일 주소를 전송하지 않는다는 짧은 안내와 동작하는 개인정보처리방침 링크를 유지한다. 공급자명과 최초 두 버튼 안내를 다시 여는 재검토 버튼은 표시하지 않으며 공급자·처리 상세는 개인정보처리방침에서 확인한다.
- 현재 개인정보 고지 버전을 검토한 기록이 없는 사용자는 온보딩·부화 미션·앱 투어가 끝난 뒤 OS 알림 권한 요청 다음에 최초 1회 선택 안내를 보고, 그 결정을 마친 뒤 홈으로 간다. 단, `retention.reminder.enabled`를 이미 저장한 업그레이드 사용자는 true와 false 어느 값도 덮어쓰거나 다시 활성화하지 않고 알림 권한 단계를 처리한 것으로 기록한 뒤 개인정보 안내로 계속한다. 안내에는 토글과 공급자명을 표시하지 않는다. `참여하고 계속`은 분석·진단을 모두 ON으로, `공유하지 않고 계속`은 모두 OFF로 저장하며 거부해도 앱 기능·구매 기능을 제한하지 않는다. 이후 설정에서는 두 토글을 독립적으로 변경·철회하고 비수집 안내와 개인정보처리방침 링크를 확인할 수 있으며, 같은 두 버튼 안내를 다시 여는 경로는 제공하지 않는다. 활성 레슨/게임, 결과 전환, 파일 가져오기·편집·결제 등 다른 모달 위에는 표시하지 않는다. `PrivacyNoticePolicy.currentVersion`은 1을 유지하며 이미 검토한 기존 사용자의 선택과 고지 기록을 덮어쓰지 않는다.
- AC: 모든 설정 즉시 반영·영속화 (경쟁 앱의 "설정이 저장 안 됨" 버그 이력 — 회귀 테스트).

### F12. 결과 화면 (S6) [P0] — 세션의 감정적 정점이자 다음 행동의 분기점
레슨·게임 공통 1개 컴포넌트. 역할 3가지: ① 성취의 연출 ② 학습 피드백 ③ 다음 덱/재도전으로의 유도. 광고·수익화 요소가 미래에 들어올 유일한 자리이므로 (§11) 레이아웃에 하단 여백 슬롯을 예약해 둔다.

#### F12.1 레이아웃 (위→아래 5존)
```
Z1 헤더    : 레슨은 덱 이름 칩, 게임은 게임명 + 선택 레벨 칩(추가 덱은 덱 이름) + 클리어 뱃지(레슨: 별 1~3 / 게임: 랭크 문자)
Z2 스코어  : 점수 대형 표기 + 자기 최고 기록 대비 (NEW RECORD 연출)
Z3 통계    : 정확도 / 최대 콤보 / 속도(글자·분) 3열 메트릭 카드
Z4 복습    : 틀린 항목 리스트 (0건이면 "パーフェクト!" 배너로 치환)
Z5 액션    : [もう一回] [前の画面へ] [復習する] [シェア] + 같은 태그 덱 추천 2개
```

#### F12.2 연출 시퀀스 (진입 시 1회, 총 ~2.5s, 스킵 가능 — 탭하면 최종 상태로 점프)
1. 0.0s Z1 뱃지 팝인 (scale 0.6→1.0 오버슈트). 별은 1개씩 순차 점등 + 파티클
2. 0.4s Z2 점수 카운트업 0→최종 (0.8s, ease-out). NEW RECORD면 종료 시 리본 배너 + 대형 파티클 버스트
3. 1.2s Z3 메트릭 게이지 3개 순차 채움 (각 0.3s)
4. 1.8s Z4/Z5 페이드인
- 랭크 문자(게임): S/A/B/C. 산정 = 정확도 60% + 속도 40% 가중, 경계는 콘텐츠 튜닝 파일로 외부화.
- 스트릭 달성이 이 세션에서 확정된 경우: 시퀀스 끝에 스탬프 쾅 오버레이 (0.6s) → 자동 소멸.

#### F12.3 통계 정의
- 정확도 = 정타 자모 수 / (정타+오타 자모 수). 표기 소수 1자리.
- 속도 = 완성 자모 수 / 유효 입력 시간(분). 일본 사용자에게 WPM은 낯설므로 "字/分" 표기.
- 최대 콤보, 클리어 시간, 플레이한 항목 수. 모든 수치는 GameRecord에 영속화 (§9).

#### F12.4 틀린 항목 리스트 (Z4)
- 항목별: `ko` 표기(오타 발생 자모를 빨간 밑줄 강조), 현재 앱 언어의 읽기와 뜻, 오타 횟수. 일본어는 가타카나, en/es/de/fr는 해당 뜻·공통 로마자, 미지원 UI 언어는 영어 뜻·로마자를 표시하고 일본어로 대체하지 않는다.
- 각 항목은 이미 복습 덱에 자동 수집됨(F7)을 아이콘으로 표시. 리스트 상한 5개 + "すべて見る".
- 탭하면 해당 항목 1개만 3회 반복 연습하는 퀵 드릴 모달 [P1].

#### F12.5 다음 행동 (Z5)
- 주 CTA `もう一回` (같은 덱 즉시 재시작, 1탭). 바로 아래에는 `前の画面へ`, 상단 우측에는 짧은 `完了`를 노출하며 두 액션 모두 결과 화면 진입 전 화면으로 돌아간다. 부 CTA `復習する` (틀린 항목만 재출제, 틀린 항목 0건이면 숨김).
- 첫 3개 부화 미션에서는 `もう一回`, `画像を保存`, `SNSでシェア`, `復習する`, 상단 `完了`를 숨기고 하단 주 CTA만 `次のミッションへ`(마지막은 앱 시작)로 바꾼 결과 화면을 벗어나지 않고 연속 진행한다.
- `画像を保存`·`SNSでシェア`: F9 공유 이미지 생성 뒤 각각 사진 라이브러리 저장과 OS 공유 시트로 연결.
- "같은 태그의 덱" 추천 2개 (§F5.6): 카드 미니 버전, 탭 → 덱 상세(S3a). 미설치 덱 우선.
- AC: 결과 화면 진입 후 `もう一回`까지 1탭. 연출 스킵 탭이 CTA 오작동을 일으키지 않는다 (연출 중 CTA 비활성).

### F11. 사운드 [P0]
- 키 타건음 (경쟁 앱에서 "타이핑 소리가 기분 좋다"가 호평 요소). 3종 프리셋 (기본/기계식/소프트)이며 신규·알 수 없는 설정의 기본값은 기본이다. 기본 타건음은 CC0 `Basic Mouse Click UI`의 공식 HQ 스트림에서 MP3 패딩을 제거한 19.125ms·48kHz·mono PCM을 사용하고, 리소스 누락·손상 시 같은 길이대의 원본 합성음으로 대체한다.
- 타건음은 원본 최대 레벨을 그대로 출력하지 않고 프리셋별 게인과 0.78 peak 상한을 적용한다. 일반 키는 피치·게인이 미세하게 다른 3개 변형을 결정적으로 순환하며, Backspace는 더 낮고 부드럽게, Shift는 더 짧고 조용하게 구분한다.
- 정타 완성음, 콤보 상승음(피치 상승), 오타음(불쾌하지 않은 낮은 톤).
- 앱이 제공하는 모든 고정 목표인 공식 카탈로그 26덱, 5개 게임의 3단계 프리셋 15덱, 커리큘럼과 그 부분집합인 데일리, 무료 샘플은 gTTS `ko` 보통 속도의 사전 생성 MP3를 번들에서 오프라인 재생한다. 중복을 제거한 581개 한국어 문구는 SHA-256 기반 canonical 경로로 같은 음원을 공유하며 앱은 런타임에 gTTS나 네트워크를 호출하지 않는다.
- 이후 앱 제공 고정 목표를 추가하거나 한국어 문구를 수정할 때도 같은 gTTS 설정과 canonical MP3 규칙을 의무 적용한다. 덱 밖 고정 문구는 `pronunciation_prompts.json`에 등록하고, 음원 생성·CoreAudio 디코딩·전체 번들 커버리지 프리플라이트가 통과하기 전에는 병합·출시하지 않는다. macOS/Yuna 발음, 발음용 CAF, 새 고정 문구의 기기 TTS 전용 제공으로 되돌리지 않으며 제공자·설정 변경은 명시적 제품 결정으로만 허용한다.
- 발음 resolver는 항목이 선언한 `audio` → 목표 한국어의 canonical hash MP3 → 플랫폼 기기 TTS 순서로 시도한다. iOS는 `AVSpeechSynthesizer(ko-KR)`, Android는 네트워크 연결을 요구하지 않는 `TextToSpeech(ko-KR)` 음성만 최종 폴백으로 사용한다. 선언 파일이 누락·손상되어도 canonical 파일을 다시 시도하고, 번들에 없는 사용자 덱·비공개 동적 콘텐츠와 모든 파일 재생 실패만 기기 TTS로 대체한다.
- 타건음·정오답·콤보 등 효과음은 기본 ON이다. iOS는 `AVAudioSession` category `.playback` + `.mixWithOthers`, Android는 다른 앱을 멈추거나 duck하지 않도록 audio-focus gain을 요청하지 않는 media/game usage로 재생한다. 사용자는 전역·세션 설정에서 즉시 OFF할 수 있고 OFF 상태는 영속화한다.
- 사용자가 누른 목표 발음과 설정에서 명시적으로 켠 문제 자동 발음도 `.playback` + `.mixWithOthers`로 무음 모드에서 재생한다. 발음 재생 중에는 효과음을 억제하고 종료 즉시 효과음용 `.playback` 기본 모드로 복원한다.
- 타건음과 정오답·콤보는 독립 플레이어 풀을 사용해 빠른 입력이 중요한 피드백음을 끊지 않는다. 앱 진입만으로 오디오 엔진을 시작하지 않고 첫 재생 때 지연 시작하며, 마지막 효과음 15초 뒤 또는 백그라운드 진입 시 엔진·세션을 반납한다.
- 백그라운드 진입 시 연습·받아쓰기의 진행 중인 발음을 즉시 중지한다. 연습은 복귀 후 문제 자동 발음 설정이 ON일 때 현재 목표를 다시 자동 재생하고, 받아쓰기는 복귀 카운트다운이 끝난 뒤 현재 문제 발음을 다시 자동 재생한다. 수동 재생만 했거나 연습 자동 발음이 OFF인 경우 복귀만으로 새 발음을 시작하지 않는다.
- 통화·Siri 인터럽트, 유선·Bluetooth 출력 변경, 미디어 서비스 재시작 뒤에는 실제 세션 상태를 다시 동기화하고 필요 시 오디오 그래프를 재구성해 다음 입력 전에 자동 복구한다.
- 어떤 정책에서도 다른 앱 음악(Spotify 등) 재생 중단 금지 (경쟁 앱 버그 이력 — 필수 확인).

---

## 6. 한글 조합 엔진 스펙 (플랫폼 공통, 최우선 품질 모듈)

### 6.1 원칙
- 순수 함수 상태 기계로 구현. UI와 완전 분리. iOS/Android 동일 알고리즘, 동일 테스트 벡터.
- 입력: 자모 키 이벤트 스트림 + 백스페이스. 출력: 현재 조합 중 음절(프리뷰) + 확정 문자열.

### 6.2 상태 기계
```
상태: EMPTY → CHO(초성) → CHOJUNG(초성+중성) → CHOJUNGJONG(초성+중성+종성)
전이 규칙:
- EMPTY + 자음 → CHO
- EMPTY + 모음 → 모음 단독 확정 대기 (ㅏ 단독 표기 허용)
- CHO + 모음 → CHOJUNG
- CHO + 자음 → 이전 자음 단독 확정, 새 자음 CHO
- CHOJUNG + 모음 → 복합모음 가능하면 결합(ㅗ+ㅏ=ㅘ, ㅜ+ㅓ=ㅝ, ㅡ+ㅣ=ㅢ 등),
                  불가하면 현재 음절 확정 후 새 모음
- CHOJUNG + 자음 → 종성 가능 자음이면 CHOJUNGJONG, 불가(ㄸㅃㅉ)면 음절 확정 후 새 CHO
- CHOJUNGJONG + 자음 → 복합종성 가능하면 결합(ㄱ+ㅅ=ㄳ, ㄹ+ㄱ=ㄺ 등 11종),
                     불가하면 음절 확정 후 새 CHO
- CHOJUNGJONG + 모음 → 종성(복합종성이면 마지막 요소)을 다음 음절 초성으로 이월(도깨비 현상):
                     "간"+ㅏ → "가나", "닭"+ㅏ → "달가"
- 백스페이스 → 조합 역순 해체 (자모 단위: "간"→"가"→"ㄱ"→EMPTY)
```
- 유니코드 조합: `syllable = 0xAC00 + (cho×21 + jung)×28 + jong`.

### 6.3 판정 규칙 (게임/레슨 공통)
- 판정은 자모 단위로 실시간. 목표 음절의 다음 기대 자모와 입력 자모가 일치하면 정타, 불일치하면 오타 1회 기록 + 입력 무시(목표 텍스트는 오염되지 않음).
- 이월(도깨비) 케이스: 목표가 "가나"일 때 ㄱㅏㄴㅏ 입력 순서를 정타로 판정해야 함. 즉 판정 기준은 완성 음절이 아니라 **자모 시퀀스** (목표 문자열을 자모 시퀀스로 분해해 두고 인덱스로 비교).
- Shift 필요 자모(ㅃ 등)는 Shift+ㅂ 시퀀스를 1자모로 취급.
- 한국어 10키의 반복키·모음 획은 판정기 앞의 입력 어댑터 책임이다. recipe 완성 전 raw 액션은 판정 인덱스를 전진시키지 않고, 완성 시 대응하는 표준 자모 하나만 이 규칙에 전달한다.
- AC: 오답 판정 관련 사용자 불만 사례(§1.2)를 테스트 케이스로 역구현하여 모두 통과.

### 6.4 공용 테스트 벡터 (활성 구현 통과 필수)
최소 포함: `안녕하세요`, `사랑해요`, `닭`(복합종성), `없다`(ㅄ), `달가`(이월), `외국`(ㅚ), `의자`(ㅢ), `띄어쓰기 포함 문장`, `꿀`(된소리 Shift), 백스페이스 해체 시나리오 10종. 테스트 벡터는 `shared/test_vectors.json`으로 관리하고 활성 Python reference, Swift와 웹 구현이 동일 파일을 읽는다. 동결된 Android 포트는 v6.14 이후 필수 gate가 아니다.

---

## 7. 비주얼 어포던스 스펙 (글로벌 공통, 일본 특화 미감 유지)

### 7.1 아트 디렉션
- 톤: 카와이 + 카라풀 팝 (미니멀 금지 — 직접 경쟁 앱이 미니멀을 표방하므로 정반대 포지션).
- 파스텔 베이스 + 고채도 액센트. 라운드 코너, 스티커/노트 풍 프레임.
- 마스코트 1종 [P1]: 타이핑에 반응하는 작은 캐릭터 (정타 연속 시 기뻐함, 오타 시 갸웃. 결과 화면에서 코멘트). MVP에서는 정적 3표정으로 축소 가능.
- 마스코트 옷장 우선순위: `자동`은 성장 단계의 기본 외형만 사용하고 덱·카드 태그로 소품을 장착하지 않는다. 사용자가 `없음` 또는 해금한 고정 아이템을 직접 선택하면 해당 선택을 모든 연습·게임에서 유지한다. 날짜·계절·기념일에 따라 자동으로 붙는 복장이나 장식은 제공하지 않는다.
- 옷장에는 현재 성장 단계와 선택 소품을 함께 보여 주는 최종 모습 미리보기를 둔다. 온보딩에서 목표에 따라 정한 알 무늬는 성장 기록으로 유지하되 옷장에서 별도의 4종 선택 UI를 노출하지 않는다. 한번 조건을 달성해 해금한 비계절 소품은 이후 스트릭이나 설치 덱 수가 감소해도 영구 유지한다.
- 폰트: 한글은 라운드 산세리프 (예: 다국어 지원되는 Noto Sans KR 계열 + 라운드 대체 검토), 일본어 UI는 라운드 고딕 (예: M PLUS Rounded 1c), 영어 UI는 같은 시각 무게의 Latin 글꼴을 사용한다. 라이선스 확인 후 결정.

### 7.2 코어 피드백 (P0)
| 이벤트 | 시각 | 지속 |
|---|---|---|
| 키 다운 | 키캡 scale 0.95 + 하이라이트 | ≤120ms |
| 다음 키 가이드 | 키캡 글로우 펄스 (은은한 2s 루프) | 상시 |
| 정타 자모 | 문제 텍스트에서 해당 자모 색 전환(회색→포인트색) | 즉시 |
| 음절 완성 | 조합 프리뷰에서 자모가 합쳐지는 애니메이션 (ㄱ+ㅏ가 물리적으로 슬라이드 결합→살짝 팝) | ≤200ms |
| 단어 완성 | 파티클 버스트 (별/반짝 6~10개) + 카드 팝 | ≤400ms |
| 오타 | 해당 글자 셰이크 + 빨강 플래시 (짧고 불쾌하지 않게) | ≤250ms |

**조합 프리뷰**: 문제 텍스트와 별도로, 현재 조합 중인 음절을 크게 표시하는 영역. 자모가 어떻게 합쳐져 글자가 되는지 보여주는 것 자체가 학습 콘텐츠이며 이 앱의 시그니처 연출. 최우선 폴리싱 대상.

### 7.3 콤보 점층 (P0, 게임 모드)
- 5콤보: 콤보 숫자 등장 + 소형 파티클
- 10콤보: 숫자 커짐 + 화면 가장자리 은은한 글로우
- 20콤보: 배경 톤 상승 + 파티클 리치 + 콤보음 피치 상승
- 콤보 브레이크: 숫자가 무너지는 연출 (0.3s, 과하지 않게)
- 원칙: "잘할수록 화려해진다". 단, 가독성을 해치는 화면 점유 금지.

### 7.4 진척 시각화 (P0)
- 스테이지 맵: 스탬프/도장 미학. 클리어 시 도장 쾅 애니메이션.
- 스트릭 캘린더: 스탬프 카드 스타일.
- 결과 화면: 게이지가 차오르는 카운트업 연출 (숫자 즉시 표시 금지, 0.8s 카운트업).

---

## 8. 콘텐츠 데이터 스키마

### 8.1 덱 파일 (다운로드 대상, 덱 1개 = JSON 1개)
```json
{
  "deck_id": "official_trending_korean",
  "version": 1,
  "name": "今どき韓国語",
  "author": { "id": "official_hanco", "nickname": "ピヨキー 公式" },
  "official": true,
  "type": "word",
  "level": 2,
  "tags": ["今どき", "SNS", "公式"],
  "localizations": {
    "en": {
      "name": "Trending Korean",
      "author_nickname": "typee Official",
      "tags": ["Trending", "Social", "Official"]
    },
    "ko": {
      "name": "요즘 한국어",
      "author_nickname": "typee 공식",
      "tags": ["요즘", "SNS", "공식"]
    }
  },
  "created_at": "2026-07-20T00:00:00Z",
  "updated_at": "2026-07-20T00:00:00Z",
  "items": [
    {
      "id": "i_001",
      "ko": "저점매수",
      "reading_ja": "チョジョムメス",
      "meaning_ja": "底値買い",
      "localizations": {
        "en": {
          "reading": "jeojeommaesu",
          "meaning": "buying the dip"
        }
      },
      "audio": "audio/ko_f5ce92f18ddbe70c5ece.mp3"
    }
  ]
}
```
- 기존 `name`·`author.nickname`·`tags`·`reading_ja`·`meaning_ja`는 일본어 호환 원본으로 유지한다. `localizations`는 기존 설치 덱과 저장 데이터를 깨지 않는 추가 선택 필드이며 허용 키는 `en`·`ko`·`es`·`de`·`fr`다. 새 앱은 기존 덱을 읽지만 구버전 앱은 새 키를 거부할 수 있다. `.typedeck` format/schema 버전은 1을 유지하며 새 번역 포함 파일은 새 앱끼리 교환한다.
- 글로벌 배포 대상 공식 덱은 덱 메타데이터의 `en`·`ko`·`es`·`de`·`fr` 현지화와 모든 항목의 en/es/de/fr 뜻·읽기를 필수로 갖춘다. 메타데이터로 선언한 en/es/de/fr는 전체 항목·카탈로그 미리보기에 빠짐없이 있어야 한다. 영어 `reading`은 라틴 문자 로마자다. 기존 `ko` 메타데이터·뜻·읽기는 데이터 호환성을 위해 그대로 보존하지만 앱 UI 언어 선택에는 노출하지 않는다.
- 앱의 해석 순서는 일본어 UI=`*_ja`, en/es/de/fr=`localizations.<언어>` → 누락 시 `localizations.en`, 미지원 UI 언어(한국어 포함)=`localizations.en`이다. 공용 데이터 reader의 기존 `localizations.ko` 지원과 schema는 유지한다. 일본어 외 앱 언어에서는 `*_ja`를 fallback으로 사용하지 않는다.
- 새 언어 카탈로그는 별도 정적 namespace로 게시하고 신규 앱에만 연결한다. 기존 catalog URL·구버전 자산은 보존하며, 로컬 생성만으로 원격 게시 완료로 보지 않는다. 게임 preset v3의 한국어 목표·ID·랭킹 조건은 유지하므로 번역만으로 경쟁 버전을 바꾸지 않는다.
- `created_at`·`updated_at`과 카탈로그 `generated_at`은 플랫폼별 재인코딩 차이를 막기 위해 UTC 초 단위 `YYYY-MM-DDTHH:MM:SSZ`만 허용한다. 시간대 offset과 소수 초는 가져오기 전에 정규화하거나 거부한다.

### 8.2 카탈로그 인덱스 (발견 탭 데이터 소스, 읽기 전용)
```json
{
  "catalog_version": 128,
  "generated_at": "2026-07-17T00:00:00Z",
  "decks": [
    {
      "deck_id": "official_trending_korean",
      "version": 1,
      "name": "今どき韓国語",
      "author_nickname": "ピヨキー 公式",
      "official": true,
      "featured": true,
      "type": "word",
      "level": 2,
      "tags": ["今どき", "SNS", "公式"],
      "localizations": {
        "en": {
          "name": "Trending Korean",
          "author_nickname": "typee Official",
          "tags": ["Trending", "Social", "Official"]
        },
        "ko": {
          "name": "요즘 한국어",
          "author_nickname": "typee 공식",
          "tags": ["요즘", "SNS", "공식"]
        }
      },
      "item_count": 12,
      "size_bytes": 6200,
      "downloads_total": 0,
      "downloads_7d": 0,
      "created_at": "2026-07-20T00:00:00Z",
      "preview_items": [
        {
          "ko": "저점매수",
          "meaning_ja": "底値買い",
          "localizations": { "en": { "meaning": "buying the dip" } }
        }
      ],
      "file_url": "decks/official_trending_korean_v1.json"
    }
  ],
  "tags": [
    {
      "tag": "今どき",
      "deck_count": 1,
      "category": "purpose",
      "localizations": { "en": "Trending", "ko": "요즘" }
    }
  ]
}
```
- 실제 다운로드 집계가 도입되기 전 `downloads_total`, `downloads_7d`는 0이며 UI에서 숨긴다.
- `preview_items`는 최대 10개. 검색 인덱스는 이 파일 하나로 로컬 구축하며 현재 앱 언어로 해석한 `name`·`tags`·`author_nickname`을 사용한다.

### 8.3 호스팅 가정 (MVP)
- 정적 파일 호스팅 (S3/CloudFront 또는 GitHub Pages 수준): `catalog.json` + `decks/*.json`. 쓰기 API 없음.
- F5.8의 제안·제보는 공개 지원 메일로 외부 작성되며 카탈로그 호스트에 쓰기 엔드포인트를 추가하지 않는다. 접수 내용은 운영자가 검수한 뒤 로컬 Deck Studio에서 별도로 반영한다.
- 클라이언트는 `catalog.json`을 ETag 조건부 GET. 통계(다운로드 수)는 서버 측 사전 집계 값으로 가정 — 클라이언트가 카운트를 올리는 API는 v1.1+ (MVP에선 값 표시만).
- 개발용: 동일 스키마의 공식 출시 카탈로그(기초·목적·요즘 한국어·창작 K-POP/K드라마 17덱 + 상황별 재미 표현 9덱, 총 26덱)를 fixture로 생성해 리포에 포함할 것. **에이전트는 이 목 데이터 생성기를 실제로 구현한다.**
- 검증기: 모든 `ko` 필드가 공백 포함 10자 이하이고 조합 엔진으로 자모 분해 가능한지 CI에서 검사. 스키마 검증(JSON Schema) 포함.
- MVP 공식 시드 26덱은 앱 번들 동봉 (§F5.7). 콘텐츠 검수와 `今どき韓国語`·창작 팬·상황별 재미 콘텐츠 갱신은 운영 트랙으로 관리한다.

### 8.4 `.typedeck` 사용자 문서 규격 — container v1, deck schema v1/v2 (앱 1.1)

**식별과 컨테이너**

| 항목 | v1 계약 |
|---|---|
| 확장자 | `.typedeck` |
| MIME | `application/vnd.piyokey.deck+zip` |
| Apple UTType | `app.piyokey.piyodeck` |
| 컨테이너 | 표준 ZIP, 암호화·ZIP64·다중 볼륨 없이 `STORE` 방식 |
| 문자 인코딩 | UTF-8, BOM 없음 |
| 덱 수 | package 하나에 정확히 한 덱 |
| 엔트리 | 루트의 `manifest.json`, `deck.json` 정확히 2개 |

v1은 텍스트만 지원한다. `media/`, 이미지, 녹음, 원격 URL, HTML, JavaScript, 실행 코드, 플러그인, 결제·계정 정보는 허용하지 않는다. 향후 미디어가 필요하면 v1 reader의 허용 목록을 느슨하게 바꾸지 않고 `format_version`을 올린다.

```text
my-topik-deck.typedeck
├── manifest.json
└── deck.json
```

**`manifest.json`**

```json
{
  "format": "piyokey.deck-package",
  "format_version": 1,
  "deck_schema_version": 2,
  "deck": {
    "path": "deck.json",
    "media_type": "application/json",
    "deck_id": "user_550e8400e29b41d4a716446655440000",
    "deck_version": 3,
    "item_count": 120,
    "size_bytes": 48231,
    "sha256": "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
  }
}
```

- manifest는 위 필드만 허용한다. `format_version`은 package 디렉터리 구조, `deck_schema_version`은 `deck.json` 스키마, `deck.deck_version`은 동일 논리 덱의 콘텐츠 revision을 뜻하며 서로 대신하지 않는다.
- `deck_id`, `deck_version`, `item_count`, `size_bytes`는 실제 `deck.json`과 정확히 일치해야 한다. `sha256`은 raw `deck.json` bytes의 소문자 64자리 SHA-256이며 손상·충돌 판정용이지 서명·제작자 인증·DRM이 아니다.
- 개인·기기·구매 식별자, 이메일, 영수증, 생성 기기와 절대 파일 경로를 manifest에 넣지 않는다. 알 수 없는 manifest 필드는 v1에서 거부한다.

**`deck.json` 사용자 덱 정책**

- 데이터 모양은 §8.1과 `shared/schema/deck.schema.json`을 사용하고 DeckKit 의미 검증도 통과해야 한다. package 정책은 공용 schema보다 엄격하며 항목 수를 `1...1,000`으로 제한한다.
- `deck_id`는 정규식 `^user_[0-9a-f]{32}$`, 각 item ID는 `^item_[0-9a-f]{32}$`를 만족하고 package 안에서 고유해야 한다. 앱이 생성·복제할 때 암호학적 난수 UUID의 하이픈을 제거한 소문자 32 hex를 사용한다.
- `official`은 반드시 `false`, `version`은 1 이상이다. 공식 ID·Game Center 고정 덱 ID·번들/원격 설치 ID와 충돌하면 거부한다.
- `ko`는 공백 포함 10자 이하이고 §6 조합 엔진으로 입력 가능해야 한다. deck schema v1은 배포된 계약대로 `default_locale` 없이 일본어 base field와 선택적 `en`·`ko` localizations를 사용한다.
- deck schema v2는 루트 `default_locale`을 필수로 두고 덱과 항목의 `localizations` 키에 allowlist 없는 canonical BCP 47 태그를 허용한다. malformed·noncanonical·중복 태그는 거부한다. 덱 metadata localization 키가 콘텐츠 언어 집합이며 모든 항목은 선언된 각 언어의 nonempty `meaning`과 `reading`을 가져야 한다. 항목에만 있는 미선언 언어도 거부한다.
- 공용 v2 reader·writer·model은 공식 다국어 카탈로그와 기존 파일 호환을 위해 여러 콘텐츠 언어를 계속 지원한다. 모바일 제품 계층에서 새로 만들거나 편집 저장하는 사용자 덱만 metadata localization과 `default_locale`이 같은 단일 발견용 덱 언어 태그여야 한다. 이 key는 wire 호환 매핑이며 뜻·발음이 그 언어로 번역됐다는 구조적 선언이 아니다. 기존 다국어 사용자 파일은 읽기와 변경 없는 내보내기를 허용하되 편집 저장 전 한 콘텐츠 묶음을 명시적으로 선택하며, 선택된 값과 알 수 없는 정상 태그를 손실 없이 보존한다.
- 표시 값은 일본어 요청에서 정확한 요청 태그(`ja-JP`) → 기본 언어 subtag(`ja`) → 기존 일본어 base field 순서로 조회하고, 그 밖의 요청은 정확한 요청 태그(`fr-CA`) → 기본 언어 subtag(`fr`) → `default_locale` → `en` 순서로 조회한다. 일본어 외 앱 언어에서는 기존 일본어 base field를 fallback으로 사용하지 않는다. v2 base field는 `ja`가 선언되면 일본어를, 아니면 default locale 값을 mirror한다. v1 일본어 base field를 편집·재내보낼 때는 `ja`를 명시적으로 선언하고 원래 base 값을 보존한다.
- v1 item의 `audio`는 반드시 `null`이다. 절대/상대 URL, package 내부 경로를 넣어도 거부한다. 미정의 필드와 JSON 중복 키, 유효하지 않은 Unicode, BOM과 비 UTF-8 데이터도 거부한다.
- 공식 exporter는 안정된 키 순서·RFC 3339 UTC 날짜·줄바꿈 없는 JSON을 사용해 같은 모델을 다시 내보낼 때 같은 `deck.json` bytes와 SHA가 되게 한다. importer는 키 순서와 허용 공백 차이는 받아들이되 manifest의 raw bytes hash를 그대로 검증한다.

**자원·압축 안전 제한**

- 전체 `.typedeck` 최대 8 MiB, `manifest.json` 최대 16 KiB, `deck.json` 최대 4 MiB다. 크기는 엔트리를 메모리에 모두 펼치기 전에 ZIP metadata와 제한형 stream 양쪽에서 검사한다.
- 엔트리는 정확한 루트 파일명 2개만 허용한다. 절대경로, `..`, 빈 경로 요소, 역슬래시, 이름 정규화 후 중복, directory·symlink·hardlink·device entry와 중첩 archive를 거부한다.
- ZIP 암호화, data descriptor로 크기를 숨긴 엔트리, ZIP64, 다중 볼륨, 겹치거나 범위를 벗어난 offset, CRC32 불일치를 거부한다. CRC32 확인 후 SHA-256, JSON Schema, DeckKit 의미 검증 순서로 진행한다.
- 검증은 앱 전용 staging 디렉터리의 복사본만 읽으며 원본 문서를 수정하지 않는다. 어느 단계든 실패하면 설치 payload·인덱스·학습 기록을 변경하지 않고 staging을 정리한다.
- 외부 파일은 원본 권한을 필요한 복사 시간에만 열고 접근을 즉시 해제한다. iOS는 security-scoped URL, Android는 Storage Access Framework의 `ContentResolver`로 읽으며 저장소 전체 권한은 요청하지 않는다. Android의 open/view/send URI도 먼저 8 MiB+1 제한으로 앱 전용 staging에 복사하고 검증하며, export는 `ACTION_CREATE_DOCUMENT` 또는 `content://` 공유 URI만 사용한다. 사용자 덱의 이름·항목·파일·hash는 분석이나 오류 수집 서버로 보내지 않는다.

**호환성과 소유권 경계**

- v1 reader는 `format_version=1`, `deck_schema_version=1`만 연다. 더 높은 버전은 손상으로 부르지 않고 `지원되지 않음/앱 업데이트 필요`로 구분하며 파일을 보존한다.
- 앱 밖의 어떤 호환 제작 도구도 이 공개 규격으로 파일을 만들 수 있다. importer는 PIYOKEY exporter 서명이나 `pro` 구매 증명을 요구하지 않는다.
- import는 내용의 저작권·정확성·안전성을 공식 인증하지 않는다. 내 덱에 사용자 덱 배지를 표시하고, 공식 배지·다운로드 수·Game Center 자격을 부여하지 않는다.

## 9. 로컬 데이터 모델

```
InstalledDeck: deck_id, version, installed_at, last_played_at, source(bundle|remote|imported|created), json_blob, content_sha256?, package_format_version?, is_locally_modified, derived_from_deck_id?
CatalogCache: catalog_version, fetched_at, etag, json_blob
UserProgress: stage_id, stars, best_accuracy, completed_at
DeckProgress: deck_id, plays, best_score, best_accuracy, last_played_at
GameRecord: mode, deck_id, course, score, max_combo, accuracy, input_mode (`builtin | builtin_korean_10key | os_ime`), played_at
ReviewDeckItem: item_id, source_deck_id, miss_count, consecutive_perfect, added_at, graduated_at?
StreakDay: date(JST), fulfilled
Settings: font_scale, sound, haptics, romaja_hint, key_guide, input_mode_default, theme, locale, reminder_time?
UserDeckDraft: draft_id, base_deck_id?, base_version?, created_at, updated_at, validated_json_blob
PendingDeckImport: staging_file_name, received_at, source_context, validation_state
DeckMakerEntitlementCache: product_id, verified, last_verified_at
```
- iOS: SwiftData(또는 GRDB), Android: Room. 마이그레이션 전략 포함.
- 파일 기반 JSON 저장은 원자적 쓰기와 최신 검증 백업 1개를 유지한다. 기존 데이터는 첫 정상 로드 때 백업이 없으면 즉시 생성한다. 주 파일의 디코딩·의미 검증 실패 시 손상 원본을 격리하고 백업을 자동 복구하되, 앱보다 높은 스키마 버전은 다운그레이드하지 않고 그대로 보존한다. 설치 덱 파일 하나가 복구 불가능해도 다른 덱·다운로드 태그 이력은 유지한다.
- 로컬 학습 기록 쓰기가 실패하면 실패한 변경을 재시도 가능 상태로 보존하고 홈 상단의 비모달 배너로 알린다. 세션 도중에는 배너·모달을 표시하지 않으며 사용자가 홈에 돌아온 뒤 명시적으로 재시도한다.
- `InstalledDeck.source=imported|created` payload와 package metadata도 기존 복구 가능한 JSON 정책과 동일한 primary/backup 세대를 사용한다. import·replace·edit save는 payload와 설치 인덱스를 하나의 논리 transaction으로 commit하고 시작 시 미완료 staging을 roll back 또는 완결한다.
- `UserDeckDraft`는 구매 entitlement가 아니라 사용자 작성 데이터다. 저장 실패·앱 비활성화·구매 취소 때 삭제하지 않고, 완료 저장 뒤에만 명시적으로 정리한다. 공식 사본은 `derived_from_deck_id`만 기록하며 진행 데이터는 새 ID로 독립한다.
- 앱 1.1은 기기당 활성 `UserDeckDraft` 하나를 primary/backup JSON으로 보존한다. 같은 편집 흐름의 자동 저장은 `draft_id`와 최초 `created_at`을 유지하고, 다른 제작 흐름은 새 초안 쓰기가 성공한 뒤에만 기존 활성 초안을 교체한다. 미래 schema는 보존하고 이전 앱이 덮어쓰지 않는다.
- 기존 사용자 덱 편집 commit은 `base_version`을 `DeckInstallationStore` transaction 내부의 현재 version과 비교한 뒤에만 payload와 index를 교체한다. 불일치하면 typed source-changed 오류로 중단하고 기존 덱·초안·설치 journal을 변경하지 않는다.
- `DeckMakerEntitlementCache`는 StoreKit 2 검증 결과의 오프라인 UX cache일 뿐 자체 라이선스가 아니다. 계정이나 서버 entitlement를 만들지 않고 `Transaction.currentEntitlements`와 transaction update를 기준으로 갱신한다.
- Android는 Room에 설치 메타데이터·학습/게임/복습/스트릭 기록과 복구 journal을, 앱 전용 파일에 실제 덱·카탈로그 payload와 primary/backup/quarantine·초안·SAF staging을, Preferences DataStore에 로케일·사운드·입력·마스코트·리마인더 같은 작은 설정만 저장한다. persistent journal → 같은 디렉터리의 원자적 move → Room transaction 순서와 시작 시 복구로 파일과 DB가 항상 이전 pair 또는 새 pair 중 하나로 수렴하게 한다.
- Android의 entitlement cache도 표시와 오프라인 UX를 위한 마지막 확인값일 뿐 제작 mutation의 단독 권한 근거가 아니다. foreground 연결 때 Play Billing 구매를 다시 조회하고 `PURCHASED`만 권한을 열며 pending·환불·취소가 확인돼도 기존 덱과 초안을 삭제하지 않는다.

## 10. 익명 제품 분석과 크래시 진단

- iOS/iPadOS·Android·웹 제품 분석은 PostHog Cloud EU를 사용하고, 네이티브 크래시·ANR은 Firebase Crashlytics, 웹 오류는 PostHog Error Tracking을 사용한다. 무료 제공량을 기본 운영 한도로 삼으며 초과 과금은 별도 사용자 승인 전 활성화하지 않는다.
- `shared/analytics/events.json`이 플랫폼 공통 이벤트·속성 allowlist의 단일 원본이다. 의미 이벤트만 전송하며 자동 화면/요소/클릭/키 입력 캡처, 세션 리플레이, heatmap, feature flag 자동 요청, 사용자 식별·프로필 생성을 비활성화한다.
- 분석과 진단은 F10의 독립 동의가 각각 ON인 경우에만 동작하고 기본값은 OFF다. Debug/test 빌드와 프로젝트 키·서비스 설정이 없는 빌드는 항상 no-op이며 앱 기능과 Release 빌드를 막지 않는다.
- 최초 선택 안내는 수집 목적, 전송되는 범주, 전송하지 않는 입력·사용자 콘텐츠, 선택 사항임과 철회 경로를 현재 앱 언어로 설명하되 공급자명은 사용자 안내에 노출하지 않는다. 공급자와 보존·처리 상세는 개인정보처리방침에서 설명한다. 개인정보처리방침 링크는 안내와 설정 모두에서 접근 가능해야 하고, 고지의 목적·공급자·데이터 범주가 실질적으로 바뀌면 고지 버전을 올려 다시 선택을 받는다. 광고 식별자(IDFA)를 사용하거나 ATT 권한을 요청하지 않는다.
- 공통 속성은 플랫폼, 앱 버전·빌드, 앱 로케일, 기능·세션·입력 방식·게임 난이도 같은 고정 enum과 수치 bucket만 허용한다. 이름·이메일·광고 ID·IP 저장 요청·자유 입력·키 입력·조합 중 텍스트·정답/오답 문자열·덱/항목 이름·사용자 덱 ID/hash/내용·파일 경로/URI·지원 메일 내용·구매 영수증은 전송하지 않는다.
- PostHog는 person profile을 만들지 않는 익명 device-scoped ID만 사용한다. 계정·로그인과 교차 기기 identity merge는 추가하지 않으며 광고/추적 목적으로 사용하지 않는다.
- Crashlytics에는 앱 버전·기능·세션 종류·입력 방식·게임 모드 같은 allowlist context와 비민감 breadcrumb만 남긴다. iOS dSYM과 Android R8 mapping 업로드는 실제 Firebase 설정이 있는 배포 빌드에서 검증한다.
- 운영 대시보드는 온보딩 funnel, 기능/게임/덱 종류별 사용, 세션 완료·이탈, Deck Maker·구매 흐름을 제공한다. 크래시 프리 세션 99.5% 미만과 신규 치명 오류는 알림 대상으로 한다. 정확한 대시보드·알림·retention 정의는 `docs/ANALYTICS.md`에 고정한다.
- 개인정보처리방침, App Store Privacy, Play Data safety는 실제 수집 항목과 동의 정책을 반영한 뒤에만 배포한다. 외부 콘솔 프로젝트·키·소스맵/심볼 업로드·알림·정책 URL이 준비되지 않은 상태는 출시 gate로 유지한다.
- 덱 제안·제보 CTA 탭과 OS 메일 내용은 분석 서버로 보내지 않는다. 사용자가 OS 메일 작성 화면에서 직접 전송한 내용만 공개 지원 메일 운영 정책에 따라 처리한다.

## 11. 수익화 가드레일 (MVP 이후 적용될 헌법)

MVP는 완전 무료·무광고. v1.1+에서 수익화를 넣더라도 다음을 절대 위반하지 않는다:
1. 타이핑 세션(레슨/게임) 도중 광고·모달 삽입 금지. 광고는 결과 화면 이후, 세션 경계에서만.
2. 리워드형 우선 (보고 싶은 사람만 본다).
3. 타이머·기록에 영향을 주는 어떤 인터럽트도 금지.
4. 구독 미가입자도 코어 커리큘럼 전체 이용 가능 (프리미엄은 추가 팩·테마·통계).
5. 앱 1.1의 사용자 덱은 현재 설치 3개까지 무료다. 파일 검증·미리보기, 설치된 덱의 연습·게임·내보내기·삭제·동일 ID 교체는 무료이며 비소모성 IAP `app.piyokey.deckmaker.lifetime`은 네 번째 새 ID부터의 무제한 보관과 모바일 생성·복제·편집·변경 저장을 함께 해제한다. 파일 자체에 구매 권한을 묶지 않는다.
6. `pro` 구매·복원은 iOS의 StoreKit 2 또는 Android의 Play Billing만 사용하고 세션 밖에서 사용자가 잠긴 제작 액션이나 네 번째 새 ID 설치를 명시적으로 선택했을 때만 제시한다. entitlement 상실·검증 실패를 이유로 이미 저장된 사용자 덱이나 초안을 삭제하지 않는다.

근거: 직접 경쟁 앱의 1점 리뷰가 거의 전부 광고 빈도·타이밍에서 발생 (§1.2).

## 12. 기술 요구사항

- iOS: Swift 5.10+, SwiftUI 우선 (키보드·게임 렌더링은 필요시 UIKit/SpriteKit 혼용 허용), 최소 iOS 16.
- Android(동결된 과거 포트): Kotlin 2.x, Jetpack Compose, 최소 API 26 기록을 유지하되 현행 기술 선택이나 유지보수 gate로 사용하지 않는다.
- 파티클/이펙트: iOS SpriteKit 또는 SwiftUI Canvas, Android Compose Canvas. 60fps 필수 (§F6 AC).
- 접근성: Dynamic Type 대응(문제 텍스트·키캡), VoiceOver/토크백은 메뉴 화면만 P0. 작은 아이콘도 실제 탭 영역은 최소 44×44pt를 보장한다.
- 오디오: iOS 효과음과 목표 발음은 `.playback` + `.mixWithOthers`로 재생한다. Android는 다른 앱 음악을 멈추거나 duck하지 않도록 audio-focus gain을 요청하지 않고 `SoundPool`과 로컬 MP3 player를 분리한다. 두 플랫폼 모두 효과음은 기본 ON이고 설정 최상단에서 즉시 OFF할 수 있다 (§F11).
- 사용자 문서: iOS Files/security-scoped URL과 Android Storage Access Framework/`ContentResolver`는 `.typedeck` 원본을 앱 전용 staging에 제한 복사한 뒤 §8.4 parser로 검증한다. 저장소 전체 권한, arbitrary archive extraction API, package 안의 실행 가능 콘텐츠를 사용하지 않는다.
- 결제: iOS Deck Maker는 StoreKit 2 비소모성 상품, Android는 Play Billing one-time product 하나로 구현한다. 검증된 `PURCHASED` 상태만 제작 mutation을 열고 구매 재조회·pending·acknowledge·환불을 처리하며 자체 결제·라이선스 키·계정 서버는 만들지 않는다.
- 테스트: 조합 엔진 커버리지 ≥ 95%, 공용 테스트 벡터 CI 게이트. UI 스냅샷 테스트는 결과 화면·키보드 뷰만.

### 12.1 Android M7 플랫폼 계약

- 빌드 기준은 AGP `9.3.1`, Gradle Wrapper `9.5.0`, Kotlin/Compose compiler `2.3.21`, Compose BOM `2026.08.00`, compile SDK 37, target SDK 36, min SDK 26, Java bytecode 17로 고정한다. Android 모듈은 AGP 9 built-in Kotlin을 사용하고 `org.jetbrains.kotlin.android`·kapt를 적용하지 않는다.
- 로컬 후보 application ID와 namespace는 `app.piyokey.piyokey`다. Play Console에서 충돌·소유권을 확인하고 사용자가 확정하기 전에는 서명 업로드·내부 테스트 트랙 생성 등 되돌릴 수 없는 외부 작업을 하지 않는다.
- `core:hangul`, `core:deckkit`, `core:piyodeck`, `core:session`은 Android·Compose import가 없는 순수 로직이다. `core:data`는 Room/files/DataStore, `core:platform`은 IME/audio/TTS/SAF/Billing/Play Games/reminder/share, feature 모듈은 화면과 navigation만 소유한다.
- OS IME는 표준 `EditText`를 Compose `AndroidView`로 감싸 committed/composing을 분리해 순수 `OSIMETextJudge`에 전달한다. 한국어 IME 감지는 안내 힌트이며 hard block하지 않고, 설정 이동·파일·결제·랭킹 UI는 세션 전에만 연다.
- 게임 clock과 판정은 monotonic time 기반 순수 reducer로 두고 Compose Canvas는 frame clock을 사용한다. Pixel 6 60Hz 60초 stress에서 평균 59fps 이상, p95 16.7ms 이하, jank 1% 이하, ANR/crash 0을 M7 출시 게이트로 검증한다.

### 12.2 iPad Universal·적응형 화면 계약

- 기존 App Store Connect 앱 ID `6794853985`, Bundle ID `app.piyokey.Piyokey`, Xcode project·scheme을 유지하고 같은 target을 iPhone+iPad Universal 앱으로 확장한다. 새 iPad target·앱 레코드·저장 스키마를 만들지 않는다.
- 최소 iOS/iPadOS 16을 유지한다. iPhone은 기존 portrait만, iPad는 portrait·upside-down·landscape left/right를 지원하며 `UIRequiresFullScreen`으로 Split View·Stage Manager·resizable window를 차단하지 않는다.
- 기존 5탭을 유지하고 iPad 전용 sidebar는 만들지 않는다. 레이아웃은 기기 이름이 아니라 실제 가용 폭을 기준으로 compact `<600pt`, medium `600..<900pt`, wide `>=900pt` 정책을 공통 metrics에서 계산한다.
- compact는 iPhone 구성을 재사용하고, medium은 읽기 콘텐츠 최대 720pt·카드 2~3열, wide는 허브/카탈로그 최대 1120pt·카드 3~4열을 기준으로 한다. 세션 문제 lane은 가용 창 폭에서 좌우 여백을 제외한 폭을 활용하고, 내장 두벌식 키보드는 820pt 상한 없이 하단 safe area의 전체 폭을 사용한다. 10키는 3열의 과도한 확장을 막도록 최대 600pt로 중앙 정렬한다. 600pt 미만 창은 기존 iPhone 밀도를 유지하며, 그 이상에서는 의미 기반 글자 크기를 최소 xxLarge로, 고정 학습 글자는 1.2배로 조정하되 사용자 접근성 크기를 낮추지 않는다. 키 높이는 세로/가로/짧은 창에 맞춰 확대하고 연습 문제·조합 카드는 남은 높이를 배분해 가운데의 과도한 빈 공간을 줄인다. 홈의 넓은 가로 창은 2열, 마이페이지 세로는 1열, 게임 허브는 최대 3열(접근성 크기는 1열)로 구성한다.
- 연습·단어 퀴즈·초성·받아쓰기는 가로·세로 모두 문제 → 피요와 입력 상태 → 키보드의 세로 순서를 유지한다. 가로에서 오른쪽 분할 배치를 하지 않는다. 실제 창 높이에 맞춰 글자·버튼·키보드를 확대하고 카드 안쪽 여백을 줄여 동시에 표시하며, 접근성 크기와 짧은 창에서는 세로 스크롤로 모든 내용을 유지한다.
- 방향·창 크기 변경은 현재 탭·내비게이션·문제·입력·점수·콤보·목숨·타이머를 보존하고 문제 재추첨이나 타이머 재시작을 유발하지 않는다. 전체 화면 가로 스크롤은 금지하며 현재 자모 자동 추적용 내부 트랙만 예외다.
- 연습·게임 중 설정·파일·결제·키보드 연결 안내 모달을 띄우지 않는다. pointer, Full Keyboard Access, VoiceOver, Dynamic Type, Reduce Motion을 full screen·Split View·Stage Manager에서 검증한다.
- Bluetooth·USB·Magic Keyboard의 OS IME 판정 정확성은 GitHub Issue #12, 실제 두벌식 배열·다음 키·권장 손가락 학습 UX는 Issue #17이 소유한다. iPad 공식 지원의 통합 gate는 #10·#12·#17의 필수 검증을 모두 요구한다.

## 13. 마일스톤 (에이전트 작업 순서 제안)

1. **M1 조합 엔진 + 덱 스키마**: 상태 기계 + 판정기 + 테스트 벡터 전체 통과, 덱/카탈로그 JSON Schema 검증기, 공식 카탈로그 생성기 (UI 없음, 순수 로직)
2. **M2 키보드 뷰 + 연습 화면**: 내장 키보드, 조합 프리뷰, 정타/오타 피드백 (F2, F3, §7.2)
3. **M3 덱 발견·다운로드**: 발견 탭, 검색·필터·랭킹, 덱 상세·미리보기, 다운로드·설치 관리, 내 덱 (F5 전체 — 목 카탈로그 대상)
4. **M4 게임 모드**: 덱 선택 → 흐름 모드, 콤보, 파티클 (F6, §7.3)
5. **M5 커리큘럼 + 리텐션**: 자판 학습 스테이지, 복습 덱, 스트릭, 데일리 챌린지, 리마인더, 추천 모듈 (F4, F7, F8, F5.6)
6. **M6 폴리싱 + OS 키보드 모드**: 사운드, 공유 이미지, 온보딩(첫 3챕터 부화 미션 게이트 포함), OS 키보드 모드(F2a, diff 판정기 포함), Game Center 점수·랭킹과 피요 성장 연동, 로컬라이제이션 마감 (F1, F6f, F9, F11)
7. **R1.1 사용자 덱 문서 + `pro` 제작·편집 (M6 후, M7 전)**:
   1. `shared/piyodeck/SPEC.md`, manifest JSON Schema, 정상·악성 golden fixture와 pack/inspect/validate 도구를 먼저 확정한다 (§8.4).
   2. DeckKit package reader·writer·사용자 정책 검증과 ZIP/크기/hash 공격 회귀를 구현한다. 이 단계가 통과하기 전 UI에서 파일을 설치하지 않는다.
   3. 설치 저장소 schema를 `imported|created`, hash, package version, local modification, derived source를 보존하도록 마이그레이션하고 원자적 import·replace·복구·진행 기록 재연결을 구현한다.
   4. 마이페이지 file importer, 외부 문서 open, 미리보기, pending import, 충돌 선택, 무료 export·delete를 연결하고 세션 중 모달 금지를 UI 테스트한다.
   5. StoreKit 2 비소모성 IAP와 구매·pending·복원·환불·오프라인 entitlement 테스트를 완료한 뒤 생성·편집·공식 사본 편집·검증 저장 UI를 연결한다.
   6. ja/en/es/de/fr 로컬라이제이션, 1,000항목 성능, Files/iCloud/AirDrop 실기기, 기존 설치/진행 무손실 migration, Distribution archive와 App Review 상품/복원 메타데이터를 검증한 뒤 앱 버전 1.1로 제출한다.
8. **M7 Android 포팅 [FROZEN — 2026-08-30 이후 역사적 참고 전용]**: A0에서 현재 iOS 1.1 build 7·PRD·공용 계약의 재현 가능한 기준점, Gradle wrapper/version catalog/CI와 앱 골격을 준비했다. M1에서 공용 조합 벡터, 덱·카탈로그 schema, `.typedeck` v1 reader·writer와 정상·악성 cross-platform golden을 순수 Kotlin으로 포팅했다. M2에서 순수 연습 session reducer와 내장 두벌식 키보드를 구현하고 Galaxy의 다양한 단어 16개·자모 101/101 입력 및 사용자 기능 확인을 통과했다. M3는 Room/files 복구 journal과 primary/backup/quarantine, 검증된 번들·cache 및 HTTPS 조건부 갱신, 발견 검색·필터·정렬·상세, 다운로드·업데이트·삭제, 오프라인 내 덱·연습, 다운로드 이력 기반 홈 3개와 결과 2개 추천을 구현했다. M4는 순수 monotonic 흐름 reducer, 3·2·1과 백그라운드 재카운트, 60초·노미스 +2초·3목숨·0~50초 1.8배 가속·점수/콤보, 3×100 번들/설치 덱 선택, Compose 레인·고정 키보드·파티클·공통 결과/재도전과 Room v1→v2 `GameRecord`·`DeckProgress`를 구현했다. M5는 iOS와 동일한 6챕터·7스테이지·각 10문제 커리큘럼, 80% 클리어와 별·순차 해금/챕터5+ 자유 선택, active-time 중단 복구, 레슨/게임 복습 수집·수동 추가/제거·3회 졸업, JST 주간 스탬프·스트릭·3/5/7 보상, 결정적 5문제 데일리와 기본 OFF 시간 지정 로컬 리마인더를 Room v2→v3과 Compose 홈/연습/마이페이지에 연결했다. API 35에서 Room/data 12개와 M3~M5 앱 흐름 6개를 자동 검증했다. 실제 기기 touch-down→frame-commit proxy p95 50ms 이하와 동시 2-pointer rollover 누락·중복 0, 60초 게임 frame 성능은 면제하지 않고 Android 출시 후보 통합 실기기 QA의 필수 gate로 유지한다. 이 정량 gate가 열려 있어도 M6 구현과 자동 검증은 진행할 수 있지만 Android 출시·배포 완료로 처리할 수는 없다. OS IME는 M6 범위로 유지하며 현재 iOS 1.1에 없는 새 제품 기능은 Android에서 먼저 추가하지 않는다.

   **M7 상태 정정(2026-08-27):** 위 2026-08-25 설명 이후 M6A~M6D, Android R1.1A~R1.1B와 배포 번들 계약까지 소스 구현됐다. 다만 기능 소스 완료를 출시 준비 완료로 간주하지 않는다. 브랜드 테마·벡터 아이콘·라이트/다크·큰 글자 화면 폴리싱, 홈 원탭 주간 피요컵·랜덤 5단어와 자동 회귀, en-US·ja·ko Google Play listing·출시 노트·일회성 상품·콘솔 선언 근거 초안, 512 아이콘·1024×500 피처 그래픽의 저장소 준비를 마쳤다. 최종 application ID 소유권, 운영 카탈로그, 콘텐츠·고정 발음 권리 승인, upload signing, Play Billing·Games 콘솔 리소스, 정책 설문과 최종 서명 후보 스크린샷은 여전히 외부 게이트다. 그 동일 후보에 대해서만 Issue #19의 Galaxy 실기기 통합 QA를 수행한다.

각 마일스톤 종료 시: 테스트 통과 + 시뮬레이터 스크린 녹화 + `DECISIONS.md` 갱신.

**iPad Universal 확장 [ACTIVE — Issue #10, 2026-08-25]**: M1~M6 기능과 R1.1 저장·결제 계약을 복제하지 않고 기존 iOS target에 적응형 화면을 추가한다. Universal 기반 → 공통 layout → 탐색·학습 → 세션·게임 → 접근성·실기기 증빙 순서로 진행하며 Android M7 source·fixture를 변경하지 않는다.

**iPad 상태 정정(2026-08-29):** 적응형 iPad 소스와 가로 화면 보정은 PR #70·#80으로 `main`에 통합됐다. 따라서 위 ACTIVE 표시는 과거 작업 단계 기록이다. 새 iOS 출시 후보에 해당 소스를 포함하고 실제 iPad 회전·접근성·스토어 자산 일치를 재검증하는 배포 gate만 남는다.

## 14. 확정 정책과 오픈 퀘스천

- 확정 앱 이름/브랜딩: 앱 언어 `ja`=`ピヨキー`, `en`·`es`·`de`·`fr` 및 미지원 언어 fallback=`typee`. App Store 국가가 아니라 앱 로케일을 기준으로 하며 기존 단일 앱 레코드·Bundle ID를 유지한다. 홈 화면 AppIcon과 앱 내부·공유 카드의 로고 이미지는 음영이 있는 흰색 `ㅎ` 키캡을 든 동일한 납작한 병아리 이미지를 사용하고 텍스트 브랜드만 로케일에 맞춘다. App Store 표시명은 기능 설명을 먼저 쓰고 브랜드를 마지막에 배치한다.
- 확정 1.1 언어 범위: 앱 UI는 `ja`·`en`·`es`·`de`·`fr`이며 `ko` 설정과 미지원 언어는 영어로 전환한다. 학습 원본·기존 현지화는 보존한다. 10개 시장 스토어 미디어 제작과 실제 스토어 제출은 별도 검증한다.
- 확정 1.1 availability: 앱과 Deck Maker IAP 모두 `All Countries or Regions`를 선택하고 향후 새 storefront도 자동 포함한다. 국가별 순차 활성화는 하지 않으며, 규제 미충족으로 Apple이 개별 storefront를 실제 판매 불가로 표시하는 경우는 availability 선택과 분리해 compliance 예외로 관리한다.
- Android (동결 기록): 2026-08-21 사용자 요청으로 M7을 재개했고 2026-08-25 사용자의 명시적 결정으로 실기기 의존 QA를 전체 구현 뒤 출시 후보 통합 gate로 이관했다. 로컬 iOS 1.1 build 7과 PRD v5.8·공용 fixture를 parity 기준으로 A0→M1→M6D와 R1.1A→R1.1C를 구현했다. Android 1.1.0 (8) 소스 후보는 M3 발견·다운로드·오프라인 내 덱, M4 여섯 게임·결과, M5 커리큘럼·복습·리텐션과 홈 원탭 주간컵·랜덤 5단어, M6 온보딩·설정·OS IME·오디오·공유·성장·Play Games, R1.1 `.typedeck`·Deck Maker·Play Billing과 Release R8·manifest·배포 사전검증을 포함한다. 이후 출시 준비와 통합 QA는 2026-08-30 v6.14 결정으로 종료했으며 이 구현을 배포 후보로 사용하지 않는다.
- 마스코트 디자인 외주 여부 (MVP는 플레이스홀더 도형으로 진행)
- TTS: 공식 카탈로그 26덱·게임 프리셋 15덱·커리큘럼/데일리·무료 샘플을 합친 앱 제공 고정 목표 581개는 gTTS `ko` 보통 속도의 직접 MP3로 사전 생성해 완전 오프라인으로 번들한다. 런타임은 선언 경로 → canonical hash 경로 → 플랫폼 로컬 TTS(iOS `AVSpeechSynthesizer(ko-KR)`, Android 네트워크 불필요 `TextToSpeech(ko-KR)`) 순서로 해석하며 사용자·비공개 동적 콘텐츠를 네트워크 TTS나 gTTS에 전송하지 않는다. gTTS가 문서화되지 않은 Google Translate 음성 기능을 사용하므로 `content_rights_confirmed` 출시 게이트는 배포·콘텐츠 사용 권리 검토 전까지 닫아 두고, 확인할 수 없으면 공식 지원 TTS 제공자로 같은 canonical 경로의 음원을 재생성한다.
- 외부 상표·가사·기관명 권리: 직접 인용과 근거 없는 제휴 표시는 금지하고, 일반 표현도 출시 전 운영 검수일을 기록한다.
- 앱 1.1 사용자 덱: `.typedeck` v1의 유효한 파일은 구매 없이 가져오기·학습·현재본 내보내기·삭제·교체할 수 있다. 모바일 생성·편집·사본 저장만 비소모성 `app.piyokey.deckmaker.lifetime`으로 판매하고 계정·공개 UGC·원격 쓰기 API는 추가하지 않는다.
