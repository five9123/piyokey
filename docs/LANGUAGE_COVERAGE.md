# 언어 커버리지 — #73 통합 후보, 2026-08-29

사용자가 지정한 별도 clone `outputs/local-language-expansion`의 `codex/local-es-de-fr` (`dcc32bf3d6bcfad088a251dd09e8fc6419967d30`)를 사용자 진행 승인으로 #73/PR #74에 통합했다. 원본 clone과 후속 로컬 리텐션 작업은 수정하지 않았다. 원격 main `47371de`의 UI는 아직 ja/en/es이며, 이 문서는 **5언어 통합 작업 후보**의 범위다. main 병합·스토어 배포·원격 카탈로그 게시 완료를 뜻하지 않는다.

## 통합 커버리지

| 범위 | 확인 결과 |
|---|---|
| 선택 가능한 UI | iOS·Android 모두 ja/en/es/de/fr 5개 |
| iOS | 각 언어 Localizable.strings **1,124개**, stringsdict **17개**. 영어 대비 키 누락 0개 |
| Android | 8개 모듈 각 언어 **517개** 번역 리소스(string/string-array/plurals). 영어 대비 누락 0개 |
| 레벨·첫 홈 | 모든 언어에 4단계 레벨, 한국어 예시, 첫 추천·이어하기·홈 투어 문구 포함 |
| 공식 es/de/fr 콘텐츠 | 언어별 고유 뜻 **627개**, 덱 이름 **41개**, 태그 **42개**, 공식 작성자 번역 |
| 기본 카탈로그 | 26개 덱·312개 항목; 별도 게임 preset 15개. 627은 `(ko, meaning_ja)` 사전의 고유 쌍 수 |
| 지역 변형 | es-MX/419, de-AT/CH, fr-CA/BE/CH 등을 기본 언어로 인식하며 선택 저장 |
| 한국어 | 학습 대상·호환 콘텐츠로 유지. 선택 가능한 UI에는 포함하지 않음 |

사용자 덱의 미제공 번역은 영어 fallback이다. 중국어 간체·번체, 포르투갈어, 인도네시아어는 앱 UI 지원에 포함되지 않는다. 스토어 미디어의 10개 로케일과 앱 UI 언어 수는 다르다.

## 온보딩·첫 홈 계약

- 관심사 다음에 처음/자모/단어/문장의 4단계를 선택한다. 관심사와 동일한 **2열 카드**이며 아이콘 대신 `ㄱ · ㅏ`, `가 · 나`, `학교`, `같이 걷고 싶어요` 예시를 보여준다. 한국어 예시는 공통이고 설명은 현지화한다.
- 레벨 화면에는 돌아가기 버튼이 없다. 사전 선택 없이 선택 후 다음 활성화, 소개 스킵, 재실행 선택 복원은 유지한다.
- 첫 홈은 수준·관심사에 맞는 공식 덱을 추천한다. 다운로드나 부화 미션만으로 이어하기로 바뀌지 않으며 실제 학습 이후 전환한다.
- 기존 저장 단계 ID와 완료 사용자 상태, 부화 미션 gate를 유지한다. 한국어 예시는 기존 원문을 재사용한 정적 표시이며 새 발음/문제/음원을 추가하지 않았다.

## 통합 검증

- Python 전체 회귀 96개와 repository preflight 통과. 문자열 키·서식·배열·복수형과 생성 콘텐츠 snapshot 포함.
- SwiftPM 공용 코어 45개, Android DeckKit/PiyoDeck/설정/data 단위 63개 통과.
- iOS 앱 단위 365개와 5언어 레벨 선택/재실행 복원 통과. 최종 2열 카드·예시·돌아가기 미노출 및 부화→홈 재검증 결과는 #73의 최신 SHA 증빙을 따른다.
- Android 통합 Debug 앱 빌드, UI 14개(5언어 레벨 복원 포함), lint 통과. 캡처로 de/fr 카드의 한국어 예시·줄바꿈을 확인했다.
- iOS 첫 성장 연출 실패 원인은 테스트 시뮬레이터의 앱 컨테이너 밖에 남은 촬영용 `mascot.celebrated_stage=3` 값이었다. DEBUG 새 설치 테스트가 초기값을 명시하도록 격리했으며 시뮬레이터의 원본 설정은 삭제하지 않았다. 실제 성장/네비게이션 정책은 바꾸지 않았다.
- 앱 투어는 배경 접근성을 숨기므로, 홈 추천 카드 접근성 검증은 투어를 마친 직후에 수행한다. 투어 자체의 6단계 검증은 유지한다.

자동 키 검사와 앱 테스트 통과는 원어민 번역 품질 검수를 대체하지 않는다. 이전 로컬 후보만 검사했던 결과는 `docs/LANGUAGE_REVIEW.md`와 이전 SHA 증빙에 남아 있으며, 통합 후보의 검증은 #73 최신 증빙과 구분한다.

## 남은 출시 gate

- CI/리뷰: GitHub Actions 비활성 상태. 이번 요청은 통합 작업 승인이며 CI 우회 병합 승인이 아니다.
- 원어민 검수, 전체 기능의 좁은 화면·큰 글자·접근성, Android 백그라운드 알림 언어, 실기기·구매·공유·OS 동작 확인.
- 복합 수량 문법과 지역별 숫자/날짜 관습은 기존 언어 검수 보고서의 후속 항목이다.
- 새 카탈로그 namespace 게시·구버전 URL 보존·신규 앱 연결 검증. UI 리소스만 복사하거나 기존 정적 URL에 새 schema 콘텐츠를 덮어쓰지 않는다.
- 최종 RC 스토어 촬영·원어민 문구 검토·metadata 저장/업로드/재조회. 로컬 소스 통합을 공개·배포 완료로 간주하지 않는다.
