{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hanco.example/schema/deck.schema.json",
  "title": "Hanco Deck",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "deck_id",
    "version",
    "name",
    "author",
    "official",
    "type",
    "level",
    "tags",
    "created_at",
    "updated_at",
    "items"
  ],
  "properties": {
    "deck_id": { "$ref": "#/$defs/id" },
    "version": { "type": "integer", "minimum": 1 },
    "name": { "type": "string", "minLength": 1, "maxLength": 120 },
    "author": { "$ref": "#/$defs/author" },
    "official": { "type": "boolean" },
    "type": { "$ref": "#/$defs/deckType" },
    "level": { "type": "integer", "minimum": 1, "maximum": 3 },
    "tags": { "$ref": "#/$defs/tags" },
    "localizations": { "$ref": "#/$defs/deckLocalizations" },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    },
    "items": {
      "type": "array",
      "minItems": 1,
      "maxItems": 5000,
      "items": { "$ref": "#/$defs/item" }
    }
  },
  "$defs": {
    "id": {
      "type": "string",
      "minLength": 3,
      "maxLength": 64,
      "pattern": "^[a-z0-9][a-z0-9_-]{2,63}$"
    },
    "deckType": { "type": "string", "enum": ["word", "sentence"] },
    "tags": {
      "type": "array",
      "minItems": 1,
      "maxItems": 8,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1, "maxLength": 40 }
    },
    "author": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "nickname"],
      "properties": {
        "id": { "$ref": "#/$defs/id" },
        "nickname": { "type": "string", "minLength": 1, "maxLength": 40 }
      }
    },
    "deckLocalization": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "author_nickname", "tags"],
      "properties": {
        "name": { "type": "string", "minLength": 1, "maxLength": 120 },
        "author_nickname": { "type": "string", "minLength": 1, "maxLength": 40 },
        "tags": { "$ref": "#/$defs/tags" }
      }
    },
    "deckLocalizations": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "properties": {
        "en": { "$ref": "#/$defs/deckLocalization" },
        "ko": { "$ref": "#/$defs/deckLocalization" },
        "es": { "$ref": "#/$defs/deckLocalization" },
        "de": { "$ref": "#/$defs/deckLocalization" },
        "fr": { "$ref": "#/$defs/deckLocalization" }
      }
    },
    "itemLocalization": {
      "type": "object",
      "additionalProperties": false,
      "required": ["reading", "meaning"],
      "properties": {
        "reading": { "type": "string", "minLength": 1, "maxLength": 300 },
        "meaning": { "type": "string", "minLength": 1, "maxLength": 500 }
      }
    },
    "itemLocalizations": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "properties": {
        "en": { "$ref": "#/$defs/itemLocalization" },
        "ko": { "$ref": "#/$defs/itemLocalization" },
        "es": { "$ref": "#/$defs/itemLocalization" },
        "de": { "$ref": "#/$defs/itemLocalization" },
        "fr": { "$ref": "#/$defs/itemLocalization" }
      }
    },
    "item": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "ko", "reading_ja", "meaning_ja", "audio"],
      "properties": {
        "id": { "$ref": "#/$defs/id" },
        "ko": { "type": "string", "minLength": 1, "maxLength": 10 },
        "reading_ja": { "type": "string", "minLength": 1, "maxLength": 300 },
        "meaning_ja": { "type": "string", "minLength": 1, "maxLength": 500 },
        "localizations": { "$ref": "#/$defs/itemLocalizations" },
        "audio": {
          "oneOf": [
            { "type": "string", "minLength": 1, "format": "uri-reference" },
            { "type": "null" }
          ]
        }
      }
    }
  }
}
