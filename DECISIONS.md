# DECISIONS.md — 구현 중 결정 기록

PRD가 모호한 지점에서 내린 결정을 기록한다. 형식:

```
## YYYY-MM-DD 결정 제목
- 결정:
- 근거:
- 관련 PRD 섹션:
- 영향 범위:
```

---

## 2026-07-25 부화 미션을 하나의 연속 결과 흐름으로 구성
- 결정: 첫 3개 부화 미션은 결과 화면의 재도전 CTA를 제거하고 `다음 미션으로`를 주 CTA로 제공한다. 이 CTA는 미션 맵으로 되돌아가지 않고 동일 네비게이션 흐름 안에서 다음 스테이지를 즉시 재생성한다. 마지막 미션은 동일 자리의 `피요와 함께 시작` CTA로 게이트를 해제한다.
- 근거: 결과 화면을 유지하면서도 `한번 더`와 미션 맵의 추가 탭을 없애 부화 목표까지의 단일 행동을 명확히 하기 위함이다.
- 관련 PRD 섹션: F1, F12.5
- 영향 범위: `CurriculumMapView`, `PracticeView`, `PracticeResultView`, 온보딩 UI 테스트

## 2026-07-17 프로젝트 초기화 (Cowork 세션에서 이관)
- 결정: 테스트 벡터는 Python 생성기(`tools/gen_test_vectors.py`)로 프로그램 검증 후 생성. 수기 작성 금지.
- 근거: 자모 분해를 손으로 쓰면 도깨비/복합종성 케이스에서 오류 위험. 유니코드 산술로 검증된 값만 사용.
- 관련 PRD 섹션: §6.4
- 영향 범위: iOS/Android 테스트 스위트

## 2026-07-17 M1 Swift 패키지 경계
- 결정: 앱 프로젝트와 분리된 `ios/HangulEngine` Swift 패키지에 순수 로직 타깃 `HangulEngine`과 이에 의존하는 `DeckKit` 타깃을 둔다. iOS 16을 제품 최소 버전으로 유지하고 macOS 13은 SwiftPM 호스트 테스트용으로만 선언한다.
- 근거: M1에서는 앱 타깃을 만들지 않으면서 M2에서 두 모듈을 그대로 연결할 수 있고, UI 프레임워크 의존 없이 로직을 검증할 수 있다.
- 관련 PRD 섹션: §6.1, §12, §13 M1
- 영향 범위: `ios/HangulEngine/Package.swift`, M2 앱 프로젝트의 패키지 의존성

## 2026-07-17 조합 상태와 백스페이스 복원 방식
- 결정: 조합기는 값 타입 상태와 순수 reducer로 구현하고, 각 자모 입력 전의 조합 스냅샷을 상태 내부 undo history에 보관한다. 백스페이스는 렌더링된 글자를 직접 분해하지 않고 직전 입력 전 스냅샷을 복원한다.
- 근거: 복합 중성·복합 종성뿐 아니라 `ㄱㅏㄴㅏ → 가나 → 간`처럼 도깨비 이월을 가로지르는 백스페이스도 정확히 한 입력 자모씩 되돌릴 수 있다.
- 관련 PRD 섹션: §6.1, §6.2
- 영향 범위: `CompositionState`, `HangulComposer`, Android M7 포팅 알고리즘

## 2026-07-17 판정 시퀀스와 Unicode NFD 길이 분리
- 결정: 실시간 판정의 기대값은 두벌식 실제 키 자모 시퀀스(복합모음·복합종성은 두 입력)로 생성한다. 테스트 벡터의 `nfd_length`는 fixture 무결성 확인용 Unicode NFD 스칼라 길이로만 사용한다. Shift 결과 자모는 Shift 키와 자모 키를 분리하지 않고 논리 자모 이벤트 1개로 센다.
- 근거: `닭`의 NFD 종성 `ᆰ`은 한 스칼라이지만 실제 입력은 `ㄹ`, `ㄱ` 두 번이며, PRD §6.3의 실시간 판정은 실제 자모 입력 순서를 따라야 한다.
- 관련 PRD 섹션: §6.3, §6.4
- 영향 범위: `JamoDecomposer`, `JamoSequenceJudge`, 정확도·속도 산정의 입력 단위

## 2026-07-17 ko 필드 타이핑 가능 범위
- 결정: 덱의 `ko`는 완성형 한글 음절·지원 자모를 중심으로 하며, 공백·숫자와 제한된 일반 문장부호만 literal 입력으로 허용한다. 또한 한글 음절 또는 자모가 최소 하나 있어야 한다. 라틴 문자·임의 이모지는 M1 내장 키보드 입력 대상으로 보지 않아 검증에서 거부한다.
- 근거: 모든 문자를 무조건 literal로 통과시키면 “조합 엔진으로 분해 가능” 검증이 무의미해지고, M2 내장 키보드에서 입력할 수 없는 목표가 덱에 포함될 수 있다.
- 관련 PRD 섹션: §6.3, §8.3, F2
- 영향 범위: `JamoDecomposer`, `DeckValidator`, `CatalogValidator`, 덱 콘텐츠 제작 규칙

## 2026-07-17 콘텐츠 스키마 제약과 fixture 재현성
- 결정: JSON Schema는 Draft 2020-12로 작성하고 난이도는 1...3, 태그는 1...8개, 카탈로그 미리보기는 1...10개로 제한한다. 목 카탈로그는 시드 `20260717`과 고정 `generated_at`을 기본값으로 사용해 공식 6개·커뮤니티 24개를 결정적으로 재생성한다.
- 근거: PRD의 초급~상급 3단계와 미리보기 최대 10개를 기계 검증 가능하게 만들고, 생성기 재실행으로 불필요한 fixture diff가 생기지 않게 한다.
- 관련 PRD 섹션: §8.1, §8.2, §8.3, F5.2, F5.4
- 영향 범위: `shared/schema/`, `tools/gen_mock_catalog.py`, `shared/mock_catalog/`, DeckKit 검증기

## 2026-07-18 M2 앱 프로젝트와 모바일 테스트 호스트
- 결정: `ios/Hanco/Hanco.xcodeproj`에 iOS 16+ SwiftUI 앱과 `HancoTests` 타깃을 만들고, M1의 `ios/HangulEngine`을 로컬 Swift Package로 연결한다. 외부 프로젝트 생성 도구는 도입하지 않는다.
- 근거: 실제 Simulator·기기에서 터치 입력과 레이아웃을 검증하면서도 조합 로직의 패키지 경계를 유지할 수 있다. 현재 환경에 XcodeGen/Tuist가 없어 새 빌드 의존성을 추가하지 않는 편이 재현 가능하다.
- 관련 PRD 섹션: F2, F3, §12, §13 M2
- 영향 범위: `ios/Hanco/`, Xcode scheme `Hanco`, iOS 빌드·테스트 명령

## 2026-07-18 키보드 입력 시점과 Shift 동작
- 결정: 키 입력은 SwiftUI `Button`의 터치 해제 시점이 아니라 투명 `UIControl`의 `.touchDown`에서 전달한다. 각 키는 exclusive touch를 사용하지 않으며, Shift는 다음 자모 한 번에 적용되는 sticky 방식으로 `ㅃㅉㄸㄲㅆㅒㅖ`를 논리 자모 하나로 전달한다.
- 근거: F2의 p95 50ms 입력 반영과 멀티터치 롤오버 요구를 만족하려면 touch-down 이벤트가 필요하고, Shift 결과를 엔진에 한 자모로 넘겨야 §6.3 판정 단위와 일치한다.
- 관련 PRD 섹션: F2 AC, §6.3, §7.2
- 영향 범위: `HangulKeyboardView`, 향후 입력 지연 계측·UI 테스트

## 2026-07-22 내장 키보드 연속 입력 관용 영역
- 결정: 키마다 독립된 터치 뷰를 두지 않고 키보드 전체를 덮는 단일 멀티터치 라우터에서 모든 손가락을 추적한다. 보이는 키캡 사이의 가로 5pt·세로 7pt 간격은 인접 키의 4pt×5pt 관용 영역으로 포함하고, 겹치는 지점은 실제 키캡까지의 거리와 중심 거리가 가까운 키로 보낸다. 입력 상태를 먼저 갱신한 뒤 햅틱과 타건음을 실행한다.
- 근거: 기존 구현은 키 사이 간격이 실제 입력 사각지대였고 독립 `UIControl`의 속성 검사만으로 키보드 전체의 롤오버 전달을 보장하지 못했다. 연속된 단일 터치 라우터는 iOS 키보드처럼 시각적 간격과 논리적 입력 면적을 분리하고, 상태 갱신을 피드백보다 우선해 빠른 타이핑의 누락 체감과 반응 지연을 줄인다.
- 관련 PRD 섹션: F2 AC, §7.2
- 영향 범위: `HangulKeyboardView`, `PracticeSessionViewModelTests`, 연습·온보딩·직접 타이핑 게임의 내장 키보드

## 2026-07-18 연습 화면 백스페이스 판정 복원
- 결정: 연습 ViewModel은 정타로 수락된 자모 목록을 보관한다. 백스페이스 시 조합 상태는 엔진 reducer로 한 단계 되돌리고, 판정 상태는 남은 수락 자모를 순수 판정기에 재생해 복원한다. 누적 오타 수는 백스페이스로 줄이지 않는다.
- 근거: 조합기와 판정기를 UI에서 변형하지 않으면서 도깨비 이월을 가로지르는 삭제 후에도 다음 기대 자모를 정확히 복구하고, 이미 발생한 세션 오타 기록은 유지할 수 있다.
- 관련 PRD 섹션: §6.2, §6.3, F12.3
- 영향 범위: `PracticeSessionViewModel`, 정확도 통계 연결

## 2026-07-18 로컬 Simulator 기준 대상
- 결정: 현재 설치된 최신 휴대폰 Simulator인 `iPhone 17, OS 26.5`를 로컬 iOS 테스트 명령의 기본 대상으로 사용한다.
- 근거: 기존 문서의 iPhone 16 런타임이 설치되어 있지 않아 명령이 재현되지 않으며, 앱 자체의 최소 배포 버전은 계속 iOS 16으로 유지된다.
- 관련 PRD 섹션: §12, §13 M2
- 영향 범위: `AGENTS.md`, `CLAUDE.md`, 로컬 `xcodebuild test` 명령

## 2026-07-18 입력 지연 계측과 성능 게이트
- 결정: Debug 앱은 각 키의 터치 다운부터 해당 상태 변경 뒤 첫 `CADisplayLink` 콜백까지 측정하고 최근 120개 표본의 p95를 표시한다. `50ms` AC는 실제 iPhone에서 판정하며, Simulator UI 테스트는 표본 생성과 표시 연결만 검증한다.
- 근거: 별도 프로세스의 XCUITest가 Simulator 탭을 합성하면 프레임 스케줄링 지연이 포함되어 동일 시나리오가 79.6~81.1ms로 재현됐다. 이 값은 앱 로직의 실제 기기 입력 레이턴시를 대표하지 않으므로 성능 합격값을 왜곡하거나 Simulator 환경을 제품 게이트로 삼지 않는다.
- 관련 PRD 섹션: F2 AC, §12, §13 M2
- 영향 범위: `InputLatencyMonitor`, `DisplayRefreshProbe`, `HancoUITests`, 실기기 QA 절차

## 2026-07-18 M2 코어 피드백 연출과 키보드 옵션 경계
- 결정: 조합 연출은 UI 문자열 변화 추측이 아니라 조합기의 phase 전이와 수락 자모 revision으로 구동한다. `CHO→CHOJUNG`, 복합모음, 도깨비 이월은 170ms 이내 슬라이드 결합·팝으로 표시하고, 오타는 220ms 빨강 플래시와 250ms 셰이크, 단어 완성은 8개 파티클과 400ms 이내 카드 팝으로 표시한다. 가이드·로마자·햅틱은 각각 기본 ON인 `AppStorage` 옵션으로 분리하되 세션 중 설정 모달은 만들지 않는다.
- 근거: 엔진 전이에 연출을 결합하면 오타·백스페이스 때 잘못된 합성 애니메이션이 발생하지 않는다. 키보드 옵션을 영속 상태로 선행 분리하면 후속 Settings 화면이 입력 컴포넌트를 재작성하지 않고 연결되며, 세션 중 인터럽트 금지 원칙도 유지한다.
- 관련 PRD 섹션: F2, §7.2, §11.1, §13 M2
- 영향 범위: `PracticeSessionViewModel`, `PracticeView`, `HangulKeyboardView`, 후속 Settings 화면

## 2026-07-18 제품명 Hanco 통일
- 결정: 기존 로컬 프로젝트와 충돌하던 `HanTap`/`hantap` 이름을 제품명 `Hanco`, 저장소 폴더 `hanco`, iOS 프로젝트·타깃·scheme `Hanco`, bundle identifier `com.hanco.prototype`로 통일한다. 한글 조합이라는 역할을 나타내는 모듈명 `HangulEngine`과 `DeckKit`은 제품명이 아니므로 유지한다.
- 근거: 같은 컴퓨터에 있던 동명 프로젝트와 파일·빌드 산출물·scheme을 명확히 구분하면서, 순수 로직 모듈의 의미 있는 이름은 보존하기 위함이다.
- 관련 PRD 섹션: §12, §13
- 영향 범위: 저장소 문서, `ios/Hanco/`, Xcode 프로젝트·scheme·bundle identifier, 스키마 식별자와 목 카탈로그 메타데이터

## 2026-07-18 연습 전 키보드 설정 화면
- 결정: 키 가이드·로마자 힌트·햅틱 설정은 연습 시작 전 `PracticeSetupView`에서 변경하고 `AppStorage`로 즉시 영속화한다. 시작된 연습 화면에는 설정 진입점이나 모달을 두지 않는다.
- 근거: 옵션의 발견성과 저장 회귀 검증을 제공하면서도 세션 도중 인터럽트를 금지한 제품 원칙을 지킬 수 있다.
- 관련 PRD 섹션: F2, F10, §11.1, §13 M2
- 영향 범위: `PracticeSetupView`, `KeyboardPreferenceKeys`, `HancoUITests`

## 2026-07-18 `ㅎ` 키캡 아기 펭귄 마스코트 [2026-07-19 폐기]
- 결정: 생성형 이미지로 만든 아기 펭귄이 한글 `ㅎ`이 적힌 분홍 키캡을 드는 단일 투명 배경 에셋을 사용한다. 연습 전 히어로와 조합 미리보기에 재사용하며, 정타·오타·완료 상태는 회전·크기·위치의 짧은 모션으로 표현한다. 접근성 레이블은 일본어 로컬라이제이션 리소스로 제공한다.
- 근거: PRD의 작은 반응형 마스코트 요구를 M2 화면 밀도를 해치지 않는 한 에셋으로 충족하고, 키캡의 `ㅎ`으로 앱의 한글 타이핑 정체성을 즉시 전달할 수 있다.
- 관련 PRD 섹션: §7.2, §10.3, §13 M2
- 영향 범위: `PenguinMascot.imageset`, `PenguinMascotView`, `PracticeSetupView`, `PracticeView`, 일본어 로컬라이제이션
- 폐기 사유: 사용자의 명시적 요청으로 이미지 기반 펭귄을 제거하고 Swift 벡터 병아리 성장 시스템으로 전면 교체했다. 아래 `병아리 성장 시스템 이식` 결정이 현재 기준이다.

## 2026-07-18 M2 종료 검증 산출물
- 결정: M2 시각 회귀 기준은 연습 전 설정·초기 연습·단어 완료의 일본어 화면 3장으로 정하고, iPhone 17 / iOS 26.5 Simulator 성공 흐름을 H.264 영상으로 보관한다. 입력 반영 p95와 멀티터치 롤오버는 Simulator 수치가 아니라 실제 iPhone 사용자 확인을 종료 게이트로 사용한다.
- 근거: PRD의 키보드·결과 UI 기준과 마일스톤별 녹화 요구를 재현 가능한 산출물로 남기면서, Simulator 합성 입력 지연을 실제 기기 성능으로 오판하지 않기 위함이다.
- 관련 PRD 섹션: F2 AC, §7.2, §12, §13 M2
- 영향 범위: `artifacts/m2/`, M2 회귀 검증 절차

## 2026-07-18 M3 탭 경계와 공용 목 카탈로그 로딩
- 결정: 앱 루트는 `練習`·`さがす`·`マイデッキ` 3탭으로 구성하고, 연습 세션은 자체 NavigationStack 안에서 유지해 탭 전환이나 설정 모달이 세션 중 노출되지 않게 한다. 발견 탭은 `shared/mock_catalog/catalog.json`을 복제하지 않고 Xcode 리소스로 직접 참조하며, 표시 전에 `DeckKit` 디코딩과 의미 검증을 모두 통과시킨다.
- 근거: 공용 fixture를 단일 소스로 유지하면 생성기 재실행과 앱 데이터가 어긋나지 않는다. 탭 경계를 먼저 고정하면 M3 다운로드·내 덱 기능을 연습 화면과 독립적으로 확장할 수 있다.
- 관련 PRD 섹션: F5.1, F5.5, §8.2, §8.3, §13 M3
- 영향 범위: `AppRootView`, `BundleCatalogRepository`, Xcode 리소스·DeckKit 의존성

## 2026-07-18 발견 검색·필터 초기 동작
- 결정: 검색은 덱 이름·태그·제작자 닉네임을 로컬에서 대소문자 구분 없이 즉시 검색한다. 복수 태그는 교집합으로 적용하고 타입·레벨 필터와 함께 세션 내 ViewModel 상태로 유지한다. 개인화 이력이 생기기 전 팬덤 숏컷은 `TWICE`, `SEVENTEEN`, `韓ドラ`, `日常`, `TOPIK`을 우선하고 나머지는 artist·덱 수 순으로 채운다.
- 근거: 서버 검색 API 없이 F5 검색·필터 AC를 충족하며, 다운로드 이력이 없는 콜드 스타트에서도 일본 사용자에게 의미 있는 숏컷 순서를 제공할 수 있다.
- 관련 PRD 섹션: F5.1, F5.3, §8.2
- 영향 범위: `DiscoverViewModel`, `DiscoverView`, 발견 탭 단위/UI 테스트

## 2026-07-18 iOS 16 호환 덱 설치 저장소
- 결정: 설치 인덱스와 검증을 통과한 전체 덱 JSON을 Application Support의 `Hanco/InstalledDecks`에 원자적으로 저장한다. 인덱스에는 `schema_version = 1`, 덱 버전, 설치·최근 실행 시각, bundle/remote 출처를 기록하고 알 수 없는 스키마 버전은 읽지 않는다.
- 근거: 앱 최소 버전 iOS 16에서는 SwiftData를 사용할 수 없고, M3에서 GRDB 외부 의존성을 추가하지 않아도 작은 읽기 중심 데이터셋의 완전한 오프라인 실행과 향후 명시적 마이그레이션 경계를 확보할 수 있다.
- 관련 PRD 섹션: F5.5, §8.3, §9.1, §9.3, §13 M3
- 영향 범위: `DeckInstallationStore`, `DeckLibrary`, 내 덱·업데이트·삭제, 저장소 회귀 테스트

## 2026-07-18 목 덱 소스와 운영 HTTP 경계
- 결정: M3 개발 fixture에서는 `shared/mock_catalog/decks/`의 30개 JSON을 Xcode 폴더 리소스로 참조한다. 공식 덱은 bundle, 커뮤니티 덱은 remote 출처로 설치해 앱의 상태 전이를 검증하되, 운영용 정적 HTTP/ETag 클라이언트와 카탈로그 캐시는 별도 소스로 다음 단계에 연결한다.
- 근거: 공용 fixture를 복제하지 않고 상세·설치·오프라인 흐름을 먼저 완성하면서도, 개발 번들 접근을 실제 원격 다운로드 구현으로 오인하지 않도록 소스 경계를 명확히 유지할 수 있다.
- 관련 PRD 섹션: F5.4, F5.5, §8.2, §8.3, §13 M3
- 영향 범위: `BundledMockDeckSource`, Xcode 리소스, 향후 HTTP catalog/deck source

## 2026-07-18 발견에서 연습까지 3탭 흐름
- 결정: 발견 카드 → 상세 → 다운로드 → 연습의 최대 3탭 흐름을 사용한다. 다운로드 후 별도 설정 모달을 넣지 않고 기존 영속 키보드 옵션으로 설치된 전체 덱 연습을 바로 시작하며, 세션 중에는 탭 바를 숨긴다. 상세의 최대 10개 미리보기와 추천은 세션 진입 전에만 제공한다.
- 근거: F5.5의 3탭 이내 실행 AC와 세션 중 모달·인터럽트 금지 원칙을 동시에 만족하고, 오프라인 내 덱에서도 같은 연습 진입점을 재사용할 수 있다.
- 관련 PRD 섹션: F5.4, F5.5, §11.1, §13 M3
- 영향 범위: `DeckDetailView`, `DeckPracticeDestination`, `MyDecksView`, `PracticeView`, UI 테스트

## 2026-07-18 카탈로그 캐시 우선 표시와 백그라운드 조건부 갱신
- 결정: 앱 루트 생성 시 카탈로그를 검증된 캐시→번들 순으로 즉시 표시하고, 동시에 HTTPS 조건부 GET을 시작한다. `If-None-Match`와 `If-Modified-Since`를 함께 지원하며 200 응답은 DeckKit 검증 후에만 원자적으로 저장하고, 304는 기존 JSON을 유지한 채 확인 시각과 validator만 갱신한다. 네트워크·HTTP·디코딩 실패는 유효한 현재 화면을 실패 상태로 바꾸지 않는다.
- 근거: 앱 실행 시 백그라운드 갱신하면서도 오프라인과 서버 장애가 발견 UX를 막지 않아야 하는 F5.5 AC를 만족하고, 손상되거나 호환되지 않는 캐시가 번들 콜드 스타트까지 막는 상황을 피하기 위함이다.
- 관련 PRD 섹션: F5.5, §8.2, §8.3, §9
- 영향 범위: `CatalogCacheStore`, `CatalogHTTPClient`, `CachedRemoteCatalogRepository`, `DiscoverViewModel`, 앱 시작 흐름

## 2026-07-18 정적 콘텐츠 URL과 덱 소스 정책
- 결정: 운영 카탈로그는 `HANCO_CATALOG_URL` 빌드 설정으로 주입하는 직접 HTTPS URL로 제한한다. 덱 상대 경로는 `decks/` 하위이고 동일 scheme·host·port 및 콘텐츠 루트 안일 때만 허용한다. 공식 덱은 동봉 번들을 우선하되 카탈로그에 더 높은 버전이 있으면 원격으로 갱신하고, 커뮤니티 덱은 URL 설정 시 원격만 사용한다. URL 미설정 개발 빌드만 공용 번들 fixture로 대체한다.
- 근거: 배포 환경별 호스트를 코드에 하드코딩하지 않고, 경로 이탈·호스트 변경을 차단하면서 F5.7 오프라인 공식 시드와 F5.5 원격 업데이트를 함께 지원하기 위함이다.
- 관련 PRD 섹션: F5.5, F5.7, §8.3
- 영향 범위: `StaticContentConfiguration`, `RemoteDeckSource`, `StaticDeckSource`, Xcode 빌드 설정

## 2026-07-18 결정적 v2 업데이트 fixture
- 결정: 목 카탈로그 생성기는 기본 v1 정적 호스트 외에 `shared/mock_catalog/updates/`에 전체 카탈로그 v2와 30개 덱을 생성한다. `cm_twice_cheer_007`만 v2·11항목으로 변경해 카탈로그 업데이트 감지, 원격 덱 교체, 내 덱 `更新あり`와 원탭 갱신을 재현한다.
- 근거: 모델만 조작하는 단위 테스트를 넘어 실제 카탈로그·덱 JSON의 버전·파일명·크기·스키마가 일치하는 업데이트 경로를 결정적으로 회귀 검증하기 위함이다.
- 관련 PRD 섹션: F5.5, §8.1, §8.2, §13 M3
- 영향 범위: `tools/gen_mock_catalog.py`, `shared/mock_catalog/updates/`, DeckKit·iOS 네트워크 테스트, `MyDecksView`

## 2026-07-18 삭제 후에도 유지되는 다운로드 태그 이력
- 결정: 설치 인덱스 schema v1에 선택 필드 `download_history`를 추가해 덱 ID별 마지막 태그를 보관하고, 덱 삭제 시에도 제거하지 않는다. 기존 인덱스에 필드가 없으면 현재 설치된 전체 덱 JSON의 태그로 메모리에서 보완하고 다음 쓰기 때 저장한다.
- 근거: F5.6 홈 추천이 현재 설치 목록이 아니라 다운로드 이력을 기준으로 해야 하며, 기존 사용자 설치 데이터를 무효화하는 스키마 버전 상승 없이 뒤로 호환되는 추가 필드로 구현할 수 있다.
- 관련 PRD 섹션: F5.5, F5.6, §9
- 영향 범위: `DeckInstallationStore`, `DeckLibrary`, 홈 추천, 설치 인덱스 마이그레이션 테스트

## 2026-07-18 규칙 기반 홈·결과 추천 순서
- 결정: 홈은 이력의 덱별 고유 태그 출현 수를 가중치로 합산해 점수가 높은 미설치 덱을 먼저, 동점은 featured→누적 다운로드→이름 순으로 최대 3개 추천한다. 이력이 없으면 같은 타이브레이커로 콜드 스타트한다. 결과는 현재 덱을 제외하고 공통 태그가 있는 덱을 미설치→태그 일치 수→누적 다운로드→이름 순으로 최대 2개 추천한다.
- 근거: 서버·ML 없이도 PRD의 “최다 태그 일치 + 미설치 + 인기순”을 결정적이고 단위 테스트 가능한 규칙으로 구현하고, 결과 추천의 미설치 우선 요구를 지키기 위함이다.
- 관련 PRD 섹션: F5.6, F12.5
- 영향 범위: `DeckRecommendationEngine`, `PracticeSetupView`, `PracticeResultView`, 추천 단위/UI 테스트

## 2026-07-18 M3 결과 추천 표면과 F12 확장 경계
- 결정: M3에서는 설치 덱 세션 종료 후 정확도·미스·완료 항목, 같은 태그 추천 2개, 1탭 `もう一回`을 갖는 결과 표면을 제공한다. 정확도는 정타 자모/(정타+오타 자모)로 계산한다. 점수·속도·최대 콤보·복습 목록·공유·2.5초 전체 연출과 영속 GameRecord는 M4~M6의 공통 F12 컴포넌트 확장으로 남긴다.
- 근거: 지금 필요한 F5.6의 플레이 직후 추천과 F12.5 재시작 AC를 실제 세션 경계에서 검증하면서, 아직 존재하지 않는 게임 점수·복습·공유 데이터를 임시 값으로 꾸미지 않기 위함이다.
- 관련 PRD 섹션: F5.6, F12.1~F12.5, §13 M3~M6
- 영향 범위: `PracticeSessionViewModel`, `PracticeView`, `PracticeResultView`, 후속 게임·복습·공유 결과 통합

## 2026-07-18 M3 운영 호스트와 종료 게이트 경계
- 결정: PRD §13의 M3는 `shared/mock_catalog/`의 결정적 v1·v2 fixture를 대상으로 종료한다. 실제 운영 `HANCO_CATALOG_URL`은 정적 호스트가 배정될 때 주입하고 별도 배포 smoke test를 수행하며, 호스트 미배정은 M3 기능 완료를 막지 않는다.
- 근거: HTTPS URL 주입, 조건부 GET, 캐시 fallback, 안전한 상대 경로, 덱 payload 검증은 로컬 HTTP fixture 테스트로 이미 검증된다. 존재하지 않는 운영 주소를 코드에 임시 하드코딩하면 환경 분리 원칙과 재현성을 해친다.
- 관련 PRD 섹션: F5.5, §8.2, §8.3, §13 M3
- 영향 범위: `StaticContentConfiguration`, 배포 빌드 설정, 운영 콘텐츠 배포 체크리스트

## 2026-07-18 M3 종료 검증 산출물
- 결정: M3 시각 회귀 기준은 발견 검색·덱 상세·내 덱·홈 추천·연습 결과 추천의 일본어 화면 5장으로 정한다. iPhone 17 / iOS 26.5 Simulator의 발견→다운로드→플레이→결과 추천→원탭 재도전 성공 흐름을 H.264 1206×2622, 19초 영상으로 보관한다.
- 근거: 목 카탈로그에서 F5의 핵심 사용자 흐름과 추천 경계를 재현 가능한 화면·영상으로 남기고, 전체 iOS 40개·SwiftPM 13개 테스트 및 generic iOS Release 빌드 통과와 함께 마일스톤 종료 상태를 검증할 수 있다.
- 관련 PRD 섹션: F5.1~F5.7, §8, §13 M3
- 영향 범위: `artifacts/m3/`, M3 회귀 검증 절차, M4 착수 기준

## 2026-07-18 M4 흐름 모드 시간·카드 진행 모델
- 결정: 게임 규칙 시간은 주입한 단조 증가 시각을 기준으로 계산하고, UI의 60Hz 보간 렌더링과 분리한다. 코스는 덱 타입과 평균 한글 길이에서 짧은 단어·단어·문장으로 자동 유도하며 카드 이동 시간은 각각 7·9·12초로 둔다. 화면 밖으로 나간 카드는 시간 차감 없이 다음 항목으로 진행하고 콤보만 초기화한다.
- 근거: 타이머와 카드 이탈을 결정적 단위 테스트로 검증하면서도 화면 갱신 주기에 따라 게임 결과가 달라지지 않게 한다. 카드 이탈의 시간 페널티는 PRD에 없으므로 초심자 보호 원칙에 맞춰 추가하지 않는다.
- 관련 PRD 섹션: F6, §7.3, §13 M4
- 영향 범위: `FlowGameViewModel`, `FlowGameCourse`, 게임 단위 테스트

## 2026-07-18 M4 세션 수명주기와 60Hz 표현 경계
- 결정: 게임 화면은 `TimelineView`와 `Canvas`로 카드·하트·별 파티클을 60Hz 목표로 표현하고, 규칙 갱신은 별도 ticker가 ViewModel에 시각을 전달한다. 앱이 inactive/background가 되면 즉시 규칙 시각을 일시정지하고 복귀 시 기준 시각을 재설정한다. 게임 중 탭 바·설정·모달 진입점은 노출하지 않는다.
- 근거: 백그라운드 체류 시간이 제한시간이나 카드 위치에 반영되는 회귀를 막고, 렌더 프레임과 규칙 계산을 분리해 실기기 Instruments 성능 검증과 최적화 지점을 명확히 하기 위함이다.
- 관련 PRD 섹션: F6 AC, §7.3, §11.1, §13 M4
- 영향 범위: `FlowGameView`, `FlowGameViewModel`, 게임 UI 테스트, 실기기 성능 게이트

## 2026-07-18 M4 1차 결과 화면과 완료 경계
- 결정: 첫 세로 흐름은 설치 덱 선택→60초 게임→점수·정확도·최대 콤보·속도·랭크 결과→1탭 재도전까지 구현한다. 현재 랭크 경계는 주입 가능한 `FlowGameRankTuning`으로 격리하되 M4 종료 전 콘텐츠 튜닝 파일로 외부화한다. GameRecord·최고 기록과 공통 결과 컴포넌트 통합, 결과 연출 전체, 실기기 iPhone 12급 60fps 측정은 후속 M4 작업으로 남긴다.
- 근거: F6의 핵심 플레이 루프를 먼저 실제 화면에서 검증하면서 F12의 영속화·공통화·외부 튜닝 요구를 임시 구현으로 완료 처리하지 않기 위함이다.
- 관련 PRD 섹션: F6, F12.1~F12.5, §9, §13 M4
- 영향 범위: `GameDeckSelectionView`, `FlowGameView`, `FlowGameResultView`, `FlowGameRankTuning`, `artifacts/m4/`

## 2026-07-18 게임 랭크 콘텐츠 튜닝 파일
- 결정: 정확도·속도 가중치, 속도 상한, S/A/B 경계를 `game_rank_tuning.json` schema v1에 둔다. 앱은 번들 파일을 필수 콘텐츠로 읽고, 가중치 합계 1·양의 속도 상한·S>A>B 및 0~100 범위를 검증한 뒤에만 사용한다.
- 근거: 랭크 밸런스를 앱 코드 재작성 없이 조정하고, 잘못된 튜닝 값이 모든 결과를 조용히 왜곡하지 않게 콘텐츠 오류를 개발·테스트 단계에서 즉시 드러내기 위함이다.
- 관련 PRD 섹션: F12.2, §13 M4
- 영향 범위: `game_rank_tuning.json`, `FlowGameRankTuning`, 랭크 로더·검증 테스트

## 2026-07-18 GameRecord·DeckProgress JSON 영속화
- 결정: iOS 16 호환을 유지하기 위해 Application Support의 `Hanco/Progress/game-progress.json`에 schema v1 인덱스를 원자적으로 저장한다. GameRecord에는 PRD §9 필드와 함께 속도·유효 플레이 시간·완료/이탈 항목 수를 보존하고, DeckProgress는 플레이 횟수·최고 점수·최고 정확도·최근 실행 시각을 집계한다. 첫 플레이는 `NEW RECORD`, 기존 최고와 동점은 새 기록 아님, 초과한 점수만 새 기록으로 판정한다.
- 근거: F12의 모든 표시 통계를 재계산 없이 복원하고 향후 명시적 마이그레이션 경계를 확보한다. 동점을 새 기록으로 반복 연출하지 않으면 실제 향상을 명확하게 전달할 수 있다.
- 관련 PRD 섹션: F12.1~F12.3, §9, §13 M4
- 영향 범위: `GameProgressStore`, `GameProgressLibrary`, `FlowGameView`, `FlowGameResultView`, 게임 덱 선택, 저장소·UI 회귀 테스트

## 2026-07-19 레슨·게임 공통 결과 타임라인
- 결정: 레슨과 게임 결과는 제네릭 `SessionResultView`의 Z1 헤더·Z2 스코어·Z3 통계·Z4 피드백·Z5 액션 구조를 공유한다. 진입 후 헤더 0.0s, 점수 0.4~1.2s, 세 메트릭 1.2s부터 0.3s 간격, 하단 존 1.8s, 액션 2.5s 활성 순서로 표시한다. 연출 중 화면 탭은 즉시 최종 상태로 이동시키되 하단 CTA와 완료 버튼은 그 탭이 끝난 뒤에만 활성화한다. Reduce Motion 사용자는 즉시 최종 상태를 본다.
- 근거: 화면별 비동기 애니메이션 중복을 제거하고 연출 스킵 탭이 재도전으로 전달되는 F12 AC 회귀를 결정적 타임라인·UI 테스트로 막기 위함이다.
- 관련 PRD 섹션: F12.1, F12.2, F12.5
- 영향 범위: `SessionResultView`, `PracticeResultView`, `FlowGameResultView`, 결과 타임라인·UI 테스트

## 2026-07-19 결과 수치 표현과 후속 기능 경계
- 결정: 게임 Z2는 실제 점수를, 아직 레슨 점수 산식이 없는 연습 Z2는 완료 항목 수를 카운트업한다. 메트릭 게이지 최종 길이는 정확도 100%, 별 3개, 게임 콤보 20, 속도 120자/분을 상한으로 정규화하며 오타 게이지는 10회 상한으로 표시한다. F7 복습 항목 수집·복습 CTA는 M5, F9 공유 CTA는 M6에서 공통 Z4/Z5 슬롯에 연결한다.
- 근거: PRD에 없는 레슨 점수를 임의로 발명하지 않고 현재 데이터로 정직한 성취 피드백을 제공한다. 복습·공유를 마일스톤 순서보다 앞서 임시 구현하지 않으면서 공통 결과 구조를 다시 바꾸지 않을 확장 지점을 확보한다.
- 관련 PRD 섹션: F7, F9, F12.1~F12.5, §13 M4~M6
- 영향 범위: `SessionResultMetric`, 레슨·게임 결과 UI, M5 복습과 M6 공유 연결

## 2026-07-19 M4 성공 흐름 회귀와 녹화 기준
- 결정: M4 종료 회귀에는 공식 `毎日の韓国語` 덱을 설치하고 `회사`를 정타해 50점·콤보 1·정확도 100%·`NEW RECORD` 결과를 확인한 뒤 1탭 재도전하는 결정적 UI 테스트를 사용한다. 같은 흐름의 iPhone 17 / iOS 26.5 Simulator H.264 1206×2622, 28.5초 영상을 기준 산출물로 보관한다.
- 근거: 무입력 시간 종료만으로는 정타 판정, 콤보, 보너스, 파티클, 최고 기록 연출을 한 번에 회귀 검증할 수 없다. 짧은 테스트 전용 세션은 실제 60초 규칙을 바꾸지 않으면서 핵심 성공 경로를 재현 가능하게 만든다.
- 관련 PRD 섹션: F6, F12.1~F12.5, §7.3, §13 M4
- 영향 범위: `HancoUITests`, `artifacts/m4/m4-success-flow-ja.mp4`, M4 시각 회귀 절차

## 2026-07-19 M4 실기기 성능 증거의 기준 기기 경계
- 결정: Simulator 영상의 가변 캡처 프레임률과 iPhone 15 Pro 측정은 PRD의 iPhone 12급 60fps 합격 증거로 대체하지 않는다. 연결된 iPhone 15 Pro에는 Hanco 빌드·설치까지 확인했지만, 무료 개발자 프로필의 설치 앱 3개 한도로 UI 테스트 러너가 설치되지 않아 Instruments 자동 측정을 완료 처리하지 않는다.
- 근거: Simulator 녹화 FPS는 앱 렌더 FPS가 아니며, 상위 기기의 무히치 결과도 하위 기준 기기의 프레임 예산을 보장하지 않는다. 기존 기기 앱 삭제는 사용자 데이터에 영향을 줄 수 있으므로 명시적 승인 없이 한도 확보를 위해 제거하지 않는다.
- 관련 PRD 섹션: F6 AC, §7.3, §12, §13 M4
- 영향 범위: `artifacts/m4/`, M4 종료 게이트, 실기기 Instruments 절차

## 2026-07-19 M4 iPhone 15 Pro 보조 계측과 Debug 프레임 프로브
- 결정: iPhone Mirroring에서 Instruments의 displayed-surface/FPS 테이블이 비는 환경은 Hanco 프로세스의 Animation Hitches·Game Performance trace와 Debug 전용 `CADisplayLink` 프레임 프로브를 교차 사용한다. 프레임 프로브는 120프레임부터 최근 최대 600프레임의 평균 FPS·p95 간격·20ms 초과 수를 표시하며 Release 빌드에는 포함하지 않는다. iPhone 15 Pro / iOS 26.5에서 연속 정타·파티클 2회 직후 평균 59.8 FPS, p95 16.7ms, 20ms 초과 2프레임이었고 Hanco hang·hitch는 0건이었다.
- 근거: Instruments trace는 Hanco의 hang·hitch 부재를 확인했지만 미러링 환경의 surface 테이블이 비어 정확한 FPS 숫자를 내보내지 못했다. 앱과 같은 main run loop의 display callback 간격을 직접 표본화하면 파티클 시나리오를 수치로 보강할 수 있고, Debug 한정으로 두면 제품 UI와 Release 성능에는 영향을 주지 않는다.
- 관련 PRD 섹션: F6 AC, §7.3, §12, §13 M4
- 영향 범위: `GameFrameRateMonitor`, `FlowGameView`, `FlowGameViewModelTests`, `artifacts/m4/`
- 경계: 이 결과는 상위 기종의 보조 통과 근거이며 PRD가 지정한 iPhone 12 기준 기기 Instruments 종료 게이트를 대체하지 않는다.

## 2026-07-19 M4 기준 기기 성능 게이트 자동화
- 결정: `tools/m4_ios_performance_gate.py`가 연결 기기의 devicectl 메타데이터를 읽어 `iPhone 12`(`iPhone13,2`) 여부를 먼저 판정한다. 기준 기기에서 Debug 빌드·설치·실행과 Animation Hitches·Game Performance 녹화, trace TOC·기기/시나리오 메타데이터 저장을 재현한다. 다른 기기는 `--allow-proxy`를 명시해야만 보조 증거를 만들 수 있고 그 결과는 M4 종료로 간주하지 않는다.
- 근거: 현재 연결된 기기는 iPhone 15 Pro뿐이라 기준 기기 합격값을 만들 수 없지만, M5를 먼저 시작해 순서를 건너뛰지 않고도 iPhone 12가 연결되는 즉시 같은 빌드·파티클 시나리오·증빙 형식으로 최종 게이트를 수행할 수 있어야 한다. 기기 모델의 명시적 차단은 상위 기기 결과가 실수로 기준 기기 결과처럼 기록되는 회귀를 방지한다.
- 관련 PRD 섹션: F6 AC, §12, §13 M4
- 영향 범위: `tools/m4_ios_performance_gate.py`, `tools/tests/test_m4_ios_performance_gate.py`, `artifacts/m4/README.md`

## 2026-07-19 iPhone 12 성능 검증의 출시 게이트 이관
- 결정: 사용자 명시적 승인에 따라 M4 기능 구현과 iPhone 15 Pro 보조 검증을 완료 상태로 두고 M5에 착수한다. PRD의 iPhone 12 파티클 포함 60fps 측정은 삭제하거나 통과로 간주하지 않고 M6 종료 전 출시 차단 게이트로 이관한다.
- 근거: 현재 연결 가능한 기준 기기가 없지만 M4 기능·회귀·Release 빌드와 상위 기기 보조 성능 근거는 확보됐다. 외부 하드웨어 대기로 커리큘럼·리텐션 구현 전체를 중단하지 않되, 하위 기준 기기 위험은 출시 전 반드시 해소한다.
- 관련 PRD 섹션: F6 AC, §12, §13 M4~M6
- 영향 범위: M4 완료 표기, M5 착수 순서, M6 종료 체크리스트, `artifacts/m4/`

## 2026-07-19 F7 복습 항목 콘텐츠 스냅샷
- 결정: ReviewDeckItem schema v1은 PRD §9 필드에 더해 `ko`·`reading_ja`·`meaning_ja`를 저장한다. 키는 `source_deck_id::item_id`이며, 오타는 miss_count를 누적하고 연속 노미스를 0으로 되돌린다. 활성 항목만 노미스 완료를 세며 3회째에 graduated_at을 기록한다. 수동 삭제는 항목을 제거하고, 졸업 항목의 수동 추가나 새 오타는 다시 활성화한다.
- 근거: 사용자가 원본 덱을 삭제해도 복습 항목을 표시·출제할 수 있어야 하며, 서로 다른 덱에서 같은 item_id를 사용할 가능성도 격리해야 한다. 졸업 후 새 오타는 숙련이 깨졌다는 명시적 신호이므로 복습 대상으로 되돌리는 것이 F7 목적에 맞다.
- 관련 PRD 섹션: F7, F12.4, §9, §13 M5
- 영향 범위: `ReviewDeckStore`, `ReviewDeckLibrary`, 후속 레슨·게임 수집과 복습 UI

## 2026-07-19 F7 세션 판정 이벤트와 저장 경계
- 결정: 순수 세션 ViewModel은 항목 인덱스·해당 시도 오타 횟수·오타 자모 위치 집합을 완료 이벤트로 내보내고, 화면 계층이 이를 실제 DeckItem 스냅샷과 결합해 ReviewDeckLibrary에 저장한다. 노미스 완료는 활성 복습 항목의 연속 성공을 올리고, 오타가 있었던 완료는 한 번의 복습 실패로 기록한다. 게임에서 오타 후 완료하지 못하고 이탈한 카드도 별도 복습 판정 이벤트를 내보내 수집하되, 오타 없는 단순 이탈은 수집·졸업 판정에서 제외한다.
- 근거: 입력 상태 기계를 파일 저장과 분리해 단위 테스트 가능성을 유지하면서도 F7의 “오타가 발생한 단어”에 완료하지 못한 게임 카드까지 포함해야 한다. 저장소의 miss_count는 오타 키 횟수가 아니라 실패한 항목 시도 횟수로 유지하고, 결과 화면에는 해당 세션의 실제 오타 키 횟수를 별도로 보여 준다.
- 관련 PRD 섹션: F7, F12.4, §9, §13 M5
- 영향 범위: `PracticeSessionViewModel`, `FlowGameViewModel`, `PracticeView`, `FlowGameView`, 복습 단위/UI 테스트

## 2026-07-19 F12 Z4 오답 자모 강조와 복습 진입
- 결정: 결과 Z4는 세션 오답을 `source_deck_id::item_id`로 합쳐 오타 횟수와 자모 위치 합집합을 표시하고, 최대 5개 뒤에 `すべて見る`를 제공한다. 완성형 한글 글리프 내부 자모만 따로 밑줄 칠 수 없으므로 오타 자모가 속한 음절 글자를 빨간 밑줄로 강조한다. 오답 0건은 `パーフェクト!` 배너로 치환하며, 오답이 있을 때만 `復習する`를 노출해 해당 항목만 즉시 연습한다. 기본 3문제의 요미가나·뜻도 일본어 로컬라이제이션 데이터로 제공해 덱 세션과 같은 수집 흐름을 사용한다.
- 근거: PRD의 자모 단위 피드백 의도를 완성형 글리프 제약 안에서 보존하고, 누적 복습 덱 수치와 현재 세션의 오타를 혼동하지 않게 한다. 결과에서 라이브러리를 거치지 않고 바로 복습할 수 있어 F12.5의 다음 행동 분기를 완성한다.
- 관련 PRD 섹션: F7, F12.1, F12.4, F12.5, §13 M5
- 영향 범위: `SessionResultReviewSection`, `PracticeResultView`, `FlowGameResultView`, 일본어 로컬라이제이션, 결과 UI 회귀 테스트

## 2026-07-19 F4 MVP 커리큘럼 단계와 별점 경계
- 결정: 코어 커리큘럼은 기본 자음·기본 모음·글자 조합·받침·단어·문장의 6챕터를 각각 10문제짜리 1스테이지로 시작한다. 챕터1~4는 직전 스테이지를 정확도 80% 이상으로 완료해야 순차 해금하고 챕터5~6은 처음부터 자유 선택한다. 별점은 정확도 80% 이상이면 1개, 정확도 90% 이상이면서 40자모/분 이상이면 2개, 정확도 97% 이상이면서 60자모/분 이상이면 3개로 판정한다.
- 근거: PRD는 스테이지당 10~15문제와 정확도·속도 기반 1~3별을 요구하지만 초기 스테이지 수와 속도 경계는 정하지 않았다. 각 챕터의 핵심 세로 흐름을 최소 완결 단위로 제공하고, 별점 규칙을 순수 함수로 격리해 콘텐츠 튜닝 시 UI·저장 스키마를 바꾸지 않기 위함이다.
- 관련 PRD 섹션: F4, §9, §13 M5
- 영향 범위: `CurriculumCatalog`, `CurriculumUnlockPolicy`, `CurriculumStarRating`, 스테이지 맵·결과 화면·커리큘럼 단위 테스트

## 2026-07-19 F4 중단 복구와 활성 시간 저장
- 결정: Application Support의 `Hanco/Curriculum/curriculum-progress.json` schema v1에 스테이지별 최고 별·최고 정확도·최초 완료 시각과 단일 활성 세션 체크포인트를 원자적으로 저장한다. 체크포인트는 문제 인덱스, 승인된 자모 키, 전체·현재 문제 오타, 오타 자모 위치, 누적 활성 시간을 포함한다. 타이머는 첫 입력부터 시작하고 inactive/background에서 누적 후 정지하며 active 복귀 시에만 재개한다. 실패한 최종 시도도 활성 체크포인트는 종료하되 완료 기록은 만들지 않는다.
- 근거: 앱 종료와 백그라운드 전환 뒤에도 자모 조합·판정·정확도·속도를 같은 상태에서 복원하고, 백그라운드 체류 시간이 별점 속도 기준을 낮추는 경쟁 앱 회귀를 구조적으로 차단하기 위함이다. 완료 기록과 진행 중 세션을 분리하면 실패 시 해금되지 않는 규칙도 명확해진다.
- 관련 PRD 섹션: F4 AC, §9, §13 M5
- 영향 범위: `CurriculumProgressStore`, `PracticeSessionCheckpoint`, `PracticeSessionViewModel`, `PracticeView`, 앱 수명주기·단위/UI 회귀 테스트

## 2026-07-19 F8 JST 일자와 스트릭 귀속 규칙
- 결정: 리텐션 일자는 기기 시간대와 무관하게 `Asia/Tokyo`의 `YYYY-MM-DD`로 계산한다. 커리큘럼 클리어, 게임 한 판 종료, 데일리 챌린지 완료를 같은 날 중복 없는 활동 종류로 저장하고 하나라도 있으면 그날 스탬프를 찍는다. 자정을 넘긴 세션은 시작 당시 JST 일자에 귀속하며, 결과에서 새로 재도전하는 일반 커리큘럼·게임은 새 시작일을 잡는다. 오늘 기록이 아직 없어도 어제까지 이어진 스트릭은 오늘이 끝날 때까지 유지하고 하루가 완전히 비면 0으로 초기화한다.
- 근거: 진행 중 콘텐츠와 보상이 자정에 바뀌거나 같은 게임이 양쪽 날짜에 중복 집계되는 문제를 막고, 사용자가 오늘 활동할 기회를 갖는 동안 스트릭을 성급하게 0으로 표시하지 않기 위함이다. 데일리 챌린지를 스트릭 활동으로 인정해야 “毎日3分”이 별도 과제가 아니라 핵심 리텐션 루프로 작동한다.
- 관련 PRD 섹션: F8 AC, §9, §13 M5
- 영향 범위: `JSTDay`, `RetentionSessionContext`, `RetentionStore`, 커리큘럼·게임 완료 이벤트, 자정 경계 단위 테스트

## 2026-07-19 F8 로컬 데일리 콘텐츠와 리마인더
- 결정: 데일리 챌린지는 검증된 챕터5 단어 10개를 JST 일자 ordinal로 회전해 중복 없는 5단어를 결정적으로 선택하고, 연속 날짜의 순서가 반드시 달라지게 한다. 리마인더는 20:00 JST·OFF를 기본으로 하며 홈 카드에서 15분 단위 시간을 지정한다. ON 전환 때만 알림 권한을 요청하고, 거부·스케줄 실패 시 토글을 OFF로 되돌린다. 반복 알림은 단일 고정 식별자로 교체·취소한다.
- 근거: 서버·네트워크 없이 모든 기기에서 같은 날짜의 같은 콘텐츠를 재현하고, 알림을 명시적으로 선택한 사용자에게만 하루 한 번 전달하기 위함이다. 고정 식별자는 시간 변경 때 중복 알림이 쌓이는 회귀를 막는다.
- 관련 PRD 섹션: F8, §11, §13 M5
- 영향 범위: `DailyChallengeCatalog`, `DailyReminderLibrary`, `SystemDailyReminderScheduler`, 리텐션 홈 UI, 알림 단위/UI 테스트

## 2026-07-19 M5 종료 검증과 온보딩 경계
- 결정: M5는 F4 커리큘럼, F7 복습 덱, F8 스트릭·데일리·리마인더와 기존 F5.6 홈/결과 추천을 완료 상태로 둔다. 온보딩 내부의 목표 기반 일괄 다운로드와 리마인더 제안 화면 연결은 PRD §13이 온보딩 전체를 배정한 M6 F1 세로 흐름에서 수행하되, 현재 홈 추천·리마인더 API를 재사용한다. M5 기준 산출물은 커리큘럼/리텐션 홈, 데일리 완료, 복습 결과 이미지와 데일리 성공 영상으로 보관한다.
- 근거: 존재하지 않는 온보딩 화면을 M5에서 임시로 만들었다가 M6에서 재구축하지 않으면서, M5가 소유한 추천·알림 기능 자체는 실제 사용자 UI와 테스트 가능한 상태로 끝내기 위함이다. iOS 95개·SwiftPM 13개 테스트와 generic iOS Release 빌드가 통과했다.
- 관련 PRD 섹션: F1, F4, F5.6, F7, F8, §13 M5~M6
- 영향 범위: `artifacts/m5/`, M5 완료 표기, M6 온보딩 구현 계획

## 2026-07-19 F1 3단계 온보딩과 네 번째 탭 입력 경계
- 결정: 온보딩은 목표 선택, 두벌식 좌우 손 소개, `가` 직접 입력의 정확히 3단계로 구성한다. 세 번째 단계에서 `ㄱ`·`ㅏ` 입력을 완료하면 화면을 추가하지 않고 추천·일괄 다운로드·리마인더 제안으로 전환한다. 목표 카드 선택을 1번째, 각 다음 버튼을 2·3번째 탭으로 세어 4번째 탭이 `ㄱ` 실제 입력이 되게 한다. 모든 단계에서 스킵할 수 있고 완료·스킵·목표·현재 단계를 UserDefaults의 schema v1 JSON으로 저장한다. 온보딩 상태가 없지만 기존 설정 또는 Application Support 데이터가 있는 사용자는 완료·스킵 상태로 자동 이관한다.
- 근거: 가입·설정 화면을 세션 앞에 두지 않으면서 PRD의 3단계 상한과 4번째 탭 이내 실제 입력 AC를 동시에 만족하고, 이미 M2~M5 빌드를 사용한 사용자에게 신규 온보딩이 갑자기 노출되는 회귀를 막기 위함이다.
- 관련 PRD 섹션: F1, §5 S1, §13 M6
- 영향 범위: `OnboardingStore`, `OnboardingView`, `AppRootView`, 일본어 로컬라이제이션, 온보딩 단위·UI 테스트

## 2026-07-19 F1 목표 추천과 오프라인 일괄 다운로드
- 결정: 온보딩 추천 3개는 설치되지 않은 공식 덱을 우선 풀로 사용하고, 선택 목표 태그 일치 수·featured·누적 다운로드·이름 순으로 정렬한다. 공식 덱이 3개보다 적을 때만 전체 카탈로그로 확장한다. 홈 추천에서는 다운로드 이력 태그 가중치를 2, 온보딩 목표 태그를 1로 두어 실제 이용 이력이 생기면 목표 선택보다 우선하게 한다. 추천 3개는 한 번 정한 ID를 다운로드 도중 유지하고 순차 설치해 번들 공식 덱의 오프라인 성공을 보장한다. 리마인더는 기존 20:00 JST·기본 OFF 설정을 그대로 제안하며 사용자가 ON으로 바꿀 때만 권한을 요청한다.
- 근거: 첫 실행의 불안정한 네트워크 때문에 일괄 다운로드가 부분 실패하는 위험을 낮추고, F1이 명시한 “목표는 추천 정렬에만 사용” 경계를 보존하면서 이후 실제 행동이 추천을 자연스럽게 교정하도록 하기 위함이다.
- 관련 PRD 섹션: F1, F5.6, F8, §11.1, §13 M6
- 영향 범위: `DeckRecommendationEngine`, `PracticeSetupView`, `OnboardingView`, `DeckLibrary`, 리마인더 UI

## 2026-07-19 F2a OS IME 텍스트 diff 판정 경계
- 결정: OS IME 판정은 확정 텍스트와 활성 marked text를 분리해 각각 두벌식 자모 시퀀스로 역분해한다. 전체 표시 텍스트가 목표 자모 전방 일치이면 조합 중에도 진행을 반영하고, marked text만 불일치하면 마지막 확정 전방 일치 상태로 되돌리되 오타는 기록하지 않는다. marked range가 없는 확정 텍스트의 불일치만 오타 1회로 기록하며 불일치 앞의 올바른 전방 일치는 승인하고 나머지 편집을 버린다. 목표를 이미 완성한 뒤 IME 확정을 위해 붙은 추가 입력은 완료를 우선하고 버린다. 텍스트필드 커서는 끝으로 고정해 중간 삽입으로 확정·marked 순서가 뒤섞이지 않게 한다.
- 근거: 한국어 IME의 마지막 음절은 다음 입력 전까지 marked 상태일 수 있어 조합 중의 올바른 완성까지 무조건 보류하면 문제를 끝낼 수 없다. 반대로 변환 중 흔들림을 즉시 오타로 세면 F2a의 핵심 판정 규칙을 위반하므로 확정 여부와 목표 전방 일치를 함께 사용해야 한다.
- 관련 PRD 섹션: F2a, §6.3, §6.4
- 영향 범위: `OSIMETextJudge`, `OSIMEInputPanel`, `PracticeSessionViewModel`, `FlowGameViewModel`, 공용 벡터·세션 단위 테스트

## 2026-07-19 F2a 입력 모드 전환과 미설치 안내 경계
- 결정: 입력 모드 기본값은 기존 키보드 설정과 같은 UserDefaults 영역에 `builtin | os_ime`로 저장한다. 일반 연습·복습·데일리와 챕터5~6·게임은 세션 내부 전환을 허용하고 챕터1~4는 내장 모드로 고정한다. 한국어 입력 모드는 `UITextInputMode.activeInputModes`의 `ko` 언어 코드로 감지한다. 미설치 안내의 전체 시트는 세션 전 설정 화면에서 사용자가 OS 모드를 선택할 때만 표시하고, 실행 중 토글에서는 타이머·입력을 가리는 모달 대신 일본어 인라인 안내를 표시한다.
- 근거: PRD의 설치 가이드와 세션 무중단 철칙을 동시에 만족해야 한다. 명시적 설정 동작 전에는 시각 단계 가이드를 충분히 보여 주되, 연습·게임 시작 뒤에는 키보드 미설치가 타이머나 기록에 영향을 주는 인터럽트가 되지 않게 한다.
- 관련 PRD 섹션: F2a, F10, §11.1
- 영향 범위: `KeyboardPreferenceKeys`, `PracticeSetupView`, `PracticeView`, `FlowGameView`, `KoreanKeyboardGuideView`, 일본어 로컬라이제이션

## 2026-07-19 F2a 게임 기록 모드 분리와 기존 데이터 이관
- 결정: 새 GameRecord와 DeckProgress는 PRD 값인 `input_mode: builtin | os_ime`를 기록하고, 최고 점수·정확도·플레이 횟수를 `deck_id + input_mode` 키로 별도 집계한다. 기존 schema v1의 `built_in`, `os_keyboard` 값은 각각 새 값으로 읽고 input_mode가 없던 DeckProgress는 builtin으로 이관한다. 한 게임에서 모드를 전환한 경우 내장 키를 한 번이라도 입력한 판은 `builtin`, 내장 키 입력 없이 OS 모드만 사용한 판은 `os_ime`로 분류한다.
- 근거: 내장 가이드의 도움을 일부라도 받은 판을 순수 OS 실전 기록과 같은 최고 기록으로 비교하지 않으면서 기존 사용자 기록을 잃지 않아야 한다. 혼합 판을 builtin으로 보수적으로 분류하면 OS 최고 기록의 입력 조건이 일관된다.
- 관련 PRD 섹션: F2a, §9
- 영향 범위: `SessionInputMode`, `GameRecord`, `DeckProgress`, `GameProgressStore`, 게임 덱 선택·결과 화면, 저장소 마이그레이션 테스트

## 2026-07-19 F9 오프라인 공유 카드와 지표 경계
- 결정: 공유 이미지는 네트워크나 서버 없이 SwiftUI `ImageRenderer`로 600pt 정사각형을 2배 스케일 렌더링한 1200×1200 불투명 PNG로 생성한다. 게임 카드는 점수·정확도·최대 콤보·현재 JST 스트릭을, 레슨 카드는 임의의 콤보를 만들지 않고 완료 문제 수·정확도·별·현재 JST 스트릭을 표시한다. 현재 가칭 브랜드인 Hanco와 `ㅎ` 키캡·Swift 벡터 병아리를 로고 락업으로 쓰고 일본어 다운로드 CTA를 포함한다.
- 근거: X와 Instagram 피드에서 잘리지 않는 정사각형 원본을 만들면서 앱의 오프라인 우선 원칙을 유지해야 한다. 레슨에는 콤보 데이터가 없으므로 게임 지표를 거짓으로 재사용하는 대신 실제 학습 성취를 공유하는 편이 정확하다.
- 관련 PRD 섹션: F9, F12.1, F12.5, §11.1, §13 M6
- 영향 범위: `SessionShareCard`, `PracticeResultView`, `FlowGameResultView`, 일본어 로컬라이제이션, F9 단위·UI 테스트, `artifacts/m6/`

## 2026-07-19 F9 시스템 공유 전달 형식
- 결정: 결과 화면의 공통 `シェア` 버튼은 생성된 PNG와 일본어 해시태그 캡션을 `UIActivityViewController`에 함께 전달하고, `LPLinkMetadata` 이미지 미리보기와 PNG UTI를 제공한다. 공유 시트를 닫으면 같은 결과 화면으로 돌아오며 기존 재도전 흐름을 유지한다.
- 근거: 설치된 X·Instagram 등 공유 확장을 OS가 직접 중개하게 하면 특정 서비스 SDK나 계정·쓰기 API 없이도 서비스별 공유가 가능하고, X 공유 확장에는 정사각형 이미지와 텍스트를 동시에 제공할 수 있다.
- 관련 PRD 섹션: F9, F12.5, §8.3, §11.1
- 영향 범위: `SessionShareButton`, `SessionActivityView`, `SessionShareImageSource`, 결과 화면 UI 회귀 테스트

## 2026-07-19 F11 런타임 합성 사운드와 판정 이벤트 경계
- 결정: 외부 음원 파일 없이 44.1kHz mono PCM을 런타임에 합성하고 4개의 `AVAudioPlayerNode`를 순환해 짧은 효과음을 겹쳐 재생한다. 기계식 타건음은 짧은 노이즈+고주파 하강음, 소프트 타건음은 긴 저주파 사인음으로 구분한다. 내장 키보드는 touch-down마다 타건음을 내며, 레슨·온보딩은 항목 완성/확정 오타에만 추가음을 낸다. 게임 완성음의 기준 피치는 현재 콤보에 따라 0~20 구간에서 연속 상승하고, 오타음은 210→170Hz·0.14초의 낮고 작은 톤으로 제한한다. OS IME 모드는 시스템 타건음을 중복 재생하지 않고 앱 판정의 완성·오타음만 재생한다.
- 근거: 타건 지연과 빠른 연타 누락을 줄이면서 저작권·번들 용량 의존 없이 두 프리셋을 일관되게 제공해야 한다. 입력음과 판정음을 분리하면 오타 키도 눌린 감각은 유지하되 정오 피드백은 확정 판정과 정확히 맞고, OS 키보드의 자체 타건음과도 충돌하지 않는다.
- 관련 PRD 섹션: F10, F11, §6.3, §13 M6
- 영향 범위: `HancoSoundEngine`, `PracticeSetupView`, `PracticeView`, `FlowGameView`, `OnboardingView`, 일본어 로컬라이제이션, 오디오 단위/UI 테스트

## 2026-07-19 F11 다른 앱 오디오 보존과 실패 격리
- 결정: 효과음 세션은 `AVAudioSession.Category.ambient`와 명시적 `.mixWithOthers`만 사용한다. 효과음 ON일 때 세션·엔진을 준비하고 OFF 즉시 플레이어를 정지한 뒤 `.notifyOthersOnDeactivation`으로 비활성화한다. 세션 구성이나 재생 실패는 레슨·게임 흐름에 전파하지 않는다. 이 정책은 단위 테스트 상수로 고정하고, Spotify 등 실제 외부 음악의 지속 여부는 연결된 iPhone에서 M6 실기기 체크로 확인한다.
- 근거: 소리는 학습 피드백의 보조 기능이므로 다른 앱 음악과 세션 진행보다 우선할 수 없다. 오디오 오류를 무시하고 설정 OFF에서 세션을 반납하면 PRD의 무중단 원칙과 다른 앱 음악 보존 요구를 동시에 지킬 수 있다.
- 관련 PRD 섹션: F11, §11.1, §12
- 영향 범위: `HancoAudioSessionPolicy`, `HancoSoundEngine`, F11 단위 테스트, M6 실기기 체크리스트

## 2026-07-19 F10 앱 설정의 단일 저장 경계
- 결정: 독립된 다섯 번째 설정 탭에서 글자 크기(`small | standard | large`), 테마(`light | dark`), 언어(`ja | en | ko`)를 관리하고 기존 키 가이드·영문 힌트·햅틱·기본 입력 모드·효과음·타건음 프리셋도 같은 화면에 모은다. 모든 값은 기존 설정과 동일하게 `@AppStorage`/UserDefaults에 즉시 저장한다. 글자 크기 배율은 0.88/1.0/1.16이며 문제 텍스트·조합 프리뷰·게임 카드·온보딩 문제와 키캡 자모/영문 힌트에 적용한다. 알 수 없는 저장 값은 각각 기본 크기·라이트·일본어로 복구한다.
- 근거: 설정 진입점과 저장 방식을 하나로 통일하면 연습 준비 화면과 설정 탭의 값이 자동으로 동기화되고, 별도 저장 시점 없이 앱 종료에도 값이 남아 F10의 즉시 반영·영속화 AC를 만족한다. 글자 크기는 PRD가 명시한 학습 대상과 키캡에 집중해 주변 정보 밀도를 불필요하게 바꾸지 않는다.
- 관련 PRD 섹션: F2, F2a, F10, §12
- 영향 범위: `AppSettings`, `SettingsView`, `AppRootView`, `HangulKeyboardView`, `PracticeView`, `FlowGameView`, `OnboardingView`, 설정 단위/UI 테스트

## 2026-07-19 F10 앱 내부 언어 전환과 번역 무결성
- 결정: 앱 언어의 초기값은 일본어로 유지하고 사용자가 고른 `ja/en/ko` lproj를 앱 내부 로컬라이제이션 번들로 사용한다. SwiftUI 키는 루트 locale 변경으로, 형식 문자열·알림·공유 캡션 등 직접 생성 문자열은 `AppLocalization`으로 같은 번들을 조회한다. 언어 변경 시 루트 콘텐츠를 재구성해 탭·화면·날짜·숫자·검색 정렬 로케일을 즉시 바꾸며 현재 탭과 영속 데이터는 유지한다. 세 언어는 동일한 442개 키와 동일 순서·형식의 `%@`/`%d`/부동소수점 인자를 가져야 하며 이를 단위 테스트 게이트로 고정한다.
- 근거: 프로세스의 시스템 언어만 따르는 API와 SwiftUI environment locale을 섞으면 한 화면 안에 두 언어가 남을 수 있다. 선택한 번들을 직접 사용하고 키·서식 인자를 자동 비교하면 즉시 전환과 런타임 포맷 안전성을 함께 보장한다.
- 관련 PRD 섹션: F10, §12, §13 M6
- 영향 범위: `AppLocalization`, `AppRootView`, `ja/en/ko Localizable.strings`, 사용자 노출 형식 문자열·표시 로케일, `AppSettingsTests`, F10 UI 테스트

## 2026-07-19 F12 결과 완료 버튼과 내비게이션 소유권
- 결정: 결과 연출 중 비활성화하는 대상은 Z5의 재도전·복습·공유 CTA로 한정하고, 내비게이션 바의 `완료`는 연출 상태와 무관하게 즉시 사용할 수 있게 한다. 연습과 게임 세션이 각각 `isPresented` 결과 상태를 직접 소유하며, `완료`는 결과 destination을 먼저 닫고 다음 메인 액터 턴에서 세션을 닫는다. 같은 요청의 중복 실행은 상태 가드로 차단한다.
- 근거: 연출 중 `완료`를 탭하면 입력이 버려져 종료되지 않은 것처럼 보였고, 게임 결과의 읽기 전용 표시 Binding은 dismiss 요청을 상태에 반영하지 못했다. 결과와 세션을 서로 다른 dismiss action에 암묵적으로 맡기지 않고 소유 화면에서 순서대로 해제하면 일반 연습·복습·게임의 push 깊이와 관계없이 종료 위치가 결정적이며, F12의 Z5 오작동 방지 조건도 유지된다.
- 관련 PRD 섹션: F12.2, F12.5, §11.1
- 영향 범위: `SessionResultView`, `PracticeView`, `FlowGameView`, 결과 조기 종료·재도전 UI 회귀 테스트

## 2026-07-19 S5 문제·세션 완료 자동 전환
- 결정: 레슨·일반 연습·데일리 챌린지·복습에서 모든 문제는 완성 성공 연출을 0.65초 보여 준 뒤 자동 전환한다. 중간 문제는 다음 문제로, 마지막 문제는 F12 결과 화면으로 이동하며 `Next target`과 `View results` 버튼은 모두 제거한다. 하나의 전환 작업이 두 분기를 담당하고 재시도·화면 종료·백그라운드 진입 때 취소되며, 완료 상태로 포그라운드 복귀하면 중복 없이 다시 예약된다.
- 근거: 마지막 단어만 결과 버튼을 요구하면 앞선 자동 전환으로 만들어진 타이핑 리듬이 세션 경계에서 다시 끊긴다. 같은 성공 피드백 시간을 유지한 채 결과 진입까지 자동화하면 사용자는 입력 완료 자체를 세션 완료로 이해할 수 있고, F12의 재도전·복습·공유 선택은 결과 화면에서 그대로 보존된다.
- 관련 PRD 섹션: S5, F4, F7, F8, F12, §11.1
- 영향 범위: `PracticeView`, 체크포인트 저장·복구, 내장/OS 입력 모드, 커리큘럼·일반 덱·데일리·복습 UI 회귀 테스트

## 2026-07-19 세션 헤더의 TTS와 실행 중 설정
- 결정: 연습·복습·데일리·게임 세션 우측 상단의 초기화 버튼을 제거하고, 키 가이드·영문 힌트·입력 모드·효과음·타건음·햅틱을 즉시 바꾸는 공통 `세션 설정` 메뉴로 교체한다. 연습 목표 카드의 비기능성 `Target` 레이블은 현재 한국어 목표를 `ko-KR`로 읽는 `듣기` 버튼으로 교체한다. TTS와 효과음은 모두 `.ambient + .mixWithOthers` 오디오 정책을 따르며, 목표 변경·세션 종료 때 발화를 중지한다. 결과 화면의 `다시 하기`는 세션 종료 뒤의 의도적인 CTA이므로 유지한다.
- 근거: 세션 도중 초기화는 실수로 진행을 잃기 쉽고 별도 설정 화면은 입력 흐름을 가린다. 툴바 메뉴에서 기존 영속 설정을 직접 갱신하면 화면을 떠나지 않고 즉시 반영되며, 고정 레이블을 발음 확인 기능으로 바꾸면 같은 공간의 학습 가치를 높일 수 있다.
- 관련 PRD 섹션: S5, F2, F2a, F10, F11, §11.1
- 영향 범위: `PracticeView`, `FlowGameView`, `SessionSettingsMenu`, `TargetSpeechSynthesizer`, ja/en/ko 로컬라이제이션, 세션 UI 회귀 테스트

## 2026-07-19 Swift 벡터 병아리 성장 시스템 이식
- 결정: 사용자 소유의 로컬 `hantap` 프로젝트에서 SwiftUI 도형 기반 병아리 구현을 가져와 Hanco의 이미지 기반 펭귄을 전면 교체한다. 온보딩은 목표별 무늬의 `알`로 시작하고, 메인 화면에서는 완료 챕터 0개=`금 간 알`, 1~2개=`부화 중`, 3~5개=`병아리`, 6개=`완전히 성장한 닭`으로 표시한다. 1·3·6챕터 경계의 새 단계는 커리큘럼 화면으로 돌아온 뒤 0.55초 내비게이션 안정화 시간을 거쳐 한 번만 축하하며, 첫 부화 때 이름을 받을 수 있다. 기존 진행 사용자는 저장된 완료 챕터 수로 현재 단계를 즉시 계산하고 아직 보지 않은 현재 단계 축하만 한 번 본다.
- 결정: 캐릭터 이름·장착 설정·마지막 축하 단계는 UserDefaults에 저장한다. 옷장은 3일 스트릭의 응원봉, 3챕터 완료의 졸업모, 설치 덱 7개의 헤드폰을 기존 리텐션 데이터로 해금하고 자동·미장착·고정 장착을 지원한다. 홈 캐릭터는 탭·더블탭에 반응하고 길게 누르거나 옷장 버튼으로 관리하며 계절 장식과 Reduce Motion을 지원한다. 공유 카드는 네트워크·이미지 에셋 없이 정적 병아리 벡터를 렌더링한다.
- 근거: 기존 F4·F8 진척을 하나의 감정적 보상 루프로 연결하면서 별도 서버나 중복 진척 저장소를 만들지 않고, PRD의 반응형 마스코트를 정적 이미지보다 풍부하게 충족하기 위함이다. 성장 축하를 세션 종료 후로 제한해 세션 중 모달 금지 원칙과 결과 내비게이션 종료 순서를 보존한다.
- 관련 PRD 섹션: F4, F8, F9, F12, §7.1, §9, §11.1, §13 M6
- 영향 범위: `ChickMascotView`, `MascotGrowthSystem`, `CurriculumMapView`, 온보딩·연습·결과·게임·설정·공유 카드, ja/en/ko 로컬라이제이션, M6 UI·단위 테스트

## 2026-07-19 S5 음절 성공 표시와 이벤트 기반 병아리 반응
- 결정: 목표 문자열의 각 문자를 `JamoDecomposer`의 기대 자모 범위와 연결하고, 승인된 자모 인덱스가 해당 범위 끝에 도달한 한글 음절만 초록색 키캡·체크 배지로 완료 표시한다. 아직 일부만 입력한 음절은 분홍색 진행 상태, 나머지는 기본 상태로 표시하며 완성형 문자열 비교는 판정에 사용하지 않는다. 긴 목표는 가로 스크롤하되 짧은 목표는 기존처럼 중앙 정렬한다.
- 결정: 병아리 반응은 성장 단계·기본 기분과 분리된 일회성 이벤트로 발행한다. 정타 자모는 끄덕임, 음절 완료는 날갯짓, 단어 완료는 점프, 5·10·20 연속 정타는 강도가 커지는 콤보 반응, 오타는 고개 흔들기와 다음 키 방향 응시, 무오타 세션 완료는 왕관·점프·깃털 축하를 사용한다. 응원봉·졸업모·헤드폰도 같은 이벤트에 반응하며 반복되는 같은 이벤트도 revision 값으로 다시 재생한다. Reduce Motion에서는 이동 대신 정적 배지와 글로우만 표시한다.
- 근거: 자모 단위 학습 중 어느 음절까지 정확히 끝냈는지를 즉시 보여 주면서 도깨비 이월을 포함한 실제 판정 소스는 기존 자모 시퀀스에 그대로 두어야 한다. 반응을 mood 상태와 분리하면 빠른 연속 정타도 누락 없이 보상할 수 있고, 세션을 가리는 모달 없이 캐릭터·소품·성장 시스템을 실제 입력 행동과 연결할 수 있다.
- 관련 PRD 섹션: S5, §6.3, §7.1~§7.3, F11, §11.1, §12
- 영향 범위: `PracticeSessionViewModel`, `PracticeView`, `ChickMascotView`, `MascotGrowthSystem`, `HangulKeyboardGeometry`, ja/en/ko 로컬라이제이션, 음절 진행·반응 단위/UI 회귀 테스트, `artifacts/m6/`

## 2026-07-19 외부 캐릭터 시트 43개 제안의 펫 시스템 확장
- 결정: 검토한 캐릭터 시트의 미구현 제안을 하나의 펫 계약으로 수용한다. 기존 8개 표정에 토라짐·놀람·이를 악문 집중·어지러움·윙크·수줍음·유레카·만족을 더하고, 여행 가방·커피·마이크·하트 풍선·한식 접시·리본·TOPIK 안경을 덱 태그에 연결한다. TOPIK은 안경과 졸업모를 문제마다 교대하고 응원봉 색은 덱 태그에서 결정한다. 1월 한복, 6월 우산, 7~8월 밀짚모자, 9월 송편, 12월 말 산타 모자, 부화 기념일 파티 모자를 기존 계절 레이어에 추가한다.
- 결정: 챕터 단계 외 성장축으로 누적 정타 자모의 볏 색·크기, 최장 스트릭의 몸집, 최근 7일 활동의 깃털 윤기, 온보딩에서 선택하고 옷장에서 다시 보는 알 무늬를 저장·표현한다. 이름표는 홈·결과·공유 카드에 사용하고 공유 카드에는 친구 병아리를 함께 그린다. 자주 틀린 자모는 영속 집계해 홈과 3연속 오타 상황에서 말풍선으로 안내한다.
- 결정: 병아리 아이들은 기존 7종에 하품·한 발 서기·뒤돌기·깃 고르기·제자리 돌기·두 번 발 구르기·3/4 걸음을 추가하고, 알은 일시적으로 금이 하나 더 가는 4번째 변주를 갖는다. 5콤보마다 리듬, 오타 놀람, 성장 모프, 백그라운드 복귀 스트레칭, 첫 온보딩 알 노크음, 복습 졸업 윙크, 개인 최고 콤보 놀람, 3회 연속 레슨 만족을 revision 기반 이벤트로 재생한다. 초성 게임은 현재 제품에 없으므로 게임을 새로 만들지 않고 `recordChoseongSolved(usedPass:)` 계약을 제공해 무패스 해결 시 유레카를 발행하게 한다.
- 근거: 캐릭터 시트의 시각 아이디어를 화면별 임시 분기로 흩뜨리지 않고 기존 커리큘럼·리텐션·덱 태그·복습·게임 기록이라는 실제 사용자 데이터에 연결해야 성장 보상이 누적된다. 새로운 모달이나 계정을 만들지 않고 UserDefaults와 기존 저장소 신호만 사용해 세션 무중단·오프라인 경계를 보존한다.
- 관련 PRD 섹션: F1, F4, F6, F7, F8, F9, F11, F12, §7.1~§7.4, §9, §11, §12, §13 M6
- 영향 범위: `ChickMascotView`, `MascotGrowthSystem`, 온보딩·연습·게임·결과·공유·옷장 화면, `HancoSoundEngine`, ja/en/ko 로컬라이제이션과 펫 시스템 테스트

## 2026-07-19 게임 플레이 펫 렌더링과 성능 경계
- 결정: 흐름 게임의 펫은 컨베이어 우측 아래에 배치하고 카드 진행 방향에 따라 시선과 좌우 3/4 포즈를 바꾼다. 정타·단어 완료·5배수 콤보·오타·3연속 오타·남은 10초·개인 최고 콤보·포그라운드 복귀를 각각 별도 표정/반응으로 연결한다. 펫은 카드·파티클의 60Hz `TimelineView` 바깥에서 기존 30Hz 게임 tick에 맞춰 시선만 갱신해 벡터 캐릭터가 파티클 프레임 예산을 불필요하게 점유하지 않게 한다.
- 근거: 게임 화면에서도 펫을 실제 플레이 신호와 연결하되 PRD의 파티클 포함 60fps 게이트를 약화시키지 않아야 한다. 화면을 가리지 않는 작은 오버레이와 접근성 식별자를 사용하면 기존 카드 판정·키보드·결과 자동 진입을 유지한 채 UI 회귀 테스트가 가능하다.
- 관련 PRD 섹션: F6, F11, F12, §7.1, §11.1, §12
- 영향 범위: `FlowGameViewModel`, `FlowGameView`, 게임 FPS 게이트, 게임 성공 흐름 UI 테스트

## 2026-07-20 제품명 PIYOKEY 확정과 Swift 도형 앱 아이콘
- 결정: 사용자 노출 제품명은 일본어 `ピヨキー`, 영어 `PIYOKEY`, 한국어 `피요키`로 확정한다. 홈 화면 표시는 `InfoPlist.strings`로 기기 언어에 맞춰 현지화하고 온보딩·설정·공유 이미지·공유 캡션·공식 덱 작성자도 같은 브랜드로 통일한다. 기존 설치 데이터, 테스트 명령, 모듈 의존성과 경로를 깨지 않도록 Xcode 프로젝트·타깃·scheme·bundle identifier와 Application Support 경로의 내부 코드명 `Hanco`는 유지한다.
- 결정: 앱 아이콘은 새로운 이미지형 캐릭터를 만들지 않고 런타임 `MascotView`의 타원형 몸·점 눈·다이아몬드 부리·노랑/주황 팔레트를 축약한 `PiyokeyLogoMark`와 `ㅎ` 키캡으로 구성한다. 같은 SwiftUI 도형을 공유 카드에서도 쓰며 `tools/render_piyokey_app_icon.swift`가 1024×1024 불투명 PNG를 재현 가능하게 생성한다. 외부 생성형 병아리 이미지는 제품 에셋에 포함하지 않는다.
- 결정: 캐릭터의 미설정 기본 애칭은 일본어 `ピヨちゃん`, 영어 `Piyo`, 한국어 `피요`로 표시한다. UserDefaults의 사용자 입력 이름이 비어 있을 때만 기본값을 쓰므로 기존 이름과 직접 이름 짓기·변경 기능은 그대로 보존한다.
- 근거: `ピヨ`의 병아리 울음과 `キー`의 키보드 의미를 한 번에 전달하면서 기존 성장 펫의 시각 정체성을 홈 화면과 공유 결과까지 일치시켜야 한다. 내부 식별자를 함께 바꾸면 설치 앱과 로컬 진행 데이터가 별도 앱처럼 분리될 수 있으므로 표시 브랜드와 기술 식별자의 경계를 둔다.
- 관련 PRD 섹션: F1, F8, F9, F10, §7.1, §12, §14
- 영향 범위: `Info.plist`, `InfoPlist.strings`, `Localizable.strings`, `PiyokeyLogoMark`, `AppIcon.appiconset`, 공유 카드, 목 카탈로그 공식 작성자, PRD·프로젝트 상태 문서

## 2026-07-20 AppIcon 출력 검증과 병아리 모션 경계
- 결정: AppIcon 생성은 SwiftUI `ImageRenderer`의 CGImage를 sRGB 불투명 Core Graphics 컨텍스트에 직접 합성하고, 생성 직후 내부 표본색이 검정이면 실패시킨다. Asset Catalog 원본뿐 아니라 Simulator와 실기기 빌드에 컴파일된 120px 아이콘도 디코딩해 색상을 확인한다.
- 결정: 병아리의 idle·입력 반응은 계속 재생하되 캐릭터를 완전히 사라지게 하던 180° 뒤돌기는 최대 ±24°의 3/4 시선으로 바꾼다. 모든 좌우·상하 오프셋과 반응 배율, 졸업모 이동은 캐릭터 배치 크기에 비례한 공통 `MascotMotionPolicy` 경계로 제한한다. 화면별로 캐릭터를 잘라 내는 클리핑은 말풍선·이름표·소품까지 훼손하므로 사용하지 않는다.
- 근거: 검정 아이콘은 렌더 원본이 아닌 불투명 PNG 변환 단계의 색상 손실이었고, 뒤돌기의 90° 중간 프레임은 3D 투영 폭이 0이 되어 캐릭터가 화면을 벗어난 것처럼 보였다. 렌더 산출물 검증과 공통 모션 경계로 원인을 직접 제거하면서 점프·끄덕임·날갯짓의 생동감은 유지한다.
- 관련 PRD 섹션: F6, F9, §7.1~§7.3, §12, §14
- 영향 범위: `render_piyokey_app_icon.swift`, `AppIcon.appiconset`, `ChickMascotView`, `MascotGrowthSystemTests`, M6 실기기·UI 회귀 검증

## 2026-07-20 게임 선택 카드의 정적 브랜드 마크 경계
- 결정: 게임 선택 소개 카드는 입력 이벤트가 없는 세션 전 화면이므로 `GrowingMascotView`를 제거하고, 같은 Swift 도형의 정적 `PiyokeyLogoMark`를 고정 슬롯에 표시한다. 소개 카드에는 라운드 사각형 클리핑을 적용하고 UI 테스트에서 마크 프레임이 카드 프레임에 포함되며 동적 `mascot.current`가 존재하지 않는지 확인한다. 실제 연습·게임 플레이·홈의 성장 및 이벤트 반응형 병아리는 유지한다.
- 근거: 이 화면에서 SwiftUI가 보고하는 논리 프레임은 카드 안이었지만 동적 도형의 변환 렌더 레이어만 다음 섹션으로 내려가 그려져 좌표·모션 제한 테스트로는 검출되지 않았다. 반응이 필요 없는 진입 화면을 정적 브랜드 마크로 분리하면 프레임 이탈을 구조적으로 차단하고 60Hz 게임 플레이의 펫 반응은 손상하지 않는다.
- 관련 PRD 섹션: F6, §7.1, §7.3, §11.1, §14
- 영향 범위: `GameDeckSelectionView`, `PiyokeyLogoMark`, 게임 선택 UI 회귀 테스트, M6 실기기 검증

## 2026-07-20 3D 병아리 AppIcon 채택
- 결정: 홈 화면 AppIcon은 사용자가 직접 지정한 3D 병아리와 `ㅎ` 키캡 정사각형 이미지를 사용한다. 원본 1254×1254 무알파 PNG는 `shared/brand/piyokey_app_icon_3d_source.png`에 공용 브랜드 소스로 보관하고, `render_piyokey_app_icon.swift`가 종횡비를 유지한 고품질 aspect-fill로 sRGB 1024×1024 무알파 PNG를 생성한다. 검정 출력 표본 가드는 유지한다.
- 결정: 이 결정은 같은 날짜의 “Swift 도형 앱 아이콘” 결정을 AppIcon에 한해 대체한다. `PiyokeyLogoMark`는 게임 선택 카드·공유 카드 등 앱 내부의 작은 경량 마크로 계속 사용하고, 성장 시스템과 이벤트 반응형 런타임 병아리도 이번 변경 범위에 포함하지 않는다. 제안했던 Swift 도형 병아리 3안은 모두 미채택으로 기록한다.
- 근거: 사용자가 3D 원본의 표정·입체감·매력도를 최종 AppIcon 방향으로 명시했고, 120px로 축소된 Asset Catalog 결과에서도 큰 눈·노란 실루엣·`ㅎ` 키캡이 명확하게 식별된다. 공용 원본과 정규화 도구를 분리하면 iOS와 향후 Android에서 같은 브랜드 이미지를 재현할 수 있다.
- 관련 PRD 섹션: §7.1, §12, §14
- 영향 범위: `shared/brand/`, `render_piyokey_app_icon.swift`, `AppIcon.appiconset`, iOS·향후 Android 런처 아이콘, M6 브랜드 검증 문서

## 2026-07-20 게임·설정 탭의 직접 콘텐츠 진입
- 결정: 게임 탭 상단의 모드 설명·병아리 브랜드 카드와 설정 탭 상단의 병아리·설명 카드를 모두 제거한다. 게임 탭은 설치 덱 제목/목록 또는 빈 상태 카드로 바로 시작하고, 설정 탭은 글자 크기·테마·언어가 있는 표시 설정 카드로 바로 시작한다. 제거된 안내 전용 문자열 6개는 ja/en/ko에서 함께 삭제한다.
- 근거: 두 탭 모두 내비게이션 제목이 화면의 목적을 이미 설명하고, 반복 방문 화면에서 큰 소개 카드는 핵심 기능까지의 거리와 스크롤 양만 늘린다. F6의 덱 선택과 F10의 즉시 설정 기능을 첫 화면에 노출하면 정보 밀도가 좋아지고 병아리 렌더 영역의 불필요한 시각 소음도 없어진다.
- 관련 PRD 섹션: F6, F10, §7.1
- 영향 범위: `GameDeckSelectionView`, `SettingsView`, ja/en/ko `Localizable.strings`, 로컬라이제이션 단위 테스트, 게임·설정 UI 회귀 테스트

## 2026-07-20 홈·연습 분리와 공통 설정 진입
- 결정: 기존 첫 번째 `Learn/練習/연습` 탭을 `Home/ホーム/홈`으로 바꾸고 홈에는 7일 스탬프 카드, 하루 3분 데일리 챌린지, 사용자 추천 덱 3개만 둔다. 기존 커리큘럼 챕터와 자유 연습은 세 번째의 별도 `Practice/練習/연습` 탭으로 이동한다. 커리큘럼 상단의 병아리와 `한글 키보드 코스 / 스탬프를 모아 봐요` 소개 카드는 삭제한다.
- 결정: 하단 설정 탭을 제거해 `홈 | 둘러보기 | 연습 | 내 덱 | 게임`의 5개 콘텐츠 탭으로 구성한다. 모든 탭 우측 상단의 32pt 기어 버튼은 동일한 설정 시트를 열며, 설정 시트에는 홈에서 옮긴 연습 알림과 기존 표시·키보드·사운드 설정을 둔다. 삭제된 커리큘럼 카드가 제공하던 이름·성장·아이템 관리는 설정의 `피요 설정`에서 계속 접근한다.
- 근거: 홈의 역할을 반복 방문용 오늘의 루프에 집중시키고, 단계형 학습은 별도 연습 목적지로 분리하면 첫 화면의 우선순위가 명확해진다. 설정은 사용 빈도에 비해 하단 한 자리를 점유할 필요가 없지만 어느 콘텐츠에서도 일관되게 접근할 수 있어야 하며, 공통 시트는 언어 변경으로 탭 구조가 다시 그려져도 설정 맥락을 유지한다.
- 관련 PRD 섹션: §4 S2·S4·S9, F5.6, F8, F10
- 영향 범위: `AppRootView`, `CurriculumMapView`, `RetentionHomeView`, `SettingsView`, ja/en/ko 로컬라이제이션, 홈·커리큘럼·설정 UI 회귀 테스트

## 2026-07-20 수동 옷장 선택의 계절 장식 우선순위
- 결정: 밀짚모자·산타 모자 등 달력 기반 계절 장식은 옷장 선택이 `자동`일 때만 표시한다. 사용자가 `없음`이나 응원봉·졸업모·헤드폰 등의 고정 아이템을 직접 선택하면 계절 장식 레이어를 숨기고 수동 선택만 렌더링한다. 옷장의 자동 문구에는 세 언어 모두 `계절 아이템 포함`을 명시한다.
- 근거: 기존 구현은 수동 소품과 계절 장식을 독립 레이어로 항상 합성해 7~8월에는 어떤 옷을 골라도 밀짚모자가 남았다. `자동 / 없음 / 고정`이라는 기존 선택 모델의 의미에 맞게 수동 선택을 최우선으로 두면 사용자의 명시적인 커스터마이징이 날짜 규칙에 덮이지 않는다.
- 관련 PRD 섹션: §7.1, F10
- 영향 범위: `MascotCompanionLibrary`, `GrowingMascotView`, 옷장 ja/en/ko 로컬라이제이션, `MascotGrowthSystemTests`

## 2026-07-20 게임 시작 카운트다운과 준비 상태
- 결정: 흐름 모드는 최초 시작과 재도전 모두 `ready → 3·2·1 → running` 순서로 전환하며, 제한시간·카드 이동·플레이 이력은 `running` 진입 뒤에만 시작한다. 준비 중 백그라운드로 가면 카운트다운을 취소하고 복귀 시 처음부터 다시 시작한다. Reduce Motion에서는 짧은 `1` 표시만 사용한다.
- 근거: 덱을 선택하자마자 제한시간이 줄어들면 사용자가 화면과 키보드를 파악하는 시간이 기록 손실로 이어진다. 카운트다운을 View가 소유하고 규칙 모델의 `ready` 상태를 유지하면 타이머 테스트가 결정적이고 백그라운드 일시정지 원칙도 보존된다.
- 관련 PRD 섹션: F6, §7.3, §11.1, §12
- 영향 범위: `FlowGameView`, `FlowGameViewModel`, 게임 회귀 단위/UI 테스트

## 2026-07-20 추천·라이브러리·설정의 반복 사용성 보완
- 결정: 홈 추천은 다운로드 이력이 없을 때 온보딩 목표 태그를 반영하고, 이력이 생기면 기존처럼 실제 다운로드 태그에 더 높은 가중치를 둔다. 내 덱 빈 상태는 발견 탭으로 바로 이동하는 CTA를 제공하고 설치 덱은 최근 사용·이름·설치일로 정렬할 수 있게 한다. 공통 설정 기어는 32pt 외형을 유지하되 탭 영역은 44×44pt로 확장한다.
- 근거: 첫 사용자의 목표 선택이 홈에서 즉시 이어져야 개인화가 체감되고, 빈 라이브러리는 막다른 화면이 아니라 다음 행동을 안내해야 한다. 작은 기어의 시각 밀도는 유지하면서 모바일 접근성 탭 면적을 보장할 수 있다.
- 관련 PRD 섹션: F1, F5.6, §4 S8·S9, §12
- 영향 범위: `HomeRecommendationsView`, `MyDecksView`, `AppRootView`, ja/en/ko 로컬라이제이션, UI 회귀 테스트

## 2026-07-20 옷장 최종 미리보기와 영구 해금
- 결정: 옷장 상단에 현재 성장·알 무늬·선택 소품·자동 계절 장식을 합친 최종 모습 미리보기를 둔다. 조건을 한 번 만족한 응원봉·졸업모·헤드폰은 별도 해금 집합에 저장하고, 이후 스트릭 단절·설치 덱 삭제·재실행 뒤에도 다시 잠그지 않는다. 기존 사용자에게는 현재 장착 아이템을 해금 집합으로 이관한다.
- 근거: 보상 아이템이 생활 데이터 감소로 사라지면 성장 보상보다 임시 상태처럼 느껴지고, 목록의 작은 아이콘만으로는 최종 조합을 예측하기 어렵다. 단조 증가하는 해금 기록과 실제 렌더러를 재사용한 미리보기가 수집·꾸미기 경험을 안정시킨다.
- 관련 PRD 섹션: §7.1, F8, F10
- 영향 범위: `MascotCompanionLibrary`, `MascotClosetView`, `GrowingMascotView`, `MascotGrowthSystemTests`

## 2026-07-20 로컬 JSON 최신 백업과 손상 격리 복구
- 결정: 설치 덱·카탈로그 캐시·게임 기록·커리큘럼·복습·스트릭 JSON은 저장 성공 때 같은 디렉터리에 최신 `.backup`을 유지한다. 기존 데이터는 첫 정상 로드 때 백업이 없으면 즉시 생성한다. 주 파일이 디코딩 또는 의미 검증에 실패하면 원본을 `.corrupt`로 격리하고 검증된 백업을 주 파일로 복원한다. 백업까지 손상됐으면 둘 다 격리해 다음 사용자 동작부터 새 저장을 허용한다. 앱보다 높은 스키마는 복구 대상으로 보지 않고 원본과 백업을 그대로 보존한다.
- 결정: 설치 덱은 인덱스와 각 덱 파일을 각각 백업한다. 개별 덱의 주 파일과 백업이 모두 복구 불가능하면 해당 덱만 설치 목록에서 제외하고, 나머지 덱과 삭제 후에도 쓰이는 다운로드 태그 이력은 유지한다. 온보딩의 UserDefaults JSON도 동일하게 최신 백업과 손상 증거를 유지한다.
- 근거: 기존 `.atomic` 쓰기는 중간 파일 노출은 막지만 디스크·동기화·수동 변조로 이미 저장된 JSON이 깨지는 경우를 복구하지 못했다. 작은 로컬 JSON을 한 세대 중복 저장하면 계정·서버 없이도 사용자의 학습 기록과 오프라인 덱을 보호할 수 있고, 미래 스키마를 건드리지 않으면 앱 다운그레이드 데이터 손상도 막을 수 있다.
- 관련 PRD 섹션: F4, F5.5, F7, F8, §9, §12
- 영향 범위: `RecoverableJSONFile`, `DeckInstallationStore`, `CatalogCacheStore`, `GameProgressStore`, `CurriculumProgressStore`, `ReviewDeckStore`, `RetentionStore`, `OnboardingStore`와 저장소 회귀 테스트

## 2026-07-20 게임·마이덱 GNB 순서와 초성 맞추기 규칙
- 결정: 하단 콘텐츠 GNB의 네 번째·다섯 번째 위치를 `ゲーム | マイデッキ` 순서로 바꾼다. 게임 탭 첫 화면에는 큰 소개 카드 대신 `フローモード | 初声クイズ`의 작은 모드 선택기를 두고, 같은 설치 덱 목록을 두 모드가 공유한다.
- 결정: 초성 맞추기는 설치 덱에서 결정적 시드로 최대 10문제를 만들고, 문제 초성과 겹치지 않는 오답 3개를 포함한 4지선다로 제공한다. 서로 다른 초성의 보기 4개를 만들 수 없는 덱은 충돌을 일으키지 않고 이유와 함께 비활성화한다. 선택 즉시 정오답·피요 반응·사운드·햅틱을 보여 준 뒤 0.65초 후 자동 전환하며 마지막 문제도 공통 결과 화면으로 자동 진입한다.
- 결정: 정답 점수는 기본 100 + 5초 이내 속도 보너스 + 최대 10단계 콤보 보너스이고 뜻 힌트 사용 시 30점을 차감한다. 오답은 복습 덱에 수집하고 정답은 졸업 진행에 반영한다. 초성 최고점은 `DeckProgress.game_kind = choseong`으로 흐름 모드의 내장/OS 최고점과 분리하되, 기존 schema v1 데이터는 `flow`로 무손실 해석한다.
- 근거: 게임을 마이덱보다 앞에 두면 반복 플레이 목적지가 더 빠르게 보이며, 이미 설치한 팬덤·생활 덱을 타이핑 외 인지 퀴즈에도 재사용할 수 있다. 초성이 같은 복수 정답과 소형 덱 충돌을 구조적으로 막고 기존 결과·복습·리텐션 시스템을 공유하면 새 게임의 규칙과 학습 가치가 일관된다.
- 관련 PRD 섹션: §4 S7·S8, F6, F6a, F7, F8, F9, F12, §11.1
- 영향 범위: `AppRootView`, `GameDeckSelectionView`, `ChoseongQuizView`, `FlowGameResultView`, `GameProgressStore`, ja/en/ko 로컬라이제이션, 단위·UI 회귀 테스트

## 2026-07-20 게임 허브와 모드별 덱 목록의 단계 분리
- 결정: 게임 탭 첫 화면에서 모드 선택기와 덱 목록을 함께 보여 주던 구성을 폐기하고, 게임 이름·아이콘·규칙 설명·`デッキを選ぶ` CTA를 담은 2열×2행 확장형 카드 그리드만 표시한다. 현재는 `フローモード`와 `初声クイズ` 두 카드를 제공하며, 새 게임은 같은 그리드의 다음 셀에 추가한다.
- 결정: 게임 카드를 탭하면 해당 게임 제목을 가진 별도 덱 목록 화면으로 이동한다. 설치 덱·모드 적합성·모드별 최고 기록·덱 더 찾기/빈 상태는 이 두 번째 화면에서만 노출하고, 결과의 완료 버튼은 게임 허브가 아니라 방금 선택한 게임의 덱 목록으로 돌아간다.
- 근거: 게임 종류와 학습 덱은 서로 다른 선택 축이다. 종류를 먼저 설명한 뒤 덱을 고르게 하면 게임 탭의 첫 인상이 콘텐츠 라이브러리가 아니라 플레이 선택지가 되고, 향후 세 번째·네 번째 게임도 덱 목록 구조를 복제하지 않고 자연스럽게 확장할 수 있다.
- 관련 PRD 섹션: §4 S7, F6, F6a, §7.1, §11.1
- 영향 범위: `GameDeckSelectionView`, 게임 탐색 UI 회귀 테스트, ja/en/ko 로컬라이제이션

## 2026-07-20 게임 허브 2×2를 네 개의 실제 게임으로 완성
- 결정: 게임 허브의 네 셀을 `flow`, `acid_rain`, `choseong`, `word_match`로 고정한다. 산성비는 흐름 모드의 자모 판정·점수·입력·결과 구조를 공유하되 카드를 상단에서 하단 위험선으로 낙하시키고, 단어 맞추기는 일본어 뜻을 보고 한국어 보기 4개 중 고르는 10문제 선택형 게임으로 구현한다.
- 결정: 산성비와 단어 맞추기의 최고 기록은 기존 흐름·초성 기록과 각각 분리한다. 단어 맞추기는 뜻이 있는 고유 한국어 단어 4개 이상인 덱만 허용한다.
- 근거: 첫 화면을 2×2로 구성하면서 빈 자리나 준비 중 버튼을 두기보다 타이핑·초성·어휘 회상을 서로 다른 조작 감각으로 제공하는 편이 게임 탭의 반복 플레이 가치를 높인다. 기존 순수 한글 엔진과 공통 결과 구조를 재사용하면 판정 일관성과 데이터 호환성을 유지할 수 있다.
- 관련 PRD 섹션: §4 S7, §5 F6/F6a/F6b/F6c
- 영향 범위: `GameDeckSelectionView`, `FlowGameView`, `ChoseongQuizView`, `FlowGameResultView`, `GameProgressStore`, ja/en/ko 로컬라이제이션, 단위·UI 회귀 테스트

## 2026-07-20 초성 맞추기의 직접 타이핑 전환
- 결정: 초성 맞추기의 4지선다 보기를 폐기하고, 초성만 본 사용자가 정답 한국어 전체를 직접 입력하는 게임으로 바꾼다. 내장 두벌식과 OS 한국어 IME를 모두 지원하며 정오 판정·도깨비 이월·백스페이스는 `HangulEngine`의 자모 시퀀스 상태를 사용한다. 정답을 노출할 수 있는 다음 키 가이드는 이 모드에서 항상 숨긴다.
- 결정: 오타 자모는 조합 문자열에 넣지 않고 정확도와 콤보에만 반영한 뒤 같은 문제를 계속 풀게 한다. 마지막 자모 완성 시 점수·피요 반응·사운드·햅틱을 발생시키고 0.65초 뒤 자동 전환한다. 뜻 힌트와 30점 차감, 복습 수집, 노미스 졸업, 공통 결과·재도전은 유지하며 최고 기록은 `choseong` 안에서도 내장/OS 입력별로 분리한다.
- 결정: 이제 보기가 필요 없으므로 서로 다른 초성 단어 4개 제한을 제거한다. 초성을 추출할 수 있고 조합 엔진으로 분해 가능한 한국어 항목이 하나라도 있으면 해당 덱으로 시작할 수 있다. 같은 덱에 동일 초성 단어가 둘 이상이면 그 문제에는 일본어 뜻을 자동 표시하고 힌트 감점을 적용하지 않아 복수 정답의 불공정성을 없앤다. 단어 맞추기는 일본어 뜻 기반 4지선다라는 별도 조작 정체성을 그대로 유지한다.
- 근거: 초성 회상 뒤 실제 두벌식 키 입력까지 이어져야 피요키의 핵심인 한국어 타이핑 훈련으로 전이된다. 보기 선택은 정답 인지는 측정하지만 키 위치·자모 순서·조합을 연습하지 못하며, 다음 키를 알려 주는 가이드 역시 회상 게임의 답을 노출한다.
- 관련 PRD 섹션: F2, F2a, F6a, F7, F12, §6, §11.1
- 영향 범위: `ChoseongQuizView`, `GameDeckSelectionView`, `FlowGameResultView`, `GameProgressStore`, ja/en/ko 로컬라이제이션, 초성 단위·UI 회귀 테스트

## 2026-07-20 마이덱을 마이페이지 개인 허브로 확장
- 결정: 하단 다섯 번째 탭의 이름과 아이콘을 `マイデッキ`에서 `マイページ`와 프로필 아이콘으로 바꾼다. 마이페이지는 피요 프로필, 현재 성장 단계, 완료 챕터·누적 입력 자모·현재 스트릭·최근 7일 활동의 성장 기록, 앱 설정과 피요 설정 바로가기를 먼저 보여 주고 기존 설치 덱·복습 덱·정렬·삭제 기능을 하단 `マイデッキ` 영역에 그대로 둔다.
- 결정: 설치 덱과 복습 항목이 모두 없는 첫 사용자에게도 성장 기록과 설정은 항상 노출한다. 공통 우측 상단 기어는 다른 탭과 동일하게 유지하며 마이페이지 본문의 앱 설정 행도 같은 설정 시트를 열어 접근 경로만 추가한다.
- 근거: 덱 보관함만으로는 캐릭터 성장과 개인 학습 기록을 다시 확인할 고정 목적지가 부족하다. 기존 라이브러리를 잃지 않고 개인 데이터와 설정을 한곳에 모으면 다섯 번째 탭의 정체성이 반복 방문 가능한 사용자 허브로 확장된다.
- 관련 PRD 섹션: §4 S8·S9, F7, F8, F10, §7.1
- 영향 범위: `AppRootView`, `MyPageView`, ja/en/ko 로컬라이제이션, 마이페이지 UI 회귀 테스트

## 2026-07-20 마이페이지 타이핑 분석과 공통 세션 이력
- 결정: 마이페이지에 7일/30일 전환형 `タイピング分析` 카드를 추가한다. 기간별 활동 막대, 활동일·완료 세션·완료 문제·평균 자모 정확도·시간 가중 평균 분당 자모 수, 직전 동일 기간 대비 변화, 복습 덱의 활성·추가·졸업 수를 로컬 데이터만으로 집계한다. 데이터가 없으면 임의 표본이나 백분위를 만들지 않고 빈 상태를 표시한다.
- 결정: 게임뿐 아니라 커리큘럼·자유 연습·덱 연습·복습·데일리 챌린지 완료도 기존 `GameRecord.mode = lesson`으로 저장한다. 레슨 이력은 게임 모드별 `DeckProgress` 최고 기록을 갱신하지 않으며 기존 schema v1 파일과 과거 게임 기록은 마이그레이션 없이 유지한다.
- 결정: 취약 자모는 현재 저장된 자모별 누적 오타 횟수 상위 5개를 표시한다. 기존 오타 카운터에는 발생 시각이 없으므로 7일/30일 선택과 무관한 누적 지표로 명시적으로 취급하고, 시점별 자모 개선 추이는 향후 이벤트 이력 스키마를 도입한 뒤 제공한다.
- 근거: 서버와 추적 SDK 없이도 사용자가 연습량·정확도·속도·복습 개선을 확인할 수 있어야 하며, 앞으로의 개인화 `毎日3分`이 같은 이력을 입력으로 재사용해야 한다. 과거 데이터에 없는 세부 값을 추정하지 않으면 분석의 신뢰도를 지킬 수 있다.
- 관련 PRD 섹션: §4 S8, F4, F7, F8, §9, §10, §11
- 영향 범위: `GameProgressStore`, `PracticeView`, `MyPageView`, ja/en/ko 로컬라이제이션, 기록·마이페이지 단위/UI 회귀 테스트

## 2026-07-20 연습 결과 완료의 순차 내비게이션 종료
- 결정: 연습 결과의 `完了`는 부모가 결과 목적지 바인딩을 먼저 해제하고, pop 애니메이션이 완전히 끝난 650ms 뒤 연습 목적지를 닫는 두 단계로 처리한다. 결과 공개 애니메이션 중에도 같은 경로를 사용하며 재시도는 결과만 닫아 현재 연습을 유지한다.
- 근거: SwiftUI `navigationDestination(isPresented:)`를 해제한 직후 상위 `dismiss`를 호출하면 아직 전환 중인 결과 목적지에 종료가 소비될 수 있다. 또한 완료 상태의 연습 화면이 다시 나타날 때 자동 결과 진입 타이머를 재예약하면 상위 종료와 결과 재표시가 경합한다. 종료 의도 동안 자동 진입을 차단하고 결과 pop의 실제 애니메이션 시간보다 긴 간격을 둬 두 전환을 분리한다.
- 관련 PRD 섹션: F12.5, §12
- 영향 범위: `PracticeView`, 결과 완료·성장 축하 UI 회귀 테스트

## 2026-07-20 App Store 제출 사전 게이트와 개인정보 매니페스트
- 결정: 앱의 `PrivacyInfo.xcprivacy`는 추적·수집 데이터 없음과 `UserDefaults / CA92.1`, `activeInputModes / 54BD.1`만 선언한다. `UserDefaults`는 앱 자체 설정·성장 상태에만 쓰고, 활성 키보드 목록은 한국어 OS IME 사용 가능 여부에 따라 사용자에게 다른 안내 UI를 보여 주는 데만 쓰며 두 정보 모두 외부로 전송하지 않는다. 자체 암호화는 구현하지 않고 Apple SDK의 HTTPS만 사용하므로 `ITSAppUsesNonExemptEncryption = false`를 선언한다.
- 결정: 소스·번들 구조는 `tools/release_preflight.py`, 수동 심사·실기기·메타데이터는 `release/app_store_submission.json`으로 분리해 관리한다. 최종 Bundle ID·버전, Distribution archive, 개인정보/지원 URL, 콘텐츠 권리, 스토어 스크린샷, TestFlight, 한국어 IME·TTS·외부 음악·iPhone 12 게이트 중 하나라도 열려 있으면 `--strict`가 실패하고 제출 불가로 취급한다.
- 근거: 기능 테스트 통과만으로 App Store 정책·서명·권리·메타데이터 누락을 발견할 수 없고, 개발 archive가 생성됐다는 사실을 Distribution 검증으로 오인해서도 안 된다. 코드에서 확정 가능한 선언은 자동 회귀로 고정하고 계정/법무/사람 검수가 필요한 항목은 명시적 false 상태에서만 닫히게 해야 동일 RC의 출시 근거가 남는다.
- 관련 PRD 섹션: §10, §12, §13 M6
- 영향 범위: `PrivacyInfo.xcprivacy`, `Info.plist`, `AppSettingsTests`, `release/`, `tools/release_preflight.py`, App Store RC 검증 절차

## 2026-07-20 긴 연습 문장의 현재 입력 위치 자동 추적
- 결정: 연습 화면의 목표 음절과 자모 시퀀스는 한 줄 가로 트랙을 유지하되, 입력 진행에 맞춰 현재 음절·자모를 중앙으로 자동 스크롤한다. 실제 콘텐츠 너비가 화면을 넘을 때만 남은 방향에 가장자리 페이드를 표시하고, 자모 제목에는 완료 입력 수와 전체 입력 수를 함께 표시한다. 새 문제에서는 두 트랙 모두 첫 위치로 복귀한다.
- 근거: 모든 자모를 여러 줄로 펼치면 긴 문장에서 목표 카드가 커져 시그니처 조합 프리뷰와 고정 키보드를 밀어내며, 글자나 칩 축소는 작은 글자에 대한 경쟁 앱 불만을 반복한다. 현재 위치를 따라가는 한 줄 트랙은 글자 크기와 화면 구조를 유지하면서 다음 입력과 주변 맥락을 계속 노출한다.
- 관련 PRD 섹션: F2, F4, §7.2, §12
- 영향 범위: `PracticeView`, 긴 문장 자모 진행 UI 회귀 테스트

## 2026-07-20 출시판 공식 학습 콘텐츠와 원탭 학습 전환
- 결정: 출시 카탈로그에서 아티스트·아이돌 고유명사와 가상 커뮤니티 덱을 제거하고, 키보드 입문·자음·모음·조합·받침·기초 단어·동사/형용사·일상·여행·TOPIK·요즘 한국어의 공식 덱 11개만 번들한다. 실제 집계가 없는 다운로드 수는 0으로 두고 카드에서 숨기며 `急上昇`·`人気` 대신 `まずはここから`·`今どき韓国語`·`目的から選ぶ`·`公式コンテンツ`로 탐색 섹션을 구성한다.
- 결정: 첫 진입 신뢰 문구는 `표준 두벌식 배열과 한글 조합 원리 기반`, `계정 없음·기기 내 저장`처럼 코드와 운영으로 검증 가능한 사실만 사용한다. 외부 기관명은 실제 감수·제휴와 표시 허가가 생기기 전까지 사용하지 않는다.
- 결정: 홈 첫 CTA는 진행 중 커리큘럼 → 최근 플레이 덱 → 온보딩 목적 기반 데일리 순으로 하나만 노출한다. 최근 7일 3/5/7회 출석은 응원봉·리본·헤드폰을 영구 해금하며 스탬프 상세에서 조건과 상태를 확인한다.
- 결정: 타이핑 화면의 상시 내장/OS 전환 바를 제거하고 우측 상단 세션 설정으로 이동한다. 전체 게이지, 키보드 위 1회성 힌트, 학습/집중 표시 프리셋, 목표·뜻·읽기·자동 발음·피요 표시 설정을 제공한다. 임의 순서 드래그 편집은 MVP에서 제외한다.
- 근거: 출시 직전 검수되지 않은 팬덤 콘텐츠와 가상 인기 수치는 신뢰를 약화시키고 콘텐츠 권리 리스크를 만든다. 사용자는 탐색보다 즉시 연습 진입과 학습 이유에 맞는 설명을 먼저 필요로 하며, 작은 화면에서는 상시 제어 바보다 문제·힌트·키보드의 위계가 중요하다.
- 관련 PRD 섹션: §1.3, §4, F1, F2a, F5, F8, F10, §8
- 영향 범위: `PRD.md`, 공식 카탈로그 생성기·fixture, `OnboardingView`, `HomeView`, `RetentionHomeView`, `DiscoverView`, `PracticeView`, `SettingsView`, ja/en/ko 로컬라이제이션과 관련 단위·UI 테스트

## 2026-07-20 타이핑 화면의 세로 공간 우선 재배치
- 결정: 연습 화면의 내비게이션 제목을 없애고 닫기·전체 진행 게이지/문제 수·설정을 같은 상단 행에 둔다. 듣기 버튼과 목표·뜻·읽기·자모는 하나의 문제 카드에 유지하며, 조합 카드의 `組み立てプレビュー` 제목과 조합 카드 아래의 피드백·오타·개발용 지표·힌트 버튼은 화면에서 제거한다. 오타와 입력 지연 측정값은 회귀 검증용 비가시 접근성 노드로만 유지한다.
- 결정: OS 키보드는 조합 카드에 투명 입력 필드를 겹쳐 자동 포커스를 유지하고, 기존의 `端末の韓国語キーボードで入力` 제목·실전 모드 배지·별도 입력 상자·안내 문구를 표시하지 않는다. 내장/OS 모두 조합 카드 다음에 바로 키보드가 오도록 한다.
- 근거: 문제 메타데이터가 늘어난 상태에서 내장 키보드 위 보조 행과 OS IME 입력 패널이 피요·조합 프리뷰를 화면 밖으로 밀어냈다. 진행 정보와 입력 캡처를 기존 영역에 합치면 학습 정보는 유지하면서 시그니처 캐릭터 피드백을 두 입력 방식에서 동일하게 노출할 수 있다.
- 관련 PRD 섹션: §4 S5, F2, F2a, F10, §7.2
- 영향 범위: `PracticeView`, `OSIMEInputView`, 타이핑 내장/OS UI 회귀 테스트

## 2026-07-20 타이핑 문제 정보의 순서·노출 사용자화
- 결정: 목표·일본어 뜻·일본어식 읽기는 6가지 순열 중 하나를 사용자가 고르고 영속 저장한다. 목표는 큰 굵은 진행형 음절, 뜻과 읽기는 라벨·배경 없이 작은 일반 굵기의 회색 텍스트라는 의미별 위계를 유지한다. 기본 순서는 목표→뜻→읽기다.
- 결정: 목표·뜻·읽기뿐 아니라 자모 트랙·피요·조합/입력 중 키도 각각 ON/OFF 한다. 최초 기본은 목표·뜻·자모·피요·조합/입력 중 키 ON, 읽기 OFF이며 피요와 조합을 모두 끄면 두 번째 카드를 제거한다. OS 입력 필드는 해당 카드 노출과 무관하게 입력 동기화를 유지한다.
- 근거: 같은 연습 데이터를 정답 노출형 학습, 일본어 뜻 회상, 일본어식 발음 회상으로 재사용하려면 문제 단서의 순서와 노출을 독립적으로 제어해야 한다. 동시에 기본 정보량을 줄여 작은 화면과 OS 키보드에서도 캐릭터·입력 피드백 공간을 확보해야 한다.
- 관련 PRD 섹션: §4 S5, F2, F2a, F10, §7.2
- 영향 범위: `AppSettings`, `SettingsView`, `PracticeView`, 세션 설정, ja/en/ko 로컬라이제이션과 관련 단위·UI 테스트

## 2026-07-20 자모 트랙의 가시 헤더 제거
- 결정: 타이핑 문제 카드의 자모 트랙에서 `입력할 자모` 제목과 `0/9` 형태의 완료 카운터를 제거하고 자모 칩만 표시한다. 완료 수치는 기존 회귀 테스트와 VoiceOver를 위한 비가시 접근성 값으로 유지한다.
- 근거: 완료 칩의 색상과 현재 칩의 테두리가 이미 진행 상태를 전달하므로 같은 정보를 제목·숫자로 반복할 필요가 없고, 한 줄을 제거하면 피요와 입력 영역에 사용할 세로 공간이 늘어난다.
- 관련 PRD 섹션: §4 S5, F2, F10, §7.2
- 영향 범위: `PracticeView`, 타이핑 기본 화면 UI 회귀 테스트

## 2026-07-20 공식 덱 사전 생성 음성 파일럿과 직역형 뜻
- 결정: 공식 시드 11덱과 업데이트 fixture의 한국어 문구는 SHA-256 기반 콘텐츠 주소로 같은 문구의 음성을 공유한다. 현재 파일럿은 외부 전송 없이 macOS 로컬 `Yuna` 음성을 22.05kHz μ-law CAF로 생성해 앱 번들에서 우선 재생하고, 경로 누락·손상·재생 실패 시 기존 `AVSpeechSynthesizer(ko-KR)`로 대체한다. 파일럿 113개 고유 음원은 총 1.91MiB이며 출시 전 공식 지원 TTS 제공자 음원으로 교체한다. 기존 설치에도 변경이 보이도록 공식 덱은 v2, 기본 카탈로그는 v3으로 올리고 업데이트 fixture는 각각 v3·v4로 이동한다. 영속 캐시가 새 앱 번들보다 오래됐으면 번들 카탈로그를 우선한다.
- 결정: `meaning_ja`는 타이핑 중 한 줄로 읽을 수 있는 짧은 직역만 담는다. 특히 `今どき韓国語`의 문장형 용례·주의 설명은 제거하고 `저점매수 → 底値買い`, `너 뭐 돼 → お前、何様？`처럼 번역 결과만 노출한다. 사용 맥락·격식·주의는 향후 별도 상세 메타데이터로 분리한다.
- 근거: 사전 생성 음원을 덱 데이터와 연결하면 기기별 OS 음성 편차 없이 오프라인 재생 경로와 실제 용량을 먼저 검증할 수 있다. 뜻과 설명을 한 필드에 합치면 작은 타이핑 카드에서 줄 수가 늘고 일본어 단서만 보는 회상 연습에서도 정답 외 정보가 과도하게 노출된다.
- 관련 PRD 섹션: F5, F10, F11, §8.1, §14
- 영향 범위: 공식 카탈로그 생성기·fixture, `audio/` 번들, `PracticeView`, Xcode 리소스, `DeckLibraryTests`

## 2026-07-20 타이핑 화면 일본어 뜻의 UI 괄호
- 결정: 타이핑 문제 카드의 일본어 뜻은 DB `meaning_ja`를 변경하지 않고 UI에서만 `「…」`로 감싼다. 이미 `「…」` 또는 `『…』`로 완결된 값은 중복으로 감싸지 않으며 일본어식 읽기에는 괄호를 적용하지 않는다.
- 근거: 같은 작은 회색 글씨를 사용하는 뜻과 읽기를 짧은 시각 표지로 구분하면서, 콘텐츠 제작자가 모든 덱 항목에 괄호를 반복 입력하거나 검색·게임용 뜻 데이터까지 오염시키는 일을 피할 수 있다.
- 관련 PRD 섹션: §4 S5, F10, §8.1
- 영향 범위: `PracticeView`, 뜻 표시 단위 테스트

## 2026-07-20 짧은 자모 트랙 중앙 정렬
- 결정: 타이핑 문제 카드의 자모 칩 전체 폭이 카드보다 짧으면 칩 묶음을 가로 중앙에 정렬한다. 카드 폭을 넘는 긴 자모열은 기존 한 줄 스크롤과 현재 입력 칩 중앙 자동 추적을 유지한다.
- 근거: 짧은 단어의 자모 칩이 왼쪽에 붙으면 중앙 정렬된 목표·뜻·읽기와 시각축이 어긋난다. 짧은 트랙만 중앙에 두면 카드가 더 안정적으로 보이면서 긴 문장의 진행 가시성은 그대로 유지된다.
- 관련 PRD 섹션: §4 S5, F2, §7.2
- 영향 범위: `PracticeView`, 초기 연습 화면 UI 회귀 테스트

## 2026-07-20 첫 3챕터 부화 온보딩 게이트
- 결정: 목표 선택·자판 소개·`가` 첫 입력의 3단계 소개가 끝나면 추천 덱 일괄 다운로드와 리마인더 제안을 제거하고, 챕터1 기본 자음 → 챕터2 기본 모음 → 챕터3 글자 조합만 보이는 전용 부화 미션 화면으로 연결한다. 각 시점에는 다음 미션 하나만 활성화하며 소개를 스킵해도 이 게이트는 건너뛰지 않는다.
- 결정: 첫 3챕터가 모두 클리어되기 전에는 앱 루트가 탭바를 만들지 않으므로 홈·발견·자유 연습·일반 덱·게임·마이페이지에 진입할 수 없다. 체크포인트와 챕터 완료 기록은 기존 커리큘럼 저장소를 사용해 재실행 후에도 이어지고, 챕터3 결과의 완료 전환 뒤 전체 탭을 열되 연습 탭에 머문다. 기존 사용자도 현재 커리큘럼 기록이 3챕터 미만이면 같은 성장 규칙을 적용한다.
- 결정: 캐릭터는 소개 전 `알`에서 `가` 첫 입력 직후 `금이 간 알`로 바뀌고, 챕터1~2에서는 `부화 중`, 챕터3에서 `병아리`로 전진해 화면 전환 중 이전 단계로 되돌아가지 않는다. 기존 성장 축하는 챕터1의 부화 중부터 1회씩 표시하며, 목표 기반 덱 추천과 리마인더 제안은 부화 이후 기존 홈 표면이 담당한다.
- 근거: 첫 입력 직후 일반 콘텐츠 선택지를 제시하면 부화라는 단기 목표와 연습 행동이 분리되고, 사용자가 핵심 자음·모음·조합을 경험하기 전에 이탈할 수 있다. 한 번에 하나의 다음 미션과 눈에 보이는 성장 단계를 결합하면 3챕터 완료 이유와 다음 행동이 같은 화면에서 이어진다.
- 관련 PRD 섹션: §4 S1·S4, F1, F4, F5.6, F8, §7.1
- 영향 범위: `AppRootView`, `OnboardingView`, `CurriculumMapView`, `HatchOnboardingPolicy`, `MascotStage`, ja/en/ko 로컬라이제이션, 온보딩·커리큘럼·성장 단위/UI 테스트

## 2026-07-20 HanTap 공용 AppIcon 채택
- 결정: PIYOKEY 홈 화면 AppIcon을 HanTap이 사용하는 1024×1024 무알파 PNG와 동일한 납작한 병아리+`ㅎ` 키캡 이미지로 교체한다. 공용 원본은 `shared/brand/piyokey_app_icon_source.png`에 보관하고, Asset Catalog의 `AppIcon-1024.png`도 원본과 바이트 단위로 동일하게 유지한다. 이미 규격을 만족하는 원본은 생성기가 재인코딩하지 않으며 출시 사전 검사에서 두 파일의 일치를 검증한다.
- 결정: 이 결정은 같은 날짜의 “3D 병아리 AppIcon 채택” 결정을 AppIcon에 한해 대체한다. 앱 내부 `PiyokeyLogoMark`와 성장·이벤트 반응형 런타임 병아리는 변경하지 않는다.
- 근거: 사용자가 HanTap에서 쓰는 아이콘을 PIYOKEY에도 동일하게 적용하도록 지정했다. 공용 원본과 Asset Catalog 산출물을 함께 교체하면 재생성·출시 빌드에서 이전 3D 아이콘으로 되돌아가는 일을 막을 수 있다.
- 관련 PRD 섹션: §7.1, §12, §14
- 영향 범위: `shared/brand/`, `render_piyokey_app_icon.swift`, `release_preflight.py`, `AppIcon.appiconset`, M6 브랜드 검증 문서

## 2026-07-20 발음과 효과음의 무음 모드 정책 분리
- 결정: 사용자가 직접 누른 목표 발음과 사용자가 설정에서 켠 문제 자동 발음은 사전 생성 음원과 TTS 모두 `.playback` + `.mixWithOthers`로 재생해 아이폰 무음 모드의 영향을 받지 않게 한다. 타건음·정오답·콤보 등 효과음은 기존 `.ambient` + `.mixWithOthers`를 유지해 무음 모드를 따른다.
- 결정: iOS 오디오 세션은 앱 전체에 적용되므로 발음 재생 중에는 효과음을 억제한다. 발음이 정상 종료·취소·디코딩 실패로 끝나면 효과음 세션을 즉시 `.ambient`로 복원하며, 사전 생성 음원 재생 실패 시 같은 `.playback` 세션에서 `AVSpeechSynthesizer(ko-KR)`로 대체한다.
- 근거: 언어 학습의 목표 발음은 사용자가 명시적으로 요청하거나 자동 재생을 켠 핵심 콘텐츠라 무음 모드에서도 들릴 기대가 크다. 반면 반복 타건음과 피드백음은 주변 환경을 위해 무음 모드를 존중해야 하며, 두 경로 모두 외부 음악을 중단해서는 안 된다.
- 관련 PRD 섹션: F10, F11
- 영향 범위: `HancoSoundEngine`, `TargetSpeechSynthesizer`, 사운드 정책 단위 테스트, 무음 모드·TTS·외부 음악 실기기 출시 게이트

## 2026-07-21 아이폰 표준 입력 클릭을 기본 타건음으로 채택
- 결정: 내장 키보드 타건음에 `아이폰 기본` 프리셋을 추가하고 신규 설치와 알 수 없는 저장값의 기본으로 사용한다. 기존 사용자가 명시적으로 고른 `기계식`·`소프트` 값은 마이그레이션하지 않고 유지한다.
- 결정: 실기기에서는 공개 API `UIInputViewAudioFeedback` + `UIDevice.playInputClick()`으로 시스템 입력 클릭을 요청한다. 따라서 iPhone의 키보드 피드백 사운드 설정과 무음 모드를 OS가 결정한다. SwiftUI 내장 키보드의 터치 `UIControl`과 앱 루트의 비가시 UIKit 브리지가 입력 클릭 피드백을 활성화한다. 발음 재생 중에는 다른 효과음처럼 입력 클릭도 억제한다.
- 결정: 시뮬레이터는 시스템 입력 클릭을 재생하지 못하므로 18~22ms 노이즈와 약 2.1kHz 삼각파를 조합한 짧은 원본 합성음을 사용한다. 웹에서 찾은 Apple 키보드 녹음·추출 음원은 분석 참고만 하고 앱에 내려받거나 번들하지 않는다.
- 근거: iOS 기본 키보드와 가장 일관된 촉감은 녹음 파일 복제가 아니라 Apple이 제공하는 표준 입력 클릭 경로이며, 이 경로는 사용자 시스템 설정과 무음 모드 기대도 함께 지킨다. 원본 합성 폴백은 시뮬레이터 개발·자동 테스트에서만 즉각적인 피드백을 제공하면서 외부 음원 권리 문제를 피한다.
- 관련 PRD 섹션: F10, F11
- 영향 범위: `HancoSoundEngine`, `HangulKeyboardView`, `AppRootView`, 연습·게임·온보딩 타건음 호출부, ja/en/ko 로컬라이제이션, 사운드 단위 테스트, 키보드 피드백·무음 모드 실기기 출시 게이트

## 2026-07-20 목표 문구 공백의 스페이스 키 표식
- 결정: 타이핑 문제의 목표 문구에 포함된 공백을 투명한 12pt 간격 대신 U자형 스페이스 글리프가 든 작은 키캡으로 표시한다. 아직 도달하지 않은 공백은 옅게, 현재 입력할 공백은 분홍색 테두리로, 입력을 마친 공백은 초록색 채움으로 구분하며 긴 목표에서는 음절과 함께 비례 축소한다.
- 근거: 투명한 공백은 일반 음절 간격과 차이가 작아 `한 장 주세요` 같은 문장에서 다음 입력이 스페이스라는 사실을 알아보기 어렵다. 입력 장치와 같은 모양의 표식과 기존 진행 색상을 함께 사용하면 별도 문구 없이 단어 경계와 현재 입력을 즉시 전달할 수 있다.
- 관련 PRD 섹션: §4 S5, F2, §7.2
- 영향 범위: `PracticeView`, 공백 포함 목표의 타이핑 화면 시각 회귀 검증

## 2026-07-20 일본어 사용자를 위한 띄어쓰기 미션
- 결정: 6챕터 성장축을 유지하면서 챕터6의 첫 스테이지로 `단어 사이 띄어쓰기` 10문제를 추가한다. 각 목표는 10자 이하의 두 단어 표현이며 정확히 한 번의 스페이스 입력을 포함한다. 기존 `유용한 표현`은 같은 챕터의 두 번째 스테이지로 유지하고 두 스테이지 모두 챕터5+ 자유 선택 정책에 따라 즉시 시작할 수 있다.
- 결정: 자유 선택 챕터는 포함된 스테이지 중 하나를 완료하면 성장축에서 해당 챕터를 완료한 것으로 센다. 따라서 과거 `chapter_6_sentences` 완료 사용자의 완전 성장 상태는 새 선택 스테이지 추가 뒤에도 내려가지 않으며, 새 사용자는 띄어쓰기 또는 문장 스테이지 중 자신의 학습 목적에 맞는 하나부터 완료할 수 있다.
- 근거: 일본어 문장에는 보통 단어 사이 공백이 없어 일본어 사용자가 한국어의 스페이스 입력을 독립적인 규칙으로 인식하기 어렵다. 별도 미션에서 모든 문제에 같은 경계를 반복하면 새 스페이스 표식과 키 위치를 실제 입력 습관으로 연결할 수 있고, 선택형 후반 커리큘럼에 넣으면 기존 온보딩 3챕터와 저장 진행을 변경하지 않는다.
- 관련 PRD 섹션: F2, F4, §4 S4·S5
- 영향 범위: `CurriculumCatalog`, `CurriculumChapterCompletionPolicy`, 성장 단계 집계, ja/en/ko 로컬라이제이션, 커리큘럼·설정 단위 테스트

## 2026-07-20 idle 피요의 직접 터치 반응
- 결정: 마이페이지·MY 피요 상세·연습 시작 전·OS IME 안내·옷장 미리보기처럼 입력 세션 밖에서 가만히 표시되는 피요만 직접 터치할 수 있게 한다. 한 번 탭은 0.9초 `happy`, 더블 탭은 1.1초 `cheer`, 1.8초 안의 세 번째 단일 탭부터는 `shy`로 반응하고 효과음·햅틱 설정을 그대로 따른다. 마이페이지와 MY 피요 상세에서 0.5초 길게 누르면 옷장을 연다.
- 결정: 단일 탭과 더블 탭은 배타적으로 판정해 더블 탭이 행복 효과음 두 번과 환호 효과음을 함께 내지 않게 하고, 새 쓰다듬기는 이전 복귀 작업을 취소해 반응 시간이 중간에 잘리지 않게 한다. 연습·게임 입력 중 피요는 기존 학습 이벤트 전용으로 두며 직접 터치를 받지 않는다. VoiceOver에서는 터치 가능한 피요를 버튼으로 읽고 현재 행복·환호·수줍음 상태를 현지화한 값으로 알린다.
- 근거: 외부 캐릭터 시트의 쓰다듬기 규칙은 렌더러에 들어 있었지만 모든 `GrowingMascotView` 호출이 기본 `interactive = false`를 사용해 실제 앱에서는 실행할 수 없었다. idle 표면에서만 계약을 노출하면 피요와의 애착을 높이면서 세션 입력·타이머·게임 프레임 예산은 건드리지 않는다.
- 관련 PRD 섹션: §7.1, F8, F10, F11, §11.1
- 영향 범위: `ChickMascotView`, `GrowingMascotView`, 마이페이지·MY 피요·연습 준비·OS IME 안내·옷장, ja/en/ko 로컬라이제이션, 마스코트 단위·UI 테스트

## 2026-07-21 창작 K-POP·K드라마 공식 덱 보강
- 결정: 출시 공식 카탈로그에 `オリジナルK-POP` 가사풍 3덱(ときめき·夢とエール·夜と星)과 `Kドラマ会話` 3덱(恋の予感·秘密と逆転·毎日のぬくもり)을 추가한다. 각 덱은 창작 한국어 12항목이며, 모든 항목은 공백을 제외한 완성형 한글 2~8음절, 덱별 길이 단계 4종 이상, 단어와 띄어쓰기 문구 혼합 조건을 생성기에서 검사한다. 발견 숏컷과 목적별 섹션에 `K-POP`·`Kドラマ`를 포함한다.
- 결정: 실제 노래 가사·드라마 대사와 아티스트·작품 고유명사는 사용하지 않는다. 새 덱은 제목과 `歌詞風` 태그로 창작 콘텐츠임을 구분하고, 기존 콘텐츠 주소형 오프라인 파일럿 음성 및 TTS 폴백 정책을 그대로 따른다. 기본 카탈로그는 v5, 업데이트 fixture는 v6으로 올린다.
- 근거: 공식 학습 카탈로그의 권리 안전성과 출시 신뢰를 유지하면서, 핵심 사용자 동기인 K-POP·한국 드라마를 충분한 분량과 짧은 입력 길이의 실제 연습 콘텐츠로 연결하기 위함이다.
- 관련 PRD 섹션: §1.1, F5.1, F5.7, §8.3, §14
- 영향 범위: `tools/gen_mock_catalog.py`, 공식 카탈로그·덱·오디오 fixture, `DiscoverViewModel`, DeckKit·iOS 카탈로그 테스트, `PRD.md`, `ios/Hanco/README.md`

## 2026-07-21 덱과 분리된 긴 글 띄어쓰기 사이드 게임
- 결정: 게임 허브에 여섯 번째 `띄어쓰기` 카드를 추가하고, 이 카드만 덱 선택을 건너뛰어 자체 작성한 146~181자 한국어 글 5개의 목록으로 연결한다. 콘텐츠 본문은 세 언어 로컬라이제이션 리소스에 동일한 한국어 학습 원문으로 두고 Swift 모델은 식별자와 제목 키만 관리하며, 각 카드에 공백 포함 글자 수와 정답 공백 수를 표시한다.
- 결정: 플레이는 공백을 모두 제거한 원문을 편집기에 미리 채운다. 비공백 문자와 문장부호의 삽입·삭제·변경은 거부하고, 반각·전각·줄바꿈을 포함한 공백 입력은 단일 반각 공백 경계로 정규화한다. 채점은 비공백 문자 사이의 정답 경계 집합과 제출 경계 집합을 비교해 누락·과잉을 분리하며, 정확도는 `정답 경계 교집합 / (정답 경계 전체 + 과잉 경계)`로 계산한다.
- 결정: 이 사이드 게임에는 시간 제한·카운트다운·덱 진행·자모 복습·입력 모드별 최고 기록을 적용하지 않는다. 유효 풀이 초시계만 백그라운드에서 정지하고, 제출 뒤에는 기존 공통 결과 타임라인에서 1,000점 만점·랭크·경계 통계·정답 원문·1탭 재도전을 제공한다. 완료는 일반 게임 활동과 같이 JST 스트릭에는 반영한다.
- 근거: 100~200자 글의 띄어쓰기는 단어장 자모 타이핑과 콘텐츠·판정 단위가 달라 덱 스키마와 복습 저장소에 억지로 넣으면 기존 의미가 흐려진다. 원문을 잠그고 공백 경계만 비교하면 일본어 키보드에서도 한국어 IME 설치 없이 핵심 학습 행동에 집중할 수 있으며, 자체 작성 번들은 오프라인·콘텐츠 권리 요구를 함께 만족한다.
- 관련 PRD 섹션: §4 S7, F6e, F8, F12, §11.1, §12
- 영향 범위: `GameDeckSelectionView`, `SpacingGameView`, 공백 판정·세션 모델, ja/en/ko 로컬라이제이션, 게임 허브·띄어쓰기 단위/UI 테스트

## 2026-07-22 띄어쓰기 게임의 순차 경계 탐색 조작
- 결정: 긴 글 편집기와 OS 키보드를 제거하고, 첫 글자 사이 경계에서 시작하는 전용 탐색 상태를 둔다. 플레이 화면 하단은 `이전 · 스페이스(띄어쓰기) · 다음` 3버튼으로 고정하며 좌우 버튼은 인접 경계로 한 칸씩 이동하고 가운데 버튼은 현재 경계의 공백을 추가하거나 다시 눌러 취소한다. 최종 정답 확인은 상단 체크 버튼으로 분리한다.
- 결정: 공백을 추가할 때마다 정답 경계는 초록 체크·성공 효과음·성공 햅틱, 오답 경계는 빨강 안내·오류 효과음·오류 햅틱·본문 흔들림으로 즉시 알린다. 선택한 공백은 `␣` 표식으로 본문에 남기되 정답/오답 색을 유지하고, Reduce Motion에서는 흔들림만 생략한다. 효과음과 햅틱은 기존 사용자 설정을 즉시 따른다.
- 결정: 원문은 읽기 전용 글자 흐름으로 렌더링하고 상태에는 현재 경계와 선택 경계 집합만 보관한다. 따라서 UI에서 비공백 문자를 변경할 경로를 없애면서도 최종 점수는 기존 순수 공백 경계 판정과 누락·과잉 계산을 그대로 사용한다.
- 근거: 100~200자 편집기에서 삽입 위치를 직접 찾아 탭하는 방식은 커서 정밀도와 키보드 노출 때문에 학습보다 조작 부담이 컸다. 한 방향으로 훑는 3버튼 방식은 현재 판단 위치를 항상 명확히 하고, 즉시 피드백과 되돌리기로 띄어쓰기 규칙을 한 경계씩 학습하게 한다.
- 관련 PRD 섹션: F6e, F10, F11, §11.1
- 영향 범위: `SpacingGameView`, 공백 경계 상태 모델, ja/en/ko 로컬라이제이션, 띄어쓰기 단위/UI 테스트

## 2026-07-24 띄어쓰기 게임의 1경계 1판단·최초 판단 채점
- 결정: 하단 3버튼을 `이전 · 스페이스(띄어쓰기) · 붙여쓰기`로 바꾸고, 두 판단 버튼 모두 선택 즉시 정오답을 알려 준 뒤 다음 경계로 자동 이동한다. `이전`은 방금 지나온 경계의 답을 고치는 용도로 유지하며, 마지막 경계까지 판단하면 별도 결과 CTA를 노출한다.
- 결정: 현재 위치 주변의 비공백 원문을 좌우 5자씩 확대한 포커스 카드와 위치·완료 수·진행 막대를 항상 표시한다. 포커스는 정답 공백을 미리 노출하지 않고, 이동 커서는 본문 레이아웃을 차지하지 않는 오버레이로 그려 이동할 때 줄바꿈이 흔들리지 않게 한다.
- 결정: 최초 선택은 경계별로 한 번만 보존하고 이후 선택 변경은 수정 횟수로 누적한다. 점수·랭크는 `최초 정답 경계 수 / 전체 경계 수`로 계산하며, 수정된 현재 답은 기존 공백 경계 판정으로 최종 완성도를 별도 계산한다. 결과에는 첫 판단 정확도·최종 완성도·수정 횟수·시간과 최초 오답의 `사용자 판단 → 정답` 문맥을 표시한다.
- 결정: 띄어쓰기와 붙여쓰기 양쪽 모두 성공/오류 효과음·햅틱·색상 피드백을 제공하고, VoiceOver에는 확대된 양옆 글자와 판정 결과를 announcement로 알린다. 자동 전진 지연은 정답 0.36초, 오답 0.62초이며 Reduce Motion에서는 0.18초로 줄이고 흔들림을 생략한다.
- 근거: 기존 순차 탐색은 커서 찾기는 쉬워졌지만 공백마다 `스페이스`와 `다음`을 연속으로 누르고 나머지 모든 경계에서도 `다음`을 눌러야 했다. 한 경계당 한 번의 명시적 판단은 조작량을 줄이면서 붙여쓰기까지 학습 행동으로 만들고, 최초 판단을 남기면 즉시 정답을 보고 수정해도 점수와 복습 가치가 사라지지 않는다.
- 관련 PRD 섹션: F6e, F10, F11, F12, §11.1
- 영향 범위: `SpacingGameEngine`, `SpacingGameViewModel`, `SpacingGameView`, 띄어쓰기 결과, ja/en/ko 로컬라이제이션, 단위/UI 테스트

## 2026-07-24 띄어쓰기 게임 입력을 좌우 수동 탐색으로 복귀
- 결정: 사용자 피드백에 따라 `스페이스 · 붙여쓰기` 선택 후 자동 전진을 폐기하고, 2026-07-22의 `이전 · 스페이스 · 다음` 입력으로 복귀한다. 좌우 버튼만 커서를 한 경계씩 옮기고 가운데 버튼은 현재 경계의 공백을 추가하거나 다시 눌러 취소하므로, 사용자가 피드백을 확인한 뒤 직접 이동 속도를 정한다.
- 결정: 확대 포커스·진행률·마지막 결과 CTA·최초 판단 점수·수정 횟수·문맥 오답 복습은 유지한다. 최초 판단은 해당 경계에서 처음 공백을 추가하면 `띄어쓰기`, 공백을 추가하지 않고 `다음`으로 넘어가면 `붙여쓰기`로 기록하며, 이후 가운데 버튼으로 선택을 바꾼 횟수만 수정 횟수에 더한다.
- 결정: 공백 추가 때의 정오답 색상·효과음·햅틱·VoiceOver 안내는 유지하되 공백 취소는 표식과 현재 피드백만 제거한다. 마지막 경계에 도달하기 전에는 상단 정답 확인을 비활성화하고, 마지막 경계의 현재 선택은 결과 제출 시 최초 판단으로 확정한다.
- 근거: 자동 전진은 한 번의 탭으로 진행되는 장점보다 사용자가 현재 문맥과 피드백을 확인할 시간을 빼앗는 부담이 컸고, `붙여쓰기`를 별도 정답 버튼으로 제시하면 문장을 직접 훑으며 필요한 곳에만 공백을 넣는 원래 과제의 감각이 약해졌다.
- 관련 PRD 섹션: F6e, F10, F11, F12, §11.1
- 영향 범위: `SpacingGameViewModel`, `SpacingGameView`, 띄어쓰기 결과, ja/en/ko 로컬라이제이션, 단위/UI 테스트

## 2026-07-24 띄어쓰기 게임 확대 포커스 카드 제거
- 결정: 플레이 상단의 현재 경계 주변 확대 포커스 카드를 제거하고, 원문 카드 안의 오버레이 커서와 선택된 `␣` 표식만으로 현재 위치를 표시한다. 위치·확인 수·진행 막대와 하단 수동 조작은 유지한다.
- 근거: 긴 글 바로 위에서 동일 문맥을 크게 반복하는 카드가 세로 공간을 차지하고 시선이 두 문장 영역으로 분산됐다. 원문 안 커서가 이미 현재 위치를 명확히 표시하므로 중복 강조를 줄이는 편이 실제 기기에서 읽기 흐름이 낫다.
- 관련 PRD 섹션: F6e, F10, §11.1
- 영향 범위: `SpacingGameEngine`, `SpacingGameViewModel`, `SpacingGameView`, ja/en/ko 로컬라이제이션, 띄어쓰기 단위/UI 테스트

## 2026-07-26 띄어쓰기 번들 글 6단계 난이도화
- 결정: 기존 5개 글을 레벨 1~6의 6개 글로 교체하고 선택 화면에서 위에서 아래로 레벨을 표시한다. 글 길이는 112·134·151·172·178·189자로 늘리며, 단계가 오를수록 어휘와 문장 구조 및 띄어쓰기 판단 부담을 높인다. 레벨 6은 `불확실성`, `가설`, `반례`, `이해관계`, `의사 결정`을 포함한 고급 글로 구성한다.
- 근거: 비슷한 분량과 생활 어휘로만 구성된 기존 5개 글은 선택 단계와 반복 플레이 사이의 난이도 차이가 분명하지 않았다. 6개 카드가 한 화면의 세로 밀도를 자연스럽게 채우면서 학습자가 분량과 난도를 예측할 수 있게 한다.
- 관련 PRD 섹션: F6e
- 영향 범위: `SpacingPassageCatalog`, `SpacingPassageListView`, ja/en/ko 로컬라이제이션, 띄어쓰기 단위/UI 테스트

## 2026-07-21 음성 전용 받아쓰기 게임
- 결정: 게임 허브에 덱 기반 `dictation` 모드를 추가한다. 설치 덱에서 조합 엔진으로 분해 가능한 고유 한국어를 결정적 시드로 최대 10개 고르고, 문제 화면에는 정답·초성·뜻·일본어식 읽기와 다음 키 가이드를 모두 숨긴 채 발음 버튼과 사용자가 실제로 입력한 문자열만 표시한다.
- 결정: 각 문제는 카운트다운 직후 자동으로 발음하고 사용자가 언제든 다시 들을 수 있게 한다. 이 재생은 설정의 문제 자동 발음 값과 무관한 필수 문제 단서이며, 사전 생성 음원 우선·`ko-KR` TTS 대체·`.playback` + `.mixWithOthers` 정책을 기존 목표 발음 경로와 공유한다. 백그라운드 진입 시 발음과 유효 시간을 멈추고 복귀 시 다시 카운트다운한다.
- 결정: 자모 판정·내장/OS IME·오타 복습 수집·노미스 졸업·점수·콤보·0.65초 자동 전환은 초성 직접 타이핑 상태 기계를 공유한다. 최고 기록과 공유 결과는 `GameKind.dictation`으로 다른 게임 및 입력 모드와 분리하고, 시작·재도전 모두 `3 → 2 → 1` 뒤에만 문제 시간과 발음을 시작한다.
- 근거: 시각 단서를 제거한 음성 회상과 실제 한글 입력을 한 흐름으로 묶으면서도 기존 조합 판정·오디오 세션·복습·결과 저장 계약을 재사용하면 게임별 판정 차이와 외부 음악 중단 회귀를 피할 수 있다.
- 관련 PRD 섹션: F2a, F6d, F7, F11, F12, §6, §11.1
- 영향 범위: `GameKind`, `GameDeckSelectionView`, `ChoseongTypingViewModel`, `TargetSpeechSynthesizer`, `FlowGameResultView`, ja/en/ko 로컬라이제이션, 받아쓰기 단위/UI 테스트

## 2026-07-22 홈 출석 스탬프 날짜를 시간순으로 표시
- 결정: 홈과 MY 피요 상세의 7일 출석 스탬프는 가장 오래된 날짜를 왼쪽, 기준이 되는 최신 날짜를 오른쪽에 두어 왼쪽에서 오른쪽으로 시간순 표시한다. 최근 6일 안의 마지막 출석일을 기준으로 삼는 기존 기간 선택 규칙은 유지한다.
- 근거: 최신 날짜부터 과거로 내려가는 배열은 일반적인 캘린더 읽기 방향과 반대라 지난 날짜와 이후 날짜의 선후 관계를 오인하게 한다.
- 관련 PRD 섹션: F8
- 영향 범위: `RetentionCalendar.stampCardDays`, 홈 MY 피요 결합 카드, MY 피요 상세 스탬프 카드, 리텐션 단위 테스트

## 2026-07-23 출석 스탬프를 월요일 시작 주간으로 고정
- 결정: 홈과 MY 피요 상세의 7일 출석 스탬프는 출석 이력과 무관하게 JST 기준 현재 주의 월요일부터 일요일까지를 고정 표시한다. 3/5/7일 보상 집계와 안내 문구도 같은 현재 주 범위를 사용한다. 이 결정은 2026-07-22의 최근 출석일 기준 기간 선택 규칙을 대체한다.
- 근거: 마지막 출석일에 따라 카드의 시작일이 달라지면 사용자가 어느 기간의 출석을 보고 있는지 즉시 파악하기 어렵다. 익숙한 월요일 시작 주간을 사용하면 과거·오늘·남은 날짜의 위치가 매주 일정하다.
- 관련 PRD 섹션: F8
- 영향 범위: `RetentionCalendar.stampCardDays`, 홈 MY 피요 결합 카드, MY 피요 상세 스탬프·보상, ja/en/ko 로컬라이제이션, 리텐션 단위 테스트

## 2026-07-24 주간 스탬프 상태와 요약 정보 분리
- 결정: 월요일~일요일 스탬프의 빈 칸을 하나의 미완료 상태로 다루지 않고 놓친 과거·오늘 미완료·예정인 미래로 구분한다. 홈과 MY 피요 상세에는 현지화한 주간 날짜 범위, `이번 주 n/7`, 별도의 연속 일수를 함께 표시하며 상세 화면의 각 날짜는 VoiceOver로 개별 탐색할 수 있게 한다.
- 근거: 월요일 시작 주간에는 미래 날짜가 항상 포함되므로 이를 과거 결석과 같은 점선·`미완료`로 표시하면 실패로 오인한다. 주간 합계와 스트릭도 서로 다른 지표라 동시에 명시해야 카드의 기간과 목표가 즉시 이해된다.
- 관련 PRD 섹션: F8, §7.2
- 영향 범위: `RetentionStampDayState`, `SevenDayStampRow`, 홈·MY 피요 상세, ja/en/ko 로컬라이제이션, 리텐션 단위·홈 UI 테스트

## 2026-07-24 로컬 저장 실패의 홈 비모달 재시도
- 결정: 커리큘럼·게임 기록·복습·스트릭·온보딩 저장 실패 시 마지막 쓰기 작업을 각 라이브러리에 보존하고 홈 상단에 하나의 현지화된 재시도 배너를 표시한다. 배너는 학습·게임 세션 안에는 삽입하지 않고 홈에 돌아왔을 때만 노출한다.
- 근거: 기존 `saveFailed`는 UI에서 소비되지 않아 사용자가 완료를 확인한 뒤 기록이 사라져도 원인과 복구 방법을 알 수 없었다. 실패한 입력값을 함께 보존해야 단순 재로드가 아니라 실제 쓰기를 다시 수행할 수 있다.
- 관련 PRD 섹션: F4, F6, F7, F8, §9, §11.1
- 영향 범위: 진행·게임·복습·리텐션·온보딩 라이브러리, 홈, ja/en/ko 로컬라이제이션, 저장 실패 회귀 테스트

## 2026-07-24 AVFoundation 델리게이트 격리와 시작 실패 복구
- 결정: `TargetSpeechSynthesizer`의 Objective-C AVFoundation 델리게이트 준수에는 `@preconcurrency`를 명시해 MainActor 상태 접근을 유지하면서 Swift 6 격리 진단을 해소한다. 효과음 엔진 시작 실패는 무시하지 않고 플레이어 정지·엔진 일시정지·가능한 오디오 세션 비활성화로 정리한 뒤 다음 입력에서 다시 시작할 수 있게 한다.
- 근거: iOS 16부터 지원해야 하는 Objective-C 델리게이트 계약은 Swift의 actor 격리를 선언하지 않아 메인 액터 구현과 경고가 발생한다. 오디오 시작 오류 뒤 절반만 활성화된 그래프·세션을 남기면 이후 TTS나 외부 음악 혼합까지 연쇄적으로 영향을 줄 수 있다.
- 관련 PRD 섹션: F11, §12
- 영향 범위: `TargetSpeechSynthesizer`, `HancoSoundEngine`, Swift 6 빌드 경고, TTS·효과음·외부 음악 실기기 게이트

## 2026-07-22 무음 모드에서도 재생되는 CC0 기본 타건음
- 결정: 게임형 학습의 청각 피드백을 기본 경험으로 보고 타건음·정오답·콤보 등 효과음을 기본 ON인 `.playback` + `.mixWithOthers`로 전환한다. 무음 모드에서도 재생하지만 외부 음악은 중단하지 않으며, 설정 화면의 사운드 카드를 최상단으로 옮겨 진입 직후 1탭으로 OFF할 수 있게 한다. 명시적으로 저장된 OFF는 재실행 후에도 유지한다.
- 결정: 2026-07-21의 `아이폰 표준 입력 클릭` 결정은 기본 타건음 재생 경로에 한해 대체한다. `UIDevice.playInputClick()`은 무음 모드를 따르므로 제거하고, Freesound의 `Basic Mouse Click UI`(Philip_Berger, CC0)를 기본 프리셋에 사용한다. 공식 HQ MP3의 918 유효 프레임만 디코딩해 19.125ms·48kHz·mono Float32 PCM CAF로 번들하며, 파일 누락·손상 시 기존 원본 합성음으로 대체한다.
- 결정: 원본 페이지·CC0 링크·입출력 SHA-256·변환 방식을 `Resources/Sounds/README.md`에 기록하고, 출시 사전 검사에서 번들 포함 여부와 CAF 체크섬을 고정한다. 발음 재생 중에는 효과음을 억제하고 종료 뒤 효과음용 `.playback` 기본 모드로 복원한다.
- 근거: 시스템 입력 클릭은 iOS 기본 키보드와 같지만 사용자의 무음 모드에서는 재생되지 않아 게임의 핵심 피드백을 보장할 수 없다. 독립 제작된 19ms CC0 클릭은 짧은 타건감을 유지하면서 무음 정책·권리·오프라인 재생을 앱이 일관되게 통제할 수 있다.
- 관련 PRD 섹션: F10, F11, §12
- 영향 범위: `HancoSoundEngine`, 기본 타건음 CAF·권리 기록, `SettingsView`, ja/en/ko 로컬라이제이션, 사운드 단위·설정 UI 테스트, 출시 사전 검사와 무음 모드 실기기 게이트

## 2026-07-24 날짜 기반 시즈널 복장 제거
- 결정: 피요의 별도 계절 장식 레이어와 달력 판정을 제거한다. 한복·목도리·벚꽃핀·우산·밀짚모자·송편·호박 베레·산타 모자·부화 기념일 모자는 어떤 날짜나 화면에서도 자동 표시하지 않는다. 옷장의 `자동`은 덱 태그와 성장 단계가 정하는 비계절 소품만 선택하며 `없음`·고정 소품·영구 해금 규칙은 유지한다.
- 결정: 더 이상 쓰이지 않는 부화일 저장과 계절 장식 렌더링 코드를 함께 제거하고, 옷장 자동 선택의 세 언어 문구에서 계절 아이템 안내를 삭제한다. 이 결정은 2026-07-20의 계절 장식 확대·수동 선택 우선순위·최종 미리보기 내 자동 계절 장식 결정을 대체한다.
- 근거: 날짜만으로 갑자기 바뀌는 복장은 사용자가 선택한 피요의 모습과 `자동`의 의미를 혼동시키며, 학습·성장·덱 맥락과도 직접 연결되지 않는다. 컨텍스트 소품만 남기면 모습 변화의 원인을 예측할 수 있다.
- 관련 PRD 섹션: §7.1, F8
- 영향 범위: `ChickMascotView`, `MascotGrowthSystem`, 옷장 ja/en/ko 로컬라이제이션, 마스코트 단위 테스트, 출시 QA

## 2026-07-24 홈·마이페이지 피요 데일리 응원 멘트
- 결정: 홈 MY 피요 결합 카드와 마이페이지 프로필에서 성장 단계명을 숨기고, 피요가 직접 말하는 짧은 응원 멘트 10종 중 하나를 일본어식 괄호 `「」`와 연한 분홍 말풍선으로 표시한다. 같은 멘트는 세 언어로 현지화하고, MY 피요 상세의 성장 정보에는 현재 단계명을 유지한다.
- 결정: 멘트는 학습 전 4종·오늘 완료 2종·전날에 이어 온 스트릭 2종·이틀 이상 지난 복귀 2종으로 나누고, 각 상태 안에서 JST 날짜로 결정적으로 순환한다. 서버·계정·난수 저장 없이 같은 날의 홈과 마이페이지 및 앱 재실행 사이에 동일한 멘트를 보장한다.
- 결정: 마이페이지 옷장 진입은 말풍선 아래 캡슐에서 카드 우측 상단의 44pt 원형 옷걸이 버튼으로 옮긴다. 버튼은 흰 배경·분홍 테두리와 아이콘으로 행동 요소임을 드러내고, 말풍선은 아이콘 없이 꼬리와 옅은 채움으로 피요의 대사임을 구분한다.
- 근거: 프로필 첫인상에서는 `ひよこ` 같은 상태명보다 현재 학습 맥락에 맞는 캐릭터의 응원이 정서적 보상과 재방문 동기를 더 직접 전달한다. 말풍선과 옷장 버튼의 모양·위치를 분리하면 대사와 탭 행동을 혼동하지 않으며, 성장 단계는 상세 화면에 남겨 정보 접근성을 잃지 않는다. JST 기준은 기존 스트릭·데일리 챌린지의 날짜 계약과 일치한다.
- 관련 PRD 섹션: F8, §7.1
- 영향 범위: 홈 MY 피요 카드, 마이페이지 프로필, 리텐션 날짜 로직, ja/en/ko 로컬라이제이션, 리텐션·로컬라이제이션 단위 테스트

## 2026-07-24 타건음 레벨·반복감·오디오 복구 폴리싱
- 결정: 원본 CC0 기본 클릭은 파일을 다시 변환하지 않고 런타임에서 기본 게인 0.34와 0.78 peak 상한을 적용한다. 일반 키는 피치·게인이 다른 3개 변형을 결정적으로 순환하고, Backspace는 낮고 부드럽게, Shift는 짧고 조용하게 구분한다. 기계식·소프트 프리셋도 같은 변형·키 역할 규칙을 각 프리셋 기준 게인 위에 적용한다.
- 결정: 6개 타건음 플레이어와 3개 정오답·콤보 플레이어를 분리하고 각 풀에서 재생 중이 아닌 노드를 우선 선택한다. 따라서 빠른 타건이 긴 완성·오타·콤보 피드백을 중간에 끊지 않으며, 각 풀을 모두 사용 중일 때만 순환 재사용한다.
- 결정: 효과음 ON은 허용 상태만 바꾸고 앱 시작 시 엔진을 즉시 활성화하지 않는다. 첫 효과음에서 지연 시작하고 마지막 효과음 15초 뒤 또는 앱 백그라운드 진입 시 플레이어·엔진·세션을 정리한다. 다음 입력은 같은 경로로 다시 시작한다.
- 결정: `AVAudioSession` 인터럽트·출력 경로 변경·미디어 서비스 lost/reset 알림을 별도 직렬 큐에서 처리한다. 통화·Siri 종료 시 이전 활성 상태와 `shouldResume`을 확인해 복구하고, 유선·Bluetooth 변경 시 세션을 재활성화하며, 미디어 서비스 reset에는 엔진과 플레이어 그래프를 새로 만든다.
- 근거: 원본 클릭의 실측 peak `-0.23 dBFS`, RMS `-10.7 dBFS`는 합성 효과음보다 커서 반복 입력에서 피로와 중첩 clipping 위험이 있었다. 동일 샘플 반복·공용 4노드 순환·시스템 오디오 상태 미동기화는 각각 기계적인 반복감, 중요 피드백 절단, 통화·출력 변경 뒤 무음 회귀를 만들 수 있다. 지연 시작과 유휴 반납은 첫 입력 복구를 유지하면서 홈 체류 중 불필요한 오디오 세션 점유를 줄인다.
- 관련 PRD 섹션: F10, F11, §12
- 영향 범위: `HancoSoundEngine`, `HancoAudioSessionController`, `HangulKeyboardView`, 앱 생명주기, 사운드 단위 테스트, M6 실기기 청취 게이트

## 2026-07-24 첫 App Store 제출 버전과 스탬프 스토어 이미지
- 결정: 첫 App Store 제출의 마케팅 버전과 빌드 번호를 `1.0 (1)`로 고정하고 Xcode Debug/Release 및 제출 메타데이터에 동일하게 기록한다. 일본어 스토어 이미지에는 iPhone 17 Pro Max에서 검증된 1320×2868 무알파 MY 피요 화면을 사용해 월요일 시작 주간 스탬프·진행도·보상을 실제 RC UI로 보여 준다.
- 근거: 제품 마일스톤이 M6 제출 준비 단계에 도달했으므로 프로토타입 버전 0.1을 유지할 이유가 없고, 실제 UI 테스트가 생성한 허용 규격 이미지는 구현과 스토어 표현의 불일치를 방지한다.
- 관련 PRD 섹션: F8, §12
- 영향 범위: Xcode 마케팅 버전, `release/app_store_submission.json`, `release/screenshots/ja`, App Store 출시 QA

## 2026-07-24 Game Center 점수·랭킹과 피요 성장 연동
- 결정: Game Center는 앱 계정이나 서버를 추가하지 않는 선택형 보조 계층으로 사용한다. 로컬 `GameRecord`와 피요 성장값이 계속 단일 기준이며, 인증된 플레이어에게만 최고 점수와 성장 업적을 비동기 동기화한다. 인증 UI와 대시보드는 게임 허브·결과 화면에서 사용자가 명시적으로 열 때만 표시해 세션 중 모달 금지와 오프라인 플레이를 보존한다.
- 결정: 리더보드는 원격 덱 수명주기와 Apple 구성 한도를 분리하기 위해 덱별이 아니라 게임 종류×입력 방식으로 구성한다. 흐름·산성비·초성·받아쓰기는 내장/OS IME를 분리하고 단어 맞추기는 공용으로 두어 클래식 리더보드 9개를 사용한다. 로컬 기록이 더 높으면 재인증 시 보충 제출하며, 결과에서는 해당 리더보드의 세계 순위를 표시한다.
- 결정: 기존 성장 단계(완료 챕터), 누적 입력 자모, 최장 스트릭을 Game Center 업적 5개의 진행률로 미러링한다. Game Center에 점수가 정상 제출되거나 기존 순위가 확인되면 `gameCenterTrophy` 소품을 영구 해금한다. 변동 가능한 현재 순위는 성장 단계나 영구 보상의 조건으로 사용하지 않는다.
- 근거: Game Center의 소셜 경쟁과 시스템 UI를 활용하면서도 PRD의 로컬 우선·무계정·오프라인 및 세션 무인터럽트 원칙을 유지하고, 덱 추가 때마다 영구 ID를 운영해야 하는 확장성 문제와 입력 방식별 공정성 문제를 피하기 위함이다.
- 관련 PRD 섹션: §4 S7·S8, F6~F6d, F12, §9~§12, §13 M6
- 영향 범위: `GameCenterService`, Game Center entitlement·리더보드/업적 운영 설정, 게임 허브·공통 결과, `MascotCompanionLibrary`, `MascotProp`, 옷장, ja/en/ko 로컬라이제이션, 단위 테스트

## 2026-07-24 흐름 모드 궁궐 무대와 플레이 HUD 압축
- 결정: 흐름 모드 카드는 화면 왼쪽 밖에서 들어와 오른쪽 밖으로 나가며, 기존 컨베이어를 기와지붕·단청색 보·창호·붉은 기둥·원근 돌마당을 그리는 정적 SwiftUI Canvas 궁궐 무대로 교체한다. 목표 카드는 한지 패를 연상시키는 이중 옥색·금색 테두리와 도장 문양을 사용하되 한국어·일본어 가독성 및 기존 60Hz 카드·파티클 렌더링은 유지한다.
- 결정: 플레이 내비게이션의 덱 제목과 설정 버튼을 제거하고 닫기·남은 시간·점수·콤보를 한 줄의 최상단 HUD로 통합한다. Debug FPS 오버레이는 일반 실행에서 숨기고 `HANCO_SHOW_GAME_FPS=1`인 명시적 성능 측정에서만 표시한다.
- 근거: 핵심 무대와 입력 영역에 세로 공간을 돌려주면서 한국적 장소성이 즉시 보이게 하고, 세션 중 설정·모달 진입점을 두지 않는 기존 규칙을 화면 구조에도 일치시키기 위함이다. 정적 배경과 별도의 60Hz 전경 경계를 유지하면 장식 증가가 카드 이동 규칙이나 프레임 측정에 관여하지 않는다.
- 관련 PRD 섹션: F6, §7.1, §7.3, §11.1, §12
- 영향 범위: `FlowGameView`, 흐름 이동 레이아웃 테스트, 게임 UI 회귀, M4 성능 프로브 실행 절차

## 2026-07-24 산성비 플레이 HUD와 낙하 레인 재배치
- 결정: 산성비도 플레이 내비게이션의 덱 제목과 설정 버튼을 제거하고 닫기·남은 시간·점수·콤보를 최상단 한 줄에 둔다. 일반 Debug 화면에서는 FPS 오버레이를 숨기며 명시적 성능 측정 실행만 기존 환경 변수로 표시한다.
- 결정: 낙하 무대를 점선으로 구분한 3개 세로 레인과 52pt 하단 위험 구역으로 나눈다. 카드의 시작 중심은 모드 배지 아래로 내리고, 종료 중심은 추정 카드 하단이 위험선 상단에 닿도록 계산한다. 피요는 34pt로 축소해 위험선 아래 구역 안에 배치하며 레인 컨테이너 식별자가 카드·피요의 접근성 식별자를 덮지 않도록 별도 비가시 마커를 사용한다.
- 근거: 제거한 제목·진단 영역의 공간을 실제 플레이 무대에 돌려주면서 낙하 시작점·이동 열·실패 경계를 한눈에 구분하고, 기존 화면처럼 위험선이 피요를 가로지르거나 카드가 선 아래까지 내려가는 시각적 모순을 없애기 위함이다.
- 관련 PRD 섹션: F6b, §7.1, §11.1, §12
- 영향 범위: `FlowGameView`, 산성비 레인 레이아웃 테스트, 산성비 UI 회귀

## 2026-07-25 공식 덱별 리더보드와 주간 피요컵
- 결정: 2026-07-24의 게임 종류×입력 방식 클래식 리더보드 9개 결정을 대체한다. Game Center 제출은 콘텐츠·점수 조건을 동결한 번들 공식 덱 6개의 정확한 버전에서 수행한 흐름 60초·내장 키보드 기록으로 한정하고, 각 덱에 `piyokey.v1.deck.*.flow` 클래식 리더보드 하나를 둔다. 버전이 달라진 업데이트 덱, 동적 원격·사용자 덱, OS IME 및 다른 게임은 덱별 로컬 최고 기록만 유지한다.
- 결정: 게임 허브에 덱 설치 없이 번들 `official_daily_words` v2로 바로 시작하는 주간 피요컵을 둔다. 피요컵은 흐름 60초·내장 키보드로 고정하며 매주 초기화되는 `piyokey.v1.cup.weekly.flow` recurring 리더보드와 같은 콘텐츠의 클래식 덱 보드 양쪽에 점수를 제출한다.
- 결정: `GameRecord` 저장 스키마를 v2로 올려 `deck_version`과 경쟁 계약을 기록한다. 기존 v1 기록은 그대로 복구하되 경쟁 계약이 없으므로 서버에 소급 제출하지 않는다. 계정 전환 시 플레이어별 제출·순위·업적 캐시를 비우고, 중복 전송 중에는 같은 요청을 합쳐 오프라인 복귀와 다중 보드 제출을 안전하게 처리한다.
- 결정: 주간 회차 경계는 App Store Connect 설정과 같은 JST 월요일 00:00으로 계산한다. 주차가 바뀌면 주간 제출·순위 캐시를 비우고, 보충 제출에서는 현재 회차에 플레이한 기록만 주간 보드에 포함한다. 같은 기록의 덱 클래식 보드 제출은 장기 최고 기록이므로 주차와 무관하게 유지한다.
- 근거: 모든 덱에 영구 리더보드를 만들면 업데이트·사용자 콘텐츠 수명주기와 공정성을 관리하기 어렵다. 소수의 공식 콘텐츠와 입력 조건을 버전까지 고정하면 같은 문제군에서 경쟁한다는 의미가 명확하고, 주간 초기화 보드는 신규 사용자도 누적 최고점 격차 없이 반복 참여할 수 있다. 피요컵 점수를 영구 덱 보드에도 남기면 주간 도전과 장기 성취를 동시에 제공한다.
- 관련 PRD 섹션: F6, F6f, F12, §9~§12, §13 M6
- 영향 범위: `GameCenterService`, `GameRecord` 저장 스키마, 게임 허브·흐름 결과, 번들 공식 덱 계약, App Store Connect 리더보드 설정, ja/en/ko 로컬라이제이션, 단위·UI 테스트

## 2026-07-25 흐름 모드 한글 활자공방 재설계와 이동 방향 복원
- 결정: 2026-07-24의 궁궐 무대와 왼쪽→오른쪽 이동 결정을 대체한다. 흐름 모드의 교정지는 화면 오른쪽 밖에서 들어와 왼쪽의 금속 프레스 안으로 사라지며, 피요의 시선도 같은 오른쪽→왼쪽 경로를 추적한다. 닫기·남은 시간·점수·콤보를 둔 압축 HUD와 세션 중 설정·일반 FPS 오버레이를 숨기는 결정은 유지한다.
- 결정: 로컬 웹 버전 `hanco_web`의 `回転ハングル / Kaiten Hangul`에서 공정의 시각 문법을 참조해 정적 SwiftUI Canvas 무대를 한글 활자공방으로 재구성한다. 배경은 프레스 기둥·손잡이·목재 작업대·금속 레일·활자 서랍·인쇄물 더미로 구성하고, 목표 카드는 어두운 잉크와 붉은 도장 자국을 얹은 따뜻한 한지 교정지로 표현한다. 입력 진행도는 스크롤 가능한 금속 활자 블록으로 통일하며, 콤보 파티클도 하트 대신 붉은 잉크·황동 조각을 사용한다. 흐름 화면 하위에는 어두운 색상 환경을 적용해 내장 키보드도 금속 조판 콘솔과 맞추고, 상태 아이콘이 놓이는 최상단은 중간톤 한지 가장자리 색으로 분리해 밝은 앱 테마에서도 대비를 확보한다.
- 근거: 궁궐 장식은 게임 규칙과 직접 연결되지 않아 정적인 배경처럼 보였지만, 활자공방은 `자모 입력→활자 조판→교정지 이동→프레스 도착`을 한 화면의 작업 공정으로 설명한다. 오른쪽에서 왼쪽 프레스로 향하는 이동은 참조 게임의 원래 긴장 구조와 기계 배치를 일치시키고, 고정 Canvas와 별도 60Hz 전경은 장식 증가가 카드 이동 성능에 관여하지 않게 한다.
- 관련 PRD 섹션: F6, §7.1, §7.3, §11.1, §12
- 영향 범위: `FlowGameView`, 흐름 이동 레이아웃·UI 회귀 테스트, ja/en/ko 로컬라이제이션, M4 성능 프로브 실행 절차

## 2026-07-25 전 덱 gTTS 사전 생성 MP3 전환
- 결정: 사용자의 외부 전송 승인에 따라 기본·업데이트 카탈로그의 17개 공식 덱에서 쓰는 한국어 문구 184개를 gTTS 2.5.4의 `ko`, `tld=com`, 보통 속도로 사전 생성한다. 중복 문구는 기존 SHA-256 콘텐츠 주소를 공유하고, gTTS 원본 MP3를 재인코딩 없이 번들한다. 22.05kHz μ-law 변환을 제거해 합성 결과의 음질을 보존하며, 184개 MP3의 총 크기는 1,911,168 bytes(1.82MiB)다.
- 결정: 모든 `audio` 경로를 `.mp3`로 바꾸고 기존 설치에서도 새 자산을 선택하도록 각 덱 버전을 1씩 올린다. 기본 카탈로그는 v6, 업데이트 fixture는 v7로 올리며, 184개 새 파일의 CoreAudio 디코딩 검증이 모두 끝난 뒤 기존 Yuna CAF 188개와 그중 미참조 파일 4개를 제거한다. 파일 누락·손상·재생 실패 시 `AVSpeechSynthesizer(ko-KR)`로 대체하는 오프라인 재생 계약과 `.playback` + `.mixWithOthers` 정책은 유지한다.
- 결정: Game Center 고정 덱 6개와 주간 피요컵도 새 번들 덱 버전을 가리키게 한다. 이번 버전 상승은 발음 파일 경로만 바꾸고 흐름 모드의 한국어 문항·순서·채점 조건은 유지하므로 기존 `piyokey.v1.*` 리더보드 ID와 점수 계약을 재사용한다. 이후 문항이나 채점 조건이 바뀌는 버전은 기존 보드에 제출하지 않는다.
- 결정: gTTS 라이브러리는 MIT지만 문서화되지 않은 Google Translate 음성 기능을 사용하고 Google Cloud Text-to-Speech와 별개이므로 `content_rights_confirmed` 출시 게이트는 닫힌 상태로 유지한다. App Store 제출 전 생성 음원의 배포·콘텐츠 사용 권리를 확인하고, 확인할 수 없으면 공식 지원 TTS 제공자로 동일한 콘텐츠 주소의 음원을 재생성한다.
- 근거: 기존 macOS Yuna 파일럿과 μ-law 인코딩은 사용자가 전 덱에서 음질이 낮다고 평가했다. gTTS MP3를 직접 번들하면 런타임 네트워크 없이 기기 간 동일한 발음을 제공하고, 재인코딩 열화를 피하면서 기존 CAF보다 전체 용량도 줄어든다.
- 관련 PRD 섹션: F5, F11, §8.1, §14
- 영향 범위: 공식 카탈로그 생성기·fixture, `audio/` 번들, gTTS 생성기·의존성, `DeckLibraryTests`, App Store 콘텐츠 권리·TTS 실기기 게이트

## 2026-07-25 흐름 모드 초기 라이트 레인 복원
- 결정: 2026-07-24의 궁궐 무대와 2026-07-25의 한글 활자공방 시각 디자인을 대체한다. 흐름 모드는 초기 M4와 같은 앱 공통 핑크·라벤더 그라데이션, 흰 플레이 영역, 엷은 하단 컨베이어 띠·정적 도트만 사용한다. 궁궐·프레스·작업대·활자 서랍·인쇄물 더미와 공방 간판은 제거하고, 목표 카드·자모 진행·콤보 파티클도 앱 공통 라이트 스타일로 복원한다.
- 결정: 최상단 압축 HUD, 세션 중 설정·일반 FPS 오버레이 숨김, 오른쪽→왼쪽 카드 이동, 피요의 카드 추적·반응, 60Hz 전경 렌더링은 유지한다.
- 근거: 공방 배경의 장식 요소가 타이핑 대상·입력 진행·남은 시간보다 먼저 보여 플레이 중 시각적 복잡도를 높였다. 초기 레인의 단순한 대비로 돌아가면 흐름 규칙과 입력 피드백에 즉시 주의를 둘 수 있고, 정적 Canvas 장식을 제거해 프레임 예산도 줄인다.
- 관련 PRD 섹션: F6, §7.1, §7.3, §11.1, §12
- 영향 범위: `FlowGameView`, 흐름 모드 UI 회귀, 시각 기준 이미지, M4 성능 프로브 실행 절차

## 2026-07-25 플로우 전용 TOPIK 3단계 콘텐츠
- 결정: 게임과 연습·일반 덱의 결합을 단계적으로 낮추는 첫 대상으로 플로우를 선택한다. 플로우 진입 시 일반 설치 덱 목록보다 먼저 Beginner·Mid level·High level 3개 전용 세트를 2×2 그리드로 제공하고, 네 번째 칸은 `+` 덱 추가 카드로 둔다. 각 기본 세트는 중복 없는 30단어를 앱에 번들해 다운로드 없이 플레이하며 일반 발견 카탈로그·연습 목록에는 노출하지 않는다.
- 결정: 난이도는 국립국제교육원의 TOPIK 수준 구분과 등급별 수행 설명을 따른다. Beginner는 TOPIK I 1~2급의 친숙한 생활·공공시설 어휘, Mid level은 TOPIK II 3~4급의 공공·업무·사회 어휘, High level은 TOPIK II 5~6급의 정치·경제·사회·전문·추상 어휘로 구성한다. 고정 ID는 `flow_topik_beginner`·`flow_topik_intermediate`·`flow_topik_advanced`, 콘텐츠 버전은 v1이다.
- 결정: `+` 카드는 별도 모달 없이 발견 탭의 덱 검색으로 이동한다. 사용자가 발견에서 다운로드한 덱은 플로우 선택 화면의 `추가한 덱`에 나타나며 기존 점수·복습·결과 구조를 그대로 사용한다. 이번 변경은 플로우만 대상으로 하고 산성비·초성·단어·받아쓰기의 전용 콘텐츠 분리는 후속 작업으로 남긴다.
- 결정: 플로우 전용 세트는 음성 재생을 요구하지 않으므로 항목의 선택형 `audio`는 null로 둔다. 결과에서 복습으로 이어질 때는 기존 기기 TTS fallback을 사용하며, 별도의 비공식 음원을 추가하지 않는다.
- 근거: 모드 선택 직후 다시 일반 덱을 고르게 하면 게임의 규칙과 난이도에 맞는 첫 경험을 보장하기 어렵다. 고정된 3단계 세트는 사용자가 덱 구조를 이해하지 않아도 바로 시작하게 하고, `+`와 추가한 덱 영역은 기존 콘텐츠 자유도를 잃지 않게 한다.
- 관련 PRD 섹션: F5, F6, §8, §9, §11, §13 M6
- 영향 범위: 플로우 전용 JSON 생성기·번들 로더, 게임 덱 선택 화면, ja/en/ko 로컬라이제이션, 콘텐츠 스키마·단위·UI 테스트

## 2026-07-25 산성비 전용 TOPIK 3단계 콘텐츠
- 결정: 산성비 진입 시 설치 덱보다 먼저 Beginner·Mid level·High level 3개 전용 세트와 `+` 덱 추가 카드를 플로우와 같은 2×2 선택 구조로 제공한다. 각 세트는 중복 없는 30단어를 앱에 번들하며 고정 ID는 `acid_rain_topik_beginner`·`acid_rain_topik_intermediate`·`acid_rain_topik_advanced`, 콘텐츠 버전은 v1이다.
- 결정: TOPIK 주제 구분은 플로우와 같게 유지하되 낙하 중 단어를 인지하고 입력하는 게임 문법에 맞춰 단어 길이를 별도로 조정한다. Beginner는 1~3음절 생활·장소·자연어, Mid level은 2~3음절 공공·업무·사회어, High level은 4~6음절 사회·경제·정책·전문 복합어로 구성하며 단계별 평균 글자 길이가 증가하도록 고정한다.
- 결정: `+`는 발견 탭의 덱 검색으로 이동하고 다운로드한 덱은 `추가한 덱`에 표시한다. 산성비 기본 세트는 일반 카탈로그·연습 목록과 플로우 전용 세트에서 분리하며, 결과·복습·입력 모드별 최고 기록은 기존 `acid_rain` 저장 구조를 사용한다.
- 결정: 전용 세트의 선택형 `audio`는 null로 두고 복습 전환 시 기존 기기 TTS fallback을 사용한다. 플로우·산성비가 공유하는 3단계 선택 UI와 번들 검증은 공통 프리셋 로더로 통합하되 게임별 ID·문구·점수 기록은 섞지 않는다.
- 근거: 산성비는 화면에서 아래로 움직이는 짧은 시간 안에 단어 형태를 파악해야 하므로 플로우의 추상 명사 목록을 그대로 재사용하는 것보다 초급의 짧은 단어에서 고급의 긴 복합어로 시각·입력 부담을 함께 높이는 구성이 게임 경험과 난이도를 더 잘 일치시킨다.
- 관련 PRD 섹션: F6, F6b, §8, §9, §11, §13 M6
- 영향 범위: 산성비 전용 JSON 생성기·번들 로더, 공통 게임 프리셋 선택 화면, ja/en/ko 로컬라이제이션, 콘텐츠 스키마·단위·UI 테스트

## 2026-07-25 단어 맞추기·초성·받아쓰기 전용 TOPIK 3단계 콘텐츠
- 결정: 단어 맞추기·초성 맞추기·받아쓰기에도 Beginner·Mid level·High level 전용 30단어 세트와 `+` 덱 검색 카드를 흐름·산성비와 같은 2×2 구조로 제공한다. 고정 ID는 각 게임의 `word_match`·`choseong`·`dictation` 접두사와 `topik_beginner`·`topik_intermediate`·`topik_advanced`를 조합하고 콘텐츠 버전은 v1로 둔다. 전용 세트는 일반 발견·연습 목록에 넣지 않으며 사용자가 받은 덱만 각 모드의 `추가한 덱`에 표시한다.
- 결정: 단어 맞추기 전용 세트는 30단어를 5개씩 6개 의미 주제군으로 배열하고, 4지선다 오답을 정답과 같은 5단어 주제군에서 우선 선택한다. 초급은 가족·음식·장소 등 친숙한 명사, 중급은 업무·행정·사회생활, 고급은 정책·경제·가치·분석처럼 의미가 가까운 추상어를 구분하게 한다.
- 결정: 초성 전용 세트는 초급에서 30개의 서로 다른 짧은 초성열, 중급에서 동일 초성열을 공유하는 15쌍과 자동 무료 뜻 힌트, 고급에서 긴 사회·전문 복합어 초성열을 제공한다. 받아쓰기는 초급의 명료한 기본어, 중급의 받침 포함 일상어, 고급의 연음·비음화·유음화·구개음화 등 표기와 발음이 달라지는 단어로 난이도를 높인다.
- 결정: 새 전용 세트 9개의 선택형 `audio`는 null로 두고 받아쓰기에서는 기존 `AVSpeechSynthesizer(ko-KR)` 오프라인 fallback을 필수 문제 음성으로 사용한다. 향후 배포 권리가 확인된 사전 생성 음원을 추가해도 동일 덱 ID의 버전만 올리고 게임 선택 계약은 유지한다.
- 근거: 각 게임은 요구하는 회상·변별·청취 능력이 다르므로 일반 덱 하나를 모든 모드에 재사용하는 것보다 규칙에 맞춘 첫 콘텐츠가 학습 곡선과 공정성을 더 안정적으로 만든다. 동시에 `+`와 추가 덱 영역을 유지하면 기존 카탈로그의 자유도와 재사용성도 보존된다.
- 관련 PRD 섹션: F6, F6a, F6c, F6d, §8, §9, §11, §13 M6
- 영향 범위: 게임 전용 JSON 생성기·번들 로더, 공통 게임 프리셋 선택 화면, 단어 맞추기 보기 생성기, ja/en/ko 로컬라이제이션, 콘텐츠 스키마·단위·UI 테스트

## 2026-07-25 흐름·산성비 점진 가속과 목숨 종료
- 결정: 흐름과 산성비는 3목숨을 공유하고 카드가 레인 끝까지 이탈할 때마다 1개를 차감한다. 세 번째 놓침은 남은 시간이 있어도 즉시 게임을 끝내며, 시간 만료와 목숨 소진을 결과 제목에서 구분한다.
- 결정: 이동 속도는 플레이 경과 0~50초 동안 시작 대비 1.0배에서 1.5배까지 선형으로 높인다. 진행 중인 카드의 속도는 고정하고 다음 카드가 시작될 때 새 속도를 적용해 위치가 순간 이동하지 않게 한다. 흐름과 산성비는 같은 가속 배율을 사용하되 기존의 모드·코스별 시작 이동 시간 차이는 유지한다.
- 결정: 두 모드의 피요는 이동 카드의 위치를 눈동자로 계속 추적하지만 몸체 포즈는 정면으로 고정한다. 좌우 카드 위치에 맞춘 3D yaw는 제거해 앱의 다른 화면과 같은 실루엣 비율을 유지한다.
- 결정: 3목숨과 점진 가속은 흐름 점수 조건을 바꾸므로 2026-07-25의 `piyokey.v1.*` 재사용 결정을 대체한다. 공식 덱 클래식 6개와 주간 피요컵을 `piyokey.v2.*` 신규 계약으로 분리하며 v1 보드에 새 규칙 점수를 제출하지 않는다.
- 근거: 제한시간만으로 끝나는 구조보다 놓침이 즉시 생존 압박으로 이어지고, 완주에 가까울수록 조금씩 빨라지는 이동이 두 게임의 핵심 문법을 강화한다. 카드 중간 속도 변경과 병아리 몸체 회전은 각각 이동 점프와 가로 눌림처럼 보일 수 있어 카드 경계·눈동자 추적으로 표현 범위를 제한한다.
- 관련 PRD 섹션: F6, F6b, §7.1, §7.3, §11.1, §12
- 영향 범위: `FlowGameViewModel`, `FlowGameView`, 공통 게임 결과, ja/en/ko 로컬라이제이션, 흐름·산성비 단위·UI 회귀, M4 성능 프로브

## 2026-07-25 산성비 시간차 동시 낙하와 입력 대상 우선순위
- 결정: 산성비는 앞 카드의 완료·이탈과 무관하게 해당 시점 카드 이동 시간의 42%를 기준으로 다음 카드를 생성하고, 생성 간격은 1.4~3.6초 범위와 이동 시간의 최대 72%를 함께 적용한다. 3개 레인을 순환하며 덱 항목 수 범위에서 최대 4장까지 동시에 유지한다.
- 결정: 입력 대상은 가장 먼저 내려와 위험선에 가까운 카드로 시작하고 한 단어를 입력하는 동안에는 다른 카드로 자동 전환하지 않는다. 대상 카드가 완성되거나 이탈하면 화면에 남은 카드 중 진행률이 가장 높은 카드를 선택하며, 강조 테두리와 입력 진행 바로 현재 대상을 구분한다. 대상이 아닌 카드도 독립적으로 계속 낙하하고 위험선 도달 시 각각 목숨을 차감한다.
- 근거: 카드 하나를 처리한 뒤에만 다음 카드를 생성하면 산성비 특유의 누적 압박이 생기지 않는다. 반대로 입력 도중 더 낮은 카드로 대상을 바꾸면 이미 입력한 자모가 갑자기 무효가 되므로, 현재 입력을 고정하면서 완료 직후 가장 위험한 카드로 이어지는 규칙이 예측 가능성과 긴장감을 함께 보존한다. 최대 4장과 레인 순환은 작은 화면의 판독성과 60fps 예산을 지킨다.
- 관련 PRD 섹션: F6b, §7.1, §7.3, §11.1, §12
- 영향 범위: `FlowGameViewModel`, `FlowGameView`, 산성비 생성·이탈·입력 대상 단위 테스트, 산성비 UI 회귀, M4 성능 게이트

## 2026-07-25 산성비 OS 키보드 자유 단어 입력
- 결정: 산성비에서 OS 한국어 IME를 사용하면 가장 위험선에 가까운 카드로 입력을 고정하지 않고, 현재 화면에 낙하 중인 모든 단어를 입력 후보로 판정한다. 조합 중인 텍스트가 어느 후보와 일치하기 시작하면 해당 카드의 자모 진행을 표시하고, 완성된 입력이 어떤 후보와든 일치하면 그 카드만 즉시 완료 처리한다. 같은 단어가 여러 장이면 위험선에 가장 가까운 카드를 우선 제거한다.
- 결정: 내장 키보드는 다음 자모 가이드와 오타 위치 판정이 한 목표를 요구하므로 기존의 가장 위험한 카드 고정 규칙을 유지한다. OS 키보드의 입력 대기 상태에서는 모든 카드에 동일한 후보 테두리를 표시하고 임의의 하단 카드를 목표처럼 강조하지 않으며, 입력이 후보와 일치한 뒤에만 선택 카드를 강하게 강조한다.
- 근거: OS IME는 사용자가 완성 텍스트를 직접 만들 수 있어 여러 낙하 단어를 동시에 비교한 뒤 원하는 순서로 제거하는 산성비 문법에 적합하다. 내장 키보드의 단계별 자모 안내까지 자유 후보 방식으로 바꾸면 다음 키 표시와 오타 피드백이 모호해지므로 입력 방식에 따라 규칙을 분리한다.
- 관련 PRD 섹션: F2a, F6b, §6.3, §11.1
- 영향 범위: `OSIMEInputPanel`, `FlowGameViewModel`, `FlowGameView`, ja/en/ko 로컬라이제이션, 산성비 OS IME 단위·UI 회귀

## 2026-07-25 게임 입력 상태 단순화와 목숨 손실 피드백 강화
- 결정: 흐름·산성비·초성 맞추기·받아쓰기의 키보드 위 입력 상태에서 조합 중인 한글을 별도 큰 텍스트로 표시하지 않는다. 카드·초성·음성 문제와 자모 진행 트랙이 이미 입력 맥락을 제공하므로 준비·정타·오타·완료 안내만 남겨 전체 폭 중앙에 정렬한다. OS IME의 실제 입력 수집용 필드는 계속 숨겨 유지한다.
- 결정: 흐름·산성비에서 카드 이탈은 일반 자모 오타와 구분한 `lifeLost` 전용 하강음, 오류 알림+강한 충격 햅틱, 0.6초 이내의 화면 흔들림·붉은 플래시·목숨 HUD 펄스로 알린다. 마지막 목숨은 같은 연출을 0.52초 보여 준 뒤 결과로 이동하며 연출 레이어는 터치 입력을 차단하지 않는다.
- 근거: 키보드 바로 위의 큰 조합 텍스트는 자모 트랙과 카드의 입력 진행을 반복해 상태 문구를 왼쪽으로 밀었다. 입력 피드백의 위계를 단순화하는 대신 실제 실패는 소리·시각·촉각을 함께 사용해 남은 생존 횟수의 감소를 즉시 인지하게 한다.
- 관련 PRD 섹션: F2, F2a, F6, F6a, F6b, F6d, §7.1, §7.3, §11.1
- 영향 범위: `FlowGameView`, `ChoseongTypingView`, `HancoSoundEngine`, 게임 UI·사운드 회귀 테스트

## 2026-07-25 게임 플레이 HUD 정렬 단순화
- 결정: 최신 게임 플레이 HUD의 닫기 버튼은 흰 원형 배경과 X 아이콘만 유지하고 중복으로 둘렀던 1pt 외곽선을 제거한다. 흐름·산성비의 남은 시간·점수·콤보·목숨 숫자와 초성·받아쓰기의 문제 수·점수·콤보 숫자는 아이콘 위치와 무관하게 각 지표 카드의 중앙축에 맞춘다.
- 근거: 원형 배경만으로도 닫기 버튼의 44pt 탭 영역과 형태가 충분히 구분되며, 아이콘 다음에 왼쪽 정렬된 숫자는 카드마다 자릿수와 라벨 폭이 달라 시각적 기준선이 흔들렸다.
- 관련 PRD 섹션: F6, F6a, F6b, F6d, §7.1
- 영향 범위: `FlowGameView`, `ChoseongTypingView`, 게임 UI 회귀 캡처

## 2026-07-25 초성 맞추기 입력·진행 피드백 고도화
- 결정: 2026-07-25의 게임 입력 상태 단순화 결정을 초성 맞추기에 한해 조정한다. 초성 화면은 일반 연습의 피요·원형 한글 조합 프리뷰·`入力中` 누적 텍스트를 한 흰 카드로 재사용하고, 기존 문제 카드 오른쪽 아래 피요는 제거한다. 받아쓰기와 흐름·산성비의 단순 입력 상태는 그대로 유지한다.
- 결정: 중앙 초성열은 정답 음절의 자모 범위에 연결한다. 입력 전 다음 음절은 보조색 배경·테두리와 짧은 스프링으로 강조하고, 한 음절의 전체 자모를 마치면 그 초성을 성공색으로 채운다. 공백은 단어 경계로만 유지하고 긴 초성열은 문제 카드 안에서 비례 축소한다.
- 결정: 노출된 일본어 뜻은 최소 17pt 굵기, 중앙 정렬, 최대 두 줄의 주황색 워드박스로 표시한다. 자동 무료 힌트와 사용자가 연 유료 힌트는 같은 시각 구조를 사용하되 기존 점수 차감 규칙은 바꾸지 않는다.
- 결정: 후속 사용자 피드백에 따라 일본어 뜻 워드박스는 모든 초성 문제에서 기본 ON으로 표시하고, 설정의 별도 `게임 표시 > 초성 게임 뜻 표시` 토글로 끌 수 있게 한다. 기본 노출은 점수를 차감하지 않는다. OFF에서는 힌트 버튼으로 열며 일반 문제는 기존 30점 차감을 적용하고 동일 초성 중복 문제는 필수 구분 단서이므로 수동으로 열어도 무료로 처리한다.
- 결정: 마지막 자모를 맞추면 중앙 초성열을 체크와 성공색을 가진 완성 한국어 단어로 교체해 1.35초 유지한 뒤 다음 문제 또는 결과로 자동 전환한다. 받아쓰기와 선택형 단어 맞추기의 0.65초 전환은 유지한다.
- 근거: 피요가 문제 카드 모서리에 떨어져 있으면 입력 피드백과 관계가 약하고, 진행 막대만으로는 현재 타이핑이 어느 초성 음절을 해결하는지 바로 연결하기 어렵다. 연습에서 익힌 조합 카드와 음절별 상태색을 결합하면 입력 결과·목표·캐릭터 반응을 한 시선 흐름으로 읽을 수 있으며, 큰 뜻 워드박스는 일본어 학습 단서를 실제 플레이 거리에서도 판독 가능하게 한다.
- 관련 PRD 섹션: F6, F6a, §7.1, §7.3, §11.1
- 영향 범위: `ChoseongTypingViewModel`, `ChoseongTypingView`, 일반 연습 조합 프리뷰의 모듈 내 재사용 범위, 초성 단위·UI 회귀, PRD v3.0

## 2026-07-25 받아쓰기 연습형 조합 입력 카드 적용
- 결정: 후속 사용자 피드백에 따라 2026-07-25의 게임 입력 상태 단순화와 초성 전용 예외 결정을 확장한다. 받아쓰기도 일반 연습 및 초성과 같은 피요·원형 한글 조합 프리뷰·`入力中` 누적 텍스트·준비/정타/오타/완료 피드백을 하나의 흰 입력 카드로 사용한다. 기존 받아쓰기 문제 카드 오른쪽 아래 피요와 입력 진행 막대는 제거한다.
- 결정: 입력 카드가 조합 중인 사용자의 입력을 표시하더라도 받아쓰기 문제의 정답·초성·일본어 뜻·읽기는 완료 전 시각·접근성 트리에 노출하지 않는다. 완료 뒤 다음 문제 전환 시간도 기존 0.65초를 유지한다.
- 근거: 음성 단서와 실제 입력 상태가 분리되어 있으면 사용자가 현재 조합된 글자와 오타 피드백을 확인하기 어렵다. 초성에서 검증한 입력 카드를 재사용하면 직접 타이핑 게임의 시각 문법이 일관되고, 문제 카드의 중복 피요를 옮겨 화면 밀도를 늘리지 않으면서 캐릭터 반응을 입력과 연결할 수 있다.
- 관련 PRD 섹션: F6, F6d, §7.1, §7.3, §11.1
- 영향 범위: `ChoseongTypingView`, 받아쓰기 UI 회귀, PRD v3.2

## 2026-07-26 게임 허브 받아쓰기·단어 맞추기 위치 교환
- 결정: 게임 허브의 2열 카드 순서를 `흐름·산성비 / 초성 맞추기·받아쓰기 / 단어 맞추기·띄어쓰기`로 고정한다. 게임 종류의 raw value와 저장된 기록 키는 유지하고 화면 열거 순서만 바꾼다.
- 근거: 사용자가 직접 입력하는 초성 맞추기와 받아쓰기를 같은 행에 묶고, 선택형 단어 맞추기는 띄어쓰기와 마지막 행에 배치해 허브 탐색 순서를 조정한다.
- 관련 PRD 섹션: S7, F6
- 영향 범위: `GameKind` 열거 순서, 게임 허브 UI 회귀, PRD v3.3

## 2026-07-26 게임 전용 세트 100단어·타이핑 난이도·무작위 출제
- 결정: 2026-07-25의 게임별 TOPIK 3단계 콘텐츠 결정을 대체한다. 흐름·산성비·초성 맞추기·단어 맞추기·받아쓰기의 Beginner·Mid level·High level 세트를 각각 중복 없는 100단어로 확대하고 콘텐츠 버전을 v2로 올린다. 저장된 최고 기록과 화면 접근성 ID 호환을 위해 기존 `*_topik_beginner`·`*_topik_intermediate`·`*_topik_advanced` 덱 ID는 레거시 계약으로 유지하되, 사용자 문구·태그·로더 검증에서는 TOPIK 기준을 제거한다.
- 결정: 난이도는 한국어 음절을 두벌식으로 입력할 때 필요한 키 수를 기본 점수로 삼고 음절 수·받침 수·복합 모음·복합 받침·쌍자음 부담을 함께 반영한다. 초급은 1~2음절·받침 최대 1개, 고급은 3음절 이상 또는 받침 2개 이상을 생성기 불변식으로 검증하며 중급은 두 구간 사이의 입력량을 사용한다. 어휘 주제나 시험 등급이 어려워도 입력량이 짧으면 초·중급에, 친숙한 단어여도 자모열과 받침이 복잡하면 고급에 놓일 수 있다.
- 결정: 번들 전용 세트는 플레이 화면 진입과 결과 화면의 재도전마다 시스템 난수로 새 순서 또는 새 10문제 표본을 만든다. 흐름·산성비는 100단어 전체 순서를 다시 섞고, 초성·단어 맞추기·받아쓰기는 새 시드로 문제와 보기를 다시 구성한다. 단위/UI 테스트에서는 명시적 시드와 기존 UI 테스트 환경만 결정적 순서를 사용해 회귀를 재현한다. 사용자가 추가한 일반 덱은 이번 무작위화 계약의 대상이 아니다.
- 근거: 시험 주제 수준은 실제 한글 타이핑에서 필요한 손가락 이동과 입력 길이를 직접 설명하지 못하고, 30단어 고정 순서는 반복 플레이에서 다음 문제를 쉽게 예측하게 했다. 입력 구조 기반 100단어와 세션별 셔플은 단계별 체감 난이도와 반복 플레이의 변화를 동시에 높인다.
- 관련 PRD 섹션: F6, F6a, F6b, F6c, F6d, §8, §9, §13 M6
- 영향 범위: 게임 전용 JSON 생성기·번들 콘텐츠 v2, 프리셋 로더·선택 화면, 흐름·산성비·초성·단어 맞추기·받아쓰기 세션/재도전 상태, ja/en/ko 로컬라이제이션, 단위·UI 테스트

## 2026-07-26 상황별 재미 표현 공식 덱 8종
- 결정: 기존 공식 17덱에 여행지 한마디·카페/맛집·연인과의 갈등·화해·첫 데이트·채팅 플러팅·친구 리액션·주말 모임의 상황별 재미 표현 8덱을 추가한다. 각 덱은 공백 포함 10자 이하의 짧은 한국어 회화 12개와 일본어 읽기·직역을 제공하며 신규 덱 버전은 v1, 기본/업데이트 카탈로그는 각각 v7/v8로 올린다.
- 결정: 연애·갈등 표현은 상대를 조종하거나 위협하는 문구 대신 연락·감정·경청·존중·사과·화해를 직접 말하는 표현으로 구성한다. 플러팅 표현도 상대의 선택을 전제로 한 질문과 가벼운 호감 전달 범위로 제한한다.
- 결정: 신규 96문장의 오프라인 발음은 덱 문구를 외부 TTS에 전송하지 않고 macOS 내장 `Yuna` 한국어 음성의 IMA4 CAF로 생성한다. 기존 17덱의 gTTS MP3와 경로·생성기를 분리하고 두 형식 모두 CoreAudio로 검증한다.
- 근거: 실제 여행·연애·친구 대화처럼 사용 장면이 분명한 표현은 반복 타이핑의 재미와 검색 목적성을 높인다. 갈등과 데이팅 소재의 오락성은 유지하되 그대로 따라 말해도 관계를 해치지 않는 짧은 문구로 편집할 필요가 있다.
- 관련 PRD 섹션: F5, F5.7, §8.1~8.3, §11
- 영향 범위: 공식 카탈로그 생성기, 기본·업데이트 덱 JSON, 오프라인 발음 에셋, 발견/DeckKit 카탈로그 회귀, PRD v3.6

## 2026-07-26 게임 결과 모드·레벨 표기와 이미지 저장·SNS 공유
- 결정: 흐름·산성비·초성 맞추기·단어 맞추기·받아쓰기의 공통 결과 헤더에서 덱 이름만 보여 주던 구조를 게임명과 레벨 칩으로 바꾼다. 번들 프리셋은 기존 덱 ID로 Beginner·Mid level·High level을 판별해 현지화하고, 사용자가 추가한 덱은 레벨을 임의 추정하지 않고 덱 이름을 표시한다. 주간 피요컵은 게임명과 주간 배지를 사용한다.
- 결정: 공유 이미지에도 같은 게임명·레벨 정보를 넣어 점수만 캡처된 이미지가 어떤 플레이 결과인지 식별할 수 있게 한다. 결과 액션은 `이미지 저장`과 `SNS로 공유`로 분리하되 두 경로 모두 같은 1,200×1,200 PNG 렌더러를 사용한다.
- 결정: 이미지 저장은 iOS 사진 라이브러리의 추가 전용 권한을 버튼 탭 시 요청하고, SNS 공유는 이미지와 현지화 캡션을 `UIActivityViewController`에 전달한다. 특정 SNS SDK나 계정 연동은 추가하지 않아 X·Threads 등 설치된 공유 확장을 OS가 노출하도록 한다.
- 근거: 덱명이 `초급`처럼 레벨만 나타내면 결과에서 플레이한 게임을 알기 어렵고, OS 공유 시트 안의 저장 동작만으로는 결과 이미지를 기기에 보관할 수 있다는 사실이 드러나지 않는다. 게임 맥락과 두 개의 명시적 후속 액션을 함께 표시하면 결과 확인·보관·공유의 목적이 분명해진다.
- 관련 PRD 섹션: F9, F12.1, F12.5, §10
- 영향 범위: `FlowGameResultView`, `SessionShareCard`, 사진 추가 권한 문구, ja/en/ko 로컬라이제이션, 결과·공유 단위/UI 회귀

## 2026-07-26 최애 아이돌 라이브 댓글 공식 덱
- 결정: 상황별 재미 표현에 `official_fun_idol_live_comments` v1을 추가한다. 일본어 덱명은 `推しのライブ配信コメント`, 태그는 `K-POP`·`推し活`·`ライブ`·`公式`로 두고, 공백 포함 10자 이하의 창작 한국어 댓글 12개와 일본어 읽기·직역을 제공한다. 기본/업데이트 카탈로그는 각각 v8/v9로 올린다.
- 결정: 댓글은 특정 아이돌·곡·실제 라이브 발언을 인용하지 않고 목소리·착장·노래 요청·댓글 읽기·하트·건강·응원·다음 방송 약속을 다룬다. 신규 발음은 기존 재미 덱과 같이 외부 전송 없는 macOS `Yuna` IMA4 CAF로 생성한다.
- 근거: 라이브 채팅은 팬 학습자가 짧은 한국어를 즉시 쓰는 대표 상황이며, 댓글 길이 제한과 반복되는 응원 문법이 타이핑 연습에 잘 맞는다. 고유명사와 실제 콘텐츠 인용을 피하면 공식 카탈로그의 권리 정책도 유지할 수 있다.
- 관련 PRD 섹션: F5, F5.7, §8.1~8.3, §11
- 영향 범위: 공식 카탈로그 생성기, 기본·업데이트 덱 JSON, 오프라인 발음 에셋, 발견/DeckKit 카탈로그 회귀, PRD v3.7

## 2026-07-26 App Store 1.0 제출 계약 확정
- 결정: iOS 앱의 최종 Bundle ID를 `app.piyokey.Piyokey`, 마케팅 버전과 빌드 번호를 `1.0 (1)`, 실제 출시 계정의 Apple Developer Team을 `X44BQNTAH9`로 고정한다. App Store 기본 언어는 일본어, 1차 판매 지역은 일본, 기본 카테고리는 Education, 보조 카테고리는 Games/Word, 가격은 무료, 출시는 수동으로 둔다.
- 결정: 공개 마케팅·개인정보처리방침·지원 주소를 각각 `https://hancoweb.vercel.app/`, `/privacy`, `/support`로 고정하고 개인정보처리방침과 지원 링크는 앱 설정 화면에서도 연다. 스토어 메타데이터는 ja/en-US/ko로 준비하되 1차 제출 스크린샷은 일본어 6.9형 1320×2868 실제 RC 화면 8장을 사용한다.
- 결정: 제출 가능 상태는 `release/app_store_submission.json`의 수동 게이트와 `tools/release_preflight.py --strict`가 모두 통과한 경우로 한정한다. gTTS·macOS 합성 음원은 별도 권리 기록과 운영자 승인이 없으면 콘텐츠 권리 게이트를 닫아 두며, App Store Distribution archive와 동일 빌드를 TestFlight 실기기 QA 뒤에 제출한다.
- 근거: App Store의 앱 레코드·서명·심사 메타데이터·스크린샷·권리 답변과 실제 바이너리가 서로 다른 상태로 제출되는 것을 막고, 일본어 우선 출시 범위를 재현 가능한 단일 계약으로 관리하기 위함이다.
- 관련 PRD 섹션: F1, F2a, F5, F6, F9~F12, §8, §11, §13 M6, §14
- 영향 범위: Xcode 프로젝트 서명 설정, 앱 설정·로컬라이제이션, `release/` 제출 문서와 이미지, 출시 preflight, App Store Connect, TestFlight·실기기 QA

## 2026-07-26 Game Center 게임×난이도 15개 계약
- 결정: 2026-07-25의 번들 공식 덱 6개 흐름 리더보드 결정을 대체한다. Game Center 클래식 리더보드는 흐름·산성비·초성 맞추기·단어 맞추기·받아쓰기의 번들 전용 100단어 세트 v2에 초급·중급·고급별로 하나씩, 총 15개를 둔다. ID는 `piyokey.v2.<game>.<beginner|intermediate|advanced>`로 고정하고 게임 종류나 난이도가 다른 점수는 섞지 않는다.
- 결정: 클래식 보드는 Best Score·High to Low·정수 0~1,000,000이며 내장 키보드 기록만 제출한다. 주간 피요컵은 `flow_topik_beginner` v2를 사용해 `piyokey.v2.cup.weekly.flow` recurring 보드와 `piyokey.v2.flow.beginner` 클래식 보드 양쪽에 제출한다.
- 근거: 출시 게임 허브의 일차 선택 단위가 기존 일반 덱이 아니라 게임별 Beginner·Mid level·High level 전용 세트로 바뀌었으므로, 예전 6개 일반 덱 리더보드는 실제 플레이 진입 구조와 일치하지 않는다. 게임별 채점 규칙도 서로 달라 모드까지 분리해야 순위가 비교 가능하다.
- 관련 PRD 섹션: F6, F6a~F6d, §11.3, §13 M6
- 영향 범위: `GameCenterService`, 주간 피요컵 번들 로더, Game Center 단위 테스트, `release/GAME_CENTER_SETUP.md`, App Store Connect 리더보드 15개

## 2026-07-26 게임 전용 100단어의 실용 TOPIK·타이핑 복합 난이도
- 결정: 2026-07-26의 타이핑 난이도 단독 분류 결정을 대체한다. 기존 게임별 TOPIK 큐레이션을 의미 난이도의 기준으로 복원하고, 초급은 일상 핵심·기본 동작, 중급은 생활·업무·공공 서비스, 고급은 뉴스·사회·경제·전문 상황에서 실제 사용할 수 있는 어휘를 먼저 선정한다. 발음 규칙 확인에만 유용한 저빈도 단어와 동식물 예시는 핵심 실용어·생활어로 교체한다.
- 결정: 의미 단계가 확정된 뒤 두벌식 키 수·음절·받침·복합 모음/받침·쌍자음 부담으로 각 100단어를 정렬한다. 초급은 가벼운 입력 70개 이상, 고급은 3음절 이상 또는 받침 2개 이상의 복잡한 입력 80개 이상을 유지하고, 단계별 복잡 어휘 수와 평균 입력 점수는 모두 단조 증가해야 한다. 일상성이 높은 긴 기본어는 초급에 포함할 수 있고 짧지만 추상적인 고급어도 고급 의미 단계에 남길 수 있다.
- 결정: 흐름·산성비·초성 맞추기·단어 맞추기·받아쓰기는 같은 3단계 실용 어휘 코어를 공유해 게임을 바꿔도 학습 전이가 일어나게 하되, 각 모드의 덱 ID·점수·무작위 출제 계약은 분리한다. 세 단계 사이에는 단어를 중복하지 않으며 각 덱에는 `TOPIK`과 `タイピング` 태그를 모두 넣는다.
- 결정: 단어 구성이 바뀌어 기존 점수와 직접 비교할 수 없으므로 번들 콘텐츠를 v3로 올리고 15개 클래식 리더보드와 주간 피요컵을 `piyokey.v3.*` 신규 계약으로 분리한다. v2 점수는 v3 보드에 제출하지 않는다.
- 근거: 입력 구조만으로 300단어를 나누면 타이핑 체감은 선명하지만 학습자가 일상에서 다시 쓸 가능성이 낮은 어휘가 과도하게 포함된다. 의미 수준과 입력 부담을 순차적으로 결합하면 실용성과 게임 난이도를 동시에 유지할 수 있다.
- 관련 PRD 섹션: F6, F6a~F6d, §8, §9, §11.3, §13 M6
- 영향 범위: 게임 전용 JSON 생성기·번들 콘텐츠 v3, 프리셋 로더, Game Center v3 계약, ja/en/ko 선택 문구, 단위·UI 테스트, PRD v3.8

## 2026-07-27 출시 회귀의 UI 자동화 변동 2건 승인
- 결정: `testFlowKeepsLongJamoTrackAndKeyboardInsideScreen`과 `testIdlePiyoReactsToTouchAndLongPressOpensCloset`의 시뮬레이터 자동화 실패는 실제 기능 회귀가 없는 알려진 UI 테스트 변동으로 승인하고 1.0 출시 차단 항목에서 제외한다. 최신 RC 전체 회귀에서는 두 케이스를 명시적으로 제외하고 나머지 단위·UI 테스트를 실행하며, 긴 자모 트랙의 화면 내 배치와 마스코트 탭/길게 누르기 동작은 수동 확인 대상으로 기록한다.
- 근거: 긴 자모 트랙 케이스는 전체 실행에서 30초 게임 시간이 먼저 끝난 뒤 키 입력을 시도해 실패했고 독립 재실행에서는 통과했다. 마스코트 케이스는 SwiftUI 접근성 값의 짧은 반응 상태를 XCTest가 안정적으로 관찰하지 못하지만 사용자가 실제 탭 반응에 문제가 없음을 확인하고 알려진 이슈로 진행하도록 승인했다.
- 관련 PRD 섹션: §7.1, §12, §13 M6
- 영향 범위: App Store 출시 QA, iOS 전체 회귀 판정, TestFlight 수동 스모크

## 2026-07-27 부화 완료 후 메인 앱 6단계 튜토리얼
- 결정: 세 번째 부화 미션과 최종 성장 연출이 끝나면 연습 탭이 아니라 홈 탭으로 이동한다. 이어서 홈의 오늘 학습 CTA, 둘러보기 검색, 연습 커리큘럼, 게임 모드, 마이페이지의 피요·성장 기록, 공통 설정 버튼 순서로 6단계 튜토리얼을 1회 제공한다.
- 결정: 각 단계는 해당 탭으로 자동 이동하고 실제 대상 프레임을 둥근 스포트라이트로 남기며 나머지 화면과 입력을 딤 처리한다. 하단 진행 표시와 다음 버튼, 좌측 상단 건너뛰기를 제공하고 완료·건너뛰기 모두 로컬 상태로 저장한다. 마지막 단계 뒤에는 다시 홈으로 돌아간다.
- 근거: 첫 홈에서 가장 짧은 학습 시작점을 먼저 이해한 뒤 콘텐츠 발견·정규 연습·게임·성장 확인 순으로 앱의 학습 루프를 보여 주는 것이 기능 나열보다 행동 흐름을 만들기 쉽다. 설정은 모든 탭에 공통이므로 마지막에 한 번만 설명하고, 튜토리얼이 실제 콘텐츠 진입을 유발하지 않도록 강조 대상의 탭 입력은 차단한다.
- 관련 PRD 섹션: F1, S1, §7.1, §7.3
- 영향 범위: `AppRootView`, 홈·둘러보기·연습·게임·마이페이지 강조 대상, 온보딩 로컬 상태, ja/en/ko 로컬라이제이션, 온보딩 UI 회귀, PRD v4.0

## 2026-07-27 음영 키캡 브랜드 이미지 통일
- 결정: 기존 납작한 병아리 원본에서 흰색 `ㅎ` 키캡의 측면·하단 음영이 강화된 사용자 제공 이미지로 공용 브랜드 원본을 교체한다. 640×640 입력은 기존 렌더러로 sRGB 1024×1024 무알파 PNG로 정규화하고, 같은 바이트를 AppIcon과 앱 내부 `PiyokeyLogo` 이미지 세트에 사용한다.
- 결정: `PiyokeyLogoMark`의 별도 Swift 도형 마크를 제거하고 AppIcon과 동일한 이미지 에셋을 공유 카드 등 모든 내부 로고 영역에서 사용한다. Game Center 업적 이미지도 같은 새 원본으로 교체하되 성장·이벤트 반응형 런타임 병아리는 변경하지 않는다.
- 근거: 기존 평면 키캡은 작은 크기에서 키보드 키로 인식되기 어려웠다. 흰색 키캡의 입체 음영을 유지한 단일 원본을 모든 브랜드 표면에 적용하면 타이핑 앱 정체성과 로고 일관성이 함께 높아진다.
- 관련 PRD 섹션: F9, §7.1, §12, §14
- 영향 범위: `shared/brand/`, `AppIcon.appiconset`, `PiyokeyLogo.imageset`, `PiyokeyLogoMark`, 공유 카드, Game Center 업적 이미지, 출시 preflight, PRD v4.1

## 2026-07-27 연습·게임 세션 중 피요 복장 고정
- 결정: 모든 연습·게임 플레이 피요는 옷장의 `자동`·`없음`·고정 아이템 선택을 그대로 존중한다. `자동` 선택은 덱 태그 소품을 장착하지 않고 성장 단계의 기본 외형만 사용하며, TOPIK 카드 인덱스에 따라 안경과 졸업모를 교대하던 연출을 포함한 문제별 소품 재계산을 제거한다. 입력에 따른 표정·시선·동작 반응은 유지한다.
- 결정: `TOPIK 안경`은 TOPIK 태그 덱 설치만으로 해금하지 않는다. 흐름·산성비·초성 맞추기·단어 맞추기·받아쓰기 중 TOPIK 태그 게임의 결과 점수가 0점 이상이면 영구 해금하고, 사용자가 옷장에서 직접 고정 장착할 수 있게 한다. 기존에 이미 영구 해금된 안경은 회수하지 않는다.
- 근거: 문제 전환 때 캐릭터 복장이 바뀌면 입력 피드백보다 외형 변화에 시선이 분산되고 사용자가 선택한 캐릭터 설정이 덮인 것으로 오해할 수 있다.
- 관련 PRD 섹션: S5, F6, F6a~F6d, §7.3
- 영향 범위: `MascotSessionAppearance`, `MascotCompanionLibrary`, `PracticeView`, `FlowGameView`, `ChoseongQuizView`, `FlowGameResultView`, 옷장 해금 문구, 마스코트 단위 회귀, PRD v4.4

## 2026-07-27 단어 퀴즈를 한글 직접 타이핑으로 전환
- 결정: 2026-07-23의 단어 맞추기 선택형 결정을 대체한다. 일본어 뜻은 중앙 문제 카드의 필수 단서로 유지하되 한국어 4지선다 보기를 제거하고, 사용자가 내장 두벌식 또는 OS 한국어 IME로 정답 한글 전체를 직접 입력한다. 뜻이 있고 조합 엔진으로 분해 가능한 고유 항목이 하나라도 있으면 덱을 시작할 수 있다.
- 결정: 플레이 화면과 판정은 초성 맞추기·받아쓰기의 공통 직접입력 프레임을 사용한다. 상단 HUD, 뜻 워드박스, 피요·원형 조합 프리뷰·누적 입력·정타/오타/완료 피드백 카드, 하단 고정 키보드를 배치하며 오타 자모는 입력 문자열에 반영하지 않는다. 성공 뒤 0.65초 자동 전환, 복습 수집·노미스 졸업, 자모 정확도, 내장/OS 기록 분리를 적용한다.
- 결정: 최초 시작·재도전·백그라운드 복귀는 3·2·1 카운트다운 뒤 유효 문제 시간을 시작한다. 기존 `word_match` 덱 ID·로컬 진행 기록은 유지하되, 선택형 점수와 직접입력 점수를 섞지 않도록 Game Center는 `piyokey.v4.word_match.<level>` 신규 계약으로 분리한다.
- 근거: 의미를 보고 정답을 고르는 것보다 직접 한글을 조합하는 흐름이 앱의 핵심 학습 가치와 다른 타이핑 게임의 조작 문법에 일치한다. 선택지 제거로 우연 정답을 없애고 뜻 회상과 자모 입력을 한 문제 안에서 함께 연습할 수 있다.
- 관련 PRD 섹션: F2a, F6, F6c, §6.3, §7.2~7.3
- 영향 범위: `WordMatchTypingBuilder`, `ChoseongTypingViewModel`, `ChoseongTypingView`, 게임 선택 가능 조건, 결과 입력 방식, ja/en/ko 로컬라이제이션, 단위·UI 회귀, PRD v4.5

## 2026-07-27 게임 상단 HUD 긴 수치 충돌 방지
- 결정: 흐름·산성비·초성 맞추기·단어 맞추기·받아쓰기의 압축 HUD에서 아이콘 폭과 수치 폭을 분리한다. 수치는 표시 문자열 길이에 따라 기본 글자 크기를 단계적으로 줄이고, 기기·현지화에 따른 실제 남은 폭이 더 좁으면 한 줄 안에서 추가 축소한다. 짧은 수치는 기존 중앙축을 유지하며 라벨도 한 줄 가변 축소한다.
- 근거: 점수가 네 자리 이상으로 늘어날 때 중앙 정렬 수치가 왼쪽 아이콘 영역까지 사용해 별 아이콘과 숫자가 겹쳐 보였다. 아이콘 양쪽에 대칭 안전 영역을 두면 카드 중앙 정렬을 유지하면서도 수치가 아이콘이나 인접 카드로 침범하지 않는다.
- 관련 PRD 섹션: F6, F6a~F6d
- 영향 범위: `FlowGameView`, `ChoseongTypingView`, 공통 HUD 수치 레이아웃, 게임 UI·단위 회귀, PRD v4.6

## 2026-08-01 플로우 점진 가속 체감 강화
- 결정: 2026-07-25의 흐름·산성비 공용 1.5배 상한을 모드별로 분리한다. 플로우는 플레이 경과 0~50초 동안 시작 대비 1.0배에서 1.8배까지 선형 가속하고, 산성비는 기존 1.0배에서 1.5배 상한을 유지한다. 진행 중인 카드의 이동 시간은 바꾸지 않고 다음 카드가 시작될 때 해당 시점의 배율을 적용한다.
- 결정: 플로우의 생존·점수 조건이 달라졌으므로 기존 `piyokey.v3.flow.*`와 `piyokey.v3.cup.weekly.flow` 기록을 재사용하지 않는다. 다음 빌드는 `piyokey.v4.flow.<beginner|intermediate|advanced>`와 `piyokey.v4.cup.weekly.flow`에 제출하며, App Store Connect의 새 보드 생성은 다음 빌드 업로드 전 출시 게이트로 둔다. 산성비 등 규칙이 바뀌지 않은 게임의 계약은 유지한다.
- 근거: 기존 선형 가속은 10초 1.1배·25초 1.25배여서 카드 단위 적용과 긴 기본 이동 시간이 겹치면 중반까지 속도 차이가 잘 느껴지지 않았다. 1.8배 상향은 10초 1.16배·25초 1.4배·50초 1.8배로 후반 압박을 분명히 하면서도 카드 도중의 순간 이동과 산성비 난이도 상승을 피한다.
- 관련 PRD 섹션: F6, F6b, §11.3, §12
- 영향 범위: `FlowGamePacing`, `FlowGameViewModel`, 플로우·산성비 단위 테스트, `GameCenterLeaderboard`, 출시 Game Center 설정 문서, PRD v4.7

## 2026-08-01 정적 덱 로컬 백오피스 도입
- 결정: 신규 덱 생성과 기존 덱 텍스트 수정을 위한 운영 도구는 로컬 루프백에서만 실행되는 브라우저 편집기로 제공한다. 앱·원격 카탈로그에는 쓰기 API, 계정, 업로드 경로를 추가하지 않고 저장소의 `shared/mock_catalog/` 정적 JSON만 운영자가 명시적으로 저장할 때 갱신한다.
- 결정: 저장 시 덱 파일과 카탈로그 미리보기·바이트 크기·태그 집계를 함께 갱신하고, 전체 덱 원본을 `shared/deck_overrides/main/`에 기록한다. 공식 카탈로그 생성기는 내장 스펙 생성 뒤 동일 `deck_id`의 override를 적용하고 신규 override는 추가해 편집 결과가 재생성에서 사라지지 않게 한다.
- 결정: 편집기 저장 게이트는 PRD §8.1의 JSON 필드 제한과 DeckKit의 ID·날짜 순서·한국어 10자·조합 가능 문자·중복 ID 규칙을 미러링한다. `audio: null`은 저장 가능 경고로 두고 앱의 기기 TTS 대체 계약을 유지한다.
- 근거: 운영자가 Python 생성기와 JSON 여러 파일을 직접 맞춰 수정하면 카탈로그 메타데이터 불일치와 재생성 시 유실 가능성이 크다. 로컬 도구와 생성기 override를 결합하면 앱의 읽기 전용·무계정 구조를 바꾸지 않고 콘텐츠 편집과 검수 반복을 단축할 수 있다.
- 관련 PRD 섹션: §2.3, F5, §8.1~8.3, §13 M6
- 영향 범위: `tools/deck_backoffice/`, `shared/deck_overrides/`, 공식 카탈로그 생성기·검증 테스트

## 2026-08-01 덱 텍스트 AI 보조와 로컬 음성 생성
- 결정: 덱 항목의 한국어를 운영자가 명시적으로 선택했을 때만 OpenAI Responses API로 전송해 가타카나 읽기와 간결한 일본어 뜻을 구조화 출력으로 생성한다. 요청은 `store: false`이며 API 키는 로컬 서버의 `.env` 또는 프로세스 환경에서만 읽고 브라우저에는 전달하지 않는다.
- 결정: 기본 모델은 짧고 반복적인 구조화 추출 역할에 맞춘 `gpt-5.6-luna`와 `reasoning.effort: none`으로 두되 `PIYOKEY_OPENAI_MODEL`에서 교체할 수 있게 한다. 항목별 생성은 미리보기에서 선택한 값만 적용하고, 일괄 생성은 한 번에 최대 25개씩 기존 값이 비어 있는 필드만 채운다.
- 결정: 한국어 오디오는 외부 API로 보내지 않고 기존 콘텐츠 주소 규칙과 macOS `Yuna` 음성을 사용해 IMA4 CAF로 로컬 생성한다. 생성 파일은 CoreAudio 검증 뒤 원자적으로 배치하고 같은 한국어는 재사용한다.
- 근거: 읽기·뜻 초안 생성을 자동화하면 반복 입력을 줄일 수 있지만 운영 검수 없이 기존 값을 덮어쓰면 학습 데이터 오류가 퍼질 수 있다. 텍스트만 명시적 외부 전송하고 음원은 로컬 생성하면 편집 속도·검수 가능성·콘텐츠 외부 전송 최소화를 함께 유지할 수 있다.
- 관련 PRD 섹션: F5, F11, §8.1~8.3, §13 M6
- 영향 범위: Deck Studio 로컬 서버·항목 편집 UI·로컬 오디오 에셋·운영 설정과 단위 테스트

## 2026-08-02 일본어 App Preview 실제 플레이 중심 구성
- 결정: 일본어 iPhone App Preview는 별도 로고 인트로나 기기 목업 없이 실제 RC 화면만 사용한다. 27초 안에 `사랑해요` 연습 완성, 6개 게임 허브, 플로우 콤보 5, 산성비 카드 제거, 126점·정확도 100% 결과, 성장한 피요가 보이는 홈을 순서대로 연결하고 움직이는 화면 위에만 짧은 일본어 설명을 둔다.
- 결정: 플로우·산성비 플레이에는 실제로 달성 가능한 병아리 성장 상태를 사용하고 카드 추적·성공 반응을 함께 보여 준다. 촬영 자동화의 짧은 결과 세션에서만 `DEBUG` 환경 변수로 정타 보너스 시간을 0으로 만들며 배포 빌드의 기본 2초 보너스와 게임 규칙은 바꾸지 않는다.
- 결정: 출력은 세로 886×1920, H.264 High Profile, 30fps, 15~30초 계약으로 검증하고 자동 재생 음소거에서도 이해되도록 무음으로 구성한다. 사용자가 영상 시안을 확인하기 전에는 App Store Connect에 업로드하지 않는다.
- 근거: 기존 단일 테스트 영상은 메뉴 대기 시간이 길고 한 단어만 입력해 병아리·타이핑·게임의 핵심 가치와 실제 성취감을 전달하지 못했다. 성공 순간과 결과를 짧은 실제 플레이 클립으로 분리하면 Apple의 실제 앱 화면 원칙을 지키면서도 스토어 전환용 메시지를 선명하게 전달할 수 있다.
- 관련 PRD 섹션: F3, F6, F6b, F8, §7.2~7.3, §13 M6
- 영향 범위: App Preview 촬영 UI 테스트, `FlowGameView` DEBUG 테스트 훅, `tools/compose_app_preview.swift`, `release/app-preview/ja/`, App Store Connect 미디어

## 2026-08-03 App Store 스크린샷 진행 상태·피요 일관성
- 결정: 일본어 App Store 6.9형 스크린샷 8장은 동일한 성장한 피요 상태와 실제 진행 데이터를 사용해 미션 입력·플로우·출석·산성비·단어 맞추기·초성·데일리 미션·보상을 진행 중인 순간으로 보여 준다. 알·0점·0콤보·0/7·전부 잠김 같은 초기·빈 상태는 사용하지 않는다.
- 근거: 실제 행동과 성취가 보이는 연속 장면은 기능 목록보다 학습 루프를 빠르게 전달하고, 화면마다 같은 피요 성장 상태를 유지하면 브랜드와 진행 맥락이 끊기지 않는다.
- 관련 PRD 섹션: F4, F6, F6a~F6c, F8, §7.3, §13 M6
- 영향 범위: 일본어 6.9형 원본 캡처, 마케팅 스크린샷 생성기, App Store Connect 미디어

## 2026-08-03 승인된 1.0 이후 App Store 빌드 버전
- 결정: 승인된 마케팅 버전 `1.0`의 업로드 트레인이 종료됐으므로 최신 소스의 다음 App Store 빌드는 `1.0.1 (4)`로 올린다. Apple 서버가 버전 중복으로 거절한 `1.0 (4)` 아카이브는 제출본으로 사용하지 않고, 소스 변경 없이 마케팅 버전을 올린 새 아카이브를 생성·검증해 업로드한다.
- 결정: 이번 작업은 App Store Connect 빌드 업로드까지만 수행한다. 처리 완료 확인, TestFlight 수동 게이트, 플로우 v4 Game Center 항목 구성, 새 앱 버전 연결과 App Review 제출은 각각 별도 게이트로 유지한다.
- 근거: App Store Connect가 `1.0 (4)`에 대해 이전 승인 버전보다 높은 `CFBundleShortVersionString`을 요구하는 90186·90062 오류를 반환했다. `1.0.1 (4)`는 서버 분석과 업로드에 성공해 처리 단계에 진입했다.
- 관련 PRD 섹션: §13 M6, §14
- 영향 범위: Xcode 마케팅 버전·빌드 번호, App Store archive·IPA, 출시 QA·제출 상태 문서

## 2026-08-10 옷장 알 무늬 선택 UI 제거
- 결정: 온보딩 목표에서 정해진 알 무늬와 기존 저장 값은 성장 기록 및 알 단계 렌더링을 위해 유지하되, 옷장에서는 알 4종 비교·재선택 섹션을 노출하지 않는다. 옷장 미리보기·이름·성장 기록·소품 선택은 그대로 유지한다.
- 근거: 부화 이후 주로 사용하는 옷장에서 과거 알 외형 4종을 다시 고르는 인터페이스는 현재 피요의 복장 관리 흐름과 직접 관련이 적고 화면 길이만 늘린다.
- 관련 PRD 섹션: §7.1, F1, F8, §13 M6
- 영향 범위: `MascotClosetView`, ja/en/ko 로컬라이제이션, 옷장 UI 회귀 테스트, PRD v4.8

## 2026-08-10 게임 허브 Game Center 전체 랭킹 버튼 제거
- 결정: 게임 탭 첫 화면 최상단의 `Game Center 랭킹` 카드를 제거하고 주간 피요컵을 첫 콘텐츠로 표시한다. Game Center 점수·업적의 백그라운드 동기화와 게임 결과 화면의 해당 리더보드 버튼은 유지한다.
- 근거: 전체 랭킹 카드는 게임 모드 선택보다 먼저 노출되어 게임 허브의 핵심 진입 흐름을 밀어냈다. 결과 화면의 기록 맥락에서 해당 리더보드를 여는 경로가 남아 있으므로 별도 최상단 버튼 없이도 랭킹 접근성을 유지할 수 있다.
- 관련 PRD 섹션: S7, F6, F6f
- 영향 범위: 게임 허브 레이아웃, Game Center 대시보드 진입 경로, 게임 허브 UI 회귀

## 2026-08-10 외부 메일 기반 덱 제안·내용 제보
- 결정: 발견 하단에는 신규 덱 제안, 덱 상세에는 현재 덱의 내용 제보, 설정 앱 정보에는 두 목적의 통합 진입점을 추가한다. 세 경로는 계정·업로드·카탈로그 쓰기 API 없이 공개 지원 메일 `contact@typee.app`의 OS 작성 화면을 연다.
- 결정: 메일 본문은 ja/en/ko로 현지화하고 접수 유형·진입 화면·앱 버전/빌드·언어를 자동 포함한다. 덱 상세에서만 덱 ID·버전을 더하며 사용자 ID·기기 식별자·학습 기록은 넣지 않는다. 실제 가사·드라마 대사와 개인정보를 적지 말라는 안내 및 운영 검수 전 자동 게시되지 않는다는 안내를 함께 제공한다.
- 결정: 채택된 제안과 제보는 운영자가 기존 로컬 Deck Studio에서 작성·수정하고 스키마 검증을 거쳐 정적 카탈로그로 배포한다. 별도 웹폼은 현재 공개 사이트 소스와 배포 경로가 이 저장소에 없으므로 1차 범위에서 만들지 않으며, 추후 폼이 배포되면 중앙 URL 빌더만 교체한다.
- 근거: 구조화된 외부 작성 화면은 사용자가 원하는 덱과 공식 콘텐츠 오류를 전달할 경로를 제공하면서 앱의 무계정·오프라인 우선·정적 읽기 전용 카탈로그 구조와 세션 무인터럽트 원칙을 유지한다.
- 관련 PRD 섹션: §2.3, F5.8, F10, §8.3, §10
- 영향 범위: `ContentFeedbackLinkBuilder`, 발견·덱 상세·설정 UI, ja/en/ko 로컬라이제이션, 설정 단위·UI 테스트, 운영 Deck Studio

## 2026-08-10 App Store 1.0.2 빌드 5 출시 후보
- 결정: 승인·배포된 `1.0.1 (4)` 이후 현재 M6 변경분은 `1.0.2 (5)`로 아카이브하고 App Store Connect에 신규 버전으로 제출한다. 릴리스 방식은 기존 운영 정책과 같은 자동 출시를 사용한다.
- 결정: UI 테스트의 짧은 피요 반응 확인은 XCTest의 약 1초 predicate 폴링 대신 0.05초 직접 폴링을 사용한다. 앱의 0.9초 반응 연출은 변경하지 않고 테스트만 실제 연출 지속 시간 안에 관찰하도록 한다.
- 근거: 옷장 정리, 게임 허브 정리, 외부 메일 기반 덱 제안·제보를 포함한 최신 소스는 SwiftPM 21개와 iOS 단위·UI 289개 시나리오를 통과했다. 기존 테스트 대기 방식은 앱의 정상적인 0.9초 상태를 첫 재평가 전에 놓쳐 출시 회귀를 거짓 실패로 만들었다.
- 관련 PRD 섹션: F5.8, F6, F8, F10, §13 M6, §14
- 영향 범위: Xcode 버전·빌드 번호, UI 테스트 대기 헬퍼, App Store 메타데이터·출시 QA·아카이브

## 2026-08-10 App Store 1.0.2 빌드 5 심사 제출
- 결정: `1.0.2 (5)` 앱 버전과 `piyokey.v4.flow.*` 3개 클래식 리더보드, `piyokey.v4.cup.weekly.flow` recurring 리더보드를 하나의 App Review 제출로 묶고 자동 출시로 제출한다. 주간 보드는 App Store Connect가 과거 시각을 허용하지 않아 다음 월요일인 2026-08-17 00:00 JST부터 7일 단위로 시작한다.
- 결정: App Store Connect는 심사 전 새 리더보드에 기본 지정 액션을 제공하지 않으므로 주간 v4 보드의 기본 지정은 Live 전환 뒤 수행한다. TestFlight 새 설치·한국어 OS IME·TTS·무음 모드·외부 음악 혼합·iPhone 12 성능 게이트는 사용자 명시 승인으로 이번 제출에서 면제하되 완료 상태로 기록하지 않는다.
- 근거: 빌드 5는 Apple Binary State `Validated`, 비면제 암호화 `No`, 내부 QA 그룹 연결을 확인했고 자동 회귀는 모두 통과했다. App Review 제출 `722d6568-1321-4b44-838f-138f07ef8647`의 앱 1개와 리더보드 4개가 모두 `Waiting for Review` 상태다.
- 관련 PRD 섹션: F6, F6b, F6f, §13 M6, §14
- 영향 범위: App Store Connect 앱 버전·Game Center·자동 출시 설정, 출시 QA·제출 상태 문서

## 2026-08-11 글로벌 단일 앱 레코드와 로케일별 브랜드
- 결정: 2026-07-20의 제품명 결정 가운데 영어 `PIYOKEY` 표기를 이 결정으로 대체한다. 일본어 `ピヨキー`와 한국어 `피요키`, 내부 호환성 유지 원칙은 그대로 이어 간다.
- 결정: 기존 App Store Connect 앱 ID `6794853985`와 Bundle ID `app.piyokey.Piyokey`의 단일 앱 레코드를 유지해 한국을 포함한 글로벌 스토어로 확장한다. 스토어 국가별 중복 앱이나 별도 바이너리는 만들지 않으며 기존 `Hanco` 내부 코드명과 `piyokey.*` Game Center 계약도 변경하지 않는다.
- 결정: 사용자 노출 브랜드는 스토어 국가가 아니라 해석된 앱 언어를 기준으로 `ja`=`ピヨキー`, `ko`=`피요키`, `en` 및 미지원 기기 언어 fallback=`typee`로 고정한다. 따라서 한국어 기기에서는 국가와 무관하게 `피요키`가 표시되고, 같은 국가에서도 사용자가 앱 언어를 바꾸면 해당 언어의 브랜드로 바뀐다.
- 결정: 앱 언어의 최초 기본값은 기기 선호 언어 중 `ja`·`ko`·`en` 첫 일치 항목이며 지원하지 않는 언어는 영어다. 덱의 일본어 원본 필드는 뒤로 호환되게 유지하고, 덱명·제작자·태그는 `en`·`ko` 메타데이터를 추가한다. 항목은 일본어에서 일본어 뜻+가타카나, 영어에서 영어 뜻+로마자를 사용한다. 한국어 앱 언어는 한국어 메타데이터를 사용하되 항목 뜻·읽기에 `ko`가 없으면 영어 번역·로마자를 사용하고 일본어로는 절대 fallback하지 않는다.
- 결정: 현재 `1.0.2 (5)` 심사 제출 상태를 기록한 `release/app_store_submission.json`은 과거·현재 상태 증거로 유지한다. 글로벌 변경은 별도 `release/GLOBAL_ROLLOUT.md`와 `release/global_app_store_metadata.json`에서 계획 상태로 관리하며, 실제 availability 변경은 새 빌드·현지화·규제 게이트가 통과한 뒤 수행한다.
- 근거: App Store 앱 이름·부제·설명은 App Store 로케일별로, 홈 화면 이름은 `CFBundleDisplayName` 로컬라이제이션과 기기/앱 언어로 결정되므로 하나의 앱에서 국가만으로 서로 다른 이름을 강제할 수 없다. 단일 레코드는 학습 기록·리뷰·Game Center 연속성을 보존하고, 동일 기능의 여러 앱을 제출할 때 발생할 수 있는 App Review 4.3 위험을 피한다.
- Apple 근거: [App information localization](https://developer.apple.com/help/app-store-connect/manage-app-information/localize-app-information/), [App Store localizations reference](https://developer.apple.com/help/app-store-connect/reference/app-information/app-store-localizations/), [`CFBundleDisplayName`](https://developer.apple.com/documentation/bundleresources/information-property-list/cfbundledisplayname), [custom product pages](https://developer.apple.com/help/app-store-connect/create-custom-product-pages/configure-multiple-product-page-versions), [App Review Guidelines 4.3](https://developer.apple.com/app-store/review/guidelines/#spam)
- 관련 PRD 섹션: §0, §1.3, F5, F6, F9, F10, §8, §14
- 영향 범위: 앱 표시 이름·로컬라이제이션·덱/카탈로그 스키마·공유 카드·App Store 메타데이터·글로벌 출시 게이트

## 2026-08-11 세션 저장 비동기화와 진행 기록 상한
- 결정: 커리큘럼 체크포인트와 복습 덱 변경은 화면 상태에 즉시 반영하되 각각 latest-only debounce 뒤 actor에서 직렬 저장한다. 세션 종료·화면 이탈·앱 inactive/background에서는 pending snapshot을 비차단 flush하고, 부화 필수 미션 완료는 `finishAndWait`의 내구 저장 성공 뒤에만 다음 미션을 해금한다. 실패하면 낙관적 완료를 정확한 이전 revision으로 되돌리고 결과 화면에서 재시도 또는 부화 화면 복귀를 제공한다.
- 결정: 게임 결과 기록은 MainActor에서 전체 파일을 다시 읽고 쓰지 않고 메모리 결과를 즉시 반환한 뒤 actor에서 저장한다. raw 세션은 2,048개로 제한하되 모든 덱·게임·입력 방식별 플레이 수와 최고 기록 요약, Game Center 클래식 최고점 대표 기록, 현재 주간 피요컵 최고점 대표 기록은 보존한다. NaN·무한대 지표와 의미적으로 잘못된 복습 항목은 디스크 진입 전에 거부한다.
- 결정: 설치·삭제·최근 플레이 갱신도 actor로 옮기고, 최근 플레이는 설치 인덱스만 갱신해 세션 시작 때 모든 덱 payload를 재검증하지 않는다. Flow ticker와 countdown은 generation으로 이전 task와 새 task를 구분하고 background에서 실제 취소한 뒤 foreground에서 재생성한다.
- 근거: 입력·오타·결과·게임 시작마다 메인 스레드에서 primary/backup JSON 전체를 동기 재읽기·재인코딩·원자쓰기하던 경로가 사용자가 느끼는 정지의 직접 원인이었다. 메모리 우선·직렬 비동기 저장은 입력 응답성을 지키면서 기존 손상 복구와 미래 schema 보존 정책을 유지하며, bounded raw history는 장기 사용자 파일의 무제한 증가를 막는다.
- 관련 PRD 섹션: F2a, F4, F6, F7, F12, §9, §12, §13 M6
- 영향 범위: Curriculum/Review/GameProgress 저장소, DeckLibrary, Practice·Flow·직접입력 게임 생명주기, AppRoot background flush, 저장·경합·대용량 회귀 테스트

## 2026-08-11 Game Center 직렬 표시·Live 확인과 빌드 6 제출 게이트
- 결정: Game Center 인증 UI와 leaderboard dashboard는 명시적 사용자 탭에서만 하나의 busy transaction으로 직렬화한다. scene 비활성, 인증 VC 표시·dismiss 중, `GKAccessPoint.isPresentingGameCenter`, 동일 요청 처리 중에는 재진입을 거부하고, dashboard가 실제 표시된 뒤 닫혔을 때만 점수·업적·순위를 동기화한다. SwiftUI body의 순위 조회는 상태를 바꾸지 않는 순수 함수로 유지한다.
- 결정: `Info.plist`에는 이미 Live로 검증된 baseline 12개와 심사 대상 intended 16개를 분리한다. iOS 26 이상은 인증 후 GameKit이 반환한 전체 보드 가운데 `releaseState == released`인 intended ID만 런타임 활성화하고, probe 오류·시간 초과에는 baseline으로 닫힌 실패를 한다. iOS 16~25는 release state를 확인할 API가 없으므로 baseline만 사용한다. 다운로드·사용자 덱, OS IME, 콘텐츠 버전 불일치 기록에는 Game Center CTA를 노출하지 않는다.
- 결정: `1.0.2 (5)`와 v4 리더보드 4개를 묶은 제출 `722d6568-1321-4b44-838f-138f07ef8647`은 2026-08-11 심사 시작 전에 취소해 `Removed / Developer Rejected`로 되돌렸다. 수정본은 `1.0.2 (6)`으로 준비하되 자동 출시를 사용하지 않고 수동 보류하며, TestFlight 실기기에서 로그아웃·취소·연타·background·오프라인 복구·실제 제출/순위와 GameKit crash 부재를 확인하기 전에는 새 App Review 제출을 보내지 않는다.
- 결정: build 6의 TestFlight Game Center 실기기 확인으로 위 수동 보류 조건을 충족해 2026-08-11 21:15 JST에 iOS 앱과 Flow v4 3개·Weekly Piyo Cup v4를 제출한다. 제출 ID `45184f9b-494c-431e-a740-a3dde9080f4a`의 5개 항목은 모두 `Waiting for Review`이며, 승인 후 phased release 없이 전 사용자에게 즉시 자동 출시한다.
- 근거: 기존 결과 버튼은 전체 동기화를 시작한 직후 dashboard를 열고 presentation 재진입 가드가 없어 인증 dismiss 및 연타와 경합했다. 아직 Live가 아닌 v4 ID 호출과 SwiftUI view update 중 `@Published` mutation 경고도 관측됐다. GameKit daemon의 과거 assertion 실패가 함께 남아 있어 호출 수를 줄이고 운영 상태를 런타임에서 검증해야 한다.
- 관련 PRD 섹션: F6, F6f, F12, §11.3, §12, §13 M6, §14
- 영향 범위: GameCenterService, 게임 결과 CTA, Info.plist 운영 계약, Game Center 단위·UI 회귀, release preflight·TestFlight smoke·App Store Connect 제출 상태

## 2026-08-14 앱 1.1 `.piyodeck` 무료 문서와 유료 모바일 Deck Maker
- 결정: 앱 1.1에서 사용자 덱은 계정이나 서버에 업로드하지 않고 `.piyodeck` 로컬 문서로 교환한다. 정상 파일의 Files·iCloud Drive·AirDrop 가져오기, 검증·미리보기, 연습/게임, 현재본 내보내기, 삭제, 같은 ID의 교체 재가져오기는 무료다. 공개 카탈로그 게시·검색·댓글·신고·원격 쓰기 API와 클라우드 동기화는 범위에서 제외한다.
- 결정: 모바일 새 덱 생성, 가져온 덱 편집, 공식 덱의 사용자 사본 생성, 변경 내용 저장은 StoreKit 2 비소모성 IAP `app.piyokey.deckmaker.lifetime`으로 묶는다. 파일 안에는 영수증·라이선스·만료·premium 상태를 넣지 않고 entitlement가 없거나 검증되지 않아도 기존 덱의 읽기·학습·내보내기·삭제와 초안 보존을 막지 않는다.
- 결정: `.piyodeck` v1은 `manifest.json`과 `deck.json`만 담은 최대 8 MiB의 텍스트 전용 ZIP 문서다. 사용자 `deck_id`는 `user_` + 소문자 32 hex, item ID는 `item_` + 소문자 32 hex, 최대 1,000항목, `official=false`, `audio=null`로 제한한다. SHA-256은 무결성·충돌 판정에만 쓰고 제작자 인증이나 DRM으로 사용하지 않는다.
- 결정: 같은 ID·같은 SHA 재가져오기는 no-op, 다른 내용은 자동 병합 없이 현재본 유지 또는 무료 교체를 제공한다. Deck Maker 구매자에게만 새 ID·version 1 사본 저장을 추가한다. 교체는 덱별 기록과 같은 item ID의 복습 진행을 유지하고 삭제 payload의 기록도 같은 ID 재가져오기 때 복원한다. 공식 덱 사본은 새 사용자 deck/item ID, `version=1`, `official=false`로 원본·공식 업데이트·Game Center에서 분리한다.
- 결정: 연습·레슨·게임 중 외부 문서를 받으면 접근 가능한 동안 staging에만 복사하고 가져오기·충돌·편집·구매 UI는 세션 종료 뒤에만 표시한다. import·replace·edit save는 검증된 payload와 설치 인덱스를 원자적으로 교체하며 실패 시 기존 덱·기록·초안을 그대로 둔다.
- 근거: 파일을 무료로 소비하게 하고 모바일 제작 편의에만 과금하면 학습 접근성과 사용자 데이터 소유권을 보존하면서 앱 내부 기능 잠금은 Apple IAP 계약에 맞게 수익화할 수 있다. 로컬 문서 경계는 기존 무계정·오프라인 우선·읽기 전용 공식 카탈로그와 공개 UGC 운영 부담을 유지하고, 엄격한 package 제한과 원자적 저장은 외부 입력으로 인한 손상 범위를 차단한다.
- 관련 PRD 섹션: §1.3, §2.3, S8·S10, F5.9, §8.1, §8.3~8.4, §9, §11~§14, R1.1
- 영향 범위: 공용 `.piyodeck` 규격·schema·fixture·도구, DeckKit package reader/writer·검증, 설치 저장소 migration, Files/AirDrop import·export, 사용자 덱 편집기, StoreKit 2 entitlement·구매 복원, ja/en/ko 로컬라이제이션, App Review 1.1 출시 게이트

## 2026-08-14 글로벌 App Store 기능 우선 표시명
- 결정: 글로벌 App Store 표시명은 브랜드를 앞세운 `typee: Korean Typing` 또는 `typee - Hangul Keyboard` 형식을 사용하지 않는다. 일본판의 기존 명명 방식처럼 현지 검색 의도를 설명하는 기능명을 먼저 쓰고, ASCII 구분자 ` - ` 뒤에 서비스 브랜드를 둔다.
- 결정: 일본어 기준안은 `韓国語タイピング - ピヨキー`, 영어 기준안은 `Korean Typing - typee`, 한국어 기준안은 `한글 타자 연습 - typee`다. 영어 기준안이 특정 App Store 로케일에서 저장되지 않을 때만 `Hangul Keyboard - typee`를 2순위 후보로 사용한다. 세 기준안과 영어 대체안은 모두 App Store 이름 30자 제한 안에 들어간다.
- 결정: 2026-08-11의 로케일별 브랜드 결정 가운데 한국어 `피요키`는 최신 글로벌 통일 정책으로 대체한다. 사용자 노출 브랜드는 `ja`에서만 `ピヨキー`, `ko`·`en` 및 미지원 언어 fallback에서는 `typee`로 표시한다. App Store 이름은 이 로케일 브랜드 앞에 현지화된 기능명을 붙인다.
- 근거: 기능 검색어를 앞에 두면 제품이 한국어 타이핑·한글 자판 학습 앱임을 검색 결과에서 즉시 설명하면서, 일본판과 같은 명명 문법으로 피요키와 typee가 하나의 서비스 계열임을 유지할 수 있다.
- 관련 PRD 섹션: §0, §1.3, §14
- 영향 범위: PRD 브랜드 계약, 글로벌 App Store 메타데이터, ASO 이름 후보, 영어·한국어·일본어 스크린샷·지원 문서

## 2026-08-14 Deck Maker 단일 활성 초안과 안전 복구 UX
- 결정: 앱 1.1은 기기당 하나의 활성 `UserDeckDraft`만 primary/backup JSON으로 보존한다. 같은 create/edit/official-copy 흐름은 debounce 자동 저장에서도 `draft_id`와 최초 생성 시각을 유지하고, 다른 흐름은 새 초안의 원자 쓰기가 성공한 뒤에만 기존 초안을 교체한다.
- 결정: 활성 초안과 같은 제작 흐름은 정확한 `draft_id`로 재개한다. 다른 흐름은 기존 초안 재개·명시적 폐기 후 시작·취소를 먼저 선택하게 하며, 사용자 선택 전에는 새 빈 초안으로 기존 내용을 덮지 않는다.
- 결정: 닫기·앱 비활성·구매 취소·검증/저장 실패에는 초안을 남기고 마이페이지 재진입에서 복구한다. 최종 덱 설치 또는 편집 화면의 덱 삭제가 성공한 뒤에만 일치하는 `draft_id`를 정리하며, 지연된 autosave가 완료 초안을 되살리지 않도록 pending 작업을 먼저 취소·flush한다.
- 결정: 기존 덱 편집 저장은 초안의 `base_version`을 설치 저장소 transaction 내부에서 원자적으로 비교한다. 원본이 교체되어 달라졌으면 현재 덱과 초안을 유지하고, 최신본 확인 또는 fresh deck/item ID를 가진 별도 사용자 사본 저장을 제공한다.
- 결정: 충돌은 현재본/가져온 파일의 이름·버전·수정일·항목 수를 비교하고 현재본 유지를 기본 선택으로 둔다. 교체 전 현재본 내보내기와 파괴적 재확인을 제공한다. 일반 삭제도 덱 이름·기록 보존 안내와 사용자 덱 `내보낸 뒤 삭제`, 실패 시 행 유지로 보호한다.
- 결정: 편집 검증 실패는 첫 오류 항목을 자동으로 펼쳐 이동·포커스하고 VoiceOver로 알린다. 대형 덱 항목은 접이식 번호 행으로 구성하며, 제작/구매 화면은 Dynamic Type·좁은 폭에서 세로 배치로 전환한다.
- 근거: 편집 초안·사용자 덱은 구매 권한과 분리된 사용자 데이터이므로 취소·실패·앱 종료로 유실되어서는 안 된다. 충돌·삭제처럼 되돌리기 어려운 작업은 비교·백업·재확인 경계를 제공해야 하고, 1,000항목 및 큰 글자에서도 핵심 제작 흐름이 사용 가능해야 한다.
- 관련 PRD 섹션: F5.9, §9, §11, §12, R1.1
- 영향 범위: `UserDeckDraftStore`, `DeckEditorView`, `MyDecksView`, `PiyoDeckImportPreviewView`, Deck Maker paywall, ja/en/ko 로컬라이제이션·단위/UI 테스트

## 2026-08-14 Android M7 보류와 앱 1.1 언어 범위 동결
- 결정: Android M7의 플랫폼 공용 스펙과 향후 포팅 순서는 유지하되, 별도 재개 결정 전에는 Android 프로젝트 생성·Google Play 준비·Android 기능 구현을 시작하지 않는다. 앱 1.1 글로벌 출시는 기존 iOS 프로젝트와 단일 App Store 레코드에 집중한다.
- 결정: 1.1 앱 UI와 콘텐츠 로케일은 `ja`·`en`·`ko` 세 개로 동결한다. 미지원 기기 언어는 영어 UI·영어 콘텐츠와 `typee` 브랜드로 시작하며 일본어 뜻·가타카나를 fallback으로 사용하지 않는다.
- 결정: 1.1 App Store 메타데이터는 `en-US`를 primary 후보로 하고 `en-GB`·`en-AU`·`en-CA`·`ko`·`ja`까지 운영한다. 영어 변형 메타데이터는 같은 영어 앱 UI를 사용하며 네 번째 앱 언어를 뜻하지 않는다. 프랑스어·스페인어·중국어 등 추가 metadata-only 로케일도 1.1에서는 만들지 않는다.
- 결정: 후속 언어는 글로벌 출시 후 국가별 제품 페이지 조회·다운로드·리뷰·지원 문의를 검토해 별도 버전에서 결정한다. 추가 metadata-only 상품 페이지를 먼저 실험할 경우 실제 앱 인터페이스가 일본어·영어·한국어임을 명시하고 실제 영어 UI 스크린샷만 사용한다. RTL 언어의 UI 지원은 방향성·키보드·혼합 문자 QA를 포함한 독립 마일스톤으로 다룬다.
- 근거: 현재 세 앱 언어의 UI 키와 영어 공식 콘텐츠는 완전한 구조를 갖추고 있고, 미지원 언어의 영어 fallback도 구현돼 있다. 반면 네 번째 언어는 UI·덱 의미/읽기·스키마·검증기·스토어 자산·IAP·지원 문서 전체를 확장하며 1.1 출시 표면을 크게 늘린다. Android와 신규 언어를 동시에 시작하지 않으면 typee 글로벌 출시의 영어 품질, 브랜드 일관성, 스토어 자산과 실기기 QA에 집중할 수 있다.
- 관련 PRD 섹션: §0, F10, §8, §12~§14, R1.1, M7
- 영향 범위: PRD 1.1 로컬라이제이션 계약, 글로벌 App Store 메타데이터·출시 QA, Android 프로젝트 일정

## 2026-08-14 앱 1.1 전 세계 availability 단일 선택
- 결정: 앱 1.1은 App Store Connect에서 `All Countries or Regions`를 선택하고 향후 새로 추가되는 App Store storefront도 자동 포함한다. 국가별 wave·allowlist·순차 활성화는 운영하지 않는다. Deck Maker 비소모성 IAP도 앱과 동일한 전체 국가 availability를 사용한다.
- 결정: 전체 국가 선택과 실제 판매 가능 상태를 구분한다. EU DSA, 중국 본토 filing·게임 등록, 베트남 게임 라이선스처럼 Apple 또는 현지 규제가 요구하는 정보가 미완료이면 해당 storefront가 `Action Required` 또는 판매 불가로 남을 수 있다. 이 경우 전체 선택을 특정 국가 목록으로 되돌리지 않고 국가별 App Store 상태와 필요한 조치를 compliance 예외로 기록한다.
- 결정: 추가 UI 언어는 만들지 않는다. 앱 언어는 `ja`·`en`·`ko`, 미지원 언어는 영어 fallback이며 영어 primary metadata가 번역이 없는 storefront의 공통 상품 페이지 역할을 한다.
- 근거: Apple은 App Store가 제공되는 전체 국가·지역을 한 번에 선택하고 향후 storefront도 자동 포함할 수 있게 하며, 앱 언어·메타데이터 언어와 판매 국가를 별도 설정으로 관리한다. 영어 fallback이 준비된 현재 제품에서는 국가별 활성화 운영비보다 전 세계 발견 가능성과 수요 데이터 확보 가치가 크다.
- Apple 근거: [Manage availability for your app](https://developer.apple.com/help/app-store-connect/manage-your-apps-availability/manage-availability-for-your-app-on-the-app-store), [Localize app information](https://developer.apple.com/help/app-store-connect/manage-app-information/localize-app-information/), [Regional app information requirements](https://developer.apple.com/help/app-store-connect/reference/app-information/app-information/)
- 관련 PRD 섹션: §0, F10, §12~§14, R1.1
- 영향 범위: App Store Connect 앱·IAP availability, 글로벌 출시 계획·QA·preflight, EU·중국 본토·베트남 compliance 상태 추적

## 2026-08-15 앱 제공 고정 목표 전 영역 gTTS 오프라인 통일
- 결정: 공식 카탈로그 26덱, 흐름·산성비·초성·단어 맞추기·받아쓰기의 3단계 프리셋 15덱, 커리큘럼과 그 부분집합인 데일리, 무료 연습 샘플의 모든 고정 한국어 목표를 gTTS 2.5.4의 `ko`, `tld=com`, 보통 속도 MP3로 사전 생성한다. 중복을 제거한 581개 문구는 SHA-256 기반 canonical 경로를 공유하며 원본 MP3를 재인코딩 없이 번들해 완전 오프라인으로 재생한다.
- 결정: 이 계약은 현재 581개에만 한정하지 않는 상시 콘텐츠 규칙이다. 이후 앱 제공 고정 목표를 추가·수정하면 같은 gTTS 설정과 canonical MP3를 함께 생성해야 하며, 덱 밖 문구는 `pronunciation_prompts.json`에 등록한다. 생성기와 저장소 프리플라이트가 전체 고정 목표의 번들 MP3를 확인하기 전에는 변경을 병합·출시하지 않는다. macOS/Yuna·발음용 CAF·기기 TTS 전용 고정 콘텐츠로 회귀하거나 제공자·설정을 바꾸려면 사용자 승인과 PRD·DECISIONS 갱신이 필요하다.
- 결정: 기존 gTTS MP3 184개는 유지하고 397개를 새로 생성해 총 581개·5,951,040 bytes(5.68 MiB)로 확장한다. 상황별 재미 덱에 쓰던 macOS `Yuna` IMA4 CAF 108개는 모두 동일 문구의 gTTS MP3로 교체하고 제거한다. 생성기는 일반/업데이트 카탈로그와 게임 프리셋을 재귀 수집하고, 커리큘럼·데일리·무료 샘플은 `pronunciation_prompts.json`에서 수집해 CoreAudio 검증과 원자 교체 뒤에만 기존 파일을 대체한다.
- 결정: 게임 프리셋은 단어·출제 순서·채점 조건을 바꾸지 않고 번들 오프라인 음원 metadata만 채웠으며 앱 번들에서 직접 로드하므로 덱 버전과 `GamePresetLevel.bundledDeckVersion`을 v3로 유지한다. 일반 설치 덱도 기존 설치본이 선언한 레거시 CAF 경로의 파일이 없어지면 공통 resolver가 같은 한국어의 canonical MP3를 이어서 찾으므로 카탈로그·덱 버전을 올리지 않는다. 따라서 이번 음원 교체만으로 기존 Game Center 콘텐츠·점수 계약을 분리하지 않는다.
- 결정: 공통 런타임 resolver는 항목의 선언 `audio` 경로, 목표 한국어로 계산한 canonical hash MP3, `AVSpeechSynthesizer(ko-KR)` 순서로 시도한다. 이로써 영구 복습처럼 선언 경로가 소실된 고정 목표도 번들 MP3를 재사용하고, 누락·손상 파일과 번들에 없는 사용자 덱·비공개 동적 문구만 기기 TTS로 대체한다. 앱은 런타임에 gTTS를 호출하거나 사용자·비공개 문구를 외부로 전송하지 않는다.
- 결정: 연습과 받아쓰기는 백그라운드 진입 즉시 진행 중인 발음을 중지한다. 연습은 복귀 후 문제 자동 발음 설정이 ON일 때만 현재 목표를 다시 자동 재생하고, 받아쓰기는 복귀 카운트다운 완료 뒤 현재 문제 발음을 다시 재생한다. 기존 `.playback` + `.mixWithOthers`, 발음 중 효과음 억제, 외부 음악 유지 정책은 그대로 적용한다.
- 결정: gTTS 클라이언트의 MIT 라이선스와 생성 음원 배포 권리는 별개이므로 확장된 581개 자산의 배포·콘텐츠 사용 권리 확인 전에는 `content_rights_confirmed` 출시 게이트를 닫아 둔다. 확인할 수 없으면 같은 canonical 경로를 유지한 채 상업적 출력 조건이 명확한 공식 지원 TTS 제공자 음원으로 교체한다.
- 근거: 기기 TTS와 macOS Yuna 파일은 화면·콘텐츠 출처에 따라 음색과 자연스러움이 달랐고, 특히 받아쓰기 기본 세트와 복습에서 기계적인 음성이 필수 단서의 품질을 낮췄다. 고정 콘텐츠를 하나의 사전 생성 음원 계층으로 통일하면 기기별 편차 없이 자연스러운 발음을 오프라인으로 제공하면서 사용자 문구의 프라이버시와 장애 시 기기 폴백을 함께 유지할 수 있다.
- 관련 PRD 섹션: F6d, F10, F11, §8.1, §12, §14
- 영향 범위: 공식/게임 프리셋 덱 fixture, 커리큘럼·데일리·무료 샘플, 공통 발음 resolver, 음원 생성기·provenance, 오디오/복습/백그라운드 회귀, 콘텐츠 권리·실기기 출시 게이트

## 2026-08-20 iOS·Android·웹 소스 모노레포와 외부 산출물 보관
- 결정: PIYOKEY의 iOS, M7 Android와 향후 웹 앱은 하나의 비공개 소스 모노레포에서 관리한다. 기존 `ios/` 경로는 Xcode churn을 피하기 위해 유지하고 Android와 웹은 루트의 `android/`, `web/`에 추가한다. 플랫폼 구현은 독립적으로 유지하되 `shared/`의 schema, fixture, 콘텐츠와 `test_vectors.json`을 공통 계약으로 사용한다.
- 결정: Swift·Kotlin·TypeScript 한글 엔진은 플랫폼별 순수 함수 구현으로 두고 동일 공용 벡터를 모두 통과시킨다. `shared/` 변경은 영향받는 모든 플랫폼 계약 테스트를 요구하며 UI·저장·오디오·입력·결제 계층은 플랫폼 경계 안에 둔다.
- 결정: 소스 저장소에는 빌드 재현에 필요한 코드·공용 규격·콘텐츠·소형 canonical 발음 MP3·현재 릴리스 스크린샷을 보존한다. archive, xcresult, dSYM, IPA, provisioning profile, Instruments trace, milestone/Notion 캡처, 외부 레퍼런스와 App Preview 중간·최종 영상은 비공개 `piyokey-release-assets` 또는 버전 릴리스 첨부로 분리하고 소스에는 checksum·provenance manifest만 남긴다.
- 결정: 메인 소스 저장소는 Git LFS를 기본 사용하지 않는다. 10MB 초과 신규 파일은 저장소 정책 검토를 거치며, 생성 산출물은 LFS에 넣기보다 외부 보관한다. 콘텐츠 주소가 canonical이고 전체 용량이 작은 오프라인 MP3는 일반 Git 객체로 버전 관리한다.
- 근거: 세 플랫폼은 한글 조합·덱·콘텐츠 의미가 같아야 하므로 공용 계약의 원자적 변경과 단일 PR 검증이 유리하다. 반면 현재 로컬 산출물은 전체 5.8GB 중 대부분을 차지하고 서명·배포 정보도 포함하므로, 이를 모든 디바이스와 에이전트 clone에 복제하면 성능·비용·보안 경계가 악화된다.
- 관련 PRD 섹션: §6, §8, §13 M7, R1.1
- 영향 범위: 저장소 디렉터리, `.gitignore`, `.gitattributes`, CI path filter, 에이전트 브랜치·리뷰 정책, 릴리스 산출물 보관

## 2026-08-20 Game Center Live 16개 baseline 승격
- 결정: 2026-08-11의 Game Center baseline 12개 결정을 운영 상태에 맞게 갱신한다. App Store Connect에서 `piyokey.v4.flow.*` 3개와 `piyokey.v4.cup.weekly.flow`이 모두 Live임을 확인했으므로 `PiyokeyGameCenterAvailableLeaderboardIDs`에 네 ID를 추가해 intended와 동일한 현재 운영 16개를 baseline으로 사용한다.
- 결정: iOS 26 이상은 기존처럼 인증 후 `releaseState == released`인 intended ID만 동적으로 활성화하고 probe 오류·시간 초과에는 16개 baseline으로 fallback한다. `releaseState` 확인 API가 없는 iOS 16~25도 동일한 16개 baseline을 사용한다. 향후 새 ID는 다시 intended 전용으로 시작해 Live·실기기 확인 뒤 baseline으로 승격한다.
- 근거: 심사 당시 미출시였던 네 보드는 이후 Live가 되었지만 출고 baseline이 12개에 머물러 iOS 16~25에서 흐름 v4와 주간 피요컵의 CTA·점수 제출·순위 조회가 차단될 수 있었다. 현재 서버 상태를 다음 바이너리에 명시적으로 반영하면 구형 OS에서도 계약된 보드 전체를 사용하면서 신규 미출시 ID에 대한 fail-closed 정책은 유지할 수 있다.
- 관련 PRD 섹션: F6, F6f, F12, §11.3, §12, §14
- 영향 범위: `Info.plist` Game Center availability 계약, `GameCenterServiceTests`, Game Center 운영 문서, iOS 16~25 출시 회귀

## 2026-08-21 Android M7 재개·iOS 1.1 기준선과 플랫폼 경계
- 결정: 사용자의 명시적 Android 착수 요청으로 2026-08-14의 M7 보류를 해제한다. Android 포팅 기준은 공개 1.0.x의 부분 스냅샷이 아니라 현재 로컬 iOS 1.1 build 7, PRD v5.8, 26개 공식 덱·15개 게임 프리셋·581개 canonical gTTS MP3와 `.piyodeck` v1/Deck Maker 계약이다. Android에서 iOS 1.1에 없는 새 제품 기능을 먼저 추가하지 않는다.
- 결정: 대규모 dirty worktree를 임의로 commit·tag하거나 기존 변경을 정리하지 않는다. A0의 코드·공용 계약 준비는 진행하되, Android 기능 병렬 개발과 외부 Play 작업 전에는 사용자가 현재 iOS/shared 기준선을 review한 뒤 별도 branch의 baseline commit/tag와 contract SHA manifest를 고정해야 한다. 미제출 iOS 1.1의 IAP·콘텐츠 권리·실기기 게이트는 Android 로컬 M1 계약 작업과 분리하되 Play 출시 전에 다시 확인한다.
- 결정: Android 빌드는 AGP `9.3.1`, Gradle Wrapper `9.5.0`, Kotlin/Compose compiler `2.3.21`, Compose BOM `2026.08.00`, compile SDK 37, target SDK 36, min SDK 26, Java bytecode 17로 고정한다. AGP 9 built-in Kotlin을 사용해 Android 모듈에 `org.jetbrains.kotlin.android`와 kapt를 적용하지 않는다. CI는 JDK 17을 기준으로 하고, 현재 로컬 JDK 21은 동일 wrapper 빌드가 통과하는 동안만 개발용으로 허용한다.
- 결정: 로컬 후보 application ID와 namespace는 `app.piyokey.piyokey`로 둔다. Play Console에서 충돌·소유권을 확인하고 사용자가 확정하기 전에는 이 값을 출시 불변 ID로 간주하지 않으며 앱 생성·서명 업로드·내부 테스트 트랙 같은 외부 변경을 하지 않는다.
- 결정: `core:hangul`, `core:deckkit`, `core:piyodeck`, `core:session`은 Android·Compose import가 없는 순수 로직으로 두고 공용 fixture를 직접 읽는다. `core:data`는 Room/files/DataStore와 복구 journal, `core:platform`은 IME/audio/TTS/SAF/Billing/Play Games/reminder/share, feature 모듈은 화면과 navigation만 소유한다.
- 결정: Room은 설치 메타데이터·학습/게임/복습/스트릭 기록과 journal, 앱 전용 파일은 실제 덱·카탈로그 payload·primary/backup/quarantine·단일 초안·SAF staging, Preferences DataStore는 로케일·사운드·입력·마스코트·리마인더 설정만 저장한다. 파일과 Room의 논리 transaction은 persistent journal, 같은 디렉터리의 원자적 move, Room transaction, 시작 시 복구로 이전 pair 또는 새 pair 중 하나에 수렴한다.
- 결정: `.piyodeck` import/export는 Storage Access Framework와 `ContentResolver`/`content://`만 사용하고 저장소 전체 권한을 요청하지 않는다. 외부 URI는 원본을 수정하지 않고 8 MiB+1 제한으로 private staging에 복사한 뒤 엄격히 검증하며, 세션 중 도착한 문서는 UI 없이 pending queue에 둔다.
- 결정: Android Deck Maker는 Play Billing one-time product `app.piyokey.deckmaker.lifetime`을 사용한다. foreground에서 상품과 구매를 재조회하고 `PURCHASED`만 제작 mutation 권한을 열며 `PENDING`에는 열지 않는다. 미확인 구매는 acknowledge하고, 환불·취소·검증 실패에도 기존 덱·초안을 삭제하지 않는다. 계정·자체 entitlement 서버가 없는 client-only 한계는 현재 로컬-only 제품 경계로 수용한다.
- 결정: Play Games Services v2는 선택형 어댑터다. 번들 고정 세트의 클래식 15개 논리 리더보드와 업적 5개만 Play Console opaque ID에 매핑하고 로컬 `GameRecord`를 기준으로 유지한다. Google recurring leaderboard의 reset 경계가 JST 월요일 00:00과 같지 않으므로 Android 주간 피요컵은 로컬 주간 기록만 제공하고 원격 recurring 제출·CTA는 회차 서버 계약이 생길 때까지 보류한다.
- 결정: 고정 한국어 목표는 Android도 동일한 581개 gTTS canonical MP3를 오프라인 번들한다. 효과음은 `SoundPool`, 발음은 로컬 MP3 player를 사용하고 외부 음악을 중단·duck하지 않도록 audio-focus gain을 요청하지 않는다. 파일이 없는 사용자·비공개 동적 문구만 네트워크가 필요 없는 `TextToSpeech(ko-KR)` 음성으로 대체하며 백그라운드에서 즉시 중지한다.
- 결정: OS IME는 Compose 실험 API 대신 표준 `EditText`를 `AndroidView`로 감싸 committed/composing을 순수 `OSIMETextJudge`에 분리 전달한다. 한국어 IME 감지는 안내용이며 hard block하지 않고, IME 설정 이동과 모든 파일·결제·랭킹 UI는 세션 밖에서만 연다.
- 결정: M7-M1은 생성기로 만든 `shared/test_vectors.json`의 조합 15종·백스페이스 10종을 Swift와 Kotlin이 동시에 통과하고 순수 Hangul core line/branch coverage 95% 이상일 때 완료한다. 이후 DeckKit, `.piyodeck` cross-reader/writer golden, session reducer 순으로 확장한다. Pixel 6 출시 게이트는 60Hz 60초 stress에서 평균 59fps 이상, p95 16.7ms 이하, jank 1% 이하, ANR/crash 0으로 둔다.
- 근거: 동일 제품을 네이티브로 포팅하면서도 한글 조합·콘텐츠·결제 경계는 플랫폼 간 일치해야 한다. 반면 파일 권한, IME composing, 오디오 focus, 스토어 결제와 주간 리더보드 reset은 운영체제별 차이가 커서 공통 결과를 보존하는 명시적 adapter 계약이 필요하다. 최신 안정 빌드 도구를 고정하고 외부 Play 변경을 별도 gate로 두면 로컬 M1을 시작하면서 되돌릴 수 없는 식별자·스토어 상태 변경을 피할 수 있다.
- 관련 PRD 섹션: §0, F2a, F5.9, F6f, F11, §6, §8.4, §9, §11~§14, R1.1, M7
- 영향 범위: `android/`, shared contract CI, Android 로컬 저장·파일·결제·랭킹·오디오·IME adapter, Play Console 준비, Pixel 6 성능 게이트

## 2026-08-22 Android M7 M1 DeckKit·PiyoDeck 공용 코어 완료
- 결정: Android M1 순수 코어를 `core:hangul` → `core:deckkit` → `core:piyodeck` 단방향 의존으로 고정한다. 세 모듈에는 Android·Compose import를 허용하지 않는다. `core:deckkit`은 모델, strict JSON codec, 저장소가 사용하는 JSON Schema 2020-12 키워드, 덱·카탈로그 의미 및 bundle 검증을 소유하고, `core:piyodeck`은 strict ZIP/JSON transport, manifest/hash/metadata, 사용자 덱 정책과 결정적 reader/writer를 소유한다.
- 결정: Android DeckKit 기준 콘텐츠는 기본 catalog v10의 공식 26덱·312항목, 카탈로그 밖 게임 프리셋 5모드×3단계×100항목, 전체 교체 snapshot인 update v11·313항목이다. update에서는 `official_daily_words`만 v4/12항목에서 v5/13항목으로 바뀌고 `약속`이 추가된다. base/update 생성 시각과 deck `updated_at`은 역행하지 않도록 바로잡고 Python snapshot 회귀로 고정한다.
- 결정: `created_at`, `updated_at`, catalog `generated_at`은 UTC 초 단위 `YYYY-MM-DDTHH:MM:SSZ`만 canonical로 인정한다. 미지원 UI locale의 덱·카탈로그 metadata도 PRD대로 영어로 fallback한다. 이 과정에서 발견한 Swift의 미지원 locale nil 반환, object `minProperties` 누락, grapheme-count 기반 `minLength`/`maxLength`를 영어 fallback, object property count, Unicode scalar count로 교정해 Python·Swift·Kotlin 결과를 맞춘다.
- 결정: `.piyodeck` reader는 package/ZIP/CRC 뒤 두 JSON의 UTF-8·BOM·중복 키·64단계 nesting·유효 Unicode scalar transport를 먼저 확인하고, manifest/version, exact SHA·size·deck ID/version/item count, deck schema·의미, 사용자 덱 정책 순으로 fail closed한다. v1은 STORE-only exact 2-entry topology, 8 MiB package·16 KiB manifest·4 MiB deck·1,000항목 한계, ZIP64·암호화·descriptor·extra/comment·경로·symlink·겹침 거부를 유지한다.
- 결정: `shared/piyodeck/fixtures/valid/basic.piyodeck`을 Python·Swift·Kotlin writer가 byte-for-byte 재현하고 세 reader가 읽는 canonical golden으로 고정한다. 크기는 1,109 bytes, 전체 SHA-256은 `025efa7a0584509fd892221a01c4b3cdf828472c3ffeb13a7eec420102061c31`이다. `pretty-basic.piyodeck`은 유효한 공백·key order를 reader가 수용하는 비정규 입력 golden이며, `wrong-sha.piyodeck`과 `unpaired-surrogate.piyodeck`은 세 reader가 공통으로 거부하는 malicious golden이다. 생성기는 valid 2개·malicious 13개의 크기·hash·coarse expectation을 `cases.json`에 결정적으로 재생성하고 CI가 diff 0을 요구한다. Python·Swift는 15개 binary를 manifest-driven으로 직접 실행하고, Kotlin은 공용 SHA·Unicode binary와 나머지 동일 공격군의 deterministic mutation 회귀를 실행하는 현재 경계를 M1 인계 상태로 기록한다.
- 결정: M1 완료 게이트는 Kotlin Hangul 11·DeckKit 8·PiyoDeck 16 tests, SwiftPM 42 tests, Python 49 tests, repository release preflight, Android lintDebug·assembleDebug 통과와 Hangul JaCoCo line/branch 각각 95% 이상이다. 2026-08-22 실측은 Kotlin 35/35, SwiftPM 42/42, Python 49/49, line 310/311=99.68%, branch 159/166=95.78%이며 모두 통과했다. 마지막 PiyoDeck 회귀는 manifest metadata의 누락·오타입도 schema보다 먼저 `ManifestMismatch`로 거부하도록 공통 오류 순서를 닫는다.
- 결정: 이 M1 인계 단위에는 M2 키보드·연습 UI나 저장·오디오·Play 기능을 포함하지 않는다. 이후 사용자의 별도 요청으로 M2만 시작했으며 아래 M2 결정과 별도 인계 문서로 경계를 분리한다. 기존 대규모 dirty worktree를 commit·tag·정리하거나 remote를 추가하지 않는 원칙은 유지한다.
- 근거: 플랫폼별 독립 구현이 같은 공용 fixture와 동일한 byte·오류 경계를 통과해야 Android 포팅 중 데이터 손상과 iOS 호환성 회귀를 조기에 차단할 수 있다. 공유 binary golden과 생성기 재현성은 writer→reader 3×3 호환성을 하나의 검토 가능한 기준 파일로 전이 검증한다.
- 관련 PRD 섹션: §6, §8.1~§8.4, §12, §13 M1·R1.1·M7, §14
- 영향 범위: Android 순수 M1 core, Swift/Python DeckKit·PiyoDeck 계약, 공용 schema/catalog/package fixture, source CI와 Android 준비 문서

## 2026-08-22 Android M7 M2 내장 키보드·연습 기능 베이스 구현
- 결정: Android M2는 PRD §13의 F2·F3·§7.2로 제한한다. `app` → `feature:practice` → 순수 `core:session` → `core:hangul` 단방향 그래프로 구성하고, `core:session`에는 Android·Compose 의존을 넣지 않는다. OS IME의 `EditText` adapter와 composing diff는 PRD 순서대로 M6에 남기며 M3 저장·다운로드, 오디오, Billing, Play Games, F12 결과 화면도 이번 범위에 넣지 않는다.
- 결정: `core:session`은 불변 `PracticeSessionState`와 순수 reducer를 소유한다. 정타만 accepted 자모와 조합에 반영하고 오타는 문제별 1회로 기록한 뒤 입력을 무시하며, Backspace는 accepted 자모를 하나 되감아 판정·조합·음절 진행을 재구축한다. 문제 완료는 650ms 지연 effect와 token을 내고 stale callback을 무시하며 마지막 문제에서는 `isResultReady`와 완료 effect까지만 만든다.
- 결정: `feature:practice`는 표준 3행 두벌식, one-shot Shift의 7개 쌍자모·복합모음, space/backspace, pointer down 즉시 입력과 multi-pointer tracker를 제공한다. 다음 키 guide는 2초 pulse, 로마자 힌트와 haptic은 기본 ON hook으로 둔다. 오타 flash·shake는 최대 220ms, 조합 자모는 175ms 동안 오른쪽에서 원형 프리뷰로 이동·fade·scale되고 완료 burst는 360ms 안에 끝난다.
- 결정: 글자 크기 1.5에서 키캡을 62.5dp까지 키우고 자모·로마자 두 줄을 고정해 로마자 힌트를 보존한다. 목표는 최대 10자까지 폭에 맞추고 긴 자모열은 화면 폭을 늘리지 않는 내부 트랙에서 현재 위치를 중앙 추적한다. 사용자 노출 문자열은 ja/en/ko 리소스로만 제공하며 최신 브랜드 계약인 ja=`ピヨキー`, en/ko/fallback=`typee`를 유지한다.
- 결정: 2026-08-22 강제 재실행 결과는 Hangul 11, DeckKit 8, PiyoDeck 16, session 12, practice 8의 Android tests 55/55, Hangul line 99.68%·branch 95.78%, feature/app lint error 0, debug APK build 성공이다. API 35의 320×640 AVD에서 일본어 화면, 글자 배율 1.5, 실제 9자모 입력 뒤 650ms 자동 다음 문제 전환을 확인했다.
- 결정: 현재 M2 기능 베이스에는 코드 차단 이슈가 없지만 실제 기기 touch-down 지연 p95 50ms 이하와 진짜 동시 2-pointer rollover 계측이 남아 있으므로 전체 M2 완료로 닫지 않는다. 설정 영속화·F12 결과·최신 S5의 닫기/설정 헤더와 뜻·읽기 데이터 연동은 각 후속 범위에서 연결한다. 이 게이트 전 M3는 시작하지 않는다.
- 결정: 이번 작업에서도 기존 dirty worktree를 commit·tag·stage·stash·clean하지 않고 branch나 remote, Play Console 외부 상태를 만들지 않는다. 중앙 `piyokey` 저장소에는 `docs/ANDROID_M7_M2_HANDOFF.md`의 정확한 증분 파일만 전용 branch로 선별 이전한다.
- 근거: 한글 판정과 세션 시간을 UI에서 분리하면 iOS parity를 단위 테스트로 고정하면서 Compose는 키 입력·연출·접근성에만 집중할 수 있다. 좁은 화면과 큰 글자에서 실제 입력 흐름을 먼저 확인하면 M3 데이터 계층을 얹기 전에 키보드의 측정·배치 실패를 닫을 수 있다.
- 관련 PRD 섹션: F2, F3, §6.3, §7.2, §12, §13 M2·M6·M7
- 영향 범위: Android app 진입점, 순수 session core, Compose practice feature, Android CI, M2 런타임 증거와 인계 문서

## 2026-08-23 Android M7 M2 물리 입력 gate 계측 계약
- 결정: F2의 `touch down → 화면 반영`은 실제 pointer event의 `uptimeMillis`부터 그 입력을 수락한 composition revision이 포함된 첫 `ViewTreeObserver.registerFrameCommitCallback`까지로 계측한다. 이 callback은 실제 패널의 픽셀 발광 시각이 아니라 Android가 앱에 제공하는 GPU/frame-buffer commit proxy다. event clock은 millisecond 단위여서 50ms 경계에 약 1ms 양자화가 있음을 결과에 기록한다.
- 결정: 물리 gate는 API 29 이상 실제 Android 기기 한 대에서 `debugAndroidTest` 하네스로 실행한다. test APK가 실제 production `PracticeScreen`과 `DubeolsikKeyboard`의 Initial pointer pass·reducer·draw 경로를 감싸며 production Kotlin에는 측정 hook을 넣지 않는다. 제조사·모델·device·fingerprint·API·주사율·hardware acceleration·build variant를 반드시 JSON 증거에 포함한다.
- 결정: latency 표본은 유효한 warm-up 20입력을 제외한 정확히 100개다. p95는 오름차순 표본의 `ceil(0.95 × N)`번째 값을 쓰는 nearest-rank 방식으로 계산하고 50ms 이하여야 한다. warm-up도 20 down·20 accepted·20 frame samples, `ㄱㅏ` 순서, 누락·reject 0을 모두 만족해야 하며 그렇지 않으면 측정 전체를 실패시키고 다시 실행한다.
- 결정: 물리 rollover는 warm-up 10쌍과 측정 50쌍에서 사용자가 `ㄱ` pointer를 떼기 전에 `ㅏ` pointer를 내린다. 각 측정 쌍은 서로 다른 pointer ID 두 개가 동시에 활성인 동안 `ㄱ`, `ㅏ`가 순서대로 정확히 한 번 수락되어야 한다. missing·duplicate·undelivered·rejected·unknown·overflow는 모두 0이어야 하고 최대 동시 pointer 수는 2 이상이어야 한다.
- 결정: 물리 test는 `piyokeyPhysicalGate=true` runner argument와 명시적인 emulator hardware 제외 조건을 모두 만족할 때만 최대 180초 실행한다. 기본 CI와 에뮬레이터에서는 자동 다중 MotionEvent 회귀만 실행하고 물리 gate는 skip한다. 성공·실패 모두 target app external files의 `m2-touch-gate/`에 raw touch/delivery/frame sample과 summary JSON을 쓰고 Gradle 출력에 절대 경로를 남긴다.
- 결정: 2026-08-23 API 35 `hantap_test` AVD에서 한 gesture의 `ㄱ down → ㅏ down/up → ㄱ up`, 두 pointer 동시 활성, 수락 순서·누락·중복 0과 각 수락 revision의 frame commit 상관을 검증했다. instrumented tests는 자동 1 pass·물리 1 skip, 실패·오류 0이다. 전체 Android 강제 회귀는 JVM tests 55/55, Hangul line 99.68%·branch 95.78%, lint error 0, app/androidTest APK와 Gradle 142 tasks가 모두 통과했다.
- 결정: 현재 연결된 실제 Android 기기가 없어 물리 latency와 손가락 rollover 값은 아직 측정하지 못했다. 따라서 M2는 계속 ACTIVE이고 M3는 시작하지 않는다. 실기기 JSON의 `overallPass=true`를 검토한 뒤에만 PRD·DECISIONS·handoff를 최종 완료로 갱신한다.
- 근거: reducer 도달 시간만 재면 Compose가 화면에 반영되는 비용을 누락하고, synthetic pointer만으로는 실제 Android touch dispatch와 손가락 rollover를 증명할 수 없다. 자동 회귀와 opt-in 물리 측정을 분리하면 CI를 멈추지 않으면서 F2의 사용자 체감 경계를 재현 가능한 raw evidence로 남길 수 있다.
- 관련 PRD 섹션: F2 AC, §7.2, §12, §13 M2·M7
- 영향 범위: Android practice androidTest, Compose test dependencies, Android CI compile gate, M2 physical evidence·handoff·완료 판정

## 2026-08-25 Android 실기기 QA의 출시 후보 통합 gate 이관
- 결정: 사용자의 명시적 요청에 따라 Android 개발 중 반복적으로 로컬 실기기를 요구하는 수동·정량 QA는 기능별 면제로 처리하지 않고 M3~M6 전체 구현과 자동 검증이 끝난 출시 후보 단계의 통합 실기기 QA로 이관한다. 각 마일스톤은 JVM·instrumented 자동 회귀, lint, debug/release build와 가능한 에뮬레이터 검증을 계속 통과해야 한다.
- 결정: M2는 다양한 단어 16개·자모 101/101 실기기 기능 gate, 사용자 직접 입력 확인, 자동 representative-plan·2-pointer MotionEvent 회귀를 근거로 후속 구현을 허용한다. 물리 touch-down→frame-commit p95 50ms 이하와 50쌍 rollover 누락·중복 0은 삭제하거나 완화하지 않는다. 마지막 유효 정량 측정은 짧은 자모열 불필요 스크롤 제거 뒤 p50 38ms·p95 54ms·max 64ms였고, 키 가이드 애니메이션 재구성 범위 최적화는 자동 테스트·lint·빌드까지 통과했으나 이 결정에 따라 실기기 재측정은 출시 후보로 남긴다.
- 결정: M3부터 PRD §13 순서로 구현을 계속할 수 있지만 Android 출시 가능 판정과 사용자에게 전달하는 최종 완료 보고는 이관된 실기기 목록을 사용자가 직접 QA하고 필수 수치·기능 gate를 모두 통과한 뒤에만 한다. 중간 상태를 출시 완료나 실기기 AC 충족으로 표현하지 않는다.
- 근거: 사용자는 개발 단계마다 기기를 반복 조작하기보다 앱 전체 설계·기능을 출시 가능한 수준까지 먼저 완성하고 출시 직전에 한 번에 직접 QA하기를 선택했다. 자동 검증으로 회귀를 조기에 차단하면서 수동 검증 전환 비용을 출시 후보에 모으되, 명시된 품질 기준은 그대로 보존한다.
- 관련 PRD 섹션: F2 AC, §7.2, §12, §13 M2~M7, §14
- 영향 범위: Android 마일스톤 완료 판정, GitHub Project 상태, M2 성능 evidence, M3~M6 착수 조건, 출시 후보 실기기 QA 체크리스트

## 2026-08-25 Android M7 M3 정적 카탈로그·원자 저장·발견 흐름 완료
- 결정: Android M3는 `core:data`가 Room 설치 메타데이터·다운로드 이력·persistent journal을, 앱 전용 파일이 실제 catalog/deck payload를 소유하도록 고정한다. 쓰기는 `journal Room transaction → 같은 디렉터리 atomic move → metadata Room transaction` 순서이며, 현재 검증본과 직전 검증본·quarantine으로 시작 시 이전 또는 새 pair에 수렴한다. 손상된 최신 payload는 직전 검증본으로 복구하고 둘 다 유효하지 않을 때만 해당 설치를 격리하거나 번들 catalog로 fallback한다.
- 결정: 앱은 번들 공식 26덱 또는 검증 cache를 즉시 표시하고 `PIYOKEY_CATALOG_URL`이 주입된 경우에만 동일 HTTPS content root에서 ETag와 If-Modified-Since 조건부 GET을 수행한다. 네트워크·HTTP·schema·의미 검증 실패는 현재 화면을 실패 상태로 바꾸지 않으며 계정·쓰기 API·download count mutation을 만들지 않는다.
- 결정: `feature:discover`는 이름·태그·제작자 즉시 검색, 타입·레벨·복수 태그 교집합·항목 수 필터, 인기·신착·급상승·항목수 정렬, 섹션형 발견, 상세 통계와 최대 10항목 미리보기, 설치·업데이트·삭제, 내 덱을 제공한다. 설치된 전체 덱은 오프라인에서 기존 연습 reducer로 실행하고 세션 중 탭 bar와 파일·네트워크 UI를 숨긴다.
- 결정: 다운로드 태그 이력은 설치 삭제 뒤에도 Room에 유지한다. 홈은 이력 태그를 온보딩 목표보다 높게 가중해 미설치 3개를, 결과는 공통 태그와 미설치 우선으로 2개를 추천하며 1탭 재시작을 제공한다. ja/en/ko UI와 콘텐츠를 로케일로 해석하고 미지원 언어는 영어로 fallback하며 영어 화면의 커버·브랜드에 일본어를 노출하지 않는다.
- 결정: API 35 전용 AVD에서 Room install/delete/history, 최신/backup/quarantine 복구, v10→v11 ETag cache를 포함한 data instrumented 5개와 발견→다운로드→플레이 Compose 1개를 통과했다. 실제 Galaxy를 사용하지 않았고 M2 정량 입력을 포함한 실기기 항목은 출시 후보 Issue #19에서만 수행한다.
- 근거: 읽기 전용 정적 배포와 오프라인 우선을 지키면서 파일과 DB의 부분 갱신·손상으로 설치 전체가 무너지는 경우를 fail-closed 복구해야 한다. 실제 기기 조작을 마일스톤마다 반복하지 않고도 순수·에뮬레이터 자동 gate로 M4 착수 품질을 확보할 수 있다.
- 관련 PRD 섹션: F5.1~F5.7, F12.5, §8.1~§8.3, §9, §11, §13 M3·M7
- 영향 범위: Android Room schema v1, static content client, catalog/deck file store, 5탭 app shell, discover/detail/home/my decks/practice result, M3 자동 검증과 로컬 evidence

## 2026-08-25 Android M7 M4 흐름 게임·공통 결과 완료
- 결정: M4는 PRD §13 순서대로 흐름 게임과 이후 게임이 재사용할 결과·기록 기반까지만 구현한다. `core:game`은 Android·Compose import가 없는 불변 상태와 순수 reducer로 두고, Compose frame clock은 세션 monotonic origin에 매핑하되 제한시간·카드 이동·점수·목숨 판정은 reducer만 소유한다. 산성비·초성·단어 맞추기·받아쓰기·띄어쓰기는 허브 순서만 고정하고 실제 기능은 M6에서 같은 기반 위에 추가한다.
- 결정: 시작과 재도전은 3·2·1 뒤에만 60초와 카드 이동을 시작한다. background에서는 rule clock을 멈추고 복귀 후 다시 3·2·1을 센다. 노미스 단어는 +2초, 오타는 시간 차감 없이 콤보 0, 세 번째 카드 이탈은 즉시 종료하며 새 카드의 이동 시간만 플레이 0~50초 동안 1.0→1.8배로 가속한다. 점수는 자모 수×10과 완료 시 콤보 구간 1.0/1.2/1.5/2.0을 적용한다.
- 결정: 흐름 선택은 일반 발견 목록과 분리한 `flow_topik_beginner|intermediate|advanced` v3 각 100단어를 먼저 표시하고 세 단계 300단어의 중복을 허용하지 않는다. 진입·재도전은 새 난수 순서를 만들고 테스트만 명시적 seed를 사용한다. 설치 덱은 그 아래 선택지로 제공하며 `+`는 발견 검색으로 이동한다.
- 결정: `feature:game`은 게임명 우선 2열 허브, 밝은 단순 레인, 닫기·시간·점수·콤보·목숨 한 줄 HUD, 내용 맞춤 카드, 내부 자모 추적, 기기 폭 고정 두벌식 키보드, 완료 파티클과 공통 결과·재도전을 소유한다. ja/en/ko 리소스와 영어 fallback을 사용하고 뜻·읽기는 현재 언어 뒤 영어로 fallback하며 세션 중 덱·설정·네트워크 모달을 노출하지 않는다.
- 결정: Room schema v2는 append-only `GameRecord`와 `(deckId, mode, inputMode)` 복합키 `DeckProgress`를 추가한다. v1→v2 명시 migration은 M3 설치·이력·journal을 보존하며, 매 결과에서 plays·best score·best accuracy·last played를 같은 transaction으로 갱신한다. M4 입력 방식은 `builtin`으로 기록하고 OS IME 분리는 M6에 추가한다.
- 결정: 결과 등급은 iOS와 같은 공용 `shared/tuning/game_rank_tuning.json`의 정확도 0.6·완료 자모 CPM 0.4, 120 CPM cap, S/A/B 90/75/55를 검증해 사용한다. 로컬 단위 11개, API 35 Room/data 8개와 기존 발견 1개·새 흐름 Compose 2개, feature/app lint와 debug APK를 통과했다. 실제 기기 60초 평균/p95/jank와 M2 입력 수치는 출시 후보 Issue #19에 유지한다.
- 근거: 규칙 시계와 렌더 프레임을 분리하면 테스트 clock·실기기 frame source 차이에도 점수와 생존 판정이 재현된다. M3 schema에서 명시 migration을 검증하고 로컬 기록을 단일 기준으로 두면 이후 게임·Play Games 어댑터를 사용자 데이터 손실 없이 확장할 수 있다.
- 관련 PRD 섹션: F6, F12, §7.1, §7.3, §9, §11, §12, §13 M4·M6·M7
- 영향 범위: Android pure game core, Room schema v2, game Compose feature, app navigation, 공용 rank tuning, Android CI·에뮬레이터 회귀

## 2026-08-25 Android M7 M5 커리큘럼·복습·리텐션 계약
- 결정: Android M5 커리큘럼은 iOS와 동일한 6챕터·7스테이지·각 10문제를 Kotlin 고정 catalog로 재현한다. 80% 클리어와 별 1/2/3의 80%·90%+40자/분·97%+60자/분 경계, 챕터1~4 순차 해금과 챕터5+ 자유 선택을 순수 `core:retention` 정책으로 둔다.
- 결정: 연습 checkpoint는 현재 문제, 수락 자모, 오타/항목 resolution과 유효 입력 시간만 저장한다. lifecycle background 구간은 순수 active-duration clock에서 제외하고 복귀한 완료 문제도 token 지연 전환을 다시 안전하게 수행한다. Room v2→v3은 기존 설치·게임 기록을 건드리지 않고 커리큘럼/복습/스트릭/보상/리마인더 테이블을 추가한다.
- 결정: 일반 레슨·덱은 오타 항목만 복습에 수집하고 복습 세션의 노미스 완료만 연속 perfect를 올린다. 게임의 잘못 누른 현재 카드와 바닥 이탈 카드도 item ID별로 수집하며, 세 번 연속 노미스에 졸업한다. 설치 덱의 항목은 사용자가 수동 추가하고 활성 복습 항목은 수동 제거할 수 있다.
- 결정: 스트릭 날짜는 세션 시작 시 캡처한 JST 날짜에 귀속한다. 홈의 주간 카드는 월~일 고정이며 MY 피요 응원은 iOS와 같은 네 context·10개 현지화 문구를 날짜 기반으로 결정해 홈/마이페이지에서 동일하게 보인다. 3/5/7일 보상은 영구 threshold record로 저장하고 M6 옷장 item과 연결 가능한 계약으로 유지한다.
- 결정: Android 데일리는 챕터5~6 고정 문구 풀에서 JST 날짜마다 결정적으로 5개를 선택한다. 리마인더는 기본 OFF, Android 13+ 권한은 사용자가 켤 때만 요청하며 정확 알람 권한 없이 JST 다음 시각의 inexact daily alarm을 사용한다. 재부팅 시 Room preference로 다시 예약하고 알림 탭은 앱을 연다. 세션 화면에는 권한·시간·복습 관리 UI를 노출하지 않는다.
- 근거: iOS M5와 같은 학습 결과를 순수 정책으로 고정하면서 Android lifecycle·notification 차이는 platform adapter로 격리하면 실기기 반복 없이 JVM/Room/API 35에서 날짜·복구·졸업·migration을 먼저 닫을 수 있다. 실제 알림 수신과 입력/프레임 정량 측정은 사용자가 지정한 출시 후보 통합 QA에 유지한다.
- 관련 PRD 섹션: F4, F5.6, F7, F8, F12, §9, §13 M5·M7
- 영향 범위: `core:retention`, `core:session`, Room schema v3, 흐름 복습 수집, Compose 홈/연습/마이페이지, Android notification/boot adapter, M5 CI·출시 gate

## 2026-08-25 Android M7 M6A 온보딩·설정·OS IME 기반
- 결정: M6의 첫 병합 단위를 F1 온보딩, F2a 연습 OS IME, F10 공통 설정으로 제한한다. 새 사용자는 목표·신뢰 안내와 두벌식 소개 뒤 네 번째 탭 이내에 실제 `ㄱ` 입력을 시작하고 `가`를 완성한 뒤 챕터1~3 부화 미션을 순서대로 완료한다. 소개 스킵은 PRD대로 설명과 첫 입력만 건너뛰고 부화 gate는 유지한다. 기존 Room DB가 있던 사용자는 1회 마이그레이션으로 온보딩을 완료 처리한다.
- 결정: 작은 설정과 온보딩 진행은 Preferences DataStore가 소유한다. ja/en/ko, light/dark, 글자 크기 3단계, 효과음·타건음·햅틱, 로마자·가이드, 기본 입력 방식, 연습 표시 preset·개별 필드·6가지 순서, 자동 발음과 초성 뜻을 모든 탭의 공통 설정 시트에서 즉시 반영하고 영속화한다. 리마인더 권한은 사용자가 ON으로 바꿀 때만 요청한다.
- 결정: Android OS IME는 투명한 표준 `EditText` adapter가 composing span을 committed text와 분리하고 순수 `OSIMETextJudge`에 전달한다. composing 불일치는 무시하고 확정 불일치만 오타로 기록하며 입력 방식 변경은 reducer 상태를 재생성하지 않는다. 부화 미션·챕터1~4는 내장 고정, 챕터5+·자유/덱/데일리/복습은 OS IME를 허용한다. 한국어 IME 미감지는 안내일 뿐 hard block하지 않고 시스템 설정은 사용자 탭으로만 연다.
- 결정: Debug instrumented 회귀는 명시적 test-only preference로 새 사용자 또는 기존 사용자 시작 상태를 격리한다. Release에서는 `BuildConfig.DEBUG`가 false라 이 우회가 비활성이다. API 35에서 M3~M6 앱 10개 시나리오와 관련 JVM 41개, lint, Debug·Release APK를 자동 gate로 사용하고 실제 IME 종류·입력 지연·물리 rollover는 Issue #19 통합 QA에 남긴다.
- 근거: 온보딩과 설정을 앱 전역 상태로 먼저 고정하면 이후 오디오·게임·캐릭터가 같은 사용자 선택을 재사용할 수 있고, OS IME의 조합 중 흔들림을 순수 판정기로 격리하면 제조사 키보드 차이를 UI reducer에 퍼뜨리지 않는다.
- 관련 PRD 섹션: F1, F2a, F8, F10, §6.3, §7.2, §9, §12, §13 M6·M7
- 영향 범위: Android app shell, `core:settings`, `core:session`, practice/onboarding/settings features, ja/en/ko resources, source CI, 출시 후보 실기기 gate

## 2026-08-25 Android M7 M6B 게임 모드 parity
- 결정: Android 게임 허브는 `흐름·산성비 / 초성·받아쓰기 / 단어 맞추기·띄어쓰기` 순서로 고정하고, 다섯 타이핑 게임은 각 모드의 번들 100단어 코스 3개와 덱 찾기 카드를 2×2로 먼저 표시한다. 설치 덱은 그 아래 별도 목록으로 유지한다. 띄어쓰기는 카탈로그·덱과 분리된 자체 작성 6개 글만 사용한다.
- 결정: 산성비, 초성·단어 맞추기·받아쓰기 공통 직접 입력, 띄어쓰기는 Android/Compose 의존이 없는 `core:game` reducer가 점수·시간·pause/resume·판정·복습 데이터를 소유한다. 모든 타이핑 게임은 내장 두벌식과 OS IME의 committed/composing 계약을 공유하고, 오타 자모는 누적 조합에 넣지 않는다. 최고 기록은 mode·deck/passage·input mode별 Room record로 분리한다.
- 결정: 산성비는 3레인·최대 4장·약 42% 생성 간격·3목숨·0~50초 1.5배 가속과 독립 이탈 판정을 사용한다. OS IME는 표시 카드 중 완성 문자열과 일치하는 어떤 카드든 완료할 수 있다. 초성은 음절 진행 색과 뜻 설정/필수 중복 초성 단서를, 단어 맞추기는 현지화 뜻만, 받아쓰기는 완료 전 정답·초성·뜻·읽기 비노출을 유지한다.
- 결정: 받아쓰기 기본 300문구는 선언된 canonical gTTS MP3를 번들에서만 재생하고 API 35 content gate에서 모든 선언 파일의 존재·비어 있지 않음을 확인한다. 배경 진입 시 재생을 즉시 중지한다. 고정 자산 누락은 release preflight와 계측 content gate에서 출시 실패로 처리하며, 사용자·비공개 동적 덱의 기기 TTS resolver는 `.piyodeck` Android 연결 범위에서 별도로 완성한다.
- 결정: 띄어쓰기는 비공백 문자를 편집할 수 없는 경계 집합 UI만 제공한다. 최초 판단은 한 번만 기록하고 이후 변경은 correction으로 누적하며, 결과에서 첫 판단 정확도·최종 완성도·수정 횟수와 최대 8개 오답 문맥을 제공한다. 백그라운드 시간은 풀이 시간에서 제외한다.
- 결정: API 35 자동 gate는 다섯 새 게임 진입 5개, 흐름·직접 입력 OS IME 2개, 15개 번들 코스·1,500단어·받아쓰기 MP3 300개·띄어쓰기 6개 content 계약을 검증한다. 실제 Galaxy IME 종류·오디오 청취·60fps·터치 지연은 Issue #19 출시 후보 통합 QA에 유지하고 M6B 완료로 주장하지 않는다.
- 근거: 게임 규칙을 순수 reducer와 정적 번들 계약으로 먼저 고정하면 제조사 IME·GPU·오디오 출력에 의존하지 않는 대부분의 회귀를 CI와 에뮬레이터에서 닫을 수 있다. 실기기 전환 비용은 사용자가 선택한 출시 직전 통합 QA에 모으면서도 성능·청취 기준은 삭제하지 않는다.
- 관련 PRD 섹션: F6~F6e, F7, F10, §6.3, §7.3, §9~§13 M6·M7
- 영향 범위: `core:game`, Room 게임 기록, game/practice Compose feature, 번들 게임·띄어쓰기 콘텐츠, ja/en/ko 리소스, source CI, 출시 후보 실기기 gate

## 2026-08-25 Android M7 M6C 오디오·공유·성장 폴리싱

- 관련: PRD F8~F12, §7, §12.1, Issue #30.
- 결정: Android 고정 발음은 선언 asset→canonical asset만 사용하며 둘 다 열지 못하면 자동 테스트/프리플라이트 실패로 다룬다. 번들에 없는 사용자·비공개 동적 문구만 네트워크가 필요 없는 ko-KR `TextToSpeech` voice로 폴백한다. 발음 중 효과음은 억제한다.
- 결정: 효과음은 audio-focus gain을 요청하지 않는 `USAGE_GAME` 지연식 `SoundPool`로 재생하고 48kHz mono 합성 fallback을 사용한다. 첫 재생 전에는 엔진을 만들지 않고 15초 유휴·백그라운드에서 반납한다.
- 결정: 결과 공유는 정확히 1,200×1,200 로컬 PNG, MediaStore 저장, 캐시의 `shared_results/`만 허용한 non-exported FileProvider `content://` 공유로 구현한다. 저장소 전체 권한과 `file://`는 사용하지 않는다.
- 결정: 3/5/7일 소품과 TOPIK 안경은 DataStore의 영구 단조 집합으로 보존한다. `자동`은 태그 기반 소품을 붙이지 않으며 선택 외형을 세션 시작 스냅샷으로 고정한다.
- 결정: Android launcher/adaptive icon은 iOS와 동일한 공용 1024×1024 RGB 원본을 빌드 생성 리소스로 복사한다. 별도 Android 로고 원본을 만들지 않는다.
- 결정: 실제 외부 음악 혼합·오디오 인터럽트·Galaxy IME·60fps/터치 검증은 사용자 결정대로 Issue #19 출시 후보 통합 실기기 QA에 유지한다.

## 2026-08-25 Android 앱 1.1 R1.1A 무료 `.piyodeck` 수명주기

- 관련: PRD F5.9, §8.4, §9, §11, §13 R1.1, Issue #32.
- 결정: Android `ContentResolver` 원본 URI는 8 MiB+1 제한 복사 중에만 열고 앱 전용 `cache/piyodeck-imports` staging에 package와 최소 pending metadata를 원자 기록한다. 원본 URI나 덱 콘텐츠·hash는 장기 저장·분석 전송하지 않으며 검증 거부본은 즉시, 남은 staging은 24시간 뒤 정리한다.
- 결정: strict 공용 reader 검증 뒤에만 덱 이름·제작자·유형·난이도·태그·항목 수·첫 3항목 미리보기를 연다. 공식/카탈로그 ID 충돌은 거부하고 동일 ID는 SHA와 version으로 신규·동일·업데이트·다운그레이드·같은 버전 충돌을 분류한다. 동일 package는 디스크 무쓰기, 다른 내용은 현재본 유지가 기본이고 교체는 별도 파괴 확인을 요구한다.
- 결정: Room v3→v4의 `user_deck_history`는 payload 삭제와 독립해 최초/최근 import, 삭제, version/SHA와 last played를 유지한다. import/replace/recovery는 journal→atomic payload replace→Room transaction으로 설치본과 history를 함께 수렴시키고 기존 `GameRecord`·`DeckProgress`·동일 item 복습 row는 삭제하지 않는다.
- 결정: picker·현재본 export·삭제·내보낸 뒤 삭제·같은 ID 재가져오기는 entitlement 없이 제공한다. export 성공 뒤에만 delete를 수행한다. Deck Maker 생성·편집·사본과 Play Billing은 다음 Issue에서 추가하며 무료 문서 재생 경계에는 결제 화면을 넣지 않는다.
- 결정: 세션 중 외부 문서는 staging까지만 수행하고 미리보기·충돌·오류 UI를 노출하지 않는다. Activity intent는 소비 후 원래 launch identity를 유지한 채 `MAIN`으로 중화하고 pending sidecar로 process recreation 뒤 복구해 세션 종료 후에만 제안한다.
- 근거: 파일 선택 자체를 설치로 간주하지 않고, untrusted archive 검증과 논리 transaction을 UI보다 아래 계층에 고정해야 악성 package·중복 import·중단 복구가 기존 덱과 기록을 변경하지 않는다. 무료 재생과 유료 제작을 분리하면 F5.9의 소유권·결제 경계를 Android에서도 유지할 수 있다.
- 영향 범위: Android Room v4, `core:data` import repository, `core:platform` SAF gateway, 마이페이지/미리보기/충돌 Compose UI, 문서 intent filter, ja/en/ko 리소스, API 35 자동 회귀와 출시 후보 QA.

## 2026-08-25 Android 앱 1.1 R1.1B Deck Maker·Play Billing

- 관련: PRD F5.9, §8.4, §9, §11, §13 R1.1, Issue #34.
- 결정: Android Deck Maker는 새 덱, 가져온 사용자 덱 편집, 불변 공식 덱의 사용자 사본만 제작한다. 순수 Kotlin 초안은 새 `user_`·`item_` ID와 version 1, 편집 시 기존 ID 보존·version 증가, 공식 사본의 새 ID·version 1·`official=false`와 `derived_from_deck_id`만 기록하는 계약을 소유한다.
- 결정: 기기당 활성 초안 하나를 앱 전용 primary/backup JSON으로 자동 저장한다. 같은 흐름은 `draft_id`를 유지해 재개하고 다른 흐름은 재개·명시적 폐기·취소 선택 전 덮어쓰지 않는다. 닫기·앱 비활성 직전에 즉시 flush하며 미래 schema와 복구 불가능한 손상본은 이전 앱이 덮어쓰거나 조용히 삭제하지 않는다.
- 결정: 편집 commit은 Room transaction 안의 `base_version` 비교, recovery journal, 검증 payload 원자 교체, 설치 metadata 갱신 뒤 같은 `draft_id`만 정리한다. 원본이 바뀌면 현재본과 초안을 유지하고 새 ID의 별도 사용자 사본 저장을 제공한다. 1,000항목은 단일 lazy 접이식 목록으로 구성하고 검증 실패 시 첫 오류를 펼쳐 자동 이동·포커스하며 TalkBack live region으로 알린다.
- 결정: 유료 권한은 Google Play Billing Library 9.1.0 one-time product `app.piyokey.deckmaker.lifetime`만 사용한다. eligible one-time offer를 매 구매 직전 다시 조회하고 `PURCHASED`만 권한을 열며 미확인 구매는 acknowledge한다. `PENDING`은 권한을 열거나 acknowledge하지 않고, 시작·foreground·명시적 복원에서 구매를 재조회한다.
- 결정: 마지막 성공 `PURCHASED` 조회는 Room에 오프라인 UX cache로 남겨 재실행·일시적 오프라인에서도 제작을 이어가게 한다. 이후 성공한 빈 구매 조회는 환불·취소로 간주해 제작 mutation만 다시 잠그며 기존 덱·초안·무료 가져오기·연습·게임·내보내기·삭제는 변경하지 않는다. 자체 계정·라이선스 서버·영수증 파일은 만들지 않는다.
- 결정: 실제 Play license tester의 구매·pending·취소·중복·복원·환불과 가격/상품 메타데이터, Galaxy의 Files·공유·편집 UX는 Issue #19의 출시 후보 통합 실기기 QA에서 한 번만 수행한다. API 35 에뮬레이터와 JVM에서는 draft 복구·충돌·Billing 상태 기계·Room v4→v5·무료/유료 화면 경계를 자동 검증한다.
- 근거: 제작 권한과 사용자 문서 소유권을 분리하고 초안·설치 commit을 복구 가능한 transaction으로 고정하면 결제 중단·환불·프로세스 종료가 사용자의 덱이나 무료 기능을 손상시키지 않는다. 외부 Play 상태를 제외한 회귀를 자동화해 사용자가 요청한 단일 출시 직전 실기기 QA 원칙을 유지한다.
- 영향 범위: Android Room v5, user-deck draft/editor core, DeckRepository created/edit commit, Play Billing adapter·entitlement cache, 마이페이지 paywall/editor, ja/en/ko 리소스, Source CI·API 35 회귀, 출시 후보 수동 gate.

## 2026-08-25 Android M7 M6D Play Games v2 랭킹·업적

- 관련: PRD F6f, F8, F12, §9, §11, §12.1, §13 M6·M7, Issue #36.
- 결정: Play Games Services v2 22.0.0은 선택 어댑터다. Play Console이 발급한 project ID, 클래식 15개 leaderboard ID, 5개 achievement ID가 모두 외부 Gradle property로 주입된 빌드에서만 SDK를 초기화한다. 하나라도 없으면 로그인·프로필 UI 없이 로컬 게임·성장·Release 빌드를 그대로 제공하며 별도 검증 task가 배포 설정 누락을 실패시킨다.
- 결정: 랭킹 자격은 `flow|acid_rain|choseong|word_match|dictation`의 정확한 v3 번들 초급·중급·고급 ID와 내장 키보드의 교집합으로 순수 정책에 고정한다. OS IME, 다운로드/사용자 덱, 띄어쓰기, Android 주간컵은 제출하지 않는다. 주간컵은 같은 flow 초급 콘텐츠를 쓰더라도 `weekly_cup` 로컬 progress 슬롯에 분리해 클래식 최고 기록과 outbox를 오염시키지 않는다.
- 결정: Room v6의 최고점 outbox와 단조 achievement progress가 원본이다. 게임 결과·DeckProgress·복습·스트릭·누적 자모를 먼저 같은 로컬 transaction에 저장하고 인증된 경우에만 비차단 동기화한다. 실패는 pending 최대값을 보존하고, 성공 뒤 낮은 점수는 재등록하지 않는다. 연습 누적 자모는 session event ID로 중복 기록을 막으며 띄어쓰기 경계 판단은 자모 업적에서 제외한다.
- 결정: 업적은 챕터 1·3·6 완료, 누적 정타 자모 12,000, 최장 스트릭 30만 단조 반영한다. 최초 점수 제출 성공 또는 기존 서버 점수 확인 뒤 champion trophy를 DataStore 영구 소품으로 해금한다. 현재 랭크는 성장 조건으로 사용하지 않는다.
- 결정: 앱 시작·foreground에서는 `isAuthenticated`만 확인하고 자동 프로필 생성·로그인 창을 띄우지 않는다. 자격 있는 결과 화면의 `Play Games 랭킹` 버튼을 사용자가 누를 때만 sign-in과 해당 보드 UI를 열며 세션·온보딩·허브에는 Play Games 모달이나 CTA를 넣지 않는다.
- 결정: 실제 Play Console 프로젝트/OAuth, 15개 보드·5개 업적 리소스 생성, license tester 로그인·서버 점수·대시보드 확인은 Issue #19의 단일 출시 후보 실기기 QA에 유지한다. 자동 gate는 순수 매핑·가짜 gateway, Room v5→v6/outbox/업적, API 35 결과 UI, 전체 Source CI와 Debug/Release 빌드를 사용한다.
- 근거: 로컬 진행을 외부 서비스보다 먼저 확정하고 동기화 가능 범위를 exact ID 정책으로 닫으면 인증·오프라인·Play 장애가 플레이와 보상을 막지 않는다. 외부 콘솔 값과 실제 계정만 마지막 통합 QA에 남겨 사용자의 실기기 1회 원칙을 유지할 수 있다.
- 영향 범위: `core:game` Play Games policy, Room v6, `core:platform` v2 adapter/sync manager, 결과 화면·앱 startup, 외부 release properties, Source CI, 최종 Play Console/실기기 gate.

## 2026-08-25 Android 1.1 배포 번들·출시 사전검증 계약

- 관련: PRD §12·§12.1·§13 M7·R1.1·§14, Issue #40.
- 결정: Android 소스 후보는 `1.1.0 (8)`, minSdk 26, targetSdk 36으로 고정하고 Release에 R8과 resource shrinking을 적용한다. 일반 로컬·CI는 비밀값 없이 minified unsigned APK와 AAB, merged manifest 계약을 계속 검증하며 이 산출물을 배포용으로 사용하지 않는다.
- 결정: 실제 Play 배포는 `bundleDistributionRelease --no-configuration-cache`만 사용한다. 최종 application ID의 명시적 동일값 확인, 공개 HTTPS catalog/privacy/support, 콘텐츠·고정 발음 권리 승인, 존재하는 upload keystore와 네 서명값, Play Games project ID·20개 고유 resource ID 중 하나라도 없거나 형식이 맞지 않으면 AAB task보다 먼저 key 이름만 표시하고 실패한다.
- 결정: upload signing 값은 저장소에 넣지 않고 private Gradle property 또는 같은 이름의 환경값으로만 주입한다. store path가 설정된 상태에서 configuration cache가 켜져 있으면 password provider를 읽기 전에 configuration 단계에서 실패시켜 서명 비밀이 cache에 직렬화되지 않게 한다. 실제 값은 로그·Issue·PR 증빙에 출력하지 않는다.
- 결정: Release merged manifest는 `debuggable=true`를 금지하고 로컬-only 사용자 덱을 위해 `allowBackup=false`를 요구하며 cleartext opt-in을 금지한다. privacy/support URL은 현재 공개 200 응답을 확인했지만, 공개 static catalog·application ID 소유권·upload identity·Play Console 상품·랭킹·업적과 `content_rights_confirmed`는 소스 구현으로 대신하지 않는다.
- 결정: 최종 스택 head의 GitHub Source CI는 계정 결제·Actions 지출 한도로 checkout 전에 거부되었으므로 원격 green으로 간주하지 않는다. 같은 head의 로컬 Source CI-equivalent 959 tasks, API 35 앱 계측 31/31, Python 56/56, Swift 42/42를 구현 증빙으로 유지하고 계정 제한 해제 뒤 원격 CI를 다시 실행한다.
- 근거: unsigned 재현 빌드와 실제 배포 자격을 분리하고 모든 외부·비밀 입력을 fail closed하면 개발 중 Play 상태를 만들거나 비밀을 커밋하지 않으면서도 잘못된 package·누락된 서비스 ID·불안전한 manifest·미승인 콘텐츠로 AAB를 업로드하는 경로를 차단할 수 있다.
- 영향 범위: Android application/version identity, Release R8·resource shrink·signing, manifest security, Source CI APK/AAB, 정적 카탈로그·법무·Play Console·최종 실기기 Issue #19.
## 2026-08-26 전 플랫폼 익명 제품 분석·크래시 진단

- 관련: PRD F10, §10, §12, Issue #43.
- 결정: iOS/iPadOS·Android·웹의 익명 제품 분석은 PostHog Cloud EU, iOS/iPadOS·Android의 크래시/ANR은 Firebase Crashlytics, 웹 오류는 PostHog Error Tracking을 사용한다. 무료 제공량을 운영 기준으로 하고 초과 과금은 별도 승인 전 허용하지 않는다.
- 결정: `익명 사용 분석`과 `크래시 진단`은 서로 독립적이고 기본 OFF다. Debug/test, 프로젝트 토큰 누락, Firebase 설정 파일 누락은 모두 no-op이며 앱 핵심 기능과 일반 빌드를 실패시키지 않는다. 실제 배포 task에서만 외부 설정·심볼·mapping·정책 gate를 fail closed로 검증한다.
- 결정: `shared/analytics/events.json`을 유일한 allowlist로 두고 Swift/Kotlin/TypeScript 계약을 생성한다. 자동 UI/키 입력 캡처, 세션 리플레이, heatmap, 사용자 identify/person profile, 광고 ID, 자유 문자열·사용자 덱 내용·경로·hash·영수증을 금지한다. 이벤트는 기능·세션·게임·덱 출처·Deck Maker·구매의 의미적 상태와 bucket 수치만 포함한다.
- 결정: SDK는 앱/웹 어댑터 계층에만 위치하고 학습·게임 reducer와 공용 코어는 공급자를 import하지 않는다. 모바일 Crashlytics context도 같은 allowlist의 enum만 사용하며 웹 오류 자동 캡처는 진단 동의 뒤에만 켠다.
- 근거: 선호 기능과 이탈 지점을 익명 aggregate로 확인하면서도 타이핑 학습 앱의 입력 내용과 로컬 사용자 문서를 수집 경계 밖에 유지해야 한다. 독립 동의·기본 OFF·키 누락 no-op·배포 gate를 함께 두면 개발과 오프라인 기능을 외부 서비스 상태에 종속시키지 않는다.
- 영향 범위: 공용 이벤트 계약/생성기, iOS/iPadOS·Android 설정과 SDK 어댑터, 웹 패키지, Apple Privacy manifest·Play Data safety·개인정보처리방침·릴리스 preflight, PostHog/Firebase 운영 대시보드.

## 2026-08-26 익명 분석·진단 최초 선택 안내

- 관련: PRD F10, §10, §11, Issue #43.
- 결정: 현재 개인정보 고지를 검토하지 않은 사용자는 온보딩·부화 미션·앱 투어가 끝난 홈 유휴 상태에서 최초 1회 선택 안내를 본다. 익명 사용 분석과 크래시 진단은 각각 사전 선택하지 않고, `선택 저장`과 `공유하지 않고 계속`을 같은 화면에서 제공한다. 거부는 앱 기능·결제 접근을 제한하지 않는다.
- 결정: 검토 여부는 단순 Boolean 대신 고지 버전으로 저장한다. 목적·공급자·데이터 범주가 실질적으로 바뀔 때만 버전을 올려 다시 안내하며, 설정에서는 현재 선택·공급자·제외 범주·개인정보처리방침을 언제든 다시 확인하고 철회할 수 있다.
- 결정: 활성 레슨/게임, 결과 자동 전환, 파일 가져오기·편집·결제·다른 모달 위에는 개인정보 안내를 표시하지 않는다. 앱의 설정/온보딩을 건너뛰는 자동 UI 테스트는 고지 검토 상태도 명시적으로 고정하고, 별도 동의 UI 회귀에서만 미검토 상태를 사용한다.
- 근거: Apple의 익명 사용 데이터 동의·철회 요구와 Google Play의 명확한 고지·affirmative action 원칙을 만족하면서도 PRD §11의 세션 무인터럽트와 기본 OFF 계약을 보존해야 한다.
- 영향 범위: iOS/iPadOS AppRoot·Settings·UserDefaults·현지화/UI 회귀, Android app shell·DataStore·Settings Compose·현지화/계측 회귀, 웹 통합용 동의 문구, 스토어 심사 안내·개인정보처리방침 출시 gate.
